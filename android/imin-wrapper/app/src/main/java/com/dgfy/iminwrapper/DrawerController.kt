package com.dgfy.iminwrapper

import android.content.Context
import android.util.Log

class DrawerController(
    private val context: Context
) {
    fun openCashDrawer() {
        // TODO:
        // Replace this stub with the iMin SDK integration for your exact device model.
        // Official iMin docs expose cash drawer open through the Android SDK, not plain WebView JS.
        Log.i("DrawerController", "openCashDrawer() called")
    }
}
