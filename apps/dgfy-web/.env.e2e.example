# Copy this file to .env.e2e. Do not commit the copy.
# The default URLs are detected from the local Vite and backend configuration.
E2E_BASE_URL=http://localhost:5174
E2E_API_URL=http://localhost:5000
STOREFRONT_URL=http://localhost:5175

# The PayMongo test flow is state-changing and disabled by default.
# Use a dedicated local test tenant/item before enabling it.
E2E_PAYMENT_FLOW_ENABLED=false
E2E_START_STOREFRONT=false
E2E_STORE_SLUG=
# Set this only when the storefront Vite app must be forced to expose QR Ph test mode.
# VITE_STOREFRONT_SANDBOX_QRPH_ENABLED=true

# Required only for authenticated tests and reusable auth storage state.
# Use a dedicated, non-production account.
E2E_TEST_USER_EMAIL=
E2E_TEST_USER_PASSWORD=
# Optional when the account lookup cannot identify a single company.
E2E_TEST_COMPANY_TOKEN=
