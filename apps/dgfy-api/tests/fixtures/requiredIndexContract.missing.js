export const REQUIRED_INDEX_CONTRACT = Object.freeze({
    items: Object.freeze({
        single: Object.freeze(['column_that_should_never_exist_zz']),
        composite: Object.freeze([])
    })
});
