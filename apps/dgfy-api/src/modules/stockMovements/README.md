# Stock Movements Module

Stock movement transport/use-case boundary.

Flow:

`routes -> stock movement handlers -> stock movement use-cases -> stock movement service`

Legacy `src/controllers/stockMovementController.js` remains a compatibility facade.
