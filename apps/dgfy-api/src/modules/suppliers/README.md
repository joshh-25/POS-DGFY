# Suppliers Module

Suppliers transport/use-case boundary.

Flow:

`routes -> supplier handlers -> supplier use-cases -> supplier service`

Legacy `src/controllers/supplierController.js` remains a compatibility facade.
