# Job Orders Module

Job-order transport/use-case boundary.

Flow:

`routes -> job-order handlers -> job-order use-cases -> job-order service`

Legacy `src/controllers/jobOrderController.js` remains a compatibility facade.
