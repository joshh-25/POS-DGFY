# Tenant Locations Module

Tenant-scoped storefront and POS operational locations.

Boundary flow:
`routes -> tenant location handlers -> tenant location use-cases -> tenant location repository -> models`
