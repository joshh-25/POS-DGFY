/**
 * Receive Token Controller (Compatibility Facade)
 */

export {
  generateToken,
  validateToken,
  receiveViaToken,
  markTokenUsed
} from '../modules/receiveTokens/controllers/receiveTokenHandlers.js';

import {
  generateToken,
  validateToken,
  receiveViaToken,
  markTokenUsed
} from '../modules/receiveTokens/controllers/receiveTokenHandlers.js';

export default {
  generateToken,
  validateToken,
  receiveViaToken,
  markTokenUsed
};
