// Shared tracking map frame sizing for every storefront industry and
// fulfillment view. The 288px minimum gives mobile maps a little more room
// while the 320px cap keeps desktop maps informative without dominating the
// status flow.
export const TRACKING_MAP_HEIGHT = 'clamp(288px, 30vw, 320px)';
