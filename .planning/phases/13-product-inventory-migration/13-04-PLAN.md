---
phase: 13-product-inventory-migration
plan: 04
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - apps/dgfy-migration-runner/src/data/dryRun.js
  - apps/dgfy-migration-runner/tests/dataProductDryRun.test.js
autonomous: true
requirements: [PIM-01, PIM-03]

must_haves:
  truths:
    - "buildDryRunPlan emits product-domain plan entries in the same fixed order apply uses: product_folder before product, and product before inventory_movement/product_embedding — no product is ever planned ahead of its own folder."
    - "A retried dry-run reclassifies already-migrated product-domain records insert to update via reclassifyOperation using the resolved legacy_id_map, unchanged."
    - "Every product-domain skip/conflict/orphan finding (transfer collapse, folder flatten) is persisted before the report returns."
  artifacts:
    - apps/dgfy-migration-runner/src/data/dryRun.js
    - apps/dgfy-migration-runner/tests/dataProductDryRun.test.js
  key_links:
    - "dry-run and apply share the exact mappers so the plan cannot drift from the write path."
    - "reclassifyOperation (unchanged) flips insert to update from resolvedIdMap."
---

<objective>
Extend `src/data/dryRun.js` buildDryRunPlan() to plan the product domain (folder, product, inventory movements from stock_movements, opening-balance, embedding) in the same fixed order apply writes, reusing reclassifyOperation and the pure mappers so dry-run and apply cannot drift.

Purpose: Dry-run must report every planned product_folder insert before any product insert (PIM-01 / ROADMAP SC1) and surface the transfer-collapse and folder-flatten findings without mutating any target.

Output: extended buildDryRunPlan per-target loop + product-domain snapshot wiring in runDryRunTransformations, plus a dry-run unit test proving ordering and finding persistence.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-product-inventory-migration/13-RESEARCH.md
@.planning/phases/13-product-inventory-migration/13-PATTERNS.md
@apps/dgfy-migration-runner/src/data/mappings.js
@apps/dgfy-migration-runner/src/data/legacySource.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Product-domain entries in buildDryRunPlan + snapshot wiring</name>
  <files>apps/dgfy-migration-runner/src/data/dryRun.js, apps/dgfy-migration-runner/tests/dataProductDryRun.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/dryRun.js (whole file — buildDryRunPlan at 89-253; reclassifyOperation at 62-71; idMapLookupKey at 51; the resolve-dependency-in-plan pattern for staff-to-assignment 173-216 and location-to-terminal 220-249; runDryRunTransformations at 352-390; imports 24-38)
    - apps/dgfy-migration-runner/src/data/mappings.js (the five new mappers + their context params — from 13-01)
    - apps/dgfy-migration-runner/src/data/legacySource.js (readLegacyProductSnapshot return shape — from 13-02)
    - apps/dgfy-migration-runner/tests/dataDryRun.test.js (test conventions + fake connection + resolvedIdMap seeding)
  </read_first>
  <behavior>
    - buildDryRunPlan accepts the product snapshot per target (extend the tenantSnapshots value shape or add a productSnapshots map) and pushes entries in order: product_folder, product (folder_id pre-resolved from resolvedIdMap like locationId is for terminals), inventory_movement (from stock_movements; product_id pre-resolved from resolvedIdMap), opening-balance inventory_movement, product_embedding. Each entry goes through reclassifyOperation(result, resolvedIdMap) exactly as staff/location/terminal do.
    - The BOM composition UPDATE is apply-only; dry-run reports the parent product insert (and MAY note a planned product update) but does not emit a second composition mapper call.
    - runDryRunTransformations reads readLegacyProductSnapshot per target (alongside the existing readLegacyTenantSnapshot) and passes it into buildDryRunPlan. All findings persist via the existing recordDataQualityFinding loop before returning.
  </behavior>
  <action>
    Add the five new mapper imports. Extend runDryRunTransformations to read the product snapshot per target and thread it into buildDryRunPlan. In buildDryRunPlan, add a product-domain section to the per-target loop that pushes entries in the fixed order, pre-resolving folder_id and product_id from resolvedIdMap via idMapLookupKey (mirroring the location-to-terminal resolution). Do not change reclassifyOperation or summarizeDryRunReport (existing counters cover the new entities generically). Create tests/dataProductDryRun.test.js: seed a target with 1 folder + 2 items (one in the folder) + a transfer movement + location stocks + an embedding, and a resolvedIdMap that is initially empty. Assert plan-entry ordering (the product_folder entry index precedes the product entry index; product precedes inventory_movement and product_embedding entries), assert the transfer produces a skip entry with a LOSSY_CATEGORY_COLLAPSE finding, and assert that with a pre-populated resolvedIdMap the folder/product entries reclassify to operation 'update'.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductDryRun.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test proves the plan lists the product_folder entry before any product entry for the same tenant.
    - Test proves product entries precede their inventory_movement and product_embedding entries.
    - Test proves a transfer stock movement yields a skip plan entry carrying a LOSSY_CATEGORY_COLLAPSE finding.
    - Test proves a pre-populated resolvedIdMap reclassifies the folder and product entries from insert to update.
  </acceptance_criteria>
  <done>Dry-run plans the full product domain in apply order, reclassifies retried records, and persists the collapse/flatten findings, proven by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy snapshot to plan | dry-run reads legacy data and builds a report; it opens no target connection and mutates nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04-01 | Tampering | dry-run accidentally mutating target | medium | mitigate | dry-run opens no dgfy_* target connection and calls no bulkInsert/Update (module contract 12-22); the product-domain additions reuse the same read-only path. |
| T-13-04-02 | Repudiation | findings silently dropped | low | mitigate | Every product-domain finding persists via the existing recordDataQualityFinding loop before the report returns (D-04). |
| T-13-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductDryRun.test.js` passes.
- dryRun.js opens no target/business connection (unchanged contract).
</verification>

<success_criteria>
buildDryRunPlan reports the product domain in the same fixed order apply writes (folder before product before movements/embeddings), reclassifies already-migrated records, and persists the transfer-collapse and folder-flatten findings.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

- Product-domain plan entries in buildDryRunPlan for entity_types: `product_folder`, `product`, `inventory_movement`, `product_embedding`
- Product-snapshot wiring in runDryRunTransformations (reuses readLegacyProductSnapshot)
</artifacts>

<output>
Create `.planning/phases/13-product-inventory-migration/13-04-SUMMARY.md` when done.
</output>
