---
phase: 13-product-inventory-migration
plan: 05
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - apps/dgfy-migration-runner/src/data/verifyData.js
  - apps/dgfy-migration-runner/tests/dataProductVerify.test.js
autonomous: true
requirements: [PIM-03, PIM-04, PIM-05]

must_haves:
  truths:
    - "Verification reconciles product-domain row counts (product_folders, products, inventory_movements, product_embeddings) with mapper-aware expected counts (source minus intentional skips)."
    - "Verification computes sum-by-type totals: SUM(quantity) grouped by movement_type for inventory_movements and a category distribution for products, structured so Phase 14 can bolt on availment-side checks."
    - "Expected-lossy findings (LOSSY_CATEGORY_COLLAPSE from transfer, FOLDER_NESTING_FLATTENED) are excluded from the data_migration_ok blocking set so verify goes green while keeping them visible in the audit trail; UNRESOLVED_INGREDIENT orphans stay blocking."
    - "Each product has exactly one product_embeddings row, and (stock_count treated as 0 when null) equals SUM(quantity) of that product's inventory_movements rows filtered to reference_type='legacy_opening_balance' — NOT all rows and NOT all movement_type='adjustment' rows (opening-balance rows share movement_type 'adjustment' with real D-03/D-06/D-07 movements) (PIM-05)."
  artifacts:
    - apps/dgfy-migration-runner/src/data/verifyData.js
    - apps/dgfy-migration-runner/tests/dataProductVerify.test.js
  key_links:
    - "checkOpenFindings exclusion set references MAPPING_REASON_CODES.LOSSY_CATEGORY_COLLAPSE / FOLDER_NESTING_FLATTENED (13-01)."
    - "sum-by-type reconciliation mirrors the storefront-projection non-blocking precedent (verifyData.js 164-170) for the never-throws contract."
---

<objective>
Extend `src/data/verifyData.js` with product-domain reconciliation: row-count checks for the four new target tables, sum-by-type totals (SUM(quantity) by movement_type, product category distribution), and a defined "expected-lossy" exclusion so the transfer-collapse and folder-flatten findings do not keep every rehearsal RED.

Purpose: Proves PIM-03/04/05 against a real tenant while resolving the verify-green hazard (Pitfall 5 / Open Question 1) with option (b): a reason-code exclusion set in the blocking check.

Output: extended buildTargetDataVerification + a new sum-by-type section + the expected-lossy exclusion in the data_migration_ok gate, plus verify unit tests.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-product-inventory-migration/13-RESEARCH.md
@.planning/phases/13-product-inventory-migration/13-PATTERNS.md
@apps/dgfy-migration-runner/src/data/mappings.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expected-lossy finding exclusion in the data_migration_ok gate</name>
  <files>apps/dgfy-migration-runner/src/data/verifyData.js, apps/dgfy-migration-runner/tests/dataProductVerify.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/verifyData.js (checkOpenFindings at 140-154; summarizeStorefrontDiscoveryProjection non-blocking precedent 164-170; the data_migration_ok gate at 352; buildDataVerificationSections 309-363)
    - apps/dgfy-migration-runner/src/data/mappings.js (MAPPING_REASON_CODES incl. the new LOSSY_CATEGORY_COLLAPSE / FOLDER_NESTING_FLATTENED / UNRESOLVED_INGREDIENT — from 13-01)
    - apps/dgfy-migration-runner/tests/dataVerify.test.js (pure-function test conventions for checkOpenFindings)
    - .planning/phases/13-product-inventory-migration/13-RESEARCH.md (Pitfall 5 / Open Question 1 — recommendation (b): reason-code exclusion set)
  </read_first>
  <behavior>
    - Define a frozen EXPECTED_LOSSY_REASON_CODES set containing LOSSY_CATEGORY_COLLAPSE and FOLDER_NESTING_FLATTENED (import the constants from mappings.js — do not hardcode string literals). checkOpenFindings (or a small wrapper it delegates to) partitions open findings into blocking vs expected-lossy: an open finding whose reason_code is in the expected-lossy set is reported but does NOT set ok:false. Every other open skip/conflict/orphan (including UNRESOLVED_INGREDIENT) remains blocking.
    - The returned shape keeps the expected-lossy findings visible (a separate counter/list) so the audit trail is preserved; data_migration_ok stays true when the only open findings are expected-lossy.
  </behavior>
  <action>
    Import MAPPING_REASON_CODES into verifyData.js. Add EXPECTED_LOSSY_REASON_CODES as a frozen set built from those constants. Update checkOpenFindings so its ok reflects only non-expected-lossy open findings, while still reporting expected-lossy ones in a distinct field. Keep the function pure and never-throws. Extend tests/dataProductVerify.test.js: prove that a set of open findings containing only a LOSSY_CATEGORY_COLLAPSE + a FOLDER_NESTING_FLATTENED yields ok:true with those findings still listed; prove that adding an UNRESOLVED_INGREDIENT orphan flips ok to false; prove a plain conflict still blocks.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductVerify.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test proves open findings consisting only of LOSSY_CATEGORY_COLLAPSE and FOLDER_NESTING_FLATTENED return ok:true while remaining visible in the result.
    - Test proves an open UNRESOLVED_INGREDIENT orphan returns ok:false.
    - Test proves an unrelated open conflict returns ok:false.
    - The exclusion set is built from MAPPING_REASON_CODES constants, not inline string literals (verified by import presence).
  </acceptance_criteria>
  <done>Expected-lossy transfer/folder-flatten findings no longer block data_migration_ok while staying in the audit trail; genuine orphans/conflicts still block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Product-domain counts + sum-by-type reconciliation</name>
  <files>apps/dgfy-migration-runner/src/data/verifyData.js, apps/dgfy-migration-runner/tests/dataProductVerify.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/verifyData.js (buildTargetDataVerification 203-292 — the Promise.all target reads 218-223, skippedByEntity 225-232, checkDataCounts entries 234-259, expectedLegacyKeys 262-265, the never-throws try/catch 284-291)
    - apps/dgfy-migration-runner/src/data/legacySource.js (readLegacyProductSnapshot — source counts for items/item_folders/stock_movements — from 13-02)
    - apps/dgfy-migration-runner/src/migrations/schema/20260716100000-extend-schema-for-legacy-migration.cjs (product_embeddings unique on product_id; inventory_movements natural key)
    - .planning/phases/13-product-inventory-migration/13-RESEARCH.md (Open Question 3 — sum-by-type depth: SUM(quantity) grouped by movement_type + product category distribution)
  </read_first>
  <behavior>
    - buildTargetDataVerification also reads product_folders, products, inventory_movements, product_embeddings from the business target, and the product-domain source counts from readLegacyProductSnapshot (items, item_folders, stock_movements). Extend skippedByEntity with product/product_folder/inventory_movement/product_embedding so intentional D-08 transfer skips and blank-name skips do not read as mismatches. expected_target_count = source_count minus that entity's skip count.
    - New sum-by-type section: SELECT movement_type, SUM(quantity) FROM inventory_movements GROUP BY movement_type; and a products category distribution (COUNT grouped by category — expected all 'retail'). Also assert each product has exactly one product_embeddings row (count products vs product_embeddings, or verify uniqueness), and that (products.stock_count treated as 0 when null) equals SUM(quantity) of that product's inventory_movements rows filtered to reference_type='legacy_opening_balance'. The reference_type filter is MANDATORY and explicit: opening-balance rows carry movement_type='adjustment', shared with real D-03/D-06/D-07 movements, so summing all rows (or all adjustment rows) would over-count and produce false mismatches — the SQL WHERE clause must pin reference_type='legacy_opening_balance'. This check is coherent with the shared computeOpeningBalanceQuantity source (13-01): synthesized opening-balance rows carry the current_stock fallback, so a zero-location-stocks item still reconciles (stock_count=current_stock=opening-balance-row quantity). Structure the section so Phase 14 can add availment-side checks; follow the never-throws contract (catch to ok:false + error).
    - Extend expectedLegacyKeys with items to products and item_folders to product_folders keys so map completeness covers the new entities.
  </behavior>
  <action>
    Extend the Promise.all block to read the four product-domain target tables. Add the product-domain entries to the checkDataCounts array with mapper-aware expected counts. Add a sum-by-type reconciliation (parameterized/literal GROUP BY queries, never interpolating values) returning movement_type totals, product category distribution, one-embedding-per-product check, and a stock_count-equals-opening-balance check whose SQL explicitly filters reference_type='legacy_opening_balance' (never all rows, never all adjustment rows) and treats null stock_count as 0; fold its ok into the target verification ok. Extend expectedLegacyKeys with the two new key families. Extend tests/dataProductVerify.test.js with a fake business connection returning canned product-domain rows: assert count reconciliation net of a transfer skip, assert the sum-by-type totals per movement_type, assert the one-embedding-per-product check fails when a product has two embedding rows, assert stock_count mismatch against the reference_type='legacy_opening_balance' sum fails the target verification, and assert that a product whose adjustment (D-03) movements would inflate a naive all-adjustment sum still reconciles because only legacy_opening_balance rows are counted.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductVerify.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test proves products/product_folders/inventory_movements/product_embeddings counts reconcile with expected counts net of intentional skips (a transfer skip is not a mismatch).
    - Test proves the sum-by-type section reports SUM(quantity) per movement_type and a products category distribution.
    - Test proves the one-embedding-per-product check fails when a product has two embedding rows.
    - Test proves stock_count not equal to the reference_type='legacy_opening_balance' summed quantity fails the target verification.
    - Test proves a product with additional movement_type='adjustment' (D-03) rows still reconciles because the check counts only reference_type='legacy_opening_balance' rows (guards against the shared-adjustment-type over-count).
  </acceptance_criteria>
  <done>Verification reconciles product-domain counts and sum-by-type totals, enforces one embedding per product and stock_count-equals-opening-balance, proven by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target DB read to verdict | verify reads target counts/sums and computes data_migration_ok; a wrong exclusion could hide a real data-corruption finding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05-01 | Tampering | over-broad expected-lossy exclusion masks real gaps | high | mitigate | The exclusion set is limited to exactly LOSSY_CATEGORY_COLLAPSE + FOLDER_NESTING_FLATTENED (built from constants); UNRESOLVED_INGREDIENT and all conflicts stay blocking. Verified by a test that an orphan/conflict still fails ok. |
| T-13-05-02 | Tampering | SQL injection in sum-by-type queries | medium | mitigate | GROUP BY queries are literal in-code with no interpolated values; any parameter uses replacements. |
| T-13-05-03 | Repudiation | expected-lossy findings hidden from audit | low | mitigate | Expected-lossy findings remain in a distinct reported field — visible, just non-blocking. |
| T-13-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductVerify.test.js` passes.
- data_migration_ok stays true only when open findings are exclusively expected-lossy.
</verification>

<success_criteria>
Verification reconciles product-domain counts and sum-by-type totals, proves one embedding per product and stock_count-equals-opening-balance, and lets the rehearsal go green while keeping transfer/folder findings visible; genuine orphans/conflicts still block.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

- New verify constant: `EXPECTED_LOSSY_REASON_CODES` (frozen set of LOSSY_CATEGORY_COLLAPSE, FOLDER_NESTING_FLATTENED)
- Product-domain count + sum-by-type reconciliation section in buildTargetDataVerification
- New verification checks: one-embedding-per-product, stock_count-equals-opening-balance (filtered to reference_type='legacy_opening_balance', null stock_count treated as 0), movement_type sum-by-type totals, product category distribution
</artifacts>

<output>
Create `.planning/phases/13-product-inventory-migration/13-05-SUMMARY.md` when done.
</output>
