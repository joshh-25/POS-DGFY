---
phase: 13-product-inventory-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-migration-runner/src/data/mappings.js
  - apps/dgfy-migration-runner/tests/dataProductMappings.test.js
  - apps/dgfy-migration-runner/tests/fixtures/phase13/legacyProductRecords.js
  - docs/database/legacy-stock-movement-type-remap.md
autonomous: true
requirements: [PIM-01, PIM-02, PIM-03, PIM-04, PIM-05, PIM-06]

must_haves:
  truths:
    - "Every legacy items.category value maps to products.category='retail' (flat unconditional rule, D-09) — no 3-value whitelist that would drop raw_material/supplies/service rows."
    - "mapItemToProduct promotes the 6 typed columns and folds the 8 satellites + barcodes + BOM into attributes JSON per the folding-design doc; attributes.barcodes is always an array."
    - "stock_movements.movement_type is remapped 8-to-5 via a frozen MOVEMENT_TYPE_MAP; transfer emits a LOSSY_CATEGORY_COLLAPSE finding and inserts no row (D-08)."
    - "Every inventory_movements payload carries deterministic non-null reference_type + reference_id derived from the legacy PK, never copied from nullable legacy reference fields."
    - "A single shared helper computeOpeningBalanceQuantity (sum of item_location_stocks, else legacy current_stock, else null) feeds BOTH mapItemToProduct.stock_count and mapItemLocationStocksToOpeningBalance.quantity, so a legacy current_stock with zero location-stock rows is synthesized into an opening-balance row and never silently dropped (D-10)."
    - "item_location_stocks is removed from OUT_OF_SCOPE_LEGACY_TABLES and the movement-type remap decision is captured in a committed doc."
  artifacts:
    - apps/dgfy-migration-runner/src/data/mappings.js
    - docs/database/legacy-stock-movement-type-remap.md
    - apps/dgfy-migration-runner/tests/dataProductMappings.test.js
    - apps/dgfy-migration-runner/tests/fixtures/phase13/legacyProductRecords.js
  key_links:
    - "MOVEMENT_TYPE_MAP feeds inventory_movements.movement_type enum (restock|loss|adjustment|sale|booking)."
    - "MAPPING_REASON_CODES LOSSY_CATEGORY_COLLAPSE / FOLDER_NESTING_FLATTENED / UNRESOLVED_INGREDIENT are consumed by dry-run/apply/verify report sections."
    - "attributes fold follows docs/database/legacy-product-attributes-folding-design.md 10-key contract."
---

<objective>
Add the five pure Phase-13 mapper functions to `src/data/mappings.js` (folder, item to product, stock-movement to inventory-movement, item-location-stocks to opening-balance, item-embedding to product-embedding), plus the three new reason codes, the frozen 8-to-5 movement-type lookup, and the scope-unblock edit — following the exact Phase-3 pure-mapper contract already in the file.

Purpose: These are the single source of truth for the transform rules; dry-run (13-04) and apply (13-03) both call these exact functions so behavior cannot drift between modes. Every locked decision D-01..D-10 lands here.

Output: 5 new exported mappers, 3 new MAPPING_REASON_CODES entries, MOVEMENT_TYPE_MAP, a trimmed OUT_OF_SCOPE_LEGACY_TABLES, a committed movement-type remap decision doc, and a full unit-test file with Phase-13 fixtures.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-product-inventory-migration/13-CONTEXT.md
@.planning/phases/13-product-inventory-migration/13-RESEARCH.md
@.planning/phases/13-product-inventory-migration/13-PATTERNS.md
@docs/database/legacy-product-attributes-folding-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reason codes + category/folder + item-to-product mappers</name>
  <files>apps/dgfy-migration-runner/src/data/mappings.js, apps/dgfy-migration-runner/tests/dataProductMappings.test.js, apps/dgfy-migration-runner/tests/fixtures/phase13/legacyProductRecords.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/mappings.js (copy the mapLegacyLocationToLocation skeleton at 625-686, the MAPPING_REASON_CODES Object.freeze block at 38-48, and shared helpers isBlank/toTrimmedString/toKeyString/legacyIdMapKey/classifyMappingConflict/skipResult at 86-175)
    - docs/database/legacy-product-attributes-folding-design.md (10-key namespace-to-cardinality table; barcodes always array; compliance from regulatoryCompliance alias; omit-key-when-absent rule D-04)
    - backend/src/models/Item.js (promoted-field columns sku_code/description/unit_of_measure/cost_per_unit; category ENUM has 5 live values raw_material/packaging/product/supplies/service per Pitfall 1)
    - apps/dgfy-migration-runner/tests/dataMappings.test.js (existing test + fixture import style; fixtures live under tests/fixtures/phase03/legacyRecords.js — mirror that shape under phase13/)
    - apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs (products + product_folders column defs; product_folders has show_in_pos_filter; products.product_type ENUM('basic_inventory','non_stock'), stock_count DECIMAL)
  </read_first>
  <behavior>
    - mapItemFolderToProductFolder(legacyFolder, context): returns operation insert, entity_type 'product_folder', target_table 'product_folders', payload {business_id: context.expectedBusinessId, name, show_in_pos_filter: true, is_active: true}; legacy_id_map_key uses legacy_table 'item_folders', legacy_id folder_id. Blank name skips with MISSING_REQUIRED_FIELD. When legacyFolder.parent_id is present, push a FOLDER_NESTING_FLATTENED finding (severity 'skip', informational) AND still emit the insert (new product_folders is flat by design — nesting discarded, not the row).
    - mapItemToProduct(legacyItem, context): operation insert, entity_type 'product', target_table 'products'. category ALWAYS 'retail' regardless of legacy value (flat rule D-09, Pitfall 1 — no switch). product_type 'basic_inventory'. Promote sku_code, description, unit_of_measure, cost_per_unit from legacy columns. vat_type default 'vatable', senior_pwd_discount_eligible default false (legacy carries neither). folder_id: context.resolvedFolderId ?? null (apply/dry-run resolve it via legacy_id_map; the mapper never reads a raw legacy folder id). stock_count: the SINGLE opening-balance quantity returned by the shared pure helper computeOpeningBalanceQuantity(legacyItem, context.itemLocationStocks) — sum of item_location_stocks quantity_on_hand when ≥1 location row is present, ELSE legacy current_stock when non-null (synthesized fallback so a legacy current_stock with zero location-stock rows is NEVER dropped — see ground-truth note below), ELSE null. This is the exact same value the opening-balance mapper emits (Task 2), so products.stock_count always equals the product's one legacy_opening_balance ledger row by construction — the mapper and apply cannot disagree. Ground truth (verified this revision): migration 20260416000007 backfills one item_location_stocks row per item from COALESCE(current_stock,0), but ONLY when the tenant has ≥1 active tenant_location (the backfill is guarded by `if (primaryLocationId)`); a tenant with no active location leaves items with current_stock but zero location-stock rows, which is exactly the case the current_stock fallback covers. attributes JSON per folding doc: 7 object keys (nutrition, physicalProperties, shelfLife, packaging, qualityControl, compliance from regulatoryCompliance, costBreakdown) + 3 array keys (allergens, barcodes, composition); OMIT any key whose legacy satellite is absent (D-04); barcodes always an array when present. The mapper leaves BOM ingredient ids unresolved (apply resolves in pass 2) — include raw legacy composition lines under a shape apply can read, or omit composition here and let apply add it; document the choice and keep the mapper pure. legacy_id_map_key legacy_table 'items', legacy_id item_id. Blank name skips MISSING_REQUIRED_FIELD.
    - New MAPPING_REASON_CODES entries: LOSSY_CATEGORY_COLLAPSE, FOLDER_NESTING_FLATTENED, UNRESOLVED_INGREDIENT (snake_case string values matching the existing block's convention).
  </behavior>
  <action>
    Add the 3 new keys to the MAPPING_REASON_CODES Object.freeze block. Add a shared pure helper computeOpeningBalanceQuantity(legacyItem, itemLocationStocks) that returns the sum of itemLocationStocks quantity_on_hand when the array has ≥1 row, else legacyItem.current_stock when non-null, else null — this is the ONE stock/opening-balance value both mapItemToProduct (stock_count) and mapItemLocationStocksToOpeningBalance (quantity, Task 2) call so they can never drift. Implement mapItemFolderToProductFolder and mapItemToProduct as exported pure functions mirroring mapLegacyLocationToLocation's structure exactly (same 8-field return shape, same skipResult/classifyMappingConflict helpers — no ad-hoc objects, zero imports, zero SQL). Set stock_count from computeOpeningBalanceQuantity. Implement the category rule as an unconditional assignment to the retail value per D-09; reference the decision so no reviewer reintroduces a 3-case switch. Build attributes by iterating a fixed key-to-satellite-alias table and omitting absent keys per folding-design D-04. Create tests/fixtures/phase13/legacyProductRecords.js with fixtures: a full item (all 8 satellites + multiple barcodes + composition lines + item_location_stocks), a minimal item (name only), a folder with parent_id, a folder missing name, and items whose category is each of raw_material/supplies/service. Write tests/dataProductMappings.test.js asserting the flat category rule across all five legacy category values, barcodes-is-array, absent-satellite key omission, folder parent_id flattening finding + insert, and blank-name skip. Use classifyMappingConflict for every finding.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductMappings.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep resolves both `export function mapItemFolderToProductFolder` and `mapItemToProduct` in mappings.js.
    - Test proves mapItemToProduct returns category 'retail' for legacy category raw_material, supplies, and service.
    - Test proves attributes.barcodes is an array for a multi-barcode fixture, and a nutrition-less fixture has no nutrition key (Object.hasOwn false).
    - Test proves a folder with parent_id returns operation insert AND a finding whose reason_code equals MAPPING_REASON_CODES.FOLDER_NESTING_FLATTENED.
    - MAPPING_REASON_CODES contains LOSSY_CATEGORY_COLLAPSE, FOLDER_NESTING_FLATTENED, UNRESOLVED_INGREDIENT.
  </acceptance_criteria>
  <done>Folder + item-to-product mappers exist as pure functions with the flat category rule and folding contract, proven by unit tests; the 3 new reason codes are declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stock-movement, opening-balance, and embedding mappers</name>
  <files>apps/dgfy-migration-runner/src/data/mappings.js, apps/dgfy-migration-runner/tests/dataProductMappings.test.js, apps/dgfy-migration-runner/tests/fixtures/phase13/legacyProductRecords.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/mappings.js (mapTerminalRegistryEntryToTerminalIdentity at 697-755 for the resolved-dependency + informational-finding shape; TENANT_STATUS_MAP at 254 for the frozen-map declaration style)
    - backend/src/models/StockMovement.js (movement_type ENUM 8 values production_consumption/purchase_receipt/return/transfer/calculated_loss/adjustment/production_output/goods_issue; movement_id PK; quantity DECIMAL(24,12); nullable reference_id/reference_type)
    - backend/src/models/ItemEmbedding.js and backend/src/models/ItemLocationStock.js (vector TEXT + embedding_id; per-(item_id,location_id) quantity)
    - apps/dgfy-migration-runner/src/migrations/schema/20260716100000-extend-schema-for-legacy-migration.cjs (product_embeddings columns business_id/product_id/vector TEXT/legacy_embedding_id; inventory_movements natural-key unique index on business_id, product_id, reference_type, reference_id)
  </read_first>
  <behavior>
    - MOVEMENT_TYPE_MAP frozen: purchase_receipt to restock (D-01), calculated_loss to loss (D-02), adjustment to adjustment (D-03), goods_issue to sale (D-04), return to restock (D-05), production_consumption to adjustment (D-06), production_output to adjustment (D-07), transfer to null (D-08).
    - mapStockMovementToInventoryMovement(legacyMovement, context): when the mapped value is null (transfer, D-08) return a skip-style result with a LOSSY_CATEGORY_COLLAPSE finding (severity chosen so 13-05 verify can treat it as expected-lossy), no target row. Otherwise operation insert, entity_type 'inventory_movement', target_table 'inventory_movements', payload {business_id: context.expectedBusinessId, product_id: context.resolvedProductId, movement_type: mapped, quantity, reference_type 'legacy_stock_movement', reference_id String(movement_id), actor_account_id null, actor_staff_account_id null, before_snapshot null, after_snapshot null}. legacy_id_map_key legacy_table 'stock_movements', legacy_id movement_id. reference_type/reference_id are synthesized from movement_id, never copied from legacy reference fields (Pitfall 2).
    - mapItemLocationStocksToOpeningBalance(legacyItem, context): quantity = computeOpeningBalanceQuantity(legacyItem, context.itemLocationStocks) (the SAME shared helper mapItemToProduct uses for stock_count — sum of location quantities when ≥1 row, else legacy current_stock when non-null, else null). When that value is non-null, emit exactly ONE inventory_movements insert: movement_type 'adjustment', quantity = that value, reference_type 'legacy_opening_balance', reference_id String(item_id), product_id context.resolvedProductId. legacy_id_map_key legacy_table 'item_location_stocks', legacy_id item_id. This synthesizes an opening-balance row from current_stock even when the item has ZERO item_location_stocks rows, so no legacy current_stock is silently dropped (the checker-flagged current_stock=5 / no-location-stocks case emits quantity=5, matching stock_count=5). Skip (no row) ONLY when the helper returns null — i.e. both zero location-stock rows AND null current_stock — in which case mapItemToProduct also set stock_count null, so (stock_count ?? 0) == sum(opening-balance rows) == 0 stays coherent for verify.
    - mapItemEmbeddingToProductEmbedding(legacyEmbedding, context): operation insert, entity_type 'product_embedding', target_table 'product_embeddings', payload {business_id, product_id context.resolvedProductId, vector legacyEmbedding.vector copied verbatim (no parse/re-embed), legacy_embedding_id embedding_id}. legacy_id_map_key legacy_table 'item_embeddings', legacy_id embedding_id.
  </behavior>
  <action>
    Declare MOVEMENT_TYPE_MAP as a module-level Object.freeze alongside TENANT_STATUS_MAP. Implement the three mappers as exported pure functions. For the transfer collapse return a skip-style result carrying a LOSSY_CATEGORY_COLLAPSE finding via classifyMappingConflict (message names the legacy movement_id and that the new schema has no location dimension). Synthesize reference_type/reference_id from the legacy PK unconditionally. Extend tests/fixtures/phase13/legacyProductRecords.js with one movement per legacy movement_type (all 8), a movement with a NULL legacy reference field, an item with 3 item_location_stocks rows, an item with zero location stocks but non-null current_stock (the synthesized-fallback case), an item with zero location stocks AND null current_stock (the skip case), and an embedding fixture. Extend tests/dataProductMappings.test.js to assert every 8-to-5 mapping, the transfer no-row + finding, the NULL-legacy-reference synthesized natural key, the summed opening-balance quantity (3-location case), the synthesized opening-balance quantity equal to current_stock when location stocks are absent, the skip (no row) when both location stocks and current_stock are absent, that mapItemToProduct.stock_count equals mapItemLocationStocksToOpeningBalance.quantity for the same item across all three cases (the shared-helper agreement), and byte-identical embedding vector copy.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductMappings.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts each of the 8 legacy movement_type values maps to its D-01..D-08 target (transfer produces no target row).
    - Test asserts a movement with a null legacy reference field still produces reference_id equal to String(movement_id) and reference_type 'legacy_stock_movement'.
    - Test asserts mapItemLocationStocksToOpeningBalance sums 3 location rows into one movement whose quantity equals the sum and reference_type is 'legacy_opening_balance'.
    - Test asserts that for an item with zero item_location_stocks rows but current_stock=5, mapItemLocationStocksToOpeningBalance emits one opening-balance row with quantity 5 (synthesized from current_stock, not dropped), and that mapItemToProduct.stock_count for the same item equals 5 (shared-helper agreement).
    - Test asserts that for an item with zero location stocks AND null current_stock, mapItemLocationStocksToOpeningBalance emits no row and mapItemToProduct.stock_count is null.
    - Test asserts mapItemEmbeddingToProductEmbedding copies vector verbatim (strict equal) and sets legacy_embedding_id.
    - `grep -c "transfer: null" apps/dgfy-migration-runner/src/data/mappings.js` is at least 1.
  </acceptance_criteria>
  <done>The movement, opening-balance, and embedding mappers exist with deterministic non-null natural keys and the D-08 transfer-collapse finding, proven by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Scope unblock + committed movement-type remap decision doc</name>
  <files>apps/dgfy-migration-runner/src/data/mappings.js, docs/database/legacy-stock-movement-type-remap.md</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/mappings.js (OUT_OF_SCOPE_LEGACY_TABLES Object.freeze at 58-82 and its comment at 50-57; item_location_stocks is at line 65; pos_transaction_lines stays for Phase 14)
    - .planning/phases/13-product-inventory-migration/13-CONTEXT.md (D-01..D-08 lookup table with rationale; D-08 transfer-collapse rationale)
    - .planning/REQUIREMENTS.md (PIM-03 text — documented, reviewed 8-to-5 remapping decision with findings for lossy collapse)
  </read_first>
  <action>
    Remove the item_location_stocks entry from OUT_OF_SCOPE_LEGACY_TABLES and update the accompanying comment (50-57) to record that item_location_stocks is in scope from Phase 13 per D-10/PIM-05 (opening-balance rows), while pos_transaction_lines remains out of scope until Phase 14. Do not touch any other entry. Write docs/database/legacy-stock-movement-type-remap.md as the committed, reviewed PIM-03 decision record: an 8-to-5 table (each legacy movement_type to its target movement_type or "no row + finding"), citing D-01..D-08, the rationale for production_consumption/production_output to adjustment (productive use, not shrinkage; manufacturing distinction deferred), and the transfer to LOSSY_CATEGORY_COLLAPSE-finding rule (new schema has no location dimension). State that the table is the single source of truth mirrored by MOVEMENT_TYPE_MAP in mappings.js.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node -e "const m=require('fs').readFileSync('src/data/mappings.js','utf8'); const arr=m.match(/OUT_OF_SCOPE_LEGACY_TABLES = Object.freeze\(\[([\s\S]*?)\]\)/)[1]; if(arr.includes('item_location_stocks')) throw new Error('still out of scope'); if(!arr.includes('pos_transaction_lines')) throw new Error('phase-14 table wrongly removed'); console.log('scope OK')" && test -f ../../docs/database/legacy-stock-movement-type-remap.md</automated>
  </verify>
  <acceptance_criteria>
    - OUT_OF_SCOPE_LEGACY_TABLES no longer contains item_location_stocks and still contains pos_transaction_lines.
    - docs/database/legacy-stock-movement-type-remap.md exists and lists all 8 legacy movement_type values with their target (or no-row) mapping.
    - isInScopeLegacyTable('item_location_stocks') returns true; isInScopeLegacyTable('pos_transaction_lines') returns false.
  </acceptance_criteria>
  <done>item_location_stocks is unblocked for migration and the PIM-03 movement-type remap decision is committed as a reviewable doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy source row to mapper args | Untrusted legacy field values enter pure mapper functions; they are folded into attributes JSON and target payloads consumed downstream by apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01-01 | Tampering | mapItemToProduct attributes fold | medium | mitigate | Mappers are pure and construct plain JS objects only; no SQL is built here (module contract, header 10-33). attributes is emitted as a structured object for apply to JSON.stringify as a single bound param — the mapper never concatenates SQL. |
| T-13-01-02 | Tampering | inventory_movements natural key | high | mitigate | reference_type/reference_id are synthesized from the legacy PK unconditionally (never nullable legacy values), so the downstream unique index actually enforces idempotency (Pitfall 2). Verified by unit test on a NULL-legacy-reference fixture. |
| T-13-01-03 | Information Disclosure | embedding vector / attributes in findings | low | accept | Mappers place source values only in target_payload, never in finding message text; apply's recordResult already omits target_payload from reports. No new leak surface introduced here. |
| T-13-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). No install task; no [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductMappings.test.js` passes.
- All five new mappers are exported and pure (no import/require added to mappings.js beyond existing).
- OUT_OF_SCOPE_LEGACY_TABLES excludes item_location_stocks, retains pos_transaction_lines.
- docs/database/legacy-stock-movement-type-remap.md committed.
</verification>

<success_criteria>
The five Phase-13 mappers, three reason codes, MOVEMENT_TYPE_MAP, scope-unblock edit, and committed remap doc all exist; unit tests prove the flat category rule (D-09), the folding contract (PIM-02), the 8-to-5 movement remap + transfer collapse (PIM-03), deterministic natural keys (PIM-04), summed opening balance (PIM-05), and verbatim embedding copy (PIM-06).
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

- New mapper functions: `mapItemFolderToProductFolder`, `mapItemToProduct`, `mapStockMovementToInventoryMovement`, `mapItemLocationStocksToOpeningBalance`, `mapItemEmbeddingToProductEmbedding`
- New shared pure helper: `computeOpeningBalanceQuantity` (single source for products.stock_count + opening-balance quantity)
- New reason codes (MAPPING_REASON_CODES): `LOSSY_CATEGORY_COLLAPSE`, `FOLDER_NESTING_FLATTENED`, `UNRESOLVED_INGREDIENT`
- New frozen map: `MOVEMENT_TYPE_MAP`
- New entity_type strings: `product_folder`, `product`, `inventory_movement`, `product_embedding`
- New reference_type strings: `legacy_stock_movement`, `legacy_opening_balance`
- New committed doc: `docs/database/legacy-stock-movement-type-remap.md`
</artifacts>

<output>
Create `.planning/phases/13-product-inventory-migration/13-01-SUMMARY.md` when done.
</output>
