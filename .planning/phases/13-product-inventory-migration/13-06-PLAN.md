---
phase: 13-product-inventory-migration
plan: 06
type: execute
wave: 3
depends_on: ["13-03", "13-04", "13-05"]
files_modified:
  - apps/dgfy-migration-runner/tests/dataProductRehearsal.test.js
autonomous: false
requirements: [PIM-01, PIM-02, PIM-03, PIM-04, PIM-05, PIM-06]
user_setup:
  - service: mysql (rehearsal tenant)
    why: "Apply/verify against a real legacy-shaped tenant + its dgfy_business_* target requires working DB credentials and target config (the Phase-12 Plan-04 blocker)."
    env_vars:
      - name: DGFY_BUSINESS_DB_NAMES
        source: "Operator config — the dgfy_business_* target DB name(s) for the rehearsal tenant"
      - name: (migration-runner .env DB user)
        source: "A MySQL user provisioned on the rehearsal MySQL (lima-dgfy-dev only has sku_inventory_user; EC2 rehearsal per MEMORY arch-mismatch note) with grants on the legacy tenant source DB and the dgfy_business_* target DB"
    dashboard_config:
      - task: "Reset the target dgfy_business_* product-domain tables AND the dgfy_migration_meta rows (legacy_id_map, data_checkpoints, data_quality_findings) for run_scope='data-migration' between clean-slate rehearsal attempts"
        location: "Rehearsal MySQL — otherwise a completed checkpoint short-circuits re-processing into a verify-only pass"

must_haves:
  truths:
    - "A dry-run against a real legacy-shaped tenant plans every product_folder insert before any product insert."
    - "Apply migrates items to products (6 promoted fields + folded attributes incl. barcodes array + BOM composition), stock_movements to inventory_movements (8-to-5, transfer collapsed to a finding), item_location_stocks to one summed opening-balance row per product, and item_embeddings to product_embeddings verbatim."
    - "A second apply run inserts zero duplicate inventory_movements rows and verify reports data_migration_ok true with only expected-lossy findings open."
    - "Verification's sum-by-type totals show each product's stock_count equal to the sum of its legacy item_location_stocks, and every migrated product has exactly one product_embeddings row carrying its original vector unchanged."
  artifacts:
    - apps/dgfy-migration-runner/tests/dataProductRehearsal.test.js
    - .planning/phases/13-product-inventory-migration/13-06-SUMMARY.md
  key_links:
    - "dry-run -> apply -> retry-apply (zero new rows) -> verify against a real tenant proves the end-to-end pipeline."
    - "meta reset procedure between rehearsals prevents checkpoint short-circuit."
---

<objective>
Prove the Phase-13 product/inventory migration end-to-end against a real legacy-shaped tenant and its dgfy_business_* target: dry-run, apply, retry-apply (zero new rows), and verify (green with only expected-lossy findings), capturing the machine-readable + summary evidence. This is the re-rehearsal gate before Sales History (Phase 14) can begin.

Purpose: The mapper code (13-01..13-05) is unit-proven, but the milestone's fidelity goal requires a real-data rehearsal. This plan runs it and records evidence, blocked only by the operator DB-credential/target-config prerequisite carried since Phase 12 Plan 04.

Output: an ENV-gated end-to-end rehearsal test (skips cleanly when creds absent) plus a committed SUMMARY capturing the dry-run/apply/retry/verify evidence and the meta-reset procedure.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-product-inventory-migration/13-RESEARCH.md
@apps/dgfy-migration-runner/src/data/apply.js
@apps/dgfy-migration-runner/src/data/verifyData.js
@apps/dgfy-migration-runner/src/commands/data.js
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator provisions rehearsal DB creds + target config</name>
  <action>Pause for the operator to provision the rehearsal MySQL user, set the migration-runner .env DB user/password and DGFY_BUSINESS_DB_NAMES, confirm the Phase-12 schema is applied to the target, and reset the target product-domain tables + dgfy_migration_meta rows for a clean-slate rehearsal. Do not proceed to the live run until the operator confirms.</action>
  <what-built>The full product/inventory mapper pipeline (13-01..13-05) is ready; running it against a real tenant needs operator-provisioned DB access that Claude cannot create.</what-built>
  <how-to-verify>
    1. Provision a MySQL user (rehearsal env — EC2 per the arch-mismatch MEMORY note; lima-dgfy-dev only has sku_inventory_user) with read on the legacy tenant source DB and read/write on the dgfy_business_* target DB.
    2. Set the migration-runner .env DB user/password to that user and set DGFY_BUSINESS_DB_NAMES to the rehearsal tenant's dgfy_business_* database name(s).
    3. Confirm the dgfy_business_* target already has the Phase-12 schema applied (products.attributes + 6 typed cols, product_embeddings, inventory_movements natural-key index) — Phase 12 Plan 04 applied this; re-run schema verify if unsure.
    4. Ensure the target product-domain tables and the dgfy_migration_meta rows (legacy_id_map, data_checkpoints, data_quality_findings for run_scope='data-migration') are reset for a clean-slate rehearsal.
  </how-to-verify>
  <resume-signal>Type "creds ready" (with the tenant/target names) or "skip live — unit evidence only" to proceed without the live run.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: ENV-gated end-to-end rehearsal (dry-run to apply to retry to verify)</name>
  <files>apps/dgfy-migration-runner/tests/dataProductRehearsal.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/commands/data.js (how dry-run/apply/verify are orchestrated + how the target manifest and connections are opened)
    - apps/dgfy-migration-runner/tests/phase03Integration.test.js (the closest end-to-end / integration test precedent + any ENV-gate/skip pattern used for live-DB tests)
    - apps/dgfy-migration-runner/src/data/verifyData.js (buildDataVerificationSections — the data_migration_ok verdict shape to assert)
    - .planning/phases/13-product-inventory-migration/13-RESEARCH.md (Runtime State Inventory — the meta-reset requirement between rehearsals; Environment Availability blocker)
  </read_first>
  <behavior>
    - The test is ENV-gated: when the rehearsal DB env vars are absent it skips cleanly (describe.skip / conditional) so CI stays green; when present it runs the real sequence against the configured tenant.
    - Sequence: (1) dry-run — assert every planned product_folder entry index precedes every product entry index; (2) apply — assert items migrated to products with promoted fields + folded attributes (barcodes array present for a multi-barcode item) + resolved BOM composition, stock_movements to inventory_movements with transfer collapsed to a finding, one summed opening-balance row per product, embeddings copied verbatim; (3) retry apply — assert zero additional inventory_movements rows written on the second run; (4) verify — assert data_migration_ok is true with only expected-lossy findings open, sum-by-type totals present, stock_count equals summed item_location_stocks per product, exactly one product_embeddings row per product with an unchanged vector.
  </behavior>
  <action>
    Write tests/dataProductRehearsal.test.js as an ENV-gated end-to-end test that drives the real dry-run/apply/verify command paths (or their exported orchestration functions) against the operator-provisioned rehearsal tenant, asserting the four-stage sequence above. Guard the whole suite behind an env check so it is a no-op without creds. If the operator chose "skip live", still commit the test (skipped) and record in the SUMMARY that the live proof is pending operator creds, with the exact reset + run commands an operator will use. Capture the dry-run/apply/verify machine-readable report paths and the verify verdict in the SUMMARY.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductRehearsal.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and skips cleanly (exit 0) when rehearsal env vars are unset.
    - When run with creds: dry-run proves folders-before-products ordering; apply proves items/movements/opening-balance/embeddings migrated per PIM-01..06; a second apply writes zero additional inventory_movements rows; verify returns data_migration_ok true with only expected-lossy findings.
    - The SUMMARY records the verify verdict (or the pending-creds status), the report file paths, and the meta-reset procedure.
  </acceptance_criteria>
  <done>The end-to-end rehearsal test exists and (with operator creds) proves dry-run/apply/retry/verify fidelity against a real tenant; without creds it skips and the SUMMARY records the pending live proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the rehearsal fidelity evidence</name>
  <action>Pause for the operator to review the dry-run/apply/retry/verify evidence and confirm end-to-end fidelity before the phase is marked complete. Do not proceed until the operator approves or lists gaps.</action>
  <what-built>The rehearsal test drove dry-run/apply/retry/verify against the real tenant and produced machine-readable + summary evidence.</what-built>
  <how-to-verify>
    1. Open the verify report and confirm data_migration_ok is true with only expected-lossy (transfer collapse / folder flatten) findings open.
    2. Confirm the apply retry summary shows rows_written 0 for inventory_movements on the second run.
    3. Spot-check one migrated product: 6 promoted fields populated, attributes.barcodes is an array, attributes.composition ingredient ids resolve to real products, stock_count equals the summed legacy item_location_stocks, and exactly one product_embeddings row with the original vector.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the fidelity gaps found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator creds to real target DB | This is the only plan that writes a real dgfy_business_* DB; wrong creds/target or an un-reset meta store corrupts the rehearsal verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06-01 | Tampering | idempotency-bypass data corruption on retry | high | mitigate | The retry stage explicitly asserts zero additional inventory_movements rows; a non-zero count fails the rehearsal (catches any residual natural-key gap from 13-03). |
| T-13-06-02 | Elevation | writing to the wrong / a legacy DB | high | mitigate | Reuses the runner's assertTargetDbNameAllowed target-name guard + createBusinessTargetConnection; DGFY_BUSINESS_DB_NAMES scopes the run; the human-action checkpoint confirms the target before apply. |
| T-13-06-03 | Tampering | stale meta short-circuits re-processing | medium | mitigate | The checkpoint and SUMMARY document the mandatory meta-reset (legacy_id_map/data_checkpoints/data_quality_findings) between clean-slate rehearsals. |
| T-13-06-04 | Information Disclosure | secrets/embeddings in evidence reports | low | mitigate | Reuses recordResult which omits target_payload/findings text; embeddings/attributes never echoed into report rows. |
| T-13-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductRehearsal.test.js` passes (skips cleanly without creds).
- Full suite green: `cd apps/dgfy-migration-runner && npm test`.
- Architecture check green: `cd apps/dgfy-migration-runner && npm run check:architecture`.
- With creds: verify verdict data_migration_ok true; retry apply writes zero new inventory_movements rows.
</verification>

<success_criteria>
The product/inventory migration is proven end-to-end against a real legacy-shaped tenant (or, if creds are pending, the skipped test + SUMMARY records the exact operator run + reset procedure), satisfying the re-rehearsal gate before Phase 14.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

- New ENV-gated end-to-end test: `apps/dgfy-migration-runner/tests/dataProductRehearsal.test.js`
- Rehearsal evidence + meta-reset procedure recorded in `13-06-SUMMARY.md`
- No new schema migration (Phase 12 already shipped the target schema) — no [BLOCKING] schema push task required
</artifacts>

<output>
Create `.planning/phases/13-product-inventory-migration/13-06-SUMMARY.md` when done.
</output>
