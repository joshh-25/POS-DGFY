---
phase: 13-product-inventory-migration
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - apps/dgfy-migration-runner/src/data/apply.js
  - apps/dgfy-migration-runner/tests/dataProductApply.test.js
autonomous: true
requirements: [PIM-01, PIM-02, PIM-04, PIM-05, PIM-06]

must_haves:
  truths:
    - "Per-tenant apply writes in the fixed order product_folder, product (pass-1 insert), product (pass-2 BOM UPDATE), inventory_movement (from stock_movements), inventory_movement (opening-balance), product_embedding."
    - "products.folder_id, inventory_movements.product_id, product_embeddings.product_id, and BOM ingredient ids are all resolved via legacy_id_map (findLegacyIdMap), never a raw legacy id; string dgfy_id coerced with Number() for INT FK columns."
    - "Re-running apply against an already-migrated tenant inserts zero duplicate inventory_movements rows (skip-if-mapped + natural-key reconcile), and the two-pass BOM UPDATE rewrites the identical resolved composition array."
    - "The two-pass BOM UPDATE binds the JSON via replacements (never string-interpolates attributes); an unresolved ingredient records an UNRESOLVED_INGREDIENT orphan finding that resolveDataQualityFindings clears on a later run."
    - "products.stock_count is set to the summed item_location_stocks so the scalar matches the opening-balance ledger row (no stock_count trigger exists on inventory_movements)."
  artifacts:
    - apps/dgfy-migration-runner/src/data/apply.js
    - apps/dgfy-migration-runner/tests/dataProductApply.test.js
  key_links:
    - "ENTITY_TARGET_CONFIG new rows drive findExistingTargetRow natural-key reconciliation."
    - "findLegacyIdMap('item_folders'/'items') resolves folder_id/product_id like the existing staff/location resolution at apply.js 592-610/636-647."
    - "UPDATE products SET attributes = ? uses a single bound JSON.stringify param."
---

<objective>
Extend `src/data/apply.js` to migrate the product domain: add 4 ENTITY_TARGET_CONFIG rows, read each tenant's product snapshot (13-02), and write folder to product (pass-1) to product (pass-2 BOM UPDATE) to inventory_movement to opening-balance to product_embedding in the fixed order, reusing the battle-tested writeMappedTargetRow / applyTenantEntityBatch / findLegacyIdMap machinery.

Purpose: apply.js is the only path that writes dgfy_* target databases. This plan wires the new mappers into the checkpointed, idempotent apply pipeline and adds the first non-insert write path in the file (the two-pass BOM UPDATE).

Output: extended runApplyTransformations per-tenant loop, 4 ENTITY_TARGET_CONFIG entries, the two-pass BOM UPDATE path, stock_count sync, and an apply integration test proving ordering + idempotency + BOM resolution against the in-memory fake SQL engine.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-product-inventory-migration/13-CONTEXT.md
@.planning/phases/13-product-inventory-migration/13-RESEARCH.md
@.planning/phases/13-product-inventory-migration/13-PATTERNS.md
@apps/dgfy-migration-runner/src/data/mappings.js
@apps/dgfy-migration-runner/src/data/legacySource.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ENTITY_TARGET_CONFIG + fixed-order product-domain apply</name>
  <files>apps/dgfy-migration-runner/src/data/apply.js, apps/dgfy-migration-runner/tests/dataProductApply.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/apply.js (whole file — ENTITY_TARGET_CONFIG at 101-111; writeMappedTargetRow at 234-309 including the skip-if-mapped branch 262-282 and natural-key reconcile 284-294; applyTenantEntityBatch at 342-395; applyBatchAndRecord at 422-428; the per-tenant loop 485-661; the staff/location resolution pattern at 592-610 and 636-647; imports 52-77)
    - apps/dgfy-migration-runner/src/data/mappings.js (the five new mappers and their context params: resolvedFolderId, resolvedProductId, expectedBusinessId, itemLocationStocks — from 13-01)
    - apps/dgfy-migration-runner/src/data/legacySource.js (readLegacyProductSnapshot return shape — from 13-02)
    - apps/dgfy-migration-runner/tests/dataApply.test.js (the in-memory fake SQL engine + jest.unstable_mockModule pattern to reuse for the new test file)
  </read_first>
  <behavior>
    - Add to ENTITY_TARGET_CONFIG: product_folder {primaryKey:'id', naturalKeyColumns:['business_id','name']}; product {primaryKey:'id', naturalKeyColumns:['id']} (see idempotency note); inventory_movement {primaryKey:'id', naturalKeyColumns:['business_id','product_id','reference_type','reference_id']}; product_embedding {primaryKey:'id', naturalKeyColumns:['product_id']}.
    - product idempotency: products.id is AUTO_INCREMENT and sku_code is non-unique — there is no target-side natural key. Rely on the legacy_id_map skip-if-mapped branch as the sole product idempotency guard (documented limitation class, same as location). Give product a config whose natural-key lookup always returns null, or omit its config so the `config ? ... : null` short-circuit at line 284 applies — do not fabricate a false key.
    - Per-tenant order (mirroring the existing loop): read the tenant product snapshot, then batch product_folder (import mapItemFolderToProductFolder), then product pass-1 (mapItemToProduct with resolvedFolderId looked up from legacy_id_map item_folders, and itemLocationStocks from the snapshot for stock_count), then product pass-2 BOM UPDATE (Task 2), then inventory_movement from stock_movements (mapStockMovementToInventoryMovement with resolvedProductId from legacy_id_map items), then opening-balance inventory_movement (mapItemLocationStocksToOpeningBalance), then product_embedding (mapItemEmbeddingToProductEmbedding with resolvedProductId). Each entity type flows through applyBatchAndRecord so it gets its own (run_scope, legacy_tenant_id, entity_type) checkpoint automatically.
    - Dependency resolution: copy the staffAccountId/locationId pattern — findLegacyIdMap for item_folders to get folder_id, for items to get product_id; coerce dgfy_id string to Number() for INT FK columns.
    - stock_count sync: because inventory_movements has append-only triggers but NO stock_count trigger (verified in the commerce-foundation migration), set products.stock_count in the product pass-1 payload to the summed item_location_stocks so the scalar equals the opening-balance ledger quantity.
  </behavior>
  <action>
    Extend the mappings import to add the five new mapper names. Add the 4 ENTITY_TARGET_CONFIG rows. In runApplyTransformations' per-tenant loop, after the existing terminal_identity batch (or in a clearly product-domain section), call readLegacyProductSnapshot(tenantSequelize) and run the six-step ordered batches via applyBatchAndRecord, resolving folder_id/product_id via findLegacyIdMap exactly as staff/location are resolved. Create tests/dataProductApply.test.js reusing dataApply.test.js's fake SQL engine and module-mock pattern; seed a tenant snapshot with 1 folder, 2 items (one referencing the folder, one with 2 barcodes), stock movements incl. a transfer, an item with location stocks, and an embedding. Assert: folders are written before products (folder legacy_id_map row exists before the product insert resolves folder_id); products.folder_id is the resolved numeric target id; inventory_movements product_id is the resolved product id; the transfer produced no inventory_movements row; the opening-balance row exists with quantity = summed location stocks; product stock_count equals that sum; embedding row exists.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductApply.test.js</automated>
  </verify>
  <acceptance_criteria>
    - ENTITY_TARGET_CONFIG contains product_folder, product, inventory_movement, product_embedding.
    - Test proves a product row's folder_id equals the numeric dgfy id of its migrated product_folder (resolved via legacy_id_map, not the raw legacy folder_id).
    - Test proves inventory_movements rows carry the resolved product_id and no row exists for a transfer movement.
    - Test proves the opening-balance inventory_movement quantity equals the sum of the item's location stocks and products.stock_count equals that sum.
    - Test proves a product_embedding row is written with the resolved product_id.
  </acceptance_criteria>
  <done>The product domain migrates through the checkpointed apply pipeline in the correct dependency order with all FKs resolved via legacy_id_map, proven by an apply integration test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-pass BOM UPDATE + idempotency retry proof</name>
  <files>apps/dgfy-migration-runner/src/data/apply.js, apps/dgfy-migration-runner/tests/dataProductApply.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/apply.js (insertTargetRow at 164-178 for the transaction/parameterization style; recordDataQualityFinding + resolveDataQualityFindings usage at 268-279 and 359-368; imports 70-77)
    - apps/dgfy-migration-runner/src/metadata/dataState.js (findLegacyIdMap, recordDataQualityFinding, resolveDataQualityFindings signatures — REUSE verbatim, do not edit this file)
    - .planning/phases/13-product-inventory-migration/13-RESEARCH.md (Pattern 3 two-pass BOM; Pitfall 4; Security Domain "bind not interpolate")
    - docs/database/legacy-product-attributes-folding-design.md (D-02 composition resolves ingredient_id to migrated products.id; log a finding for unresolved ingredient)
  </read_first>
  <behavior>
    - After the whole product pass-1 insert batch for a tenant, run pass-2: for each snapshot item with productCompositions, resolve the parent product via findLegacyIdMap(items, item_id); for each composition line resolve findLegacyIdMap(items, ingredient_id). Build a resolved composition array of {ingredient_product_id: Number(ingMap.dgfy_id), composition_type, quantity_required, unit_of_measure}. Re-read the product's current attributes, merge the composition key, and UPDATE products SET attributes = ?, updated_at = ? WHERE id = ? binding JSON.stringify(mergedAttributes) as a single replacements param (never interpolated).
    - Unresolved ingredient: recordDataQualityFinding with reason_code UNRESOLVED_INGREDIENT (severity 'orphan'); a later run once the ingredient exists resolves it and resolveDataQualityFindings clears it. Idempotent by construction: a re-run rewrites the identical resolved array.
    - Idempotency: re-running the whole apply against an already-migrated tenant inserts zero new inventory_movements rows (natural-key reconcile + legacy_id_map skip) and re-writes the identical BOM attributes.
  </behavior>
  <action>
    Implement the two-pass BOM UPDATE as the first non-insert write path in apply.js, placed immediately after the product pass-1 batch and before the inventory_movement batches. Bind the JSON payload; never build attributes via string concatenation. Use findLegacyIdMap for both parent and ingredient resolution and Number()-coerce ids. Emit UNRESOLVED_INGREDIENT orphan findings via recordDataQualityFinding for genuinely missing ingredients. Extend tests/dataProductApply.test.js: (a) an item whose composition references a sibling item processed later in the same run resolves correctly after pass-2 (order-independence); (b) a composition referencing a missing item records an UNRESOLVED_INGREDIENT orphan finding and does not fail the item; (c) running runApplyTransformations twice against the same fake target inserts zero additional inventory_movements rows on the second run and the products.attributes.composition array is byte-identical across runs; (d) assert the UPDATE was issued with the attributes value passed via replacements, not interpolated into the SQL string.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductApply.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Test proves a composition referencing a later-processed sibling item resolves to that sibling's migrated product id after pass-2 (two-pass order independence).
    - Test proves a missing ingredient records an UNRESOLVED_INGREDIENT orphan finding and the parent product still migrates.
    - Test proves a second full apply run inserts zero additional inventory_movements rows and produces a byte-identical attributes.composition array.
    - Test proves the products UPDATE binds the attributes JSON via replacements (the SQL string contains a `?` placeholder, not the JSON literal).
  </acceptance_criteria>
  <done>The two-pass BOM UPDATE resolves ingredient ids order-independently, binds its JSON, records orphan findings for gaps, and the whole apply is idempotent on retry — all proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mapper payload to target DB write | apply.js writes untrusted-origin legacy data (folded attributes, embeddings) into dgfy_business_* tables; the two-pass BOM UPDATE is the first dynamic UPDATE path. |
| meta DB vs target DB | legacy_id_map (meta) and target rows are separate connections — the interruption window that idempotency must survive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03-01 | Tampering | two-pass BOM UPDATE products SET attributes | high | mitigate | JSON.stringify(mergedAttributes) is bound as a single replacements param; the SQL string is a literal with a `?` placeholder. Verified by a test asserting the value is passed via replacements, not interpolated. |
| T-13-03-02 | Tampering | idempotency bypass / duplicate ledger rows | high | mitigate | Deterministic non-null natural keys (13-01) + findExistingTargetRow reconcile + legacy_id_map skip-if-mapped. Verified by a double-run test asserting zero additional inventory_movements rows. |
| T-13-03-03 | Elevation | writing to wrong/legacy DB | medium | mitigate | Reuses createBusinessTargetConnection + the existing target-name guard; no new connection path bypasses assertTargetDbNameAllowed. |
| T-13-03-04 | Information Disclosure | secrets/attributes in reports | low | mitigate | recordResult (397-410) already omits target_payload/findings text; the product-domain entities reuse it unchanged. |
| T-13-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductApply.test.js` passes.
- No new UPDATE/INSERT in apply.js interpolates a value; all use replacements.
- Full test suite unaffected: `npm test` still green (run in the rehearsal plan or locally).
</verification>

<success_criteria>
The product domain applies in dependency order with all FKs resolved via legacy_id_map, the two-pass BOM UPDATE resolves ingredients order-independently while binding its JSON, unresolved ingredients raise self-healing orphan findings, and a retried apply inserts zero duplicate inventory_movements rows.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

- New ENTITY_TARGET_CONFIG keys: `product_folder`, `product`, `inventory_movement`, `product_embedding`
- New checkpoint keys (per run_scope, legacy_tenant_id, entity_type): `product_folder`, `product`, `inventory_movement`, `product_embedding`
- New apply write path: two-pass BOM `UPDATE products SET attributes = ?` (first non-insert write in apply.js)
- Reused finding reason code at write time: `UNRESOLVED_INGREDIENT`
</artifacts>

<output>
Create `.planning/phases/13-product-inventory-migration/13-03-SUMMARY.md` when done.
</output>
