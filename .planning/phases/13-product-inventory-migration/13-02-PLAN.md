---
phase: 13-product-inventory-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-migration-runner/src/data/legacySource.js
  - apps/dgfy-migration-runner/tests/dataProductSource.test.js
autonomous: true
requirements: [PIM-01, PIM-02, PIM-05, PIM-06]

must_haves:
  truths:
    - "readLegacyProductSnapshot() eager-reads items + its 8 satellites + item_barcodes + product_composition + item_folders + stock_movements + item_location_stocks + item_embeddings via raw parameterized SELECTs against the already-scoped legacy tenant connection."
    - "Satellites are stitched onto each item by item_id in JS (no Sequelize include, no backend/ import) matching the folding-design association aliases."
    - "Table names are literal in-code; every value is bound via replacements — zero string interpolation of legacy data (V5)."
  artifacts:
    - apps/dgfy-migration-runner/src/data/legacySource.js
    - apps/dgfy-migration-runner/tests/dataProductSource.test.js
  key_links:
    - "snapshot.items[].nutrition/allergens/... aliases feed mapItemToProduct's attributes fold (13-01)."
    - "snapshot.items[].itemLocationStocks feeds mapItemLocationStocksToOpeningBalance and mapItemToProduct.stock_count."
    - "snapshot object shape is consumed by apply.js (13-03) and dryRun.js (13-04)."
---

<objective>
Add `readLegacyProductSnapshot(tenantSequelize)` to `src/data/legacySource.js`: a read-only raw-SELECT reader that loads the product domain (items + 8 satellites + barcodes + BOM + folders + stock_movements + item_location_stocks + item_embeddings) from one already-scoped legacy tenant connection and stitches satellites onto each item in JS.

Purpose: The runner imports zero backend/ code, so there is no Sequelize include. This is the only new plumbing point that reads the product-domain source, feeding the pure mappers (13-01) with fully-stitched item snapshots.

Output: One new exported reader + a unit test proving the stitch shape and the SELECT/parameterization contract against a fake connection.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-product-inventory-migration/13-RESEARCH.md
@.planning/phases/13-product-inventory-migration/13-PATTERNS.md
@docs/database/legacy-product-attributes-folding-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: readLegacyProductSnapshot raw-SELECT reader + stitch</name>
  <files>apps/dgfy-migration-runner/src/data/legacySource.js, apps/dgfy-migration-runner/tests/dataProductSource.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/legacySource.js (whole file — copy readLegacyTenantSnapshot at 135-147: one `await tenantSequelize.query('SELECT * FROM <table>')` per table, destructure [rows], stitch in JS, return one snapshot object; the isolation contract header 1-30; the scoped-empty guard style at 51-53)
    - docs/database/legacy-product-attributes-folding-design.md (association alias names: nutrition, allergens, physicalProperties, shelfLife, packaging, qualityControl, regulatoryCompliance, costBreakdown, productCompositions, barcodes — and that item_embeddings/item_location_stocks are NOT part of the attributes fold but ARE read here)
    - backend/src/models/index.js lines 236-261 (the Item satellite + folder + composition + barcode associations and their foreign keys — item_id for satellites, product_id/ingredient_id for ProductComposition)
    - apps/dgfy-migration-runner/tests/dataMappings.test.js (test/import conventions to follow for the new source test)
  </read_first>
  <behavior>
    - readLegacyProductSnapshot(tenantSequelize) issues one SELECT per source table: items, item_nutrition, item_allergens, item_physical_properties, item_shelf_life, item_packaging, item_quality_control, item_regulatory_compliance, item_cost_breakdown, item_barcodes, product_composition, item_folders, stock_movements, item_location_stocks, item_embeddings.
    - It stitches satellites onto each item keyed by item_id: 1:1 aliases (nutrition, physicalProperties, shelfLife, packaging, qualityControl, regulatoryCompliance, costBreakdown) attach the single matching row; 1:many aliases (allergens, barcodes, itemLocationStocks) attach arrays; productCompositions attaches the composition lines where product_composition.product_id === item_id (this item as finished-product parent).
    - Returns a single object: { items (each with stitched aliases), itemFolders, stockMovements, itemEmbeddings }. When a table is empty it yields an empty array, never an unscoped enumeration.
    - Zero backend/ imports; table names literal; any WHERE value bound via replacements.
  </behavior>
  <action>
    Implement readLegacyProductSnapshot mirroring readLegacyTenantSnapshot's structure. Read each table with a literal `SELECT * FROM <table>` (the connection is already scoped to one legacy tenant DB by createLegacyTenantSourceConnection, which rejects dgfy_* names before this runs — same contract readLegacyTenantSnapshot relies on). Build item_id-keyed lookup maps for each satellite, then attach aliases onto each item using the exact folding-design alias names, so mapItemToProduct can iterate them. Attach itemLocationStocks and productCompositions arrays onto each item. Return the snapshot object. Update the module header allowed-tables comment to include the product-domain tables now in scope (Phase 13). Write tests/dataProductSource.test.js with a fake tenantSequelize whose query() returns canned rows per table; assert the stitch: an item gets its nutrition object, barcodes array, itemLocationStocks array, and productCompositions array; an item with no satellites gets none of those aliases attached (or empty arrays for 1:many — match the mapper's expectation). Assert no query string contains an interpolated value.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductSource.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function readLegacyProductSnapshot" apps/dgfy-migration-runner/src/data/legacySource.js` resolves.
    - Test proves an item fixture is returned with its nutrition (object), barcodes (array), itemLocationStocks (array), and productCompositions (array) stitched by item_id.
    - Test proves the reader issues a SELECT for item_location_stocks and item_embeddings and stock_movements and item_folders.
    - No `import` from backend/ appears in legacySource.js (`grep -c "backend/" apps/dgfy-migration-runner/src/data/legacySource.js` is 0).
    - No query passed to the fake connection contains a `${` interpolation of a value.
  </acceptance_criteria>
  <done>readLegacyProductSnapshot reads and stitches the full product domain with parameterized, backend-isolated raw SELECTs, proven by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy tenant DB to snapshot reader | The reader issues SQL against a legacy tenant MySQL DB; table names must never come from legacy data, and connection scope must stay legacy-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02-01 | Tampering | table-name interpolation | high | mitigate | Every table name is a literal in-code string; no legacy value is ever interpolated into SQL. WHERE values (if any) use replacements. Verified by test asserting no `${` value interpolation. |
| T-13-02-02 | Elevation | wrong-DB read | medium | mitigate | Reuses createLegacyTenantSourceConnection which rejects dgfy_* names before this module sees the connection; the reader never opens its own connection. |
| T-13-02-03 | Information Disclosure | embedding/attribute data in logs | low | accept | Reader returns data to the caller only; it never writes reports or logs source values. |
| T-13-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataProductSource.test.js` passes.
- legacySource.js has zero backend/ imports and no interpolated-value SQL.
</verification>

<success_criteria>
readLegacyProductSnapshot returns fully-stitched item snapshots (satellites + barcodes + BOM + location stocks) plus folders/movements/embeddings, using only parameterized raw SELECTs against the scoped legacy connection, proven by unit tests.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

- New legacySource reader function: `readLegacyProductSnapshot`
- Snapshot object shape: `{ items: [{ ...stitched aliases, itemLocationStocks, productCompositions }], itemFolders, stockMovements, itemEmbeddings }`
</artifacts>

<output>
Create `.planning/phases/13-product-inventory-migration/13-02-SUMMARY.md` when done.
</output>
