---
phase: 06-release-evidence-and-rehearsal-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dgfy-seam-smoke.js
  - scripts/dgfy-seam-smoke.test.js
autonomous: true
requirements: [CMP-04]

must_haves:
  truths:
    - "The seam-smoke runner reads every active entry in docs/architecture/compatibility-seams.json and runs that entry's tests[], picking up future seams with no code change to the runner (D-04)."
    - "A seam whose required integration env/credentials are absent fails closed (gate = not release-ready) rather than reporting PASS on skipped integration tests (Pitfall 1 — the SC3 false-pass hazard)."
    - "Per-seam pass/fail is emitted to a machine-readable seam_smoke.json under the evidence directory the orchestrator passes in."
  artifacts:
    - scripts/dgfy-seam-smoke.js
    - scripts/dgfy-seam-smoke.test.js
    - "seam_smoke.json (per-seam { id, ok, detail } results, written at runtime)"
  key_links:
    - "dgfy-seam-smoke.js reuses loadManifest exported from scripts/check-compat-seams.js (no new JSON loader)"
    - "seam-id → required-integration-env map includes db-continuity-legacy-backup → RUN_CONTINUITY_INTEGRATION (+ SOURCE_DB_* creds)"
    - "test-path → owning-package test-runner dispatch (migration-runner jest case) with a documented extension point"
---

<objective>
Deliver SC3: a generic, manifest-driven compatibility-seam smoke runner that iterates every active seam in Phase 5's compatibility-seams.json, executes each seam's tests[] through the owning package's test runner WITH the required integration env enabled, and reports pass/fail per seam — failing closed when required env/credentials are missing so a skipped integration test can never masquerade as a passing smoke check.

Purpose: Prove touched compatibility seams still preserve current behavior (CMP-04 / SC3), automatically covering seams registered in later phases with zero runner changes (D-04).
Output: A CommonJS root script `scripts/dgfy-seam-smoke.js` plus a `node --test` unit suite; it writes `seam_smoke.json` the Wave 2 orchestrator converts into a gate entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-CONTEXT.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-RESEARCH.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-PATTERNS.md
@scripts/check-compat-seams.js
@scripts/gate-release-local.js
@docs/architecture/compatibility-seams.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the generic manifest-driven seam-smoke runner (CommonJS, fail-closed)</name>
  <files>scripts/dgfy-seam-smoke.js</files>
  <read_first>
    - scripts/check-compat-seams.js lines 93-103 (loadManifest definition) and 390-403 (module.exports including loadManifest)
    - docs/architecture/compatibility-seams.json (the one seam db-continuity-legacy-backup; tests: ["apps/dgfy-migration-runner/tests/verifyContinuity.test.js"]; status enum + fields id/type/status/rationale/tests/rollback/removal_criteria)
    - scripts/gate-release-local.js lines 10-22 (runCommand spawnSync helper), 35-46 (addGate/ensureDir), 46-49 (evidence dir convention)
    - apps/dgfy-migration-runner/tests/verifyContinuity.test.js lines 23-35 (RUN_CONTINUITY_INTEGRATION default-skip gate — the false-pass hazard)
    - .planning/phases/06-release-evidence-and-rehearsal-gates/06-RESEARCH.md Pitfall 1 & 2; 06-PATTERNS.md "scripts/dgfy-seam-smoke.js" section
  </read_first>
  <action>Create CommonJS script scripts/dgfy-seam-smoke.js (shebang `#!/usr/bin/env node`, `require`, 2-space style matching sibling gate scripts). Duplicate the inline `runCommand(command, args)` spawnSync helper from gate-release-local.js (the codebase duplicates this helper per script — follow that convention). Steps:
    (1) Manifest load + active filter (D-04): `const { loadManifest } = require('./check-compat-seams.js');` then load docs/architecture/compatibility-seams.json and filter `manifest.seams` to `status === 'active'` only (Assumption A3 — 'accepted' is excluded; the single current seam is 'active'). Do NOT write a new JSON parser.
    (2) Define a seam-id → required-integration-env map and a test-path → owning-package runner map as small, explicit, documented constants (Open Question 3 / Pitfall 1 & 2). Seed the env map with `db-continuity-legacy-backup` requiring `RUN_CONTINUITY_INTEGRATION=true` (and depending on SOURCE_DB_* credentials). Seed the runner map so a testPath under `apps/dgfy-migration-runner/tests/` dispatches via `npm --prefix apps/dgfy-migration-runner test -- <testPath>` (jest). Add a clearly-labelled extension point (a lookup with a documented default) for future seams in other packages — do not pre-build runners no active seam uses.
    (3) Per-seam execution (SC3, fail-closed — Pitfall 1): for each active seam, resolve its required integration env from the map. If any required env var / credential is absent, record the seam result as `{ id, ok:false, detail }` with a detail explaining the missing integration env — a skipped integration test is NOT a pass. When the required env IS present, set it in the child env and run each testPath through its resolved package runner; a non-zero exit → `ok:false`. A seam id with no runner mapping and no extension-point default → `ok:false` (unknown = fail-closed), never a silent pass.
    (4) Output: write per-seam `{ id, ok, detail }[]` plus an overall `ok` (all seams ok) to `seam_smoke.json` under an evidence directory resolved from `--evidence-dir <dir>` / `SEAM_SMOKE_EVIDENCE_DIR` (default `.tmp/release-gates/<sha>/`, reusing the existing convention via `git rev-parse HEAD` when no sha env is set). Print a per-seam PASS/FAIL line. Exit non-zero (e.g. process.exit(2)) when any seam is not ok, so a caller that shells out gets an accurate boolean. Export the pure helpers (active-filter, required-env resolver, runner resolver) for the Task 2 unit tests via module.exports, guarding real execution behind an isMainModule (`require.main === module`) check so importing the module for tests does not run the smoke loop.</action>
  <verify>
    <automated>node -e "const m=require('./scripts/dgfy-seam-smoke.js'); if(typeof m!=='object')process.exit(1)" && grep -q "require('./check-compat-seams.js')" scripts/dgfy-seam-smoke.js && grep -q "RUN_CONTINUITY_INTEGRATION" scripts/dgfy-seam-smoke.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dgfy-seam-smoke.js` requires `loadManifest` from `./check-compat-seams.js` (grep confirms) and never defines its own manifest JSON parse of compatibility-seams.json.
    - The active filter keys off `status === 'active'` (grep confirms the literal filter).
    - A seam-id → required-env map is present and includes `RUN_CONTINUITY_INTEGRATION` mapped to `db-continuity-legacy-backup` (grep confirms both tokens present).
    - `require('./scripts/dgfy-seam-smoke.js')` does NOT execute the smoke loop (guarded by `require.main === module`), enabling in-process unit tests.
    - When required integration env is absent, the seam result is `ok:false` with an explanatory detail — verified by the Task 2 fail-closed test.
  </acceptance_criteria>
  <done>An operator (or the Wave 2 orchestrator) runs the script in a DB-reachable environment and gets a per-seam pass/fail seam_smoke.json where every active manifest seam is exercised with its integration env, and any missing-env seam fails closed.</done>
</task>

<task type="auto">
  <name>Task 2: Add node --test coverage for the seam-smoke runner</name>
  <files>scripts/dgfy-seam-smoke.test.js</files>
  <read_first>
    - scripts/check-compat-seams.test.js (existing node --test structure for a root compat-seams script — assertion style, fixture approach)
    - scripts/dgfy-seam-smoke.js (the module under test, its exported pure helpers)
    - .planning/codebase/TESTING.md (root scripts use the node built-in test runner; test naming conventions)
  </read_first>
  <action>Create scripts/dgfy-seam-smoke.test.js using the Node built-in test runner (`node --test`, matching the existing `test:compat-seams = node --test scripts/check-compat-seams.test.js` convention — NOT jest, since this is a root CJS script). Cover the pure, DB-free logic by importing the exported helpers: (a) the active-filter returns only `status === 'active'` seams from a fixture manifest and excludes provisioning/accepted/deprecated entries; (b) the required-env resolver returns `RUN_CONTINUITY_INTEGRATION` for `db-continuity-legacy-backup`; (c) fail-closed behavior — given a seam whose required env vars are unset in a stubbed env, the runner produces an `ok:false` seam result with a detail mentioning the missing env, and does NOT report pass; (d) an unknown seam id with no runner mapping resolves to fail-closed, not silent pass. Do not require a live MySQL or actually spawn jest — stub/inject the runCommand boundary or exercise only the pure resolvers so the suite runs in the no-DB sandbox.</action>
  <verify>
    <automated>node --test scripts/dgfy-seam-smoke.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/dgfy-seam-smoke.test.js` exits 0 with no live DB and without spawning the real jest seam test.
    - A test asserts the active-filter excludes non-active statuses.
    - A test asserts a missing-required-env seam yields `ok:false` (fail-closed), never a pass on a skipped integration test.
  </acceptance_criteria>
  <done>The seam-smoke runner has a green node --test suite proving generic active-seam iteration, the integration-env map, and fail-closed semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release-evidence gate → seam-smoke runner | Runner decides whether behavior-preservation was actually proven |
| seam-smoke runner → owning-package test runners (jest) | Shells out to child test processes with integration env |
| seam-smoke runner → seam_smoke.json | Result artifact consumed by the orchestrator gate and Phase 7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Tampering (false-pass) | seam test dispatch | high | mitigate | Required integration env (RUN_CONTINUITY_INTEGRATION + SOURCE_DB_*) must be set for the behavior probe to run; missing env → seam ok:false, never a pass on skipped tests (Pitfall 1); enforced by Task 2 fail-closed test |
| T-06-02-02 | Tampering (unknown seam) | runner-map lookup | medium | mitigate | A seam id / test path with no runner mapping and no documented default resolves to fail-closed (ok:false), never a silent pass |
| T-06-02-03 | Spoofing (stale manifest scope) | active-filter | medium | mitigate | Filter reuses check-compat-seams.js loadManifest (validated schema/status enums); only status==='active' entries are smoke-tested; no manifest edits in this phase |
| T-06-02-04 | Information Disclosure | seam_smoke.json / child stdio | low | accept | Results carry only seam id, ok boolean, and non-secret detail strings; child test runners inherit env but the artifact records no credential values |
</threat_model>

<verification>
- `node --test scripts/dgfy-seam-smoke.test.js` passes.
- `require('./scripts/dgfy-seam-smoke.js')` imports without executing the smoke loop.
- Runner reuses `loadManifest` from check-compat-seams.js and filters `status === 'active'`.
- Missing integration env produces a fail-closed seam result.
</verification>

<success_criteria>
- SC3 (partial): every active compatibility seam is smoke-tested with its required integration env, and per-seam pass/fail is recorded in seam_smoke.json. Aggregation into the release verdict is completed by 06-03.
- D-04 satisfied: generic manifest iteration; future seams require zero runner changes.
</success_criteria>

## Artifacts this phase produces

- `scripts/dgfy-seam-smoke.js` — NEW generic, manifest-driven seam-smoke runner (CommonJS); exports pure helpers (active-filter, required-env resolver, runner resolver) guarded behind `require.main === module`; reuses `loadManifest` from `check-compat-seams.js`.
- `scripts/dgfy-seam-smoke.test.js` — NEW `node --test` suite (active-filter, env-map, fail-closed, unknown-seam).
- Internal maps/extension points: seam-id → required-integration-env map (seeds `db-continuity-legacy-backup` → `RUN_CONTINUITY_INTEGRATION` + `SOURCE_DB_*`), and test-path → owning-package runner map (seeds `apps/dgfy-migration-runner/tests/` → jest, with a documented default extension point).
- Runtime artifact under the evidence directory: `seam_smoke.json` — per-seam `{ id, ok, detail }` results plus overall `ok`.

<output>
Create `.planning/phases/06-release-evidence-and-rehearsal-gates/06-02-SUMMARY.md` when done.
</output>
