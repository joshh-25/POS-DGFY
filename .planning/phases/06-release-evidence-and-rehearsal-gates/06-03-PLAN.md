---
phase: 06-release-evidence-and-rehearsal-gates
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - scripts/gate-release-dgfy-evidence.js
  - scripts/gate-release-dgfy-evidence.test.js
  - package.json
autonomous: true
requirements: [CMP-04]

must_haves:
  truths:
    - "One on-demand command (npm run gate:release:dgfy-evidence) produces a release_verdict-contract artifact aggregating architecture, migration/drift, and seam-smoke gates for the whole platform (D-01)."
    - "The emitted dgfy_release_evidence.json validates against scripts/verify-release-verdict.js (non-empty gates[], verdict ∈ {pass,fail,bypassed}, target_sha string, generated_at)."
    - "The architecture gate runs npm run check:architecture only when a backend/API boundary changed (git diff), otherwise records a passed not-applicable gate — and never emits an empty gates[] (Pitfall 5/6)."
    - "Per-PR CI (.github/workflows/ci.yml) and existing gate scripts are unchanged (D-05); this phase adds an additive on-demand gate only."
  artifacts:
    - scripts/gate-release-dgfy-evidence.js
    - scripts/gate-release-dgfy-evidence.test.js
    - "dgfy_release_evidence.json (verdict-shaped artifact under .tmp/release-gates/<sha>/, written at runtime)"
    - "package.json script gate:release:dgfy-evidence"
  key_links:
    - "gate-release-dgfy-evidence.js shells out (spawnSync) to the runner's release-evidence command (06-01) and to scripts/dgfy-seam-smoke.js (06-02)"
    - "verdict payload conforms to the scripts/verify-release-verdict.js contract and is self-validated as a final gate"
    - "package.json adds gate:release:dgfy-evidence + check:dgfy-seam-smoke + a node --test entry, alongside existing gate:release:* scripts"
---

<objective>
Deliver SC1 + the D-01 aggregation: a dedicated, on-demand orchestrator (scripts/gate-release-dgfy-evidence.js) that runs the architecture-checks gate, invokes the Wave 1 migration/drift command and seam-smoke runner, and writes a single release_verdict-contract artifact (dgfy_release_evidence.json) validated by the existing scripts/verify-release-verdict.js — feeding Phase 7's cutover decision without touching per-PR CI (D-05).

Purpose: Replace "health checks alone" as the release signal with one evidence-backed verdict aggregating architecture, migration verification, tenant drift, and seam smoke into the same gates[] contract the platform already uses (CMP-04, D-01).
Output: A CommonJS orchestrator script, its node --test suite, and root package.json wiring. This is the roll-up plan; it depends on 06-01 (runner command) and 06-02 (seam-smoke).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-CONTEXT.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-RESEARCH.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-PATTERNS.md
@scripts/gate-release-local.js
@scripts/verify-release-verdict.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the DGFY release-evidence orchestrator gate script</name>
  <files>scripts/gate-release-dgfy-evidence.js</files>
  <read_first>
    - scripts/gate-release-local.js lines 1-43 (require + runCommand/captureStdout/addGate/ensureDir helpers), 46-49 (target sha + .tmp/release-gates/<sha>/), 52-90 (gate accumulation incl. the architecture guardrails gate ~line 56), 92-103 (contract self-validation skip pattern), 105-124 (verdict payload + write + exit)
    - scripts/verify-release-verdict.js lines 58-77 (contract: generated_at truthy, target_sha string, verdict ∈ {pass,fail,bypassed}, non-empty gates[], sha match, --require-pass)
    - .github/workflows/ci.yml (READ-ONLY — confirm what per-PR jobs exist; D-05 forbids editing this file or existing gate scripts)
    - .planning/phases/06-release-evidence-and-rehearsal-gates/06-RESEARCH.md Pitfalls 5 & 6 (empty gates[] / "changed boundary" definition); 06-PATTERNS.md "scripts/gate-release-dgfy-evidence.js" section
  </read_first>
  <action>Create CommonJS orchestrator scripts/gate-release-dgfy-evidence.js (shebang, require, 2-space style) mirroring gate-release-local.js exactly. Duplicate the inline runCommand/captureStdout/addGate/ensureDir helpers (per the established per-script duplication convention). Steps:
    (1) Resolve targetSha from `RELEASE_TARGET_SHA` env or `git rev-parse HEAD` (lowercased); evidenceDir = `.tmp/release-gates/<sha>/`; ensureDir(evidenceDir); outputFile = evidenceDir/`dgfy_release_evidence.json`. Initialize `const gates = []`.
    (2) GATE target_sha (Pitfall 5 — always record ≥1 gate so gates[] is never empty): addGate(gates, 'release.target_sha', Boolean(targetSha), detail with the sha).
    (3) GATE architecture.dgfy (SC1, Pitfall 6 — changed-boundary definition): compute a boolean `boundaryChanged` from `git diff --name-only <base>..<sha>` (base from `RELEASE_DIFF_BASE` env, default the merge-base against origin/main or HEAD~1) touching any of `apps/dgfy-api/`, `apps/dgfy-migration-runner/`, or `backend/src/modules/`. When boundaryChanged, addGate with `runCommand('npm', ['run', 'check:architecture'])` (the SAME ADR-0004 automation gate-release-local.js already runs — do NOT reimplement guardrails; fail-closed on non-zero). When not changed, addGate(gates, 'architecture.dgfy', true, 'not applicable — no backend/API boundary change') — record a passed not-applicable gate, do not skip.
    (4) GATE migration.verification + tenant.drift (SC2): shell out to the runner's new subcommand `runCommand('node', ['apps/dgfy-migration-runner/src/cli.js', 'release-evidence', '--evidence-dir', evidenceDir])` (or `npm --prefix apps/dgfy-migration-runner ...` equivalent). Its non-zero exit → gate ok:false (fail-closed on env/DB error, Pitfall 3). addGate for the drift/migration result.
    (5) GATE compat.seam.smoke (SC3): shell out to `runCommand('node', ['scripts/dgfy-seam-smoke.js', '--evidence-dir', evidenceDir])`. Read the resulting seam_smoke.json and addGate one entry per seam id (id, ok, detail), or a single roll-up gate if the per-seam file is absent (fail-closed: absent → ok:false). Non-zero exit → not release-ready.
    (6) Verdict payload (copy the gate-release-local.js shape EXACTLY — this IS the verify-release-verdict.js contract): { generated_at: new Date().toISOString(), target_sha: targetSha, verdict: failed.length===0 ? 'pass' : 'fail', gate_count, failed_gate_count, gates, artifact_paths: { migration_verification, tenant_drift, seam_smoke } }. Write JSON to outputFile. process.exit(2) when any gate failed.
    (7) Final self-validation gate (mirrors gate-release-local.js lines 92-103): `runCommand('node', ['scripts/verify-release-verdict.js', '--file', outputFile, '--sha', targetSha])` to prove the artifact conforms to the contract.
    Constraints: Do NOT edit .github/workflows/ci.yml, existing gate scripts, backend/, or frontend/ (D-05). This is additive and on-demand only.</action>
  <verify>
    <automated>node -c scripts/gate-release-dgfy-evidence.js && grep -q 'dgfy_release_evidence.json' scripts/gate-release-dgfy-evidence.js && grep -q "check:architecture" scripts/gate-release-dgfy-evidence.js && grep -q 'dgfy-seam-smoke.js' scripts/gate-release-dgfy-evidence.js && git diff --quiet -- .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/gate-release-dgfy-evidence.js` parses cleanly (valid CJS).
    - The script writes `dgfy_release_evidence.json` under `.tmp/release-gates/<sha>/` and always records the `release.target_sha` gate (grep confirms both tokens).
    - The architecture gate runs `check:architecture` conditionally on a git-diff boundary check touching apps/dgfy-api, apps/dgfy-migration-runner, or backend/src/modules (grep confirms `check:architecture` and the three path tokens).
    - The script shells out to both `apps/dgfy-migration-runner` release-evidence and `scripts/dgfy-seam-smoke.js` (grep confirms both invocations).
    - `.github/workflows/ci.yml` is unmodified (`git diff --quiet` on that path returns clean).
    - The verdict payload contains `generated_at`, `target_sha`, `verdict`, `gates`, `gate_count`, `failed_gate_count` keys (matches verify-release-verdict.js contract).
  </acceptance_criteria>
  <done>Running `node scripts/gate-release-dgfy-evidence.js` in a DB-reachable environment produces a contract-valid dgfy_release_evidence.json aggregating architecture, migration/drift, and seam-smoke gates, self-validated by verify-release-verdict.js, with per-PR CI untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the npm scripts and add node --test coverage for the verdict contract</name>
  <files>package.json, scripts/gate-release-dgfy-evidence.test.js</files>
  <read_first>
    - package.json scripts block (existing gate:release:local / gate:release:observability / gate:release:no-staging / verify:release-verdict / check:compat-seams entries — match their style)
    - scripts/gate-release-dgfy-evidence.js (the orchestrator under test; its exported pure helpers if any — e.g. buildVerdictPayload, boundaryChanged)
    - scripts/verify-release-verdict.js lines 58-77 (the exact contract the test must assert against)
    - scripts/check-compat-seams.test.js (node --test structure reference)
  </read_first>
  <action>Add root package.json scripts (alongside the existing gate:release:* entries, matching their exact style): `"gate:release:dgfy-evidence": "node scripts/gate-release-dgfy-evidence.js"`, `"check:dgfy-seam-smoke": "node scripts/dgfy-seam-smoke.js"`, and a test entry `"test:dgfy-release-evidence": "node --test scripts/gate-release-dgfy-evidence.test.js"`. Do NOT modify any existing script value. To make the verdict-shape unit-testable without a live DB, refactor the orchestrator (if not already) so the verdict payload is built by an exported pure helper (e.g. `buildVerdictPayload({ targetSha, gates })`) and the boundary-change decision by an exported pure helper (e.g. `isBoundaryChanged(changedFiles)`), guarded behind `require.main === module` so importing the module for tests does not run the gate. Create scripts/gate-release-dgfy-evidence.test.js (Node built-in `node --test`): (a) buildVerdictPayload with all-ok gates yields verdict 'pass', with a failing gate yields 'fail', and always produces a non-empty gates[] and a string target_sha and truthy generated_at (asserting the verify-release-verdict.js contract directly); (b) isBoundaryChanged returns true for a changed file under apps/dgfy-api/ or apps/dgfy-migration-runner/ or backend/src/modules/ and false for an unrelated path; (c) an all-skipped/not-applicable run still emits at least the release.target_sha gate so gates[] is never empty (Pitfall 5).</action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts; if(!s['gate:release:dgfy-evidence']||!s['check:dgfy-seam-smoke']||!s['test:dgfy-release-evidence'])process.exit(1)" && node --test scripts/gate-release-dgfy-evidence.test.js</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `gate:release:dgfy-evidence`, `check:dgfy-seam-smoke`, and `test:dgfy-release-evidence` scripts; no existing script value changed (only additions).
    - `node --test scripts/gate-release-dgfy-evidence.test.js` exits 0.
    - A test proves the built verdict payload conforms to the verify-release-verdict.js contract (non-empty gates[], verdict ∈ {pass,fail,bypassed}, string target_sha, truthy generated_at).
    - A test proves the boundary-change helper classifies apps/dgfy-api, apps/dgfy-migration-runner, and backend/src/modules paths as changed and unrelated paths as not-changed.
  </acceptance_criteria>
  <done>The on-demand gate is invokable via npm, its verdict artifact is proven contract-conformant by a green node --test suite, and the never-empty-gates invariant is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → orchestrator gate | Operator triggers the on-demand release-evidence run; environment supplies DB creds + diff base |
| orchestrator → child processes (runner command, seam-smoke, check:architecture, verify-release-verdict) | spawnSync boundaries; exit codes drive gate booleans |
| orchestrator → dgfy_release_evidence.json → Phase 7 | The verdict artifact is the release-readiness signal consumed downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Tampering (false-pass verdict) | gate aggregation | high | mitigate | Every child non-zero exit → gate ok:false; absent seam_smoke.json / missing migration reports → fail-closed (ok:false), never assumed pass; verdict self-validated by verify-release-verdict.js (Pitfall 3/5) |
| T-06-03-02 | Tampering (empty gates bypass) | verdict payload | high | mitigate | release.target_sha gate always recorded so gates[] is never empty (Pitfall 5); enforced by Task 2 test; verify-release-verdict.js rejects empty gates[] |
| T-06-03-03 | Elevation/Scope creep | .github/workflows/ci.yml, existing gate scripts, backend/, frontend/ | high | mitigate | Additive on-demand gate only (D-05); Task 1 acceptance asserts ci.yml is unmodified; no existing script values changed |
| T-06-03-04 | Information Disclosure | dgfy_release_evidence.json | medium | mitigate | Aggregates only {name, ok, detail} gate entries and artifact_paths; Wave 1 reports it references carry no secrets; no credential values enter the verdict payload |
| T-06-03-05 | Repudiation (undefined "changed") | architecture gate boundary | low | accept | "Changed boundary" is a documented git-diff definition (apps/dgfy-api, apps/dgfy-migration-runner, backend/src/modules); not-applicable is an explicit passed gate, auditable in the verdict |
</threat_model>

<verification>
- `node -c scripts/gate-release-dgfy-evidence.js` parses; `node --test scripts/gate-release-dgfy-evidence.test.js` passes.
- package.json has gate:release:dgfy-evidence + check:dgfy-seam-smoke + test:dgfy-release-evidence; existing scripts unchanged.
- `git diff --quiet -- .github/workflows/ci.yml` is clean (D-05).
- Verdict payload conforms to the verify-release-verdict.js contract (self-validated in-script + unit-tested).
</verification>

<success_criteria>
- SC1 satisfied: architecture-checks gate runs check:architecture for changed backend/API boundaries and records a passed not-applicable gate otherwise.
- D-01 satisfied: architecture + migration/drift + seam-smoke roll up into one release_verdict-contract artifact validated by verify-release-verdict.js.
- D-05 satisfied: on-demand only; per-PR CI and existing gate scripts untouched.
- CMP-04 fully covered across 06-01 (SC2), 06-02 (SC3), and 06-03 (SC1 + aggregation).
</success_criteria>

## Artifacts this phase produces

- `scripts/gate-release-dgfy-evidence.js` — NEW on-demand orchestrator gate script (CommonJS); exports pure helpers `buildVerdictPayload({ targetSha, gates })` and `isBoundaryChanged(changedFiles)` guarded behind `require.main === module`; shells out to the runner `release-evidence` command (06-01) and `scripts/dgfy-seam-smoke.js` (06-02).
- `scripts/gate-release-dgfy-evidence.test.js` — NEW `node --test` suite (verdict-contract conformance, boundary-change classification, never-empty-gates invariant).
- `package.json` — NEW scripts: `gate:release:dgfy-evidence`, `check:dgfy-seam-smoke`, `test:dgfy-release-evidence` (no existing script values changed).
- Gate entries emitted into the verdict: `release.target_sha`, `architecture.dgfy`, `migration.verification` + `tenant.drift`, and per-seam `compat.seam.smoke` entries.
- Runtime artifact under `.tmp/release-gates/<sha>/`: `dgfy_release_evidence.json` — a `verify-release-verdict.js`-contract verdict aggregating all gates.

<output>
Create `.planning/phases/06-release-evidence-and-rehearsal-gates/06-03-SUMMARY.md` when done.
</output>
