---
phase: 06-release-evidence-and-rehearsal-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-migration-runner/package.json
  - apps/dgfy-migration-runner/src/commands/verify.js
  - apps/dgfy-migration-runner/src/commands/releaseEvidence.js
  - apps/dgfy-migration-runner/src/cli.js
  - apps/dgfy-migration-runner/tests/releaseEvidence.test.js
autonomous: true
requirements: [CMP-04]
user_setup:
  - service: mysql
    why: "SC2 tenant-drift/migration-verification reads dgfy_core.business_database_registry and connects to each selected dgfy_business_* schema. This command is operator-run in a DB-reachable environment (D-05). No credentials belong in the repo; supply them via the runner's existing env-validate-before-connect contract (RUN-03)."
    env_vars:
      - name: TARGET_DB_HOST / TARGET_DB_PORT / TARGET_DB_USER / TARGET_DB_PASSWORD / TARGET_DB_NAME
        source: "Operator's DGFY MySQL instance (dgfy_core primary target holds the registry). Validated by config/env.js validateEnv() before any connection."

must_haves:
  truths:
    - "Operator running the runner's release-evidence command sees a live checklist of active tenants sourced from dgfy_core.business_database_registry (JOIN businesses) and explicitly picks which to include (D-02/D-03)."
    - "Each operator-selected tenant is drift-checked against dgfyBusinessContract, and reviewable migration_verification + tenant_drift reports (JSON + markdown) are written under the evidence directory."
    - "The command fails closed — a non-TTY stdin or invalid env raises a clear error and NEVER falls back to an all-tenants run (Pitfall 4 / rejected auto-discovery model)."
  artifacts:
    - apps/dgfy-migration-runner/src/commands/releaseEvidence.js
    - apps/dgfy-migration-runner/tests/releaseEvidence.test.js
    - "apps/dgfy-migration-runner/package.json (declares @inquirer/prompts@^8)"
    - "apps/dgfy-migration-runner/src/commands/verify.js (exports checkContractSchema)"
  key_links:
    - "cli.js buildProgram() registers a release-evidence subcommand wired to runReleaseEvidence()"
    - "releaseEvidence.js imports checkContractSchema from ./verify.js (reuse, zero drift-engine duplication)"
    - "releaseEvidence.js reuses writeJsonReport/writeSummaryReport for RUN-05-conformant reports"
---

<objective>
Deliver SC2's genuinely-new code: an interactive, on-demand migration-runner command that discovers active tenants from the registry, lets the operator pick which businesses this release-evidence run targets (D-02/D-03), and produces reviewable migration-verification + tenant-drift reports by reusing verify.js's existing contract-comparison engine.

Purpose: Replace the static DGFY_BUSINESS_DB_NAMES env target list with a live, human-in-the-loop selection that excludes test-only businesses, so migration/drift evidence only covers real migration targets.
Output: A new `release-evidence` runner subcommand, an exported drift engine, the @inquirer/prompts dependency, and JSON+markdown drift/verification reports written under an evidence directory the Wave 2 orchestrator passes in.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-CONTEXT.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-RESEARCH.md
@.planning/phases/06-release-evidence-and-rehearsal-gates/06-PATTERNS.md
@apps/dgfy-migration-runner/src/commands/verify.js
@apps/dgfy-migration-runner/src/commands/activateTenant.js
@apps/dgfy-migration-runner/src/cli.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @inquirer/prompts to the runner and export verify.js's drift engine</name>
  <files>apps/dgfy-migration-runner/package.json, apps/dgfy-migration-runner/src/commands/verify.js</files>
  <read_first>
    - apps/dgfy-migration-runner/package.json (confirm "type":"module", no existing prompt dep, mysql2 pinned ^3.6.5 — do NOT bump)
    - apps/dgfy-migration-runner/src/commands/verify.js lines 46-137 (checkTableAgainstContract at 46, checkContractSchema at 107 — both currently module-private; runVerify already exported at 321)
    - .planning/phases/06-release-evidence-and-rehearsal-gates/06-RESEARCH.md "Package Legitimacy Audit" (verdict OK/Approved) and "Pattern 3" (reuse checkContractSchema)
    - .planning/phases/06-release-evidence-and-rehearsal-gates/06-PATTERNS.md "verify.js (MODIFY — export drift engine)"
  </read_first>
  <action>Install @inquirer/prompts pinned to major ^8 into the RUNNER package only, via `npm --prefix apps/dgfy-migration-runner install @inquirer/prompts@8`, so package.json dependencies gains "@inquirer/prompts": "^8.x". Do not touch the mysql2 ^3.6.5 pin (STATE.md package-audit decision) or any other dependency. Then in verify.js add the `export` keyword to the existing `async function checkContractSchema` declaration (line ~107) so it becomes `export async function checkContractSchema`. Leave its body, signature ({ connection, databaseName, contract } → { database, ok, tables[], rejected_tables_present }, never-throws), and the two existing internal call sites unchanged. Do NOT export checkTableAgainstContract unless table-granular reuse is needed — checkContractSchema alone suffices for the new command. This mirrors how verify.js already reuses schema.js's exported buildMigrationsForKind.</action>
  <verify>
    <automated>node -e "const d=require('./apps/dgfy-migration-runner/package.json').dependencies; if(!d['@inquirer/prompts']) process.exit(1); if(d.mysql2!=='^3.6.5') process.exit(2)" && grep -q 'export async function checkContractSchema' apps/dgfy-migration-runner/src/commands/verify.js</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dgfy-migration-runner/package.json` dependencies contains `@inquirer/prompts` at major `^8`, and `mysql2` remains exactly `^3.6.5`.
    - `grep -c 'export async function checkContractSchema' apps/dgfy-migration-runner/src/commands/verify.js` returns `1`.
    - The existing runner jest suite still passes: `npm --prefix apps/dgfy-migration-runner test` exits 0 (no regression from the export change).
  </acceptance_criteria>
  <done>@inquirer/prompts@^8 is a runner-only dependency and checkContractSchema is importable by sibling command modules with no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Build the interactive release-evidence command (registry discovery + checkbox + drift + reports)</name>
  <files>apps/dgfy-migration-runner/src/commands/releaseEvidence.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/commands/verify.js lines 1-17 (imports), 107-137 (checkContractSchema), 187-192 (checkTenantCoverage registry query), 322-330 (env-validate-before-connect), 519-520 (report writers)
    - apps/dgfy-migration-runner/src/commands/activateTenant.js lines 30-46 (validate→guard→connect lifecycle), 64-67 (registry SELECT), 140-148 (no-secrets report fields, T-04-09-04)
    - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js and dgfyCoreContract.js lines 52-108 (businesses + business_database_registry columns; registry status enum provisioning|active|migrating|deprecated)
    - apps/dgfy-migration-runner/src/config/env.js validateEnv, config/db.js (createTargetConnection, createBusinessTargetConnection), safety/targetGuard.js assertTargetDbNameAllowed, reports/reportWriter.js writeJsonReport, reports/summaryWriter.js writeSummaryReport, utils/errors.js EnvValidationError
    - .planning/phases/06-release-evidence-and-rehearsal-gates/06-PATTERNS.md "releaseEvidence.js" section (composite analog: verify.js + activateTenant.js)
  </read_first>
  <action>Create ESM module releaseEvidence.js exporting `async function runReleaseEvidence({ evidenceDir } = {})`. Order of operations, copying the runner's proven lifecycle:
    (1) Env-validate-before-connect (RUN-03, Pitfall 3): call validateEnv(); if not valid throw EnvValidationError(errors joined) — no DB touch on failure. Then assertTargetDbNameAllowed(config.targetDb.name, config.runtimeMode). Open the primary registry connection with createTargetConnection(config) (registry lives in dgfy_core).
    (2) Live tenant discovery (D-02, Pattern 4): SELECT r.business_id, r.database_name, r.status, r.verified_at, b.display_name, b.business_handle FROM business_database_registry r JOIN businesses b ON b.id = r.business_id WHERE r.status = 'active' AND r.verified_at IS NOT NULL ORDER BY b.display_name. If zero active tenants, write an empty-selection report and return a structured result flagged as no-targets (the Wave 2 gate decides pass/fail) — do not crash.
    (3) TTY guard BEFORE prompting (D-03, Pitfall 4): if process.stdin.isTTY is falsy, throw an Error stating the release-evidence tenant selection requires an interactive terminal. Never silently fall back to selecting all tenants (that reintroduces the rejected auto-discovery model D-02 forbids).
    (4) Interactive selection: import { checkbox } from '@inquirer/prompts'; present one choice per active tenant with name `${display_name} (${database_name}) [${status}]` and value database_name. The operator's checked set is the target list.
    (5) Per-tenant drift (SC2): for each selected database_name, open createBusinessTargetConnection(config, name) and call the imported checkContractSchema({ connection, databaseName: name, contract: dgfyBusinessContract }); collect each never-throws driftResult ({ database, ok, tables[], rejected_tables_present }).
    (6) Reports (RUN-05, no-secrets): reuse writeJsonReport + writeSummaryReport to emit reviewable migration_verification.json/.md and tenant_drift.json/.md. When evidenceDir is provided, write there; otherwise default to config.reportDir. Reports carry ONLY database names, business_id, table names, migration names, counts, and booleans — never host/user/password/DSN/token/credential values (T-04-09-04). Return { selected, reports: {paths}, ok: every selected driftResult.ok } so the caller can gate on it.
    Close all opened Sequelize connections in a finally block. Do NOT re-implement DB, env, or report infrastructure — import the existing modules. Fail closed on any env/connection error (Pitfall 3): surface a clear error, do not swallow it into a false pass.</action>
  <verify>
    <automated>node --input-type=module -e "import('./apps/dgfy-migration-runner/src/commands/releaseEvidence.js').then(m=>{if(typeof m.runReleaseEvidence!=='function')process.exit(1)})" && grep -q "from '@inquirer/prompts'" apps/dgfy-migration-runner/src/commands/releaseEvidence.js && grep -q 'checkContractSchema' apps/dgfy-migration-runner/src/commands/releaseEvidence.js</automated>
  </verify>
  <acceptance_criteria>
    - `releaseEvidence.js` exports `runReleaseEvidence` and imports `checkbox` from `@inquirer/prompts` and `checkContractSchema` from `./verify.js` (grep confirms both import lines exist).
    - The module references `process.stdin.isTTY` and throws (not returns a default selection) when it is falsy — verified by the Task 3 unit test.
    - The registry query text contains `business_database_registry`, `JOIN businesses`, and `status = 'active'` (grep confirms the discovery SELECT is present).
    - No literal credential field names (host/user/password/dsn/token) are written into report payload construction — verified by the Task 3 no-secrets report-shape test.
  </acceptance_criteria>
  <done>Running the command against a DB-reachable environment lists active tenants, records the operator's checkbox selection, drift-checks each selected tenant, and writes reviewable JSON+markdown reports; a non-TTY or invalid-env invocation fails closed.</done>
</task>

<task type="auto">
  <name>Task 3: Register the release-evidence subcommand in cli.js and add jest coverage</name>
  <files>apps/dgfy-migration-runner/src/cli.js, apps/dgfy-migration-runner/tests/releaseEvidence.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/cli.js lines 6-12 (command imports), 23-111 (buildProgram + existing .command() blocks: verify 66-70, activate-tenant 96-111), 133-138 (isMainModule guard)
    - apps/dgfy-migration-runner/tests/verifyContinuity.test.js lines 1-35 (jest structure; RUN_CONTINUITY_INTEGRATION gating precedent) and any cliContract.test.js in tests/ (in-process buildProgram dispatch)
    - apps/dgfy-migration-runner/jest.config.cjs (roots: ['<rootDir>/tests'])
  </read_first>
  <action>In cli.js add `import { runReleaseEvidence } from './commands/releaseEvidence.js';` alongside the existing command imports (lines 6-12), then inside buildProgram() register a new command mirroring the existing verify/activate-tenant blocks: `.command('release-evidence')`, `.description('Interactively select active tenants and produce migration-verification + tenant-drift evidence')`, `.option('--evidence-dir <dir>', 'Directory to write reviewable JSON/markdown reports')`, and `.action(async (options) => { await runReleaseEvidence({ evidenceDir: options.evidenceDir }); })`. Keep the exported buildProgram()/isMainModule guard intact so the command is dispatchable in-process by tests. Create tests/releaseEvidence.test.js (jest) with DB-free unit coverage: (a) buildProgram() registers a `release-evidence` command with an `--evidence-dir` option (introspect program.commands); (b) runReleaseEvidence throws when process.stdin.isTTY is forced falsy (assert fail-closed, no all-tenants fallback) — stub validateEnv/connection so the TTY guard is reached without a live DB; (c) a report-shape assertion that the constructed report payload contains only allowed keys (database names, business_id, table names, counts, booleans) and no credential keys. Gate any test path that needs a live MySQL connection behind a RUN_RELEASE_EVIDENCE_INTEGRATION env flag, mirroring the RUN_*_INTEGRATION precedent, so the default suite stays green in the no-DB sandbox.</action>
  <verify>
    <automated>npm --prefix apps/dgfy-migration-runner test -- releaseEvidence</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.command('release-evidence')" apps/dgfy-migration-runner/src/cli.js` returns `1` and `grep -c "runReleaseEvidence" apps/dgfy-migration-runner/src/cli.js` returns at least `2` (import + action).
    - `tests/releaseEvidence.test.js` asserts the command + `--evidence-dir` option are registered on buildProgram().
    - A test proves runReleaseEvidence throws when stdin.isTTY is falsy (fail-closed), and a test proves the report payload contains no credential-named keys.
    - `npm --prefix apps/dgfy-migration-runner test -- releaseEvidence` exits 0 with no live DB required (integration paths gated behind RUN_RELEASE_EVIDENCE_INTEGRATION).
  </acceptance_criteria>
  <done>The runner exposes `release-evidence` as a first-class subcommand with a passing DB-free jest suite proving registration, fail-closed TTY behavior, and no-secrets reports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator terminal → migration runner | Operator supplies DB credentials via env; interactive selection crosses into DB-connecting logic |
| migration runner → live tenant MySQL (dgfy_core + dgfy_business_*) | Credentialed connections to production-adjacent tenant databases |
| runner → evidence report files | Report payloads written to disk / evidence dir may be read by other operators and Phase 7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | migration_verification/tenant_drift reports | high | mitigate | Reports carry ONLY database names, business_id, table names, migration names, counts, booleans — never host/user/password/DSN/token (reuse activateTenant.js T-04-09-04 convention; enforced by Task 3 report-shape test) |
| T-06-01-02 | Spoofing/Tampering | non-TTY invocation | high | mitigate | TTY guard throws before any prompt; NEVER falls back to all-tenants (Pitfall 4); no auto-discovery model reintroduced (D-02) |
| T-06-01-03 | Denial of Service (fail-open) | env/DB connection errors | high | mitigate | Env-validate-before-connect (RUN-03); missing SOURCE/TARGET env raises EnvValidationError; connection errors surface, never swallowed into a false pass (Pitfall 3) |
| T-06-01-SC | Tampering | @inquirer/prompts install | medium | mitigate | RESEARCH Package Legitimacy Audit verdict OK/Approved (not [ASSUMED]/[SUS]); pinned to reviewed major ^8; runner-only install; no other deps touched, mysql2 pin preserved |
</threat_model>

<verification>
- `npm --prefix apps/dgfy-migration-runner test` passes (existing suite + new releaseEvidence tests, no regression).
- `grep -c 'export async function checkContractSchema' apps/dgfy-migration-runner/src/commands/verify.js` returns 1.
- `node -e "require('./apps/dgfy-migration-runner/package.json').dependencies['@inquirer/prompts']"` prints a ^8 range; `mysql2` still `^3.6.5`.
- cli.js registers `release-evidence` with `--evidence-dir`.
</verification>

<success_criteria>
- SC2 (partial): operator-selected tenants are drift-checked and reviewable migration_verification + tenant_drift reports are produced. Aggregation into the release verdict is completed by 06-03.
- D-02/D-03 satisfied: live registry discovery + interactive checkbox selection; no static env list, no auto-discovery, fail-closed on non-TTY.
</success_criteria>

<output>
Create `.planning/phases/06-release-evidence-and-rehearsal-gates/06-01-SUMMARY.md` when done.
</output>
