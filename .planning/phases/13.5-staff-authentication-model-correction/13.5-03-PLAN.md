---
phase: 13.5-staff-authentication-model-correction
plan: 03
type: execute
wave: 3
depends_on: ["13.5-02"]
files_modified:
  - apps/dgfy-migration-runner/src/data/mappings.js
  - apps/dgfy-migration-runner/src/data/apply.js
  - apps/dgfy-migration-runner/tests/dataMappings.test.js
  - apps/dgfy-migration-runner/tests/dataApply.test.js
autonomous: true
requirements: [STAFF-03, STAFF-01]

must_haves:
  truths:
    - "Legacy users with a real bcrypt password_hash migrate to a staff_credentials row with the hash copied byte-for-byte — staff login capability preserved (STAFF-03, D-13.5-04)"
    - "Legacy users with the pending-invitation placeholder or any non-bcrypt value get credential_status='reset_required' plus a structured staff_credential_reset_required finding naming the reinvite path — intentional invalidation, no silent discard (D-13.5-04)"
    - "No mapper finding message, remediation, or reason payload ever contains a hash value"
    - "mapLegacyUserToStaffAccount and mapLegacyAccountToDgfyAccount emit only valid target ENUM statuses (is_active=false → 'suspended', fixing the invalid 'inactive' defect)"
    - "Retry apply writes zero duplicate staff_credentials rows (lookup-before-insert on staff_account_id)"
  artifacts:
    - "apps/dgfy-migration-runner/src/data/mappings.js (credential classification helper, related_targets emission, new reason code, enum fix)"
    - "apps/dgfy-migration-runner/src/data/apply.js (staff_credential ENTITY_TARGET_CONFIG entry + staff related-target loop with resolved-id injection)"
    - "extended tests/dataMappings.test.js and tests/dataApply.test.js"
  key_links:
    - "mapper target_payload field names ↔ staff_credentials DDL from Plan 02 migration (password_hash, pos_approval_pin_hash, credential_status, password_updated_at)"
    - "MAPPING_REASON_CODES.STAFF_CREDENTIAL_RESET_REQUIRED ↔ Plan 04 STAFF_LINKAGE_REASON_CODES set and Plan 01 migration-map reason-code table"
    - "apply staff block per-entry {status, dgfyId} ↔ credential payload staff_account_id injection (mapper cannot know the auto-increment id)"
---

<objective>
Extend the pure staff mapper to classify and migrate legacy tenant credentials into `staff_credentials` via the existing `related_targets` mechanism, fix the invalid `'inactive'` ENUM defect in both affected mappers, and teach `apply.js` to consume staff related-targets idempotently with resolved staff_account_id injection.

Purpose: STAFF-03 — legacy `users.password_hash`/POS PIN state gets an explicit migration target or an explicit reset finding; never silently discarded.
Output: mappings.js + apply.js changes with unit tests proving classification, purity, no-secret-leakage into findings, ENUM validity, and retry idempotency.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-CONTEXT.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-RESEARCH.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-PATTERNS.md
</context>

## Artifacts this phase produces

Plan 03 artifacts (new symbols):
- `MAPPING_REASON_CODES.STAFF_CREDENTIAL_RESET_REQUIRED` = `'staff_credential_reset_required'` (mappings.js)
- Exported pure helper `isRealBcryptHash(value)` (mappings.js) using `/^\$2[abxy]\$\d{2}\$/`
- `related_targets` entry shape `entity_type: 'staff_credential'`, `target_table: 'staff_credentials'` emitted by `mapLegacyUserToStaffAccount()`
- `ENTITY_TARGET_CONFIG.staff_credential = { primaryKey: 'id', naturalKeyColumns: ['staff_account_id'] }` (apply.js)
- Staff related-target consumption loop in the staff apply block (apply.js) with resolved `dgfyId` injection into `staff_account_id`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Credential classification + related_targets in mapLegacyUserToStaffAccount, enum fix in both mappers</name>
  <files>apps/dgfy-migration-runner/src/data/mappings.js, apps/dgfy-migration-runner/tests/dataMappings.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/mappings.js (full — header purity contract lines ~10-20; MAPPING_REASON_CODES lines ~38-51; classifyMappingConflict lines ~125-142; mapLegacyAccountToDgfyAccount hash-copy + status defect lines ~282-319; mapLegacyTenantToBusiness related_targets analog lines ~368-418; mapLegacyUserToStaffAccount + status defect lines ~570-620)
    - apps/dgfy-migration-runner/tests/dataMappings.test.js (existing test style for mapper assertions)
    - apps/dgfy-migration-runner/src/migrations/schema/20260717000000-add-dgfy-business-staff-credentials.cjs (Plan 02 output — authoritative payload field names/ENUM values)
    - backend/src/models/User.js (legacy source fields: password_hash, pos_approval_pin_hash, invitation state, is_active, deleted_at)
  </read_first>
  <behavior>
    - Test: legacy user with a real bcrypt password_hash (e.g. a `$2a$10$...` fixture string) → result.related_targets contains exactly one entry with entity_type 'staff_credential', target_table 'staff_credentials', payload.password_hash strictly equal (===) to the input string, payload.credential_status 'active', and zero reset findings
    - Test: legacy user whose password_hash is the legacy pending-invitation placeholder marker (non-bcrypt string) → related_targets credential entry has password_hash null and credential_status 'reset_required', and findings contains one entry with reason_code 'staff_credential_reset_required' whose remediation mentions staff_invitations reinvite
    - Test: legacy user with real bcrypt pos_approval_pin_hash but placeholder password_hash → payload.pos_approval_pin_hash copied byte-for-byte, password_hash null, credential_status 'reset_required'
    - Test: no finding message/remediation string in any staff mapping result matches /\$2[abxy]\$/ (hashes never appear in findings text)
    - Test: mapLegacyUserToStaffAccount with is_active=false emits status 'suspended'; with deleted_at set emits 'removed'; every emitted status is in ['active','suspended','removed']
    - Test: mapLegacyAccountToDgfyAccount with is_active=false emits status 'suspended'; every emitted status is in ['active','suspended','deleted']
    - Test: purity — calling the mapper twice with the same input yields deeply equal output (no Date.now()/randomness in payloads except deterministic derivations)
  </behavior>
  <action>
    In `mappings.js` (keep the file's purity contract: zero imports, no SQL, no DB access — dry-run and apply share these functions):

    1. Add `STAFF_CREDENTIAL_RESET_REQUIRED: 'staff_credential_reset_required'` to the frozen `MAPPING_REASON_CODES` object.
    2. Add an exported pure helper `isRealBcryptHash(value)` returning true iff the value is a string matching `/^\$2[abxy]\$\d{2}\$/`. This is the single classification rule for both password and PIN fields (per 13.5-RESEARCH.md Pattern 3; the legacy pending-invitation placeholder written by backend/src/services/userService.js:1688 is a non-hash marker string and must classify false).
    3. Extend `mapLegacyUserToStaffAccount()` to always emit one `related_targets` entry (copy the conditional-push shape of `mapLegacyTenantToBusiness()`): `{ operation: 'insert', entity_type: 'staff_credential', target_table: 'staff_credentials', target_database: <same tenant business DB as the staff_account>, target_payload: {...} }`. Payload rules (explicit field allow-list, per D-13.5-04):
       - `password_hash`: legacy value copied byte-for-byte when `isRealBcryptHash(legacyUser.password_hash)`, else null. Never re-hash, never transform (entity-1 precedent).
       - `pos_approval_pin_hash`: legacy value copied byte-for-byte when `isRealBcryptHash(legacyUser.pos_approval_pin_hash)`, else null.
       - `credential_status`: 'active' when a real bcrypt password_hash was copied; otherwise 'reset_required' (the explicit D-13.5-04 invalidation branch — staff visibly flagged, never stranded).
       - `password_updated_at`: null (legacy source has no reliable password-change timestamp; do not fabricate).
       - Do NOT include `staff_account_id` — the parent auto-increment id is unknown at mapping time; apply injects it (Task 2).
    4. When `credential_status` is 'reset_required', also push a finding via `classifyMappingConflict(MAPPING_REASON_CODES.STAFF_CREDENTIAL_RESET_REQUIRED, {...})` with entityType 'staff_credential', legacyTable 'users', the legacy user id, severity 'skip', a message that states WHY (placeholder/pending-invitation vs non-bcrypt format) WITHOUT quoting the placeholder value or any hash content, and a remediation naming the reinvite path through the new `staff_invitations` flow. Pending legacy invitation tokens are never carried over (threat note 2; research Open Question 3 resolution).
    5. Fix the invalid ENUM defect (Pitfall 1) in BOTH mappers: `mapLegacyUserToStaffAccount()` maps `is_active === false` → `'suspended'` (target ENUM ('active','suspended','removed')); `mapLegacyAccountToDgfyAccount()` maps its inactive branch → `'suspended'` (target ENUM ('active','suspended','deleted')). `deleted_at` branches stay 'removed'/'deleted' respectively.
    6. Preserve the header rule that membership/assignment mappers branch only on `status === 'accepted'` — this plan does not touch linkage evidence logic (D-13.5-03 reframing happens in verify, Plan 04).

    Write the failing tests in `tests/dataMappings.test.js` first (behavior list above), then implement, RED→GREEN.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataMappings.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests above pass
    - `MAPPING_REASON_CODES.STAFF_CREDENTIAL_RESET_REQUIRED === 'staff_credential_reset_required'` (source assertion)
    - `isRealBcryptHash` is exported and pure (no imports added to mappings.js — the file still has zero import statements)
    - Credential payload field names are exactly the Plan 02 migration column names: password_hash, pos_approval_pin_hash, credential_status, password_updated_at (no staff_account_id at mapping time)
    - Existing dataMappings tests still pass unmodified except where they asserted the old invalid 'inactive' status (those assertions updated to 'suspended')
  </acceptance_criteria>
  <done>Pure mapper classifies legacy credentials, emits credential related-targets and reset findings with zero secret leakage, and only emits valid ENUM statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply.js consumes staff credential related-targets idempotently</name>
  <files>apps/dgfy-migration-runner/src/data/apply.js, apps/dgfy-migration-runner/tests/dataApply.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/apply.js (ENTITY_TARGET_CONFIG lines ~106-120; writeRelatedTargetRow lines ~213-229; recordResult payload-omission lines ~430-442; applyBatchAndRecord per-entry {entry,status,dgfyId} return ~456-459; business related-target loop lines ~684-688; staff apply block lines ~690-712; assignment id-resolution precedent lines ~733-752)
    - apps/dgfy-migration-runner/tests/dataApply.test.js (existing staff_account apply test fixtures and mocking style)
    - apps/dgfy-migration-runner/src/data/mappings.js (Task 1 output — the related_targets shape being consumed)
  </read_first>
  <behavior>
    - Test: staff batch entry with a credential related_target and parent write status 'inserted' → writeRelatedTargetRow called once with target_payload containing staff_account_id equal to the parent's resolved dgfyId plus the mapper's credential fields
    - Test: parent staff_account write status 'skipped'/failed → NO credential write attempted for that entry
    - Test: retry semantics — when a staff_credentials row already exists for the resolved staff_account_id (findExistingTargetRow returns a row), no insert is performed (idempotent skip returns existing id)
    - Test: apply results/summary rows for staff_credential writes contain no target_payload (recordResult precedent — no hash content in report rows)
  </behavior>
  <action>
    In `apply.js`:

    1. Register the new entity in `ENTITY_TARGET_CONFIG`: `staff_credential: { primaryKey: 'id', naturalKeyColumns: ['staff_account_id'] }` — natural-key lookup-before-insert gives retry idempotency against the `unique_staff_credentials_staff_account` unique key (Phase 13 precedent: legacy_id_map/natural-key guard survived a 2124-row reconciled retry).
    2. Extend the staff apply block (lines ~690-712) with a related-target consumption loop mirroring the business loop at ~684-688, with two required differences (PATTERNS "No Analog Found" note): (a) run the loop ONLY for entries whose parent staff_account write returned status 'inserted' or 'reconciled' from `applyBatchAndRecord()`; (b) before calling `writeRelatedTargetRow()`, inject the parent's resolved auto-increment id into the credential payload as `staff_account_id` (use the per-entry `dgfyId` from the apply result; Number() coercion per the assignment-block precedent at ~733-752). Route the write to the tenant business connection (credential rows live in the same tenant DB as the staff account).
    3. Keep report rows payload-free: any recording of credential writes goes through the existing `recordResult()`-style field allow-list — status/table/database/id only, never `target_payload` (threat note 2).
    4. Do not add `legacy_id_map` rows for credentials — related targets inherit the parent's legacy mapping (entity-8 pattern); idempotency is natural-key only.

    Write the failing tests in `tests/dataApply.test.js` first (behavior list above) using the file's existing mock-sequelize fixtures, then implement, RED→GREEN.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataApply.test.js && npm run check:architecture</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests above pass
    - `ENTITY_TARGET_CONFIG.staff_credential` exists with naturalKeyColumns ['staff_account_id'] (source assertion)
    - Credential write is gated on parent status 'inserted'/'reconciled' (test proves 'skipped' parents produce no credential write)
    - Double-apply of the same staff batch produces exactly one staff_credentials write per staff account (idempotency test)
    - Full runner suite still green: `cd apps/dgfy-migration-runner && npm test` exits 0
  </acceptance_criteria>
  <done>Apply writes tenant-local credentials exactly once per staff account, only after a successful parent write, with no hash content in apply reports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy tenant DB (untrusted historical data) → mapper | password_hash values may be real bcrypt, placeholder markers, or unknown legacy formats |
| mapper output → findings/reports (durable metadata) | findings text is persisted and later surfaced in reports — must never carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13.5-07 | Information Disclosure | mapper findings / apply report rows | high | mitigate | Findings messages state classification reason without quoting values; apply recording stays payload-free allow-list; explicit no-/\$2[abxy]\$/-in-findings test (Task 1) |
| T-13.5-08 | Tampering (data integrity) | staff_credentials content | high | mitigate | isRealBcryptHash classification — placeholder/non-bcrypt values never stored as credentials; they become reset_required + finding (Pitfall 3) |
| T-13.5-09 | Denial of Service (business) | staff stranded by reset branch | medium | mitigate | reset_required is an explicit visible status plus a finding with reinvite remediation (D-13.5-04, threat note 4); Plan 04 counts these per tenant |
| T-13.5-10 | Tampering | duplicate credential rows on retry | medium | mitigate | natural-key lookup-before-insert on unique staff_account_id (Task 2) |
| T-13.5-11 | Spoofing | credential attached to wrong staff row | high | mitigate | staff_account_id injected only from the parent entry's own resolved dgfyId in the same apply iteration — never looked up by email/phone (ADR 0028 non-inference rule) |
| T-13.5-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed in this phase |
</threat_model>

<verification>
- `cd apps/dgfy-migration-runner && npm test` exits 0 (mappings, apply, schema, architecture checks)
- No test fixture or source change introduces a real production hash; fixtures use synthetic `$2a$10$` strings
</verification>

<success_criteria>
- Phase success criterion 3 satisfied: migration preserves real legacy staff credentials byte-for-byte and intentionally invalidates non-migratable ones with an explicit, audited reset/reinvite flow (STAFF-03, D-13.5-04)
- Pitfall 1 ENUM defect fixed in both mappers with regression tests
- STAFF-01 data half done: migrated staff can carry tenant-local credentials independent of any DGFY account
</success_criteria>

<output>
Create `.planning/phases/13.5-staff-authentication-model-correction/13.5-03-SUMMARY.md` when done.
</output>
