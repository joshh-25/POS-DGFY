---
phase: 13.5-staff-authentication-model-correction
plan: 02
type: execute
wave: 2
depends_on: ["13.5-01"]
files_modified:
  - apps/dgfy-migration-runner/src/migrations/schema/20260717000000-add-dgfy-business-staff-credentials.cjs
  - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js
  - apps/dgfy-migration-runner/tests/dgfyBusinessSchema.test.js
  - apps/dgfy-api/src/models/Tenant/StaffCredential.js
  - apps/dgfy-api/src/models/Tenant/StaffAccount.js
  - apps/dgfy-api/src/modules/businesses/repositories/staffOnboardingRepository.js
autonomous: true
requirements: [STAFF-01, STAFF-02, STAFF-04]

must_haves:
  truths:
    - "The dgfy_business_* schema has an explicit tenant-local credential target: additive migration creates staff_credentials 1:1 with staff_accounts (STAFF-01, D-13.5-02)"
    - "dgfyBusinessContract.js declares the staff_credentials table in the SAME commit as the migration, so schema verify checks it (Pitfall 4)"
    - "StaffCredential.js persistence-only model mirrors the applied migration column-for-column"
    - "staffOnboardingRepository.js and StaffAccount.js no longer state that staff credentials always come from DGFY accounts (STAFF-02)"
    - "Re-running the migration is a no-op (existence-guarded, DBF-04)"
  artifacts:
    - "apps/dgfy-migration-runner/src/migrations/schema/20260717000000-add-dgfy-business-staff-credentials.cjs"
    - "apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js (staff_credentials entry + corrected D-14 comment)"
    - "apps/dgfy-api/src/models/Tenant/StaffCredential.js"
    - "apps/dgfy-migration-runner/tests/dgfyBusinessSchema.test.js (staff_credentials describe block)"
  key_links:
    - "Migration DDL column list ↔ dgfyBusinessContract.tables.staff_credentials columns/indexes/foreignKeys (same commit — schema verify silently skips uncontracted tables)"
    - "Migration DDL ↔ StaffCredential.js init() (repo convention: model mirrors APPLIED migration, never plan prose)"
    - "staff_credentials DDL ↔ Plan 03 mapper target_payload field names (mapper written against this migration file)"
---

<objective>
Create the tenant-local staff credential target: an additive, existence-guarded `staff_credentials` migration for every `dgfy_business_*` DB, its schema-contract entry (same commit), the matching persistence-only dgfy-api model, and removal of the wrong "credentials come from DGFY accounts" assumption from dgfy-api model/repository comments.

Purpose: gives STAFF-01 a durable schema target and converges dgfy-api assumptions with the amended ADR 0028 (Plan 01). API convergence is MINIMAL per 13.5-RESEARCH.md Open Question 1 recommendation (Claude's discretion): model + comment/contract corrections only; no staff login endpoint or credential write path is added in this phase (that surface triggers the full governance hardening contract and belongs to a dedicated phase).
Output: migration + contract + tests + StaffCredential model + corrected comments, with a blocking schema-apply gate.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-CONTEXT.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-RESEARCH.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-PATTERNS.md
</context>

## Artifacts this phase produces

Plan 02 artifacts:
- Migration file: `20260717000000-add-dgfy-business-staff-credentials.cjs` (table `staff_credentials`; indexes `unique_staff_credentials_staff_account`, `idx_staff_credentials_status`; ENUM `enum_staff_credentials_credential_status`)
- Contract entry: `dgfyBusinessContract.tables.staff_credentials`
- Model: `StaffCredential` exported from `apps/dgfy-api/src/models/Tenant/StaffCredential.js` (modelName `StaffCredential`, tableName `staff_credentials`)
- Repository model registration: `StaffCredential` key in `staffOnboardingRepository.resolveModels()` return object
- Test suite: `describe('dgfy_business staff_credentials additive migration ...)` block in `tests/dgfyBusinessSchema.test.js`

<tasks>

<task type="auto">
  <name>Task 1: Additive staff_credentials migration + contract entry + schema tests (single commit)</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260717000000-add-dgfy-business-staff-credentials.cjs, apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js, apps/dgfy-migration-runner/tests/dgfyBusinessSchema.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/migrations/schema/20260711143000-add-dgfy-business-staff-invitations.cjs (the exact file shape to copy: meta block with targetKind 'business', tableExists/hasIndex/addIndexIfMissing guards, down() with MySQL ENUM cleanup, header-comment pattern)
    - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js (staff_invitations entry at ~lines 104-132 is the entry-shape analog; D-14 comment at ~lines 57-59 must be corrected)
    - apps/dgfy-migration-runner/tests/dgfyBusinessSchema.test.js (existing staff_invitations migration describe block at ~line 289 — copy its mock-queryInterface test approach)
    - .planning/phases/13.5-staff-authentication-model-correction/13.5-RESEARCH.md (Pattern 2 — authoritative column list)
    - docs/database/dgfy-foundation.md (Plan 01's staff_credentials row — the DDL must match the doc exactly)
  </read_first>
  <action>
    **Migration** `20260717000000-add-dgfy-business-staff-credentials.cjs` (timestamp must sort after the current latest migration `20260716100000-extend-schema-for-legacy-migration.cjs`; never edit any applied migration — DBF-04). Copy the staff-invitations migration shape wholesale:
    - `meta`: `destructive: false`, `targetKind: 'business'` (REQUIRED — omitting silently defaults to 'core'), `rollbackDescription` naming only this table, `estimatedRisk: 'low'`.
    - Header comment: explain why this is a separate additive migration (foundation migration is applied on 26 rehearsal tenants; editing it violates DBF-04) and state the security invariant inline: this table only ever stores bcrypt hashes copied from legacy — no column stores a raw password, PIN, or token (per D-13.5-02 and threat note 2).
    - `up()`: existence-guarded `createTable('staff_credentials', ...)` with columns exactly: `id` INTEGER PK autoIncrement; `staff_account_id` INTEGER `allowNull: false` references `staff_accounts.id` `onDelete: 'CASCADE'` `onUpdate: 'CASCADE'`; `password_hash` STRING(255) allowNull true; `pos_approval_pin_hash` STRING(255) allowNull true; `credential_status` ENUM('active','reset_required','disabled') allowNull false defaultValue 'active'; `password_updated_at` DATE allowNull true; `created_at`/`updated_at` DATE NOT NULL default CURRENT_TIMESTAMP. Then `addIndexIfMissing` for `unique_staff_credentials_staff_account` (unique, ['staff_account_id']) and `idx_staff_credentials_status` (['credential_status']).
    - `down()`: dropTable + MySQL `DROP TYPE IF EXISTS enum_staff_credentials_credential_status` guarded by dialect check, mirroring the analog.

    **Contract** (`dgfyBusinessContract.js`, SAME commit — Pitfall 4): add a `staff_credentials` entry following the `staff_invitations` entry shape: `columns` ['id','staff_account_id','password_hash','pos_approval_pin_hash','credential_status','password_updated_at','created_at','updated_at']; `indexes` ['unique_staff_credentials_staff_account','idx_staff_credentials_status']; `uniqueConstraints` ['unique_staff_credentials_staff_account']; `foreignKeys` [{ column: 'staff_account_id', referencesTable: 'staff_accounts', referencesColumn: 'id' }]; `projectionOnly: false`. Header comment on the entry references migration `20260717000000-add-dgfy-business-staff-credentials.cjs` and the phase (13.5, STAFF-01). Also correct the existing D-14 comment on `staff_accounts` (~lines 57-59): replace the claim that login/identity is DGFY-account-based with: tenant-local staff authorization profile; login is tenant-local via `staff_credentials`; DGFY account linkage via `account_staff_assignments` is optional (D-13.5-01, D-13.5-03; ADR 0028 Amendment).

    **Tests** (`tests/dgfyBusinessSchema.test.js`): add a describe block mirroring the staff_invitations migration block: (a) contract has a `staff_credentials` entry whose columns/indexes/FK match the migration's createTable/addIndex calls (assert against `dgfyBusinessContract.tables.staff_credentials`); (b) `up()` creates the table when absent with the contract columns, FK and both indexes; (c) `up()` is idempotent — with `showAllTables` mocked to include 'staff_credentials' and `showIndex` returning both index names, no createTable/addIndex call is made; (d) `meta.targetKind === 'business'` and `meta.destructive === false`.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dgfyBusinessSchema.test.js</automated>
  </verify>
  <acceptance_criteria>
    - New migration file exists with `targetKind: 'business'` and `destructive: false` in its meta block
    - `dgfyBusinessContract.tables.staff_credentials` exists with exactly the 8 columns, 2 indexes, 1 unique constraint, and the staff_accounts FK listed above
    - Migration and contract change are in the same git commit (git log shows both paths under one commit)
    - `tests/dgfyBusinessSchema.test.js` passes, including the idempotency (no-op re-run) test
    - The corrected `staff_accounts` contract comment mentions `staff_credentials` and describes DGFY linkage as optional
  </acceptance_criteria>
  <done>staff_credentials exists as contracted, verified, re-run-safe additive DDL scoped to business tenant DBs.</done>
</task>

<task type="auto">
  <name>Task 2: dgfy-api convergence — StaffCredential model + corrected staff assumptions</name>
  <files>apps/dgfy-api/src/models/Tenant/StaffCredential.js, apps/dgfy-api/src/models/Tenant/StaffAccount.js, apps/dgfy-api/src/modules/businesses/repositories/staffOnboardingRepository.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/migrations/schema/20260717000000-add-dgfy-business-staff-credentials.cjs (the FINAL applied-migration shape from Task 1 — the model must mirror it column-for-column; write the model AFTER this file is final)
    - apps/dgfy-api/src/models/Tenant/StaffInvitation.js (the file-structure analog: header comment naming the mirrored migration, associate() guard, init() matching migration, underscored/timestamps/named indexes)
    - apps/dgfy-api/src/models/Tenant/StaffAccount.js (D-14 doc comment at lines ~3-10 to correct; ENUM at line ~60 stays untouched)
    - apps/dgfy-api/src/modules/businesses/repositories/staffOnboardingRepository.js (wrong-assumption JSDoc + createStaffAccount at lines ~187-203; resolveModels at lines ~91-102; toPlainStaffAccount allow-list at lines ~244-257)
  </read_first>
  <action>
    **NEW `StaffCredential.js`**: persistence-only Sequelize model copying `StaffInvitation.js`'s structure exactly. Header comment names migration `20260717000000-add-dgfy-business-staff-credentials.cjs` as the mirrored source and states the no-business-logic rule. `associate()`: `belongsTo(models.StaffAccount, { foreignKey: 'staff_account_id', as: 'staffAccount' })` with the existing-association guard. `init()` columns mirror the applied migration exactly (`staff_account_id` allowNull false, references staff_accounts, onDelete CASCADE; `password_hash`/`pos_approval_pin_hash` STRING(255) null; `credential_status` ENUM('active','reset_required','disabled') NOT NULL default 'active'; `password_updated_at` DATE null). Options: `modelName: 'StaffCredential'`, `tableName: 'staff_credentials'`, `underscored: true`, timestamps mapped to created_at/updated_at, indexes named `unique_staff_credentials_staff_account` (unique) and `idx_staff_credentials_status`.

    **`StaffAccount.js`**: correct only the header doc comment (lines ~3-10): replace "identity/login itself is DGFY-account-based" with the tenant-local-first statement — tenant-local staff authorization profile; credentials live in `staff_credentials` (see StaffCredential.js); `account_staff_assignments` linkage to a DGFY account is optional (ADR 0028 Amendment, D-13.5-01). No schema/ENUM change.

    **`staffOnboardingRepository.js`** (minimal convergence — per 13.5-RESEARCH.md Open Question 1, documented as Claude's discretion): (a) rewrite the `createStaffAccount()` JSDoc (lines ~187-203) — remove the claim that `initialPassword` is intentionally not accepted because credentials always come from DGFY accounts; state instead that tenant-local credentials live in `staff_credentials` (ADR 0028 Amendment) and that the credential write path (invitation-accept password setup / login) is a future dedicated phase, so this method still creates profile-only rows today; (b) import `defineStaffCredentialModel` from `../../../models/Tenant/StaffCredential.js` and register `StaffCredential` in `resolveModels()` beside `StaffAccount`/`StaffInvitation` (and update the `modelsByDatabase` and `withModels` JSDoc mentions of the model set) so the model participates in tenant connections and future usecases; call `associate()` consistently with how StaffInvitation/StaffAccount associations are wired in this file, if they are; (c) leave `toPlainStaffAccount()` untouched — it must remain an allow-list that never includes credential fields. Do NOT add any endpoint, usecase, or credential write/read logic.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/integration/businesses/staffOnboardingRepository.test.js && cd ../.. && npm run check:architecture:dgfy-api</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dgfy-api/src/models/Tenant/StaffCredential.js` exists; its `init()` field names and ENUM values are identical to the migration's createTable columns (manual diff of column lists: id, staff_account_id, password_hash, pos_approval_pin_hash, credential_status, password_updated_at + timestamps)
    - `resolveModels()` return object includes a `StaffCredential` key
    - `grep -c "DGFY-account-based" apps/dgfy-api/src/models/Tenant/StaffAccount.js` returns 0
    - `createStaffAccount()` runtime behavior unchanged: existing integration test `staffOnboardingRepository.test.js` passes without test modifications to createStaffAccount expectations
    - `toPlainStaffAccount()` field list unchanged (no credential fields added)
    - `npm run check:architecture:dgfy-api` exits 0 (models stay persistence-only, layering intact)
  </acceptance_criteria>
  <done>dgfy-api no longer contradicts the amended ADR; StaffCredential model exists mirroring the applied migration; no new auth surface introduced.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply the staff_credentials schema migration before verification</name>
  <files>apps/dgfy-migration-runner (no source changes — execution gate)</files>
  <read_first>
    - apps/dgfy-migration-runner/src/commands/schema.js (schema migrate command behavior and env validation)
    - .planning/STATE.md (known 12-04 blocker: lima-dgfy-dev MySQL rejects runner .env user 'sieitzsqladmin'; DGFY_BUSINESS_DB_NAMES unset locally)
  </read_first>
  <action>
    Schema-gate task (BLOCKING): after Tasks 1-2 are complete, apply the new migration via the project's documented migration command so verification does not pass on stale schema. From `apps/dgfy-migration-runner`, run `node src/cli.js schema migrate` against the configured environment. Expected outcome: the runner executes `20260717000000-add-dgfy-business-staff-credentials.cjs` against each configured `dgfy_business_*` target and its schema report lists the new migration as executed; a second run is a no-op (existence guards).

    Known environment constraint (STATE.md, 12-04-SUMMARY): the local lima-dgfy-dev MySQL rejects the runner's configured DB user and `DGFY_BUSINESS_DB_NAMES` is unset, so a local apply may fail at env validation, not at migration correctness. If and only if the command fails with the documented credential/target-config blocker: (a) run the full runner test suite (`npm test` in apps/dgfy-migration-runner) as the schema-correctness evidence (the Task 1 mock-queryInterface tests prove up() DDL, contract alignment, and idempotency without a DB), and (b) record in the SUMMARY that the DB-backed apply is deferred to the Phase 14 re-rehearsal on the dgfy-temp rehearsal host (`~/dgfy-rehearsal-template` reset procedure), which is the phase that owns end-to-end re-rehearsal per ROADMAP. Do not weaken or skip this task silently — the SUMMARY must state which of the two outcomes occurred.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && npm test</automated>
  </verify>
  <acceptance_criteria>
    - EITHER: `node src/cli.js schema migrate` output/report shows `20260717000000-add-dgfy-business-staff-credentials.cjs` executed against business targets, and an immediate re-run reports it as already applied / no-op
    - OR: the command fails ONLY with the pre-existing 12-04 env blocker (DB user rejected / DGFY_BUSINESS_DB_NAMES unset), `npm test` in apps/dgfy-migration-runner exits 0 (including the new dgfyBusinessSchema staff_credentials tests), and the plan SUMMARY explicitly records the deferred DB-backed apply with the rehearsal-host follow-up
    - `npm test` (runner) exits 0 in both outcomes — includes check:architecture for the runner
  </acceptance_criteria>
  <done>The new schema is applied (or its apply is explicitly evidence-deferred to the rehearsal host with full mock-based proof), never silently assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff profile reads → credential storage | staff_accounts serializers must never reach hash columns |
| migration runner → 26 populated rehearsal tenant DBs | DDL must be additive and re-run-safe; no mutation of existing rows/tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13.5-04 | Information Disclosure | staff_accounts profile serializers | high | mitigate | Credentials live in a SEPARATE staff_credentials table (D-13.5-02, research Pattern 2) — hashes structurally absent from whole-row staff_accounts reads; toPlainStaffAccount allow-list asserted unchanged (Task 2 acceptance) |
| T-13.5-05 | Tampering | populated rehearsal tenant DBs | high | mitigate | Additive existence-guarded migration only; foundation migration never edited (DBF-04); down() scoped to the one new table (Task 1) |
| T-13.5-06 | Denial of Service | schema verify vs. contract drift | medium | mitigate | Contract entry in same commit as migration (Pitfall 4); jest test asserts contract↔DDL alignment (Task 1) |
| T-13.5-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed in this phase |
</threat_model>

<verification>
- Runner suite green: `cd apps/dgfy-migration-runner && npm test`
- dgfy-api staff onboarding integration test green + `npm run check:architecture:dgfy-api` exits 0
- Blocking schema-apply gate outcome recorded (applied, or evidence-deferred per Task 3 criteria)
</verification>

<success_criteria>
- Phase success criterion 2 satisfied: explicit tenant-local credential target exists in the dgfy_business_* schema; staff_accounts profile is not confused with global DGFY identity (comments corrected in contract, model, repository)
- STAFF-01 schema half done; STAFF-02 assumption convergence done; STAFF-04 contract surface amended
- No new auth endpoint added (minimal API convergence decision documented in objective)
</success_criteria>

<output>
Create `.planning/phases/13.5-staff-authentication-model-correction/13.5-02-SUMMARY.md` when done.
</output>
