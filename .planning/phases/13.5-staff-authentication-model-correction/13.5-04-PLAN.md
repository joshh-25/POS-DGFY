---
phase: 13.5-staff-authentication-model-correction
plan: 04
type: execute
wave: 4
depends_on: ["13.5-03"]
files_modified:
  - apps/dgfy-migration-runner/src/commands/data.js
  - apps/dgfy-migration-runner/src/data/dryRun.js
  - apps/dgfy-migration-runner/src/data/verifyData.js
  - apps/dgfy-migration-runner/tests/dataDryRun.test.js
  - apps/dgfy-migration-runner/tests/reportCommands.test.js
  - apps/dgfy-migration-runner/tests/dataVerify.test.js
autonomous: true
requirements: [STAFF-04, STAFF-02, STAFF-03]

must_haves:
  truths:
    - "Dry-run report files contain no bcrypt hash content — credential-bearing payload fields are replaced by boolean evidence flags (closes the existing entity-1 password_hash leak too)"
    - "Re-running verify against the Phase 13 run scope yields data_migration_ok=true while every missing_accepted_membership finding remains visible under a new staff_auth section (D-13.5-05)"
    - "Only staff-linkage reason codes become non-blocking; missing_required_field, duplicate_email, owner_mismatch and all other codes still block (Pitfall 5)"
    - "An existing account_staff_assignments row without an accepted membership is still a BLOCKING relationship violation (corrupt linkage ≠ pending linkage)"
    - "staff_auth section reports per-tenant credential coverage (staff accounts vs credential rows vs reset_required) so accepted staff credential migration remains auditable (STAFF-03)"
  artifacts:
    - "apps/dgfy-migration-runner/src/data/verifyData.js (STAFF_LINKAGE_REASON_CODES set + staff_auth section + split ok computation)"
    - "apps/dgfy-migration-runner/src/commands/data.js + src/data/dryRun.js (redacted dry-run report assembly)"
    - "extended tests: dataVerify.test.js, dataDryRun.test.js, reportCommands.test.js"
  key_links:
    - "STAFF_LINKAGE_REASON_CODES members ↔ MAPPING_REASON_CODES values from Plan 03 (missing_accepted_membership, orphan_tenant_user_link, staff_credential_reset_required)"
    - "staff_auth credential counters ↔ staff_credentials table from Plan 02 (selectAll on the new table beside staff_accounts)"
    - "verify JSON/summary output shape ↔ RUN-05 machine-readable + human-readable report contract"
---

<objective>
Split verification into product/inventory fidelity vs staff-auth linkage (non-blocking but fully auditable), and redact credential-bearing fields from dry-run report files (fixing the pre-existing landlord password_hash leak as well).

Purpose: phase success criterion 5 — Phase 13's data proof turns green on product/inventory fidelity without deleting or hiding staff-linkage findings; threat note 2 — no hashes on disk in reports.
Output: verifyData.js staff_auth split + dry-run redaction, all unit-tested; re-running verify on the existing rehearsal run scope is designed to re-establish Phase 13's proof without a fresh apply cycle.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-CONTEXT.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-RESEARCH.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-PATTERNS.md
</context>

## Artifacts this phase produces

Plan 04 artifacts (new symbols):
- `STAFF_LINKAGE_REASON_CODES` frozen Set exported from verifyData.js (members: missing_accepted_membership, orphan_tenant_user_link, staff_credential_reset_required)
- `summarizeStaffAuth(...)` (or equivalently named exported builder) producing the `staff_auth` report section with `staff_linkage_ok` and per-tenant counters
- `staff_auth` key in the verify report's `data_migration` object
- `checkOpenFindings()` return fields `staff_linkage_count` / `staff_linkage_findings` (parallel to expected_lossy fields)
- `redactTargetPayload(entry)` (or equivalently named) report-assembly helper used by the dry-run report path
- Dry-run report evidence flags: `has_password_hash`, `has_pos_approval_pin_hash`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redact credential fields from dry-run report assembly</name>
  <files>apps/dgfy-migration-runner/src/commands/data.js, apps/dgfy-migration-runner/src/data/dryRun.js, apps/dgfy-migration-runner/tests/dataDryRun.test.js, apps/dgfy-migration-runner/tests/reportCommands.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/commands/data.js (dry-run report assembly at lines ~65-83 — `results: entries` currently writes full mapper target_payload to disk)
    - apps/dgfy-migration-runner/src/data/dryRun.js (related_targets flattened into standalone plan entries at lines ~134-143 — credential payloads flow into results through here)
    - apps/dgfy-migration-runner/src/data/apply.js (recordResult lines ~430-442 — the allow-list precedent this task copies)
    - apps/dgfy-migration-runner/tests/reportCommands.test.js and tests/dataDryRun.test.js (existing report/dry-run test style)
  </read_first>
  <behavior>
    - Test: a dry-run report assembled from entries whose payloads contain a synthetic bcrypt string (`$2a$10$` fixture) in password_hash or pos_approval_pin_hash — for any entity type including 'account' and 'staff_credential' — serializes (JSON.stringify) with zero matches of /\$2[abxy]\$/
    - Test: redacted entries carry `has_password_hash: true` / `has_pos_approval_pin_hash: true` evidence flags in place of the removed values (false when the source field was null/absent for credential-bearing tables)
    - Test: non-credential payload fields (email, display_name, status, target_table, operation, findings summaries) remain present in report entries — evidence richness preserved
    - Test: the dry-run summary counters (planned_inserts, skips, conflicts) are unchanged by redaction
  </behavior>
  <action>
    Fix Pitfall 2 at report-assembly time (mapper and apply continue to see full payloads internally — redaction applies only to what is written to disk):

    1. Add a pure report-redaction helper (in dryRun.js or a small shared location within the runner data layer — NOT in mappings.js, which must stay import-free): `redactTargetPayload(entry)` returns a report-safe copy of a plan entry where the payload fields named `password_hash` and `pos_approval_pin_hash` are removed and replaced with boolean evidence flags `has_password_hash` / `has_pos_approval_pin_hash` (true iff the field held a non-empty string). Build the redacted entry field-by-field in the spirit of apply.js `recordResult()` (allow-list construction, not denylist mutation), keeping all other payload fields intact.
    2. In `commands/data.js` dry-run report assembly (~lines 65-83), map `entries` through the redaction helper before placing them in `report.results`. This must cover BOTH top-level entries and the related_targets entries that `dryRun.js` flattens into the plan (lines ~134-143) — after Plan 03, `staff_credential` entries flow through this path, and `account` entries (entity 1) already leak landlord password hashes today; this fix closes both.
    3. Do not change apply-report assembly (already payload-free) or the mapper outputs themselves.

    Write the failing tests first (behavior list above), then implement, RED→GREEN.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataDryRun.test.js tests/reportCommands.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests above pass
    - JSON.stringify of an assembled dry-run report built from hash-bearing fixtures has zero /\$2[abxy]\$/ matches (test assertion)
    - Evidence flags present and truthful for both credential-bearing entity types (account, staff_credential)
    - Existing dry-run/report tests pass (summary counters and non-credential evidence unchanged)
  </acceptance_criteria>
  <done>Dry-run reports on disk are structurally hash-free while preserving migration evidence via boolean flags; the pre-existing entity-1 leak is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verifyData.js staff_auth split — non-blocking staff-linkage findings + credential coverage counters</name>
  <files>apps/dgfy-migration-runner/src/data/verifyData.js, apps/dgfy-migration-runner/tests/dataVerify.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/verifyData.js (full — EXPECTED_LOSSY_REASON_CODES + checkOpenFindings lines ~32-35/146-172; checkRequiredRelationships lines ~78-115 KEEP BLOCKING; summarizeStorefrontDiscoveryProjection non-blocking section shape lines ~182-188; per-target counting/selectAll('staff_accounts') around lines ~365-453; final data_migration_ok assembly lines ~559-568)
    - apps/dgfy-migration-runner/src/data/mappings.js (Plan 03 output — MAPPING_REASON_CODES members referenced by the new set)
    - apps/dgfy-migration-runner/tests/dataVerify.test.js (existing checkOpenFindings/verify assembly test style)
    - .planning/phases/13.5-staff-authentication-model-correction/13.5-CONTEXT.md (Phase 13 rehearsal evidence: all 26 targets passed, global data_migration_ok=false solely due to open missing_accepted_membership findings — the exact state this task must re-classify)
  </read_first>
  <behavior>
    - Test: checkOpenFindings with only missing_accepted_membership findings open → ok=true, staff_linkage_count equals the finding count, blocking_count=0, findings still present in the returned staff-linkage list (visible, not deleted)
    - Test: checkOpenFindings with a staff_credential_reset_required finding → non-blocking, routed to staff-linkage list
    - Test: checkOpenFindings with missing_required_field or duplicate_email or owner_mismatch open → ok=false (still blocking; Pitfall 5 guard)
    - Test: checkRequiredRelationships still returns a violation for an account_staff_assignments row without accepted membership, and that violation still fails data_migration_ok
    - Test: verify assembly with 26 passing targets + open missing_accepted_membership findings → data_migration.ok === true AND data_migration.staff_auth.staff_linkage_ok reflects the open linkage findings with counts (the exact Phase 13 re-proof scenario, per research Open Question 2 recommendation: re-running verify on the same run_scope re-establishes proof without a fresh apply)
    - Test: staff_auth per-tenant counters: given a tenant with N staff_accounts rows, M staff_credentials rows, and R rows with credential_status 'reset_required', section reports staff_account_count=N, credential_count=M, reset_required_count=R, and linked-assignment count
    - Test: no field of the staff_auth section ever contains password_hash/pos_approval_pin_hash values (counters and statuses only)
  </behavior>
  <action>
    In `verifyData.js`:

    1. Add `export const STAFF_LINKAGE_REASON_CODES = Object.freeze(new Set([...]))` containing `MAPPING_REASON_CODES.MISSING_ACCEPTED_MEMBERSHIP`, the existing orphan-tenant-user-link code (use the exact MAPPING_REASON_CODES member — locate it in mappings.js), and `MAPPING_REASON_CODES.STAFF_CREDENTIAL_RESET_REQUIRED` (from Plan 03). Nothing else (Pitfall 5).
    2. Extend `checkOpenFindings()` with a third routing bucket parallel to expected-lossy: findings whose reason_code is in STAFF_LINKAGE_REASON_CODES go to `staff_linkage_findings` (returned + counted via `staff_linkage_count`) and do NOT enter `blocking_findings`; `ok` remains `blocking_findings.length === 0`. Preserve the existing expected-lossy behavior and return shape (additive fields only, so existing consumers keep working).
    3. Add a `staff_auth` section builder (exported, e.g. `summarizeStaffAuth`) following the `summarizeStorefrontDiscoveryProjection()` non-blocking-section shape but carrying its own `staff_linkage_ok` plus per-tenant counters. Data sources: in the per-target verification pass, alongside the existing `selectAll(businessSequelize, 'staff_accounts')`, add `selectAll(businessSequelize, 'staff_credentials')` and count: staff_account_count, credential_count, reset_required_count (credential_status='reset_required'), active_credential_count, assignment_count (existing account_staff_assignments data already read for relationship checks). `staff_linkage_ok` = no staff-linkage findings open AND no relationship violations; it is reported but NEVER folded into data_migration_ok. Counters only — never select or echo hash column VALUES into the section (select column names explicitly or strip hash fields; the section must be secrets-free by construction).
    4. In the final assembly (~lines 559-568), keep `data_migration_ok = targetVerifications.every(ok) && openFindingsCheck.ok` — which now excludes staff-linkage findings via step 2 — and add `staff_auth: <section>` to the returned `data_migration` object. `checkRequiredRelationships()` violations must keep failing their target's ok (unchanged): an EXISTING assignment row without accepted membership is corrupt linkage (D-13.5-03 evidence rule survives; only pending-linkage findings became non-blocking per D-13.5-05).
    5. Handle the schema-transition edge: tenants whose DBs predate the Plan 02 migration (no staff_credentials table yet, e.g. current rehearsal DBs before re-migration) must not crash verify — if the table is absent, report credential_count=null with a `staff_credentials_table_missing: true` flag in that tenant's staff_auth entry (auditable, non-throwing).

    Confirm the verify command (`src/commands/verify.js`) serializes the new section into both the JSON report and the human summary without changes, or make the minimal wiring addition there if the summary writer enumerates sections explicitly (RUN-05: every command emits machine-readable + human-readable evidence).

    Write the failing tests first (behavior list above), then implement, RED→GREEN.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataVerify.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests above pass
    - STAFF_LINKAGE_REASON_CODES has exactly 3 members (source assertion in a test)
    - The Phase 13 re-proof scenario test passes: passing targets + open missing_accepted_membership findings → data_migration.ok true, findings visible under staff_auth
    - Blocking-codes regression test passes (missing_required_field et al. still fail verification)
    - checkRequiredRelationships assignment-without-membership violation still blocking (test)
    - Full runner suite green: `npm test` in apps/dgfy-migration-runner exits 0
  </acceptance_criteria>
  <done>Verification distinguishes staff-auth linkage repair from product/inventory fidelity: Phase 13 proof is re-establishable by re-running verify, while every staff finding stays auditable and every non-staff gate stays blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner → report files on disk (rehearsal host) | dry-run reports are readable operator artifacts; must never carry hashes |
| findings metadata → verification verdict | reclassification must not weaken non-staff integrity gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13.5-12 | Information Disclosure | dry-run report files | high | mitigate | Allow-list report assembly with boolean evidence flags; zero-/\$2[abxy]\$/-in-serialized-report test; closes pre-existing entity-1 leak (Task 1) |
| T-13.5-13 | Elevation of Privilege | verify linkage gates | high | mitigate | STAFF_LINKAGE_REASON_CODES scoped to exactly 3 codes; checkRequiredRelationships assignment-without-membership stays blocking (D-13.5-03 evidence rule); regression tests for blocking codes (Task 2, Pitfall 5) |
| T-13.5-14 | Repudiation | staff-linkage audit trail | medium | mitigate | Findings re-routed, never deleted; staff_auth section reports per-tenant counters and finding lists (criterion 5 auditability) |
| T-13.5-15 | Information Disclosure | staff_auth verify section | medium | mitigate | Section built from counts/statuses only; explicit no-hash-values-in-section test (Task 2) |
| T-13.5-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed in this phase |
</threat_model>

<verification>
- `cd apps/dgfy-migration-runner && npm test` exits 0 (dry-run, reports, verify, mappings, apply, schema, architecture)
- Phase 13 re-proof scenario covered by unit test (26-targets-pass + open membership findings → data_migration_ok=true); the live re-run of `node src/cli.js verify` against the dgfy-temp rehearsal run scope is executed during Phase 14 re-rehearsal (which owns end-to-end proof per ROADMAP) — this plan makes that re-run sufficient without a fresh apply cycle
</verification>

<success_criteria>
- Phase success criteria 4 and 5 satisfied: DGFY membership/assignment migration is optional linkage evidence (pending membership no longer fails data proof, D-13.5-05) while accepted-staff credential migration remains auditable per tenant
- STAFF-04 verification-gates surface amended; STAFF-02 optionality enforced in verify semantics; STAFF-03 audit trail (reset_required counters) reportable
- No hash content can reach report files from either dry-run or verify paths
</success_criteria>

<output>
Create `.planning/phases/13.5-staff-authentication-model-correction/13.5-04-SUMMARY.md` when done.
</output>
