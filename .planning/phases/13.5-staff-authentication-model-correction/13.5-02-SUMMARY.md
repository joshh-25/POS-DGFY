---
phase: 13.5-staff-authentication-model-correction
plan: 02
subsystem: auth
tags: [staff-auth, migration-runner, dgfy-api, sequelize, mysql]

requires:
  - phase: 13.5
    plan: 01
    provides: [tenant-local-staff-first ADR and database docs]
provides:
  - additive dgfy_business staff_credentials migration and schema contract entry
  - persistence-only dgfy-api StaffCredential model
  - corrected dgfy-api staff onboarding assumptions
affects: [phase-13.5, phase-14, dgfy-migration-runner, dgfy-api]

tech-stack:
  added: []
  patterns:
    - additive business-scoped Sequelize migration with existence guards
    - schema contract entry committed atomically with migration
    - persistence-only Sequelize model mirroring applied migration DDL

key-files:
  created:
    - apps/dgfy-migration-runner/src/migrations/schema/20260717000000-add-dgfy-business-staff-credentials.cjs
    - apps/dgfy-api/src/models/Tenant/StaffCredential.js
    - .planning/phases/13.5-staff-authentication-model-correction/13.5-02-SUMMARY.md
  modified:
    - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js
    - apps/dgfy-migration-runner/tests/dgfyBusinessSchema.test.js
    - apps/dgfy-api/src/models/Tenant/StaffAccount.js
    - apps/dgfy-api/src/modules/businesses/repositories/staffOnboardingRepository.js

key-decisions:
  - "staff_credentials is a separate same-tenant table, not columns on staff_accounts."
  - "dgfy-api adds a persistence-only StaffCredential model but no credential write/read endpoint in this plan."
  - "DB-backed schema apply is deferred to the Phase 14 rehearsal host because local MySQL still rejects the configured runner user."

patterns-established:
  - "Contract and migration for additive tenant tables must be committed together and covered by contract-to-DDL tests."
  - "Staff profile serializers remain credential-free by keeping credential hashes structurally separate."

requirements-completed: [STAFF-01, STAFF-02, STAFF-04]

coverage:
  - id: D1
    description: "Tenant-local staff_credentials schema target exists with contract, migration, and idempotency tests."
    requirement: STAFF-01
    verification:
      - kind: unit
        ref: "cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dgfyBusinessSchema.test.js"
        status: pass
      - kind: unit
        ref: "cd apps/dgfy-migration-runner && npm test"
        status: pass
    human_judgment: false
  - id: D2
    description: "dgfy-api StaffCredential model mirrors the migration and staff onboarding comments no longer require DGFY-account-based credentials."
    requirement: STAFF-02
    verification:
      - kind: integration
        ref: "cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/integration/businesses/staffOnboardingRepository.test.js"
        status: pass
      - kind: other
        ref: "npm run check:architecture:dgfy-api"
        status: pass
    human_judgment: false
  - id: D3
    description: "Schema apply gate attempted real runner migration and explicitly evidence-deferred only because of the known local DB credential blocker."
    requirement: STAFF-04
    verification:
      - kind: other
        ref: "cd apps/dgfy-migration-runner && node src/cli.js schema migrate"
        status: fail
      - kind: unit
        ref: "cd apps/dgfy-migration-runner && npm test"
        status: pass
    human_judgment: true
    rationale: "DB-backed apply requires operator-fixed local/rehearsal MySQL credentials and DGFY_BUSINESS_DB_NAMES; mock DDL and runner suite passed."

duration: 4min
completed: 2026-07-14
status: complete
---

# Phase 13.5 Plan 02: Staff Authentication Model Correction Summary

**Tenant-local staff credentials now have an additive tenant schema target, matching contract/tests, and a persistence-only dgfy-api model without adding auth behavior.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-07-14T22:16:00Z
- **Completed:** 2026-07-14T22:19:50Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Added `staff_credentials` as an existence-guarded, business-scoped schema migration with the contract entry committed in the same task.
- Added schema tests proving contract columns/indexes/FK alignment, non-destructive business scoping, and no-op rerun behavior.
- Added `StaffCredential.js` as a persistence-only Sequelize model mirroring the applied migration column-for-column.
- Corrected dgfy-api comments/JSDoc so staff credentials are tenant-local via `staff_credentials`, with DGFY account linkage optional.
- Ran the blocking schema-apply gate and recorded the known local DB credential blocker with full runner-suite fallback evidence.

## Task Commits

Each source task was committed atomically:

1. **Task 1: Additive staff_credentials migration + contract entry + schema tests** - `a82aa0aa` (feat)
2. **Task 2: dgfy-api convergence — StaffCredential model + corrected staff assumptions** - `1558e856` (feat)
3. **Task 3: Blocking schema-apply gate** - no source commit; execution-only gate recorded in this summary.

## Files Created/Modified

- `apps/dgfy-migration-runner/src/migrations/schema/20260717000000-add-dgfy-business-staff-credentials.cjs` - New additive tenant-local credential table migration.
- `apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js` - Added `staff_credentials` contract entry and corrected `staff_accounts` comment.
- `apps/dgfy-migration-runner/tests/dgfyBusinessSchema.test.js` - Added staff_credentials contract/DDL/idempotency tests.
- `apps/dgfy-api/src/models/Tenant/StaffCredential.js` - New persistence-only StaffCredential model.
- `apps/dgfy-api/src/models/Tenant/StaffAccount.js` - Corrected staff profile comment and added StaffCredential association.
- `apps/dgfy-api/src/modules/businesses/repositories/staffOnboardingRepository.js` - Registered StaffCredential and corrected createStaffAccount JSDoc.
- `.planning/phases/13.5-staff-authentication-model-correction/13.5-02-SUMMARY.md` - Captures this plan result.

## Decisions Made

- `staff_credentials` remains separate from `staff_accounts` so profile reads and `toPlainStaffAccount()` cannot expose hash columns.
- API convergence is intentionally minimal: model registration and assumptions only; no endpoint, use case, staff login, or credential write/read path was added.
- The DB-backed apply remains deferred to the Phase 14 re-rehearsal host because local schema migration failed only on the known MySQL credential blocker.

## Deviations from Plan

None - plan executed as written. The schema-apply fallback path was explicitly defined by Task 3.

## Issues Encountered

- `cd apps/dgfy-migration-runner && node src/cli.js schema migrate` failed with `Access denied for user 'sieitzsqladmin'@'172.18.0.1' (using password: YES)`, matching the pre-existing 12-04 local environment blocker. The command did not reach migration correctness.
- Per Task 3, `cd apps/dgfy-migration-runner && npm test` was run as fallback schema-correctness evidence and passed.

## Known Stubs

None. Stub-pattern scan found only test fixture empty arrays/objects and optional function defaults; no UI/data-source stub was introduced.

## Threat Flags

None. The new credential storage surface is the planned mitigated surface from T-13.5-04/T-13.5-05: hashes live in a separate table, profile serializers stay credential-free, and the migration is additive/re-run-safe.

## User Setup Required

The DB-backed schema apply still requires the existing environment blocker to be fixed before rehearsal:

- Configure runner credentials that can access the target MySQL host.
- Configure `DGFY_BUSINESS_DB_NAMES` for the intended `dgfy_business_*` targets.
- Re-run the schema migration on the Phase 14 dgfy-temp rehearsal host after resetting `~/dgfy-rehearsal-template`.

## Verification

- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dgfyBusinessSchema.test.js` passed.
- `cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/integration/businesses/staffOnboardingRepository.test.js && cd ../.. && npm run check:architecture:dgfy-api` passed.
- `cd apps/dgfy-migration-runner && node src/cli.js schema migrate` failed only on the known MySQL credential blocker.
- `cd apps/dgfy-migration-runner && npm test` passed: 31 test suites passed, 5 skipped; 374 tests passed, 10 skipped; architecture test passed 4/4.
- Pre-commit hooks ran on both source commits and passed.

## Next Phase Readiness

Plan 03 can implement mapper/apply behavior against a concrete `staff_credentials` target and a dgfy-api model that no longer contradicts the tenant-local-first staff-auth model. DB-backed apply proof is still deferred to the Phase 14 rehearsal environment because local runner credentials remain blocked.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/13.5-staff-authentication-model-correction/13.5-02-SUMMARY.md`.
- Task commit `a82aa0aa` exists and contains the migration, contract, and schema test paths together.
- Task commit `1558e856` exists and contains the StaffCredential model plus dgfy-api assumption corrections.
- Required validation commands were run; the only failed command was the expected DB credential-gated schema apply.

---
*Phase: 13.5-staff-authentication-model-correction*
*Completed: 2026-07-14*
