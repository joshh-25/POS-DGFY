---
phase: 13.5-staff-authentication-model-correction
plan: 04
subsystem: auth
tags: [staff-auth, migration-runner, dry-run, verify, reports]

requires:
  - phase: 13.5
    plan: 03
    provides: [staff credential mapper/apply reason codes and staff_credentials related targets]
provides:
  - dry-run report redaction for password_hash and pos_approval_pin_hash payloads
  - staff_auth verification section with staff-linkage findings and credential counters
  - non-blocking staff-linkage finding routing for Phase 13 re-proof
  - human-readable verify summary fields for staff-auth linkage status
affects: [phase-13, phase-13.5, phase-14, dgfy-migration-runner]

tech-stack:
  added: []
  patterns:
    - report-safe dry-run entry assembly with boolean credential evidence flags
    - visible-but-non-blocking staff-linkage findings parallel to expected-lossy findings
    - staff credential verification counters selected without hash values

key-files:
  created:
    - .planning/phases/13.5-staff-authentication-model-correction/13.5-04-SUMMARY.md
  modified:
    - apps/dgfy-migration-runner/src/commands/data.js
    - apps/dgfy-migration-runner/src/data/dryRun.js
    - apps/dgfy-migration-runner/src/data/verifyData.js
    - apps/dgfy-migration-runner/src/commands/verify.js
    - apps/dgfy-migration-runner/tests/dataDryRun.test.js
    - apps/dgfy-migration-runner/tests/dataCommand.test.js
    - apps/dgfy-migration-runner/tests/dataVerify.test.js

key-decisions:
  - "Dry-run reports redact credential-bearing target_payload fields at report assembly, not inside mapper/apply internals."
  - "Only missing_accepted_membership, orphan_tenant_user_link, and staff_credential_reset_required are staff-linkage non-blocking reason codes."
  - "Existing account_staff_assignments rows without accepted membership remain blocking relationship violations."
  - "staff_auth reports counters/statuses only and never echoes password_hash or pos_approval_pin_hash values."

patterns-established:
  - "Use redactTargetPayload() for dry-run report-safe copies when mapper payloads can contain secrets."
  - "Route optional linkage repair findings into staff_auth while keeping non-staff integrity findings blocking."

requirements-completed: [STAFF-04, STAFF-02, STAFF-03]

coverage:
  - id: D1
    description: "Dry-run JSON reports are structurally free of bcrypt hash values while preserving credential evidence flags."
    requirement: STAFF-03
    verification:
      - kind: unit
        ref: "cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataDryRun.test.js tests/reportCommands.test.js"
        status: pass
      - kind: unit
        ref: "apps/dgfy-migration-runner/tests/dataCommand.test.js#runDataDryRun redacts credential-bearing payload fields"
        status: pass
    human_judgment: false
  - id: D2
    description: "Verification separates staff-auth linkage repair from product/inventory data fidelity while preserving blocking integrity gates."
    requirement: STAFF-04
    verification:
      - kind: unit
        ref: "cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataVerify.test.js"
        status: pass
      - kind: unit
        ref: "cd apps/dgfy-migration-runner && npm test"
        status: pass
    human_judgment: false

duration: 45min
completed: 2026-07-14
status: complete
---

# Phase 13.5 Plan 04: Staff Authentication Model Correction Summary

**Dry-run reports now redact credential hashes, and verification reports staff-auth linkage separately from product/inventory data fidelity.**

## Performance

- **Duration:** 45 min
- **Started:** 2026-07-14T21:51:00Z
- **Completed:** 2026-07-14T22:36:07Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments

- Added `redactTargetPayload()` and wired `runDataDryRun()` to write report-safe entries with `has_password_hash` / `has_pos_approval_pin_hash` evidence flags.
- Closed the pre-existing dry-run account `password_hash` report leak and the new staff credential payload leak path.
- Added `STAFF_LINKAGE_REASON_CODES` with exactly three non-blocking codes: `missing_accepted_membership`, `orphan_tenant_user_link`, and `staff_credential_reset_required`.
- Added a `staff_auth` verification section with per-tenant staff account, credential, reset-required, active credential, and assignment counters.
- Preserved blocking behavior for non-staff findings and corrupt existing assignment rows without accepted membership evidence.
- Added scalar `verify` summary fields for staff-auth linkage status so the human-readable `.summary.txt` includes the new evidence.

## Task Commits

Each TDD task was committed atomically:

1. **Task 1 RED: Dry-run credential redaction coverage** - `abd28c7d` (test)
2. **Task 1 GREEN: Dry-run report credential redaction** - `dff180f8` (feat)
3. **Task 2 RED: Staff-auth verification split coverage** - `6406a6ba` (test)
4. **Task 2 GREEN: Staff-auth verification split implementation** - `46edc108` (feat)

## Files Created/Modified

- `apps/dgfy-migration-runner/src/data/dryRun.js` - Added pure report-safe redaction helper.
- `apps/dgfy-migration-runner/src/commands/data.js` - Applies redaction before dry-run report assembly.
- `apps/dgfy-migration-runner/src/data/verifyData.js` - Adds staff-linkage routing, `staff_auth` counters, missing-table handling, and no-hash credential coverage reads.
- `apps/dgfy-migration-runner/src/commands/verify.js` - Adds staff-auth linkage summary fields for human-readable summaries.
- `apps/dgfy-migration-runner/tests/dataDryRun.test.js` - Covers redaction helper behavior and evidence flags.
- `apps/dgfy-migration-runner/tests/dataCommand.test.js` - Covers returned/written dry-run report redaction.
- `apps/dgfy-migration-runner/tests/dataVerify.test.js` - Covers staff-linkage routing, counters, re-proof scenario, blocking-code regression, and assignment corruption guard.
- `.planning/phases/13.5-staff-authentication-model-correction/13.5-04-SUMMARY.md` - Captures this plan result.

## Decisions Made

- Redaction happens at dry-run report assembly so mapper and apply internals continue to see full target payloads.
- The staff-linkage non-blocking set is intentionally narrow; missing required fields, duplicate email, owner mismatch, and unrelated orphans still block.
- Existing `account_staff_assignments` rows without accepted memberships remain corrupt linkage and still fail target verification.
- `staff_credentials` verification reads only `id`, `staff_account_id`, and `credential_status`, and the report section is counter-only.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Watchman emitted a recurring recrawl warning during Jest runs. Test execution was not affected.

## Known Stubs

None. Stub-pattern scan found only intentional test/documentation references to legacy placeholder credential classification and empty fixtures.

## Threat Flags

None. The new report and verification surfaces are the planned mitigations for T-13.5-12 through T-13.5-15; tests prove hash-free report sections and scoped non-blocking reason codes.

## User Setup Required

None for this plan. The live `node src/cli.js verify` re-run against the dgfy-temp rehearsal run scope is intentionally owned by Phase 14 re-rehearsal.

## Verification

- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataDryRun.test.js tests/reportCommands.test.js` passed: 31 tests.
- `cd apps/dgfy-migration-runner && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataVerify.test.js` passed: 24 tests.
- `cd apps/dgfy-migration-runner && npm test` passed after final source commit: 31 suites passed, 5 skipped; 391 tests passed, 10 skipped; architecture subtest passed 4/4.

## Next Phase Readiness

Phase 13.5 is complete. Phase 14 can re-run verification against the existing Phase 13 run scope and expect product/inventory `data_migration.ok` to turn green when only staff-linkage findings remain open, while `staff_auth` keeps every linkage and credential coverage gap auditable.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/13.5-staff-authentication-model-correction/13.5-04-SUMMARY.md`.
- Task commits `abd28c7d`, `dff180f8`, `6406a6ba`, and `46edc108` exist.
- Key modified files exist.
- Required validation commands passed.

---
*Phase: 13.5-staff-authentication-model-correction*
*Completed: 2026-07-14*
