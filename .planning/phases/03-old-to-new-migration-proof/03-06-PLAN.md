---
phase: 03-old-to-new-migration-proof
plan: 06
type: execute
gap_closure: true
wave: 6
depends_on: ["03-05"]
files_modified:
  - apps/dgfy-migration-runner/src/data/verifyData.js
  - apps/dgfy-migration-runner/tests/dataVerify.test.js
  - apps/dgfy-migration-runner/tests/phase03Integration.test.js
requirements: ["MIG-02", "MIG-03", "MIG-04", "MIG-05"]
autonomous: true
must_haves:
  truths:
    - "MIG-05 verification builds tenant-local legacy_id_map expected keys from each manifest target's legacy_tenant_db_name, not a fixed tenant source label."
    - "The gated Phase 03 live MySQL rehearsal can seed disposable schemas, run dry-run, apply, retry, and verify through data_migration.ok true."
    - "Phase 03 canonical verification can be rerun from gaps_found to passed after this gap plan executes."
  artifacts:
    - "apps/dgfy-migration-runner/src/data/verifyData.js"
    - "apps/dgfy-migration-runner/tests/dataVerify.test.js"
    - "apps/dgfy-migration-runner/tests/phase03Integration.test.js"
  key_links:
    - "docs/database/dgfy-data-migration-map.md legacy_id_map tenant-local source contract -> verifyData.js expectedLegacyKeys"
    - "tests/fixtures/phase03/legacyRecords.js legacyDgfyAccountFixture full shape -> phase03Integration.test.js disposable dgfy_accounts seed/table schema"
    - "03-VERIFICATION.md GAP-03-01 + 03-UAT.md live rehearsal failure -> final verification gates"
---

<objective>
Close the two Phase 03 blocking verification gaps without replanning completed work.

Purpose: MIG-05 cannot be trusted until data verification uses the same tenant-local `legacy_source` contract as apply, and UAT cannot prove the dry-run -> apply -> retry -> verify rehearsal while the disposable landlord fixture insert fails before the migration flow begins.

Output: A narrow implementation/test patch in `apps/dgfy-migration-runner` that resolves GAP-03-01, fixes the Phase 03 integration rehearsal seed/schema mismatch, and leaves canonical verification ready to rerun to `passed`.
</objective>

<execution_context>
@/Users/pat/.codex/gsd-core/workflows/execute-plan.md
@/Users/pat/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-old-to-new-migration-proof/03-CONTEXT.md
@.planning/phases/03-old-to-new-migration-proof/03-RESEARCH.md
@.planning/phases/03-old-to-new-migration-proof/03-PATTERNS.md
@.planning/phases/03-old-to-new-migration-proof/03-VERIFICATION.md
@.planning/phases/03-old-to-new-migration-proof/03-UAT.md
@docs/START_HERE.md
@docs/architecture/ARCHITECTURE_BOUNDARIES.md
@docs/architecture/ARCHITECTURE_GOVERNANCE.md
@docs/architecture/adr/0003-migration-facade-strategy.md
@docs/architecture/adr/0028-dgfy-account-company-switching.md
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
@docs/database/dgfy-foundation.md
@docs/database/dgfy-data-migration-map.md
@docs/database/dgfy-migration-rehearsal.md
@apps/dgfy-migration-runner/src/data/verifyData.js
@apps/dgfy-migration-runner/tests/dataVerify.test.js
@apps/dgfy-migration-runner/tests/phase03Integration.test.js
@apps/dgfy-migration-runner/tests/fixtures/phase03/legacyRecords.js
</context>

## Critical Assessment

This is a valid gap-closure slice. The requested fixes stay within the existing migration runner and disposable-test boundary, do not touch backend APIs, POS, Storefront, Product, or frontend surfaces, and do not require an ADR change.

Architecture classification: `within-existing-boundary`. ADR impact: not needed. Authoritative docs used: `docs/START_HERE.md` last reviewed 2026-03-06, `docs/architecture/ARCHITECTURE_BOUNDARIES.md` last reviewed 2026-03-06, `docs/architecture/ARCHITECTURE_GOVERNANCE.md` last reviewed 2026-05-21, ADR 0003, ADR 0028 last reviewed 2026-07-02, ADR 0029 last reviewed 2026-06-29, `docs/database/dgfy-foundation.md` last reviewed 2026-07-11, `docs/database/dgfy-data-migration-map.md` last reviewed 2026-07-11, and `docs/database/dgfy-migration-rehearsal.md` last reviewed 2026-07-11. No deprecated or archive docs are planning sources.

## Artifacts This Phase Produces

- Modifies `apps/dgfy-migration-runner/src/data/verifyData.js`: tenant-local `expectedLegacyKeys` for `users` and `tenant_locations` use `target.legacy_tenant_db_name`.
- Modifies `apps/dgfy-migration-runner/tests/dataVerify.test.js`: regression coverage proves `buildDataVerificationSections()` expects and accepts keys like `sku_tenant_1|users|9001` and `sku_tenant_1|tenant_locations|701`.
- Modifies `apps/dgfy-migration-runner/tests/phase03Integration.test.js`: the disposable `dgfy_accounts` seed no longer fails because fixture payload and disposable table columns match.
- Produces `.planning/phases/03-old-to-new-migration-proof/03-06-SUMMARY.md` after execution.

## Source Audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Phase 03 goal: operators can transform legacy/current data into DGFY-owned schemas with dry-run, apply, checkpoint, retry, and verification evidence. | Covered by Task 1 map-completeness verification and Task 2 live dry-run/apply/retry/verify rehearsal. |
| REQ | MIG-02 dry-run proof. | Covered by Task 2 final live rehearsal command. |
| REQ | MIG-03 apply with durable maps/checkpoints. | Covered by Task 2 final live rehearsal command. |
| REQ | MIG-04 retry without duplicates. | Covered by Task 2 final live rehearsal command. |
| REQ | MIG-05 verification reports source/target counts, maps, relationships, and unresolved findings. | Covered by Task 1 and Task 2. |
| RESEARCH | Verify must compare source/target counts, map completeness, required relationships, and open findings without false-clean signals. | Covered by Task 1. |
| RESEARCH | Gated real MySQL integration is the live Phase 03 evidence path. | Covered by Task 2. |
| CONTEXT | D-01 explicit operator-supplied target list per run. | Covered by Task 1 using manifest `target.legacy_tenant_db_name` and Task 2 using `DGFY_MIGRATION_TARGET_MANIFEST`. |
| CONTEXT | D-02 durable `legacy_id_map`. | Covered by Task 1 map-completeness expected-key regression. |
| CONTEXT | D-03 per-entity-per-tenant checkpoints. | Covered by Task 2 retry/live rehearsal verification. |
| CONTEXT | D-04 skip-and-report per record, never silent drop. | Covered by Task 1 preserving open-finding failure behavior and Task 2 final verify. |
| VERIFICATION | GAP-03-01 hardcoded tenant-local map-completeness key source. | Covered by Task 1. |
| UAT | Live rehearsal fails while seeding reduced `dgfy_accounts` table from full fixture payload. | Covered by Task 2. |

No source item is unplanned. Deferred Product/POS/Storefront/fiscal/frontend/backend API/cutover work remains excluded.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix tenant-local map-completeness keys</name>
  <files>apps/dgfy-migration-runner/src/data/verifyData.js, apps/dgfy-migration-runner/tests/dataVerify.test.js</files>
  <read_first>
    - `.planning/phases/03-old-to-new-migration-proof/03-VERIFICATION.md` GAP-03-01
    - `.planning/phases/03-old-to-new-migration-proof/03-UAT.md` canonical verification gap
    - `docs/database/dgfy-data-migration-map.md` `legacy_id_map` key shape for tenant-local `users` and `tenant_locations`
    - `apps/dgfy-migration-runner/src/data/verifyData.js` `buildTargetDataVerification()`
    - `apps/dgfy-migration-runner/tests/dataVerify.test.js` `buildDataVerificationSections orchestration`
  </read_first>
  <behavior>
    - Regression test: with a manifest target whose `legacy_tenant_db_name` is `sku_tenant_1`, mapped keys `sku_tenant_1|users|1` and `sku_tenant_1|tenant_locations|1` make `map_completeness.ok` true.
    - Negative proof: the test would fail if `expectedLegacyKeys` used any fixed tenant-source label instead of `target.legacy_tenant_db_name`.
    - Existing open-finding, relationship, and count checks keep their current pass/fail behavior.
  </behavior>
  <action>First add or update a focused `dataVerify.test.js` orchestration test that builds a target with `legacy_tenant_db_name: 'sku_tenant_1'`, returns one legacy user and one tenant location from the tenant-source mock, returns matching target rows, and makes `metaSequelize.query()` return `legacy_id_map` rows whose `legacy_source` is `sku_tenant_1`. Then update `verifyData.js` inside `buildTargetDataVerification()` so the tenant-local `expectedLegacyKeys` for `users` and `tenant_locations` are built with `target.legacy_tenant_db_name` per D-01 and D-02. Keep landlord-scoped keys unchanged. Do not alter the `legacy_id_map` row format, do not introduce email/phone inference, and do not change optional storefront projection behavior.</action>
  <verify>
    <automated>npm --prefix apps/dgfy-migration-runner test -- dataVerify.test.js --watchman=false</automated>
    <automated>npm --prefix apps/dgfy-migration-runner test -- dataMappings.test.js dataDryRun.test.js dataApply.test.js dataVerify.test.js dataCommand.test.js verifyCommand.test.js --watchman=false</automated>
  </verify>
  <acceptance_criteria>
    - `verifyData.js` contains no tenant-local expected map key construction that substitutes a fixed source label for `target.legacy_tenant_db_name`.
    - `dataVerify.test.js` proves a manifest tenant database name such as `sku_tenant_1` is the expected tenant-local map source.
    - The targeted data verification test command passes.
  </acceptance_criteria>
  <done>GAP-03-01 is fixed with regression coverage, and MIG-05 map-completeness verification matches apply's durable `legacy_id_map.legacy_source` contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix live rehearsal disposable landlord fixture seeding</name>
  <files>apps/dgfy-migration-runner/tests/phase03Integration.test.js</files>
  <read_first>
    - `.planning/phases/03-old-to-new-migration-proof/03-UAT.md` live MySQL dry-run/apply/retry/verify rehearsal gap
    - `docs/database/dgfy-migration-rehearsal.md` Section 5 automated gated rehearsal test
    - `docs/database/dgfy-data-migration-map.md` Account Identity deferred fields
    - `apps/dgfy-migration-runner/tests/phase03Integration.test.js` landlord `dgfy_accounts` table creation and seed
    - `apps/dgfy-migration-runner/tests/fixtures/phase03/legacyRecords.js` `legacyDgfyAccountFixture()`
  </read_first>
  <behavior>
    - Regression proof: the integration test can insert the account fixture into disposable `dgfy_accounts` before running schema migrate.
    - The seeded account still includes all columns required by mapper verification: `id`, `first_name`, `last_name`, `email`, `phone`, `password_hash`, `is_active`, `email_verified_at`, `phone_verified_at`, `last_login_at`, and `deleted_at`.
    - Full-payload fixture fields that are deferred by `docs/database/dgfy-data-migration-map.md` either have matching disposable columns or are stripped before insert; the chosen approach is explicit in the test helper or table definition.
  </behavior>
  <action>Fix `phase03Integration.test.js` at the landlord `dgfy_accounts` seed so MySQL no longer receives fields absent from the disposable table. Choose the smaller durable option after inspecting the test: either add the full legacy-shaped deferred columns from `legacyDgfyAccountFixture()` to the disposable `dgfy_accounts` table, or build a local seed object containing only columns created by that table. The action must be explicit and local to the integration test. Preserve the fixture itself because unit tests use the full legacy shape to prove deferred fields. Preserve the rest of the gated rehearsal flow: create disposable schemas, write manifest, run `runSchemaMigrate`, run dry-run with zero target mutation, run apply, rerun apply for retry safety, and run verify expecting `data_migration.ok === true`.</action>
  <verify>
    <automated>npm --prefix apps/dgfy-migration-runner test -- phase03Integration.test.js --watchman=false</automated>
    <automated>RUN_PHASE03_INTEGRATION=true PHASE03_IT_DB_HOST=127.0.0.1 PHASE03_IT_DB_PORT=3306 PHASE03_IT_DB_USER=root PHASE03_IT_DB_PASSWORD=localtest_root_pw npm --prefix apps/dgfy-migration-runner test -- phase03Integration.test.js --watchman=false</automated>
    <automated>npm --prefix apps/dgfy-migration-runner test -- --watchman=false</automated>
    <automated>npm run lint:docs</automated>
  </verify>
  <acceptance_criteria>
    - With `RUN_PHASE03_INTEGRATION` unset, `phase03Integration.test.js` still skips cleanly.
    - With the local MySQL env shown in UAT, the test gets past landlord account seeding and completes the dry-run -> apply -> retry -> verify rehearsal with `data_migration.ok === true`.
    - The full `apps/dgfy-migration-runner` Jest suite passes.
    - `npm run lint:docs` passes because this gap plan changes Phase 03 planning docs. `npm run check:architecture` is not required by file sensitivity because only the migration runner test/data verification code is touched and no backend/API boundary files change, but it is allowed as a final standard Phase 03 gate.
  </acceptance_criteria>
  <done>The live Phase 03 rehearsal blocker from `03-UAT.md` is removed and the gated MySQL integration command proves MIG-02 through MIG-05 end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| manifest -> verification | Operator-supplied migration manifest fields define source tenant database names used to build expected verification keys. |
| test fixture -> disposable MySQL | Legacy-shaped fixture data crosses into disposable schemas during live integration rehearsal. |
| metadata -> report | `legacy_id_map` and data-quality metadata drive `verify` pass/fail evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-06-01 | Tampering | `verifyData.js` map-completeness expected keys | high | mitigate | Build tenant-local expected keys from validated `target.legacy_tenant_db_name` and add regression coverage for `sku_tenant_1` per D-01/D-02. |
| T-03-06-02 | Repudiation | Phase 03 live rehearsal evidence | medium | mitigate | Fix disposable seeding so the gated real-MySQL rehearsal reaches dry-run/apply/retry/verify and produces command/report evidence. |
| T-03-06-03 | Information Disclosure | Integration fixture/report path | medium | mitigate | Keep fixture-only deferred/secret fields out of reports and do not modify apply report sanitization. |
| T-03-06-04 | Elevation of Privilege | Migration verification relationship proof | high | mitigate | Preserve ADR 0028 checks that account-staff assignments require accepted business membership evidence; do not introduce email/phone matching. |
| T-03-06-SC | Tampering | npm installs | high | accept | No package install or dependency change is planned. Existing package legitimacy audit remains sufficient. |
</threat_model>

<verification>
Run these commands after both tasks:

<automated>npm --prefix apps/dgfy-migration-runner test -- dataVerify.test.js phase03Integration.test.js --watchman=false</automated>
<automated>npm --prefix apps/dgfy-migration-runner test -- dataMappings.test.js dataDryRun.test.js dataApply.test.js dataVerify.test.js dataCommand.test.js verifyCommand.test.js phase03Integration.test.js --watchman=false</automated>
<automated>RUN_PHASE03_INTEGRATION=true PHASE03_IT_DB_HOST=127.0.0.1 PHASE03_IT_DB_PORT=3306 PHASE03_IT_DB_USER=root PHASE03_IT_DB_PASSWORD=localtest_root_pw npm --prefix apps/dgfy-migration-runner test -- phase03Integration.test.js --watchman=false</automated>
<automated>npm --prefix apps/dgfy-migration-runner test -- --watchman=false</automated>
<automated>npm run lint:docs</automated>

Architecture gate note: `npm run check:architecture` is not required for this plan unless execution touches architecture-sensitive backend/API boundary files or allowlists. It remains allowed as a final Phase 03 confidence gate if already standard in the executor's environment.
</verification>

<success_criteria>
- GAP-03-01 from `03-VERIFICATION.md` is covered by code and regression tests.
- The live rehearsal failure from `03-UAT.md` is covered by test harness correction and a passing gated MySQL run.
- Existing Phase 03 scope boundaries remain intact: no product/POS/Storefront/frontend/backend API implementation, no ADR change, no deprecated/archive docs.
- Phase 03 canonical verification can be rerun and updated from `gaps_found` to `passed`.
</success_criteria>

<output>
Create `.planning/phases/03-old-to-new-migration-proof/03-06-SUMMARY.md` when done.
</output>
