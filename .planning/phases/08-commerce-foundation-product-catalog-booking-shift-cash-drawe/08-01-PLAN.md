---
phase: 08-commerce-foundation-product-catalog-booking-shift-cash-drawe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs
  - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js
autonomous: true
requirements: [PRD-04, PRD-05, SFT-01, SFT-03]
must_haves:
  truths:
    - "Running the migration-runner `schema` command against a dgfy_business_* tenant creates 8 new tables (products, product_folders, inventory_movements, bookings, booking_capacity, shifts, cash_drawer_events, compliance_mode_state)."
    - "Running the migration-runner `verify` command against that tenant returns ok:true with rejected_tables_present empty even though products and shifts now exist."
    - "An UPDATE or DELETE against any inventory_movements or cash_drawer_events row is rejected at the database layer with SQLSTATE 45000."
    - "A second concurrent INSERT of an open shift for the same (terminal_id, cashier_account_id) is rejected by the unique index on active_terminal_cashier_key."
  artifacts:
    - "apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs"
    - "Updated apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js"
  key_links:
    - "verify.js reads dgfyBusinessContract.tables{} and rejectedTables[]; the migration's created tables must all appear in tables{} and be absent from rejectedTables."
  prohibitions:
    - "No file under backend/ is written (backend/ migrations are read-only pattern sources only)."
    - "The append-only ledger table is named inventory_movements, NEVER stock_movements (stock_movements stays in rejectedTables)."
    - "No new table declares a foreign key into legacy items or PosTransactionLine tables; cross-database references (business_id, customer_account_id, dgfy_account_id) are opaque UUID columns with no FK."
---

<objective>
Create the authoritative tenant-schema foundation for the entire Commerce Domain: one idempotent business-target migration that creates all 8 new tenant tables with their DB-level integrity guarantees (append-only triggers, the one-open-shift generated-column unique index), and update the migration-runner schema contract so the tenant-schema verify gate accepts the new tables.

Purpose: Every downstream module (products, inventory, booking, shifts, compliance) and Phase 9/10 depends on these tables existing with correct columns and DB-enforced invariants. This plan is the single source of truth for column shapes — model files (08-02) are ported FROM this migration to prevent drift.

Output: A `create-commerce-foundation` migration (`meta.targetKind: 'business'`) and an updated `dgfyBusinessContract.js` whose `verify` passes against a tenant carrying the new tables.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-CONTEXT.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-RESEARCH.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the commerce-foundation business migration (8 tables + integrity guarantees)</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs</files>
  <read_first>
    - apps/dgfy-migration-runner/src/migrations/schema/20260710021000-create-dgfy-business-foundation.cjs (EXACT template: `meta` block with `targetKind:'business'`, idempotent helpers `tableExists`/`hasIndex`/`addIndexIfMissing`/`timestampColumns()`, guarded `createTable`, INTEGER autoincrement PKs, opaque-UUID cross-DB columns with NO FK, same-DB FK style, `down()` reverse-order drops + ENUM cleanup)
    - backend/migrations/20260703000002-enforce-one-open-shift-per-terminal.cjs (READ-ONLY: generated-column + unique-index SQL to port for the shifts one-open-shift invariant)
    - backend/migrations/20260408000003-harden-compliance-audit-immutability.cjs (READ-ONLY: BEFORE UPDATE / BEFORE DELETE SIGNAL SQLSTATE '45000' trigger pattern, with DROP TRIGGER IF EXISTS for idempotency)
    - backend/src/models/StockMovement.js (READ-ONLY: field-list source for inventory_movements — quantity DECIMAL(24,12), movement_type, reference_type/reference_id)
    - backend/src/models/PosTerminalShift.js (READ-ONLY: money-column shapes DECIMAL(14,4) for shifts)
    - apps/dgfy-api/src/models/Tenant/TerminalIdentity.js (id/type conventions for FK targets — terminal_identities.id is INTEGER)
  </read_first>
  <action>
    Create an idempotent Umzug migration mirroring the foundation migration's structure exactly (`module.exports = { meta, async up(queryInterface, Sequelize){...}, async down(queryInterface){...} }`), `meta.targetKind:'business'`, `meta.destructive:false`. Copy the foundation migration's `tableExists`, `hasIndex`, `addIndexIfMissing`, `timestampColumns()` helpers verbatim and guard every `createTable` with `if (!await tableExists(...))`. Use `INTEGER` autoincrement PKs on all new tables to match existing tenant-table convention and keep same-DB FKs type-aligned (locations.id, staff_accounts.id, terminal_identities.id are all INTEGER). Cross-database columns (business_id, customer_account_id, cashier_dgfy_account_id, actor_account_id) are CHAR(36) opaque UUID columns with NO foreignKey. Create these 8 tables:

    (1) product_folders — business_id CHAR(36); name VARCHAR(100); description TEXT null; show_in_pos_filter BOOLEAN default true; is_active BOOLEAN default true; timestamps. UNIQUE index (business_id, name). NO parent_id column (flat, per D-14). 
    (2) products — business_id CHAR(36); folder_id INTEGER null same-DB FK references product_folders(id); name VARCHAR(255); category ENUM('food','service','retail'); inventory_mode ENUM('basic_inventory','non_stock') default 'non_stock'; stock_count DECIMAL(24,12) null; base_price DECIMAL(14,4) null; is_bookable BOOLEAN default false; slot_duration_minutes INTEGER null; concurrent_capacity INTEGER null; is_active BOOLEAN default true; timestamps. Indexes on (business_id), (folder_id), (category). Do NOT add stock_effect_type (per D-07 — it belongs on AvailmentItem in Phase 9).
    (3) inventory_movements — business_id CHAR(36); product_id INTEGER same-DB FK references products(id); movement_type ENUM('restock','loss','adjustment','sale','booking') (sale/booking reserved-unwired per D-06); quantity DECIMAL(24,12); reference_type VARCHAR(64) null; reference_id VARCHAR(64) null; actor_account_id CHAR(36) null; actor_staff_account_id INTEGER null; before_snapshot JSON null; after_snapshot JSON null; created_at only (NO updated_at). Indexes (business_id, product_id), (movement_type).
    (4) bookings — business_id CHAR(36); product_id INTEGER FK products(id); branch_id INTEGER FK locations(id); customer_account_id CHAR(36) null (opaque UUID, NO FK); slot_start DATETIME; slot_end DATETIME null; status ENUM('booked','cancelled','fulfilled') default 'booked'; availment_id INTEGER null (RESERVED, NO FK — Availment table is Phase 9, BOK-03); cancelled_at DATETIME null; timestamps. Indexes (business_id, product_id, branch_id, slot_start), (customer_account_id).
    (5) booking_capacity — business_id CHAR(36); product_id INTEGER FK products(id); branch_id INTEGER FK locations(id); slot_start DATETIME; slots_remaining INTEGER; timestamps. UNIQUE index (product_id, branch_id, slot_start).
    (6) shifts — business_id CHAR(36); terminal_id INTEGER FK terminal_identities(id); cashier_account_id INTEGER FK staff_accounts(id) (tenant-local, per D-13); cashier_dgfy_account_id CHAR(36) null (audit); status ENUM('open','closed') default 'open'; opening_float_amount DECIMAL(14,4); expected_cash_amount DECIMAL(14,4) null; closing_cash_amount DECIMAL(14,4) null; cash_variance_amount DECIMAL(14,4) null; opened_at DATETIME; closed_at DATETIME null; timestamps. After createTable, via queryInterface.sequelize.query add the generated column and unique index (guarded by describeTable/hasIndex): active_terminal_cashier_key VARCHAR(150) GENERATED ALWAYS AS (CASE WHEN status='open' THEN CONCAT_WS('|', terminal_id, cashier_account_id) ELSE NULL END) STORED, then UNIQUE INDEX uq_shifts_active_terminal_cashier on (active_terminal_cashier_key).
    (7) cash_drawer_events — business_id CHAR(36); shift_id INTEGER FK shifts(id); event_type ENUM('open','close','no_sale_pop','pay_in','pay_out') (pay_in/pay_out reserved-unwired per D-10); amount DECIMAL(14,4) null; reason VARCHAR(255) null; actor_staff_account_id INTEGER null; created_at only (NO updated_at). Indexes (shift_id), (event_type).
    (8) compliance_mode_state — business_id CHAR(36); branch_id INTEGER null FK locations(id); state ENUM('non_compliant_active','compliant_pending','compliant_active') default 'non_compliant_active'; compliance_profile JSON null; active_policy_pack_version VARCHAR(32) null; verification_status ENUM('pending_review','verified','rejected','revoked') null; verified_by_actor_type ENUM('tenant_master_admin','platform_admin') null; verified_at DATETIME null; timestamps. UNIQUE index (business_id, branch_id).

    After creating inventory_movements and cash_drawer_events, add append-only triggers via queryInterface.sequelize.query, each preceded by DROP TRIGGER IF EXISTS for idempotency: trg_inventory_movements_append_only_update (BEFORE UPDATE), trg_inventory_movements_append_only_delete (BEFORE DELETE), and the identical pair for cash_drawer_events — each body: SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = '<table> is append-only and cannot be <updated|deleted>'.

    In down(), drop tables in reverse dependency order (cash_drawer_events, shifts, booking_capacity, bookings, inventory_movements, products, product_folders, compliance_mode_state), dropping triggers and the generated column/index first.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node -e "const m=require('./src/migrations/schema/20260712100000-create-commerce-foundation.cjs'); if(m.meta.targetKind!=='business') throw new Error('targetKind'); if(typeof m.up!=='function'||typeof m.down!=='function') throw new Error('up/down'); console.log('migration module OK')"</automated>
    <automated>grep -q "inventory_movements" apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs && ! grep -qE "createTable\(['\"]stock_movements" apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs && echo "ledger-name OK"</automated>
    <automated>grep -q "GENERATED ALWAYS AS" apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs && grep -q "SQLSTATE '45000'" apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs && echo "invariants present"</automated>
  </verify>
  <acceptance_criteria>
    - Migration module loads, exports up/down, and declares meta.targetKind:'business'.
    - All 8 tables are created via guarded (idempotent) createTable calls; re-running the migration is a no-op.
    - shifts carries the STORED generated column active_terminal_cashier_key and unique index uq_shifts_active_terminal_cashier.
    - inventory_movements and cash_drawer_events each have BEFORE UPDATE and BEFORE DELETE SIGNAL '45000' triggers and no updated_at column.
    - No backend/ file is modified; no createTable named stock_movements; no FK into items/PosTransactionLine.
  </acceptance_criteria>
  <done>The commerce-foundation migration exists, is idempotent, targets business schemas only, and encodes the append-only triggers plus one-open-shift generated-column unique index.</done>
</task>

<task type="auto">
  <name>Task 2: Update the tenant schema contract so verify accepts the new tables</name>
  <files>apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js (lines 37-252: existing `tables{}` entry shape — columns[], indexes[], uniqueConstraints[], foreignKeys[], projectionOnly:false — and the `rejectedTables[]` array at lines 224-251)
    - apps/dgfy-migration-runner/src/commands/verify.js (line 129 rejectedTablesPresent filter; line 133 ok computation `tables.every(ok) && rejectedTablesPresent.length===0`)
  </read_first>
  <action>
    Add 8 new entries to the contract's `tables{}` object (mirroring the existing `locations`/`terminal_identities` entry shape with `columns[]`, `indexes[]`, `uniqueConstraints[]`, `foreignKeys[]`, `projectionOnly:false`), one per new table from Task 1 (product_folders, products, inventory_movements, bookings, booking_capacity, shifts, cash_drawer_events, compliance_mode_state), listing each table's columns, indexes, unique constraints, and same-DB foreign keys exactly as created in the migration (including shifts' generated column active_terminal_cashier_key and its unique index). Then remove the now-legitimate names `'products'` and `'shifts'` from the `rejectedTables` array. Keep `'stock_movements'` and every other legacy IMS name in `rejectedTables` unchanged — the new ledger is inventory_movements, which is not and must not be in rejectedTables.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node -e "const c=require('./src/schemaContracts/dgfyBusinessContract.js'); const t=c.tables||c.default?.tables; const need=['products','product_folders','inventory_movements','bookings','booking_capacity','shifts','cash_drawer_events','compliance_mode_state']; for(const n of need){ if(!t[n]) throw new Error('missing table entry '+n);} const rej=c.rejectedTables||c.default?.rejectedTables; if(rej.includes('products')||rej.includes('shifts')) throw new Error('products/shifts still rejected'); if(!rej.includes('stock_movements')) throw new Error('stock_movements must stay rejected'); console.log('contract OK')"</automated>
    <automated>cd apps/dgfy-migration-runner && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - dgfyBusinessContract.tables{} contains all 8 new table entries with column/index/FK definitions matching the migration.
    - 'products' and 'shifts' are removed from rejectedTables; 'stock_movements' remains rejected.
    - The migration-runner test suite (including architecture check) still passes.
    - The `verify` command's ok computation would return true for a tenant carrying the 8 tables (rejected_tables_present would be empty).
  </acceptance_criteria>
  <done>The schema contract recognizes all 8 commerce tables and no longer rejects products/shifts, so the tenant-schema verify gate passes for the new schema.</done>
</task>

</tasks>

<artifacts_produced>
New symbols and files this plan introduces (downstream drift ledger):
- Migration file: `20260712100000-create-commerce-foundation.cjs` (targetKind: business)
- Tenant tables: `product_folders`, `products`, `inventory_movements`, `bookings`, `booking_capacity`, `shifts`, `cash_drawer_events`, `compliance_mode_state`
- Generated column: `shifts.active_terminal_cashier_key`; unique index `uq_shifts_active_terminal_cashier`
- Triggers: `trg_inventory_movements_append_only_update/delete`, `trg_cash_drawer_events_append_only_update/delete`
- Contract change: 8 new `tables{}` entries in `dgfyBusinessContract.js`; `products`/`shifts` removed from `rejectedTables`
- Reserved (unwired) ENUM values: `inventory_movements.movement_type` ∈ {sale, booking}; `cash_drawer_events.event_type` ∈ {pay_in, pay_out}
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → tenant DB | All ledger/shift writes cross here; DB-level constraints are the last line of defense |
| migration-runner verify → tenant schema | The gate that certifies a tenant schema is well-formed before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering/Repudiation | inventory_movements, cash_drawer_events | high | mitigate | BEFORE UPDATE/DELETE SIGNAL '45000' triggers make ledger rows immutable at the DB layer (Task 1) |
| T-08-01-02 | Tampering | shifts one-open-shift invariant | high | mitigate | GENERATED STORED column + unique index rejects a second concurrent open shift for the same terminal+cashier at the DB layer (Task 1) |
| T-08-01-03 | Tampering | schema verify gate | medium | mitigate | Contract lists exact columns/indexes/FKs so drift (missing index/column) is caught by verify (Task 2) |
| T-08-01-04 | Info Disclosure | cross-DB references | medium | accept | Cross-DB refs are opaque UUID columns, no FK; tenant isolation enforced by TenantConnector at the app layer (not this plan's scope) |
</threat_model>

<verification>
- `node -e` module-load check on the migration passes; `meta.targetKind==='business'`.
- Contract load check confirms 8 table entries present and products/shifts removed from rejectedTables while stock_movements remains.
- `cd apps/dgfy-migration-runner && npm test` passes (jest + architecture check).
- Grep confirms generated column, SQLSTATE '45000', and inventory_movements (not stock_movements) present.
</verification>

<success_criteria>
- 8 commerce tables defined in one idempotent business-target migration with append-only triggers and the one-open-shift generated-column unique index.
- Schema contract updated so verify would pass for a tenant carrying the new tables; stock_movements stays rejected.
- Zero backend/ writes; no legacy items/PosTransactionLine FK; ledger named inventory_movements.
</success_criteria>

<output>
Create `.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-01-SUMMARY.md` when done, recording the final column list per table (the authoritative reference 08-02's models port from).
</output>
