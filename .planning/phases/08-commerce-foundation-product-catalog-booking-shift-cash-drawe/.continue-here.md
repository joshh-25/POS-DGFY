---
context: phase
phase: 08-commerce-foundation-product-catalog-booking-shift-cash-drawe
task: UAT Test 1 of 6
total_tasks: 6
status: blocked
last_updated: 2026-07-13T02:54:44.085Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No sandbox network/docker access — this Claude Code sandbox has no route to `192.168.64.19` (lima-dgfy-dev) and no `docker` binary at all. Confirmed via failed `ping`, failed `nc` on ports 22/3306, and `docker: command not found`. Do NOT re-attempt live-MySQL or docker-context commands from a fresh sandbox session without first re-checking network/docker availability — it will fail the same way and waste a round-trip.

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Assuming sandbox has operator network access | User asked to "automate" a test against a real remote docker context (`lima-dgfy-dev`); this sandbox cannot reach it | blocking | Always verify network/binary availability (`which docker`, `ping`, `nc -zv`) before attempting live-environment verification; if unavailable, hand a concrete runbook back to the human instead of retrying |

_Remove rows that do not apply. The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
Mid-way through `/gsd-verify-work 08` (resumed an existing UAT session — `08-UAT.md` already had Tests 4 and 5 resolved from a prior session). Currently presenting/working **Test 1**: "Verify migrations against a real tenant DB (re-test after 08-13's CASCADE→RESTRICT fix)". User is testing against `lima-dgfy-dev` (192.168.64.19) and reported the tenant DB currently has empty data. User asked Claude to automate the test via `docker --context=lima-dgfy-dev` — confirmed this sandbox has no docker binary and no network route to that host, so Claude handed back a manual runbook instead. Session was paused here.
</current_state>

<completed_work>

Completed Tasks (from 08-UAT.md, resolved in a prior session):
- Test 4: Shift-close reconciliation input (CR-02) — decision recorded: accepted as intentional temporary gap, expected to close when Phase 9 lands real sales-data source.
- Test 5: Compliance-state non-atomicity (CR-03) — decision recorded: accepted current two-write sequence for Phase 8 sign-off; tracked as a required backlog item (`recordVerificationAndState()` transactional repo method) to close before milestone ships — this is the same item captured in `.planning/todos/pending/`.

This session (no file changes made — pure investigation/handoff):
- Confirmed via `docker context ls` / `which docker` / `ping` / `nc` that this sandbox cannot reach `lima-dgfy-dev` or run docker at all.
- Produced a step-by-step manual runbook for the operator to run Test 1 themselves.
</completed_work>

<remaining_work>

- **Test 1** (pending, blocking): Operator must run against `lima-dgfy-dev` (192.168.64.19):
  1. `SHOW CREATE TABLE shifts\G` (and `compliance_mode_state`) on the tenant `dgfy_business_*` DB — check whether `terminal_id`/`cashier_account_id` (or `branch_id`) FKs still show `ON DELETE CASCADE` (the WR-01 stale-table risk from the earlier failed run).
  2. Since data is confirmed empty, `DROP TABLE` any stale table(s) found with the old CASCADE FK.
  3. From `apps/dgfy-migration-runner`, with env pointed at that tenant (`TARGET_DB_HOST=192.168.64.19`, `TARGET_DB_NAME`/`DGFY_BUSINESS_DB_NAMES` set appropriately): `node src/cli.js schema migrate`.
  4. `node src/cli.js verify` — confirm `ok:true`, no MySQL error 1215.
  5. `DESCRIBE shifts` — confirm `active_terminal_cashier_key` generated column exists and is correctly derived.
  6. Report the actual output back so Claude can record the Test 1 result in `08-UAT.md`.
- **Test 2** (blocked_by Test 1): Append-only trigger firing on `inventory_movements`/`cash_drawer_events` — needs live MySQL, only testable once Test 1's tables exist cleanly.
- **Test 3** (blocked_by Test 1): Real-concurrency reproduction of `cancelBooking`/`closeShift` row locks — needs live MySQL under REPEATABLE READ, only testable once Test 1's tables exist cleanly.
- **Test 6** (pending, non-blocking for Test 1 but still open): Decide how to close the WR-01 stale-table risk long-term — (a) treat as one-off manual cleanup of `lima-dgfy-dev`, or (b) commission a follow-up plan for an idempotent FK-repair guard (`information_schema.referential_constraints` check) for any future environment that hits this. See `08-VERIFICATION.md` "A New, Independently-Confirmed Risk" section.

Once all 4 remaining tests resolve, `/gsd-verify-work 08` will auto-complete the session, and (assuming no new issues) canonicalize `08-VERIFICATION.md` from `human_needed` to `passed`, unblocking phase 08 → phase 9 transition.
</remaining_work>

<decisions_made>

- Decided not to attempt any docker/mysql command against `192.168.64.19` from this sandbox after confirming no network route and no docker binary — handing the runbook to the operator instead of guessing/faking a result.
</decisions_made>

<blockers>
- Live MySQL / docker access to `lima-dgfy-dev`: not reachable from this Claude Code sandbox. Requires operator action (see remaining_work Test 1 runbook above). This is the single blocker gating Tests 1, 2, and 3.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-UAT.md` — current test results and full Test 1 note/context (stale-table risk analysis)
2. `.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-VERIFICATION.md` — WR-01 "A New, Independently-Confirmed Risk" section (lines ~76-80, ~150-184) for the full technical rationale
3. `.planning/todos/pending/` — the compliance-transaction backlog item from Test 5's decision (required before milestone ships, not owned by any current roadmap phase)

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Assuming "automate the test" means the current sandbox has the access described (docker context, network route) → always verify (`which docker`, `ping`, `nc -zv`) before attempting, then hand back a concrete runbook if unavailable rather than stalling or fabricating results.

## Infrastructure State
- `lima-dgfy-dev` (192.168.64.19): real MySQL tenant environment, per user confirmed to have empty data currently. Likely has a stale `shifts` table with old CASCADE FKs from the original failed migration attempt (unconfirmed — operator needs to check via `SHOW CREATE TABLE`).
- This sandbox: no docker, no network route to 192.168.64.19.

<context>
This is a UAT verification session (`/gsd-verify-work 08`), not a plan-execution session — no code changes are pending in this phase, only a live-environment human-verification checkpoint. The migration fix itself (08-13-PLAN.md, CASCADE→RESTRICT) is already merged and independently re-confirmed correct by code read and full green test suites in `08-VERIFICATION.md`. What remains is purely: run it against the one real environment already touched by the original failed attempt, working around that environment's likely stale state.
</context>

<next_action>
Start with: resume `/gsd-verify-work 08`, which will re-render the Test 1 checkpoint from `08-UAT.md` (still `result: pending`). Wait for the operator to report the runbook results (SHOW CREATE TABLE findings + schema migrate + verify output), then record the Test 1 result and continue to Tests 2, 3, 6.
</next_action>
