---
phase: 08-commerce-foundation-product-catalog-booking-shift-cash-drawe
plan: 06
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/dgfy-api/src/modules/compliance/policy/constants.js
  - apps/dgfy-api/src/modules/compliance/policy/policyPacks.js
  - apps/dgfy-api/src/modules/compliance/policy/policyEngine.js
  - apps/dgfy-api/src/modules/compliance/usecases/complianceGate.js
  - apps/dgfy-api/src/modules/compliance/usecases/complianceUseCases.js
  - apps/dgfy-api/src/modules/compliance/repositories/complianceModeStateRepository.js
  - apps/dgfy-api/src/modules/compliance/entities/complianceEntity.js
  - apps/dgfy-api/src/modules/compliance/controllers/complianceController.js
  - apps/dgfy-api/src/modules/compliance/routes.js
  - apps/dgfy-api/src/modules/compliance/index.js
  - apps/dgfy-api/tests/unit/modules/compliance/complianceGate.test.js
autonomous: true
requirements: [FSC-01, FSC-02]
must_haves:
  truths:
    - "A tenant/branch carries a compliance_mode_state row in one of non_compliant_active / compliant_pending / compliant_active, with the full BIR/NPC/BSP policy-pack profile depth ported from legacy."
    - "One shared gate port assertComplianceGate({ businessId, operation, requestedDocumentContext }) evaluates eligibility through the ported policy engine — the single shared check FSC-02 requires (Phase 9 wires the actual call sites)."
    - "D-05 DEVIATION: a compliant_active business is ALLOWED for BOTH requestedDocumentContext='fiscal' AND requestedDocumentContext='non_fiscal' — non-fiscal is NOT denied once active (Omni-default, Fiscal-on-request)."
    - "An authorized staff/owner can submit compliance evidence and a tenant_master_admin/platform_admin can review and transition state (manual path; automation deferred)."
  artifacts:
    - "modules/compliance/policy/{constants.js,policyPacks.js,policyEngine.js} ported from legacy"
    - "usecases/complianceGate.js exposing assertComplianceGate"
    - "complianceGate.test.js with the D-05 regression test"
  key_links:
    - "assertComplianceGate loads ComplianceModeState via the repository, calls the ported policyEngine, and maps decision → ALLOW / forbiddenError (DENY) / domain error (REQUIRES_SETUP)."
  prohibitions:
    - "The ported policyEngine must NOT contain a DOCUMENT_CONTEXT_NOT_ALLOWED deny branch for compliant_active (the legacy fiscal-forcing block, ~lines 903-918, must NOT be ported)."
    - "No backend/ writes; compliancePolicyEngine.js/policyPacks.js/complianceConstants.js are read-only port sources."
    - "The gate must accept requestedDocumentContext as a first-class input, not just an operation enum."
---

<objective>
Build the compliance module: the tenant-scoped compliance-mode state machine (full BIR/NPC/BSP policy-pack depth, manual review/transition path) and the single shared gate port assertComplianceGate, ported from the legacy policy engine with exactly ONE deliberate deviation (D-05) — compliant_active allows BOTH fiscal and non_fiscal document contexts, never forcing Fiscal.

Purpose: This gate port is Phase 9's hand-off contract; its shape (especially the first-class requestedDocumentContext input) must be stable and documented. FSC-02's "one shared check, not duplicated per surface" is satisfied by building the single port here; Phase 9 wires it into Checkout/Shift/receipt call sites. D-05 is the single most important decision in this phase.

Output: modules/compliance with the ported policy engine/packs/constants, the state machine usecases, and assertComplianceGate — with a dedicated regression test proving the D-05 deviation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-PATTERNS.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-CONTEXT.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port policy constants, packs, and engine — WITH the D-05 deviation</name>
  <files>apps/dgfy-api/src/modules/compliance/policy/constants.js, apps/dgfy-api/src/modules/compliance/policy/policyPacks.js, apps/dgfy-api/src/modules/compliance/policy/policyEngine.js, apps/dgfy-api/tests/unit/modules/compliance/complianceGate.test.js</files>
  <read_first>
    - backend/src/modules/compliance/policy/complianceConstants.js (READ-ONLY: port all frozen enum objects verbatim — COMPLIANCE_MODE_STATE, COMPLIANCE_DECISION, COMPLIANCE_OPERATION, COMPLIANCE_REASON_CODE, COMPLIANCE_VERIFICATION_STATUS, COMPLIANCE_VERIFIER_ACTOR_TYPE, COMPLIANCE_PROFILE_DEFAULT, DOCUMENT_CONTEXTS, POS_OPERATIONS)
    - backend/src/modules/compliance/policy/policyPacks.js (READ-ONLY: port whole file — versioned POLICY_PACKS[], getActivePolicyPack(now), listPolicyPacks())
    - backend/src/modules/compliance/policy/compliancePolicyEngine.js (READ-ONLY: port evaluateComplianceDecision (entry ~line 725) reading context.requested_document_context; the NON_COMPLIANT_ACTIVE branch ~804-859 and COMPLIANT_PENDING branch ~861-901 port UNCHANGED; the COMPLIANT_ACTIVE block ~903-918 DOCUMENT_CONTEXT_NOT_ALLOWED deny branch is the ONLY thing to DELETE — everything after line 918 ports unchanged)
    - .planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-CONTEXT.md (D-05 full text — the authoritative spec for compliant_active behavior)
  </read_first>
  <action>
    Port constants.js from complianceConstants.js (all 8 frozen enum objects verbatim, ESM export). Port policyPacks.js whole (POLICY_PACKS, getActivePolicyPack, listPolicyPacks). Port policyEngine.js from compliancePolicyEngine.js as a pure-function module keeping evaluateComplianceDecision's signature and reading requestedDocumentContext from context.requested_document_context. Port the NON_COMPLIANT_ACTIVE and COMPLIANT_PENDING branches unchanged. In the COMPLIANT_ACTIVE branch, DO NOT port the deny branch that returns reasonCode DOCUMENT_CONTEXT_NOT_ALLOWED for a non-fiscal POS operation — omit it entirely so compliant_active allows both fiscal and non_fiscal; keep the settings-update and checklist-completeness branches that follow. requestedDocumentContext ('fiscal'|'non_fiscal'|'training_test') is a first-class input the client supplies (which app build is running); the engine only gates eligibility by state, never forces a mode.

    Write complianceGate.test.js as the D-05 regression guard with, at minimum: (a) state compliant_active + requestedDocumentContext 'non_fiscal' + a POS operation → decision ALLOW (the exact case legacy denies); (b) compliant_active + 'fiscal' → ALLOW; (c) non_compliant_active + 'fiscal' → DENY with NON_COMPLIANT_FISCAL_DOCUMENT_BLOCKED; (d) compliant_pending + 'fiscal' → REQUIRES_SETUP with COMPLIANT_ACTIVATION_PENDING; (e) non_compliant_active + 'non_fiscal' → ALLOW. Assert the ported engine source contains no DOCUMENT_CONTEXT_NOT_ALLOWED deny path for compliant_active.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/unit/modules/compliance/complianceGate.test.js 2>&1 | tail -25</automated>
    <automated>! grep -n "DOCUMENT_CONTEXT_NOT_ALLOWED" apps/dgfy-api/src/modules/compliance/policy/policyEngine.js && echo "D-05 deny branch absent OK"</automated>
  </verify>
  <acceptance_criteria>
    - constants.js, policyPacks.js, policyEngine.js ported with ESM exports; enum values match legacy.
    - The compliant_active + non_fiscal → ALLOW test passes (D-05 deviation proven); fiscal/pending/non_compliant cases behave as specified.
    - policyEngine.js contains no DOCUMENT_CONTEXT_NOT_ALLOWED deny branch (prohibition grep passes).
    - No backend/ files modified.
  </acceptance_criteria>
  <done>The policy engine is ported with the exact D-05 deviation and guarded by a regression test.</done>
</task>

<task type="auto">
  <name>Task 2: State-machine repository/usecases + gate port + controller/routes/DI factory</name>
  <files>apps/dgfy-api/src/modules/compliance/repositories/complianceModeStateRepository.js, apps/dgfy-api/src/modules/compliance/entities/complianceEntity.js, apps/dgfy-api/src/modules/compliance/usecases/complianceGate.js, apps/dgfy-api/src/modules/compliance/usecases/complianceUseCases.js, apps/dgfy-api/src/modules/compliance/controllers/complianceController.js, apps/dgfy-api/src/modules/compliance/routes.js, apps/dgfy-api/src/modules/compliance/index.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/businesses/repositories/locationRepository.js (tenant-DB scaffold)
    - apps/dgfy-api/src/modules/businesses/usecases/locationUseCases.js (DomainError helpers incl. forbiddenError; requireMembership; ApplicationResult; tenant-error mapping)
    - apps/dgfy-api/src/modules/businesses/entities/businessEntity.js (entity shape)
    - apps/dgfy-api/src/modules/businesses/controllers/locationController.js (transport-only controller)
    - apps/dgfy-api/src/modules/accounts/routes.js (createXRoutes with authenticateAccount + .catch(next))
    - apps/dgfy-api/src/modules/businesses/index.js (buildXModule DI factory)
  </read_first>
  <action>
    Build complianceModeStateRepository on the tenant-DB scaffold resolving ComplianceModeState, exposing getForBusinessBranch/upsertState/recordVerification. complianceEntity mirrors businessEntity with helper allowsFiscalChoice() (state === compliant_active). Build usecases: getComplianceState, submitComplianceEvidence (authorized staff/owner writes verification_status=pending_review + profile), reviewComplianceState (requires verifier actor type tenant_master_admin/platform_admin — manual transition per D-04; automation NOT built). Build complianceGate.js exposing assertComplianceGate({ businessId, operation, requestedDocumentContext, branchId? }): load the ComplianceModeState via the repository, build the engine context (context.requested_document_context = requestedDocumentContext), call evaluateComplianceDecision, and map ALLOW → return the decision, DENY → throw forbiddenError, REQUIRES_SETUP → throw a domain error carrying the reason code. Build transport-only complianceController and createComplianceRoutes(useCases,{authenticateAccount}): GET /compliance/state, POST /compliance/evidence, POST /compliance/review — behind authenticateAccount + .catch(next). Build buildComplianceModule({ tenantConnector, businessDatabaseRegistryRepository, businessRepository }) returning { repository, useCases, assertComplianceGate } and re-export createComplianceRoutes + assertComplianceGate so 08-08 and Phase 9 can inject the gate. Do NOT edit routes/index.js here.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node -e "import('./src/modules/compliance/index.js').then(m=>{const mod=m.buildComplianceModule({tenantConnector:{getModels(){return{}}},businessDatabaseRegistryRepository:{},businessRepository:{}});if(!mod.useCases||typeof m.createComplianceRoutes!=='function'||typeof mod.assertComplianceGate!=='function')throw new Error('factory/route/gate export missing');console.log('compliance module OK')})" 2>&1 | tail -5</automated>
    <automated>cd apps/dgfy-api && node -e "import('./src/modules/compliance/index.js').then(m=>{try{m.createComplianceRoutes({},{})}catch(e){if(/authenticateAccount/.test(e.message)){console.log('auth guard OK');return}}throw new Error('missing auth guard')})" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - buildComplianceModule returns { repository, useCases, assertComplianceGate }; createComplianceRoutes exported and throws without authenticateAccount.
    - assertComplianceGate accepts requestedDocumentContext as a first-class input and maps decisions to ALLOW/forbiddenError/REQUIRES_SETUP.
    - reviewComplianceState enforces verifier actor type; no automatic transition path built.
    - Controllers transport-only; routes behind authenticateAccount with .catch(next).
  </acceptance_criteria>
  <done>The compliance state machine and the shared gate port exist, documented as Phase 9's hand-off contract.</done>
</task>

</tasks>

<artifacts_produced>
- Ported policy files: `policy/constants.js`, `policy/policyPacks.js`, `policy/policyEngine.js` (evaluateComplianceDecision, WITHOUT the D-05 deny branch)
- Gate port: `assertComplianceGate({ businessId, operation, requestedDocumentContext, branchId? })` (Phase 9 hand-off contract)
- Usecases: getComplianceState, submitComplianceEvidence, reviewComplianceState
- Repository: complianceModeStateRepository (getForBusinessBranch/upsertState/recordVerification)
- Entity: ComplianceEntity with allowsFiscalChoice()
- Module factory: `buildComplianceModule({...}) → { repository, useCases, assertComplianceGate }`
- Route factory: `createComplianceRoutes(useCases, { authenticateAccount })`
- Endpoints (mounted under /compliance in 08-08): GET /compliance/state, POST /compliance/evidence, POST /compliance/review
- Behavioral contract (D-05): compliant_active ALLOWs both fiscal and non_fiscal
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → compliance API | Untrusted evidence/review payloads; role/verifier authz required |
| gated usecase → gate port | Eligibility decision boundary; over/under-gating is a fiscal-integrity risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Elevation of Privilege | over-gating: compliant_active wrongly denies non_fiscal | high | mitigate | D-05: omit the DOCUMENT_CONTEXT_NOT_ALLOWED deny branch; regression test asserts ALLOW for compliant_active+non_fiscal |
| T-08-06-02 | Elevation of Privilege | under-gating: non_compliant business issues fiscal | high | mitigate | NON_COMPLIANT_ACTIVE branch ported unchanged → DENY fiscal; regression test asserts DENY |
| T-08-06-03 | Elevation of Privilege | unauthorized state transition | high | mitigate | reviewComplianceState requires verifier actor type (tenant_master_admin/platform_admin); evidence submit requires staff/owner membership |
| T-08-06-04 | Repudiation | who verified compliance | medium | mitigate | verified_by_actor_type + verified_at captured on the state row |
| T-08-06-05 | Info Disclosure | cross-tenant state read | high | mitigate | complianceModeStateRepository resolves tenant DB via resolveDatabaseName (active+verified) |
</threat_model>

<verification>
- complianceGate.test.js passes with the full D-05 truth table (esp. compliant_active + non_fiscal → ALLOW).
- Prohibition grep confirms no DOCUMENT_CONTEXT_NOT_ALLOWED in policyEngine.js.
- Module factory + gate + route + auth-guard smoke checks pass.
</verification>

<success_criteria>
- Tenant-scoped compliance-mode state with full policy-pack depth (FSC-01) and one shared gate port (FSC-02), unwired this phase.
- D-05 deviation implemented and regression-tested; no fiscal-forcing on compliant_active.
- Manual review/transition path only (D-04); zero backend/ writes.
</success_criteria>

<output>
Create `.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-06-SUMMARY.md` when done, documenting the exact assertComplianceGate signature + decision mapping as the stable Phase 9 hand-off contract.
</output>
