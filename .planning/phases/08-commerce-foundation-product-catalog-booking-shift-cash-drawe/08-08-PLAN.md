---
phase: 08-commerce-foundation-product-catalog-booking-shift-cash-drawe
plan: 08
type: execute
wave: 5
depends_on: ["08-03", "08-04", "08-05", "08-06", "08-07"]
files_modified:
  - apps/dgfy-api/src/routes/index.js
  - apps/dgfy-api/tests/integration/commerce/commerceModulesMount.test.js
autonomous: true
requirements: [PRD-01, PRD-03, BOK-02, SFT-01, FSC-02]
must_haves:
  truths:
    - "All 5 commerce modules (products, inventory, booking, shifts, compliance) are built in the composition root reusing the SAME tenantConnector, businessRepository, and businessDatabaseRegistryRepository instances as the existing modules."
    - "The 5 module route groups are mounted under /products, /inventory, /bookings, /shifts, /compliance behind the shared authenticateAccount middleware."
    - "The booking module receives the products module's productRepository and the inventory module's reserved effectContracts by injection; the shifts module is constructed gate-ready (assertComplianceGate available but NOT invoked this phase)."
    - "The dgfy-api app boots with all commerce routes registered and the existing test suite still passes."
  artifacts:
    - "Updated apps/dgfy-api/src/routes/index.js composition root"
    - "commerceModulesMount integration/smoke test"
  key_links:
    - "routes/index.js builds each module via buildXModule({ tenantConnector, businessDatabaseRegistryRepository, businessRepository, ... }) and mounts createXRoutes(useCases,{authenticateAccount})."
  prohibitions:
    - "No backend/ writes."
    - "Do NOT wire assertComplianceGate into shift-open or any call site this phase (Phase 9 wires all gate call sites); only make the gate available for injection."
    - "Do NOT construct a second/divergent tenantConnector or businessRepository instance — reuse the ones already built in routes/index.js."
---

<objective>
Wire all 5 commerce modules into the dgfy-api composition root (routes/index.js), mounting their route groups under the shared authenticateAccount middleware and threading cross-module dependencies (productRepository into booking, inventory effectContracts into booking, compliance gate made available but unwired), then prove the app boots with every commerce route registered.

Purpose: The 5 modules were built in isolation (waves 3-4) with no edits to the shared composition root to keep them parallel. This final plan is the single place routes/index.js changes, avoiding wave-3/4 file conflicts and giving one integration surface to verify the whole phase mounts cleanly.

Output: An updated composition root and a mount smoke/integration test confirming all commerce endpoints exist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-PATTERNS.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-03-SUMMARY.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-04-SUMMARY.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-05-SUMMARY.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-06-SUMMARY.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and mount the 5 commerce modules in the composition root</name>
  <files>apps/dgfy-api/src/routes/index.js</files>
  <read_first>
    - apps/dgfy-api/src/routes/index.js (FULL: existing composition — buildBusinessesModule built first so businessRepository is reused; buildAccountsModule; authenticateAccount built once; router.use('/accounts'|'/businesses'|'/invitations'); note the SAME tenantConnector/businessDatabaseRegistryRepository must be reused)
    - apps/dgfy-api/src/modules/products/index.js (buildProductsModule signature + createProductRoutes export)
    - apps/dgfy-api/src/modules/inventory/index.js (buildInventoryModule + createInventoryRoutes + effectContracts export)
    - apps/dgfy-api/src/modules/shifts/index.js (buildShiftsModule + createShiftRoutes; optional assertComplianceGate/staleThresholdMinutes)
    - apps/dgfy-api/src/modules/compliance/index.js (buildComplianceModule + createComplianceRoutes + assertComplianceGate export)
    - apps/dgfy-api/src/modules/booking/index.js (buildBookingModule needs productRepository + inventoryEffectContracts + createBookingRoutes)
  </read_first>
  <action>
    In routes/index.js, after the existing businesses/accounts wiring, obtain the shared tenantConnector and businessDatabaseRegistryRepository already in scope (reuse the exact instances the businesses module uses — do not new up new ones). Build the modules in dependency order: buildProductsModule → buildInventoryModule → buildComplianceModule → buildShiftsModule (pass the compliance module's assertComplianceGate as an available-but-unused injection AND staleThresholdMinutes from config/env) → buildBookingModule (inject the products module's productRepository and the inventory module's effectContracts). Then mount: router.use('/products', createProductRoutes(productUseCases,{authenticateAccount})), and likewise '/inventory', '/compliance', '/shifts', '/bookings'. Import the buildXModule + createXRoutes symbols from each module's index.js at the top. Add a brief comment noting the compliance gate is injected into shifts but intentionally NOT called until Phase 9 (per CONTEXT out-of-scope).
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node -e "import('./src/routes/index.js').then(()=>console.log('composition root imports OK')).catch(e=>{console.error(e);process.exit(1)})" 2>&1 | tail -8</automated>
    <automated>grep -Eq "'/products'|\"/products\"" apps/dgfy-api/src/routes/index.js && grep -Eq "/inventory" apps/dgfy-api/src/routes/index.js && grep -Eq "/bookings" apps/dgfy-api/src/routes/index.js && grep -Eq "/shifts" apps/dgfy-api/src/routes/index.js && grep -Eq "/compliance" apps/dgfy-api/src/routes/index.js && echo "all 5 mounts present"</automated>
  </verify>
  <acceptance_criteria>
    - routes/index.js imports and builds all 5 modules reusing the existing tenantConnector/businessRepository/businessDatabaseRegistryRepository instances.
    - All 5 route groups mounted under authenticateAccount; booking receives productRepository + effectContracts; shifts receives assertComplianceGate (uninvoked) + staleThresholdMinutes.
    - The module loads without throwing (imports resolve; no divergent connector).
    - No backend/ writes; gate not wired into any call site.
  </acceptance_criteria>
  <done>All commerce modules are composed and mounted in one place with correct cross-module injection.</done>
</task>

<task type="auto">
  <name>Task 2: Commerce mount smoke/integration test + full-suite green</name>
  <files>apps/dgfy-api/tests/integration/commerce/commerceModulesMount.test.js</files>
  <read_first>
    - apps/dgfy-api/tests/health.transport.test.js (transport/app-boot test style using the Express app)
    - apps/dgfy-api/tests/integration/businesses/businessRoutes.test.js (route-mount + auth-gate assertion style; DB-availability gating)
    - apps/dgfy-api/src/app.js (how the router mounts under /v1)
  </read_first>
  <action>
    Write commerceModulesMount.test.js that boots the Express app (or the router) and asserts each commerce route group is registered and enforces auth: an unauthenticated request to a protected commerce endpoint (e.g. GET /v1/products, GET /v1/shifts) returns 401 (auth required) rather than 404 (route missing), proving the routes are mounted behind authenticateAccount. Keep it transport-level (no real tenant DB needed) so it runs without MySQL; gate any DB-dependent assertion behind the existing availability check. Then run the full dgfy-api test suite and confirm it is green.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/integration/commerce/commerceModulesMount.test.js 2>&1 | tail -20</automated>
    <automated>cd apps/dgfy-api && npm test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - commerceModulesMount test passes: each commerce route group is mounted (protected endpoints return 401 not 404 when unauthenticated).
    - The full dgfy-api test suite passes.
    - Test runs without requiring a live MySQL (DB-dependent assertions gated/skipped per existing convention).
  </acceptance_criteria>
  <done>The whole phase's modules are proven mounted and the dgfy-api suite is green.</done>
</task>

</tasks>

<artifacts_produced>
- Composition-root wiring in `apps/dgfy-api/src/routes/index.js`: build + mount of products, inventory, compliance, shifts, booking modules
- Mounted route groups: `/products`, `/inventory`, `/compliance`, `/shifts`, `/bookings` (all under authenticateAccount, under /v1)
- Cross-module injection: productRepository + inventory effectContracts → booking; assertComplianceGate (available, uninvoked) + staleThresholdMinutes → shifts
- Integration test: `commerceModulesMount.test.js`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /v1 commerce routes | All commerce endpoints must sit behind authenticateAccount |
| composition root → modules | Shared singletons (tenantConnector, businessRepository) must not diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08-01 | Elevation of Privilege | unauthenticated commerce access | high | mitigate | Every route group mounted behind the shared authenticateAccount; mount test asserts 401-not-404 |
| T-08-08-02 | Tampering | divergent tenantConnector/businessRepository instances | medium | mitigate | Reuse the existing singletons in routes/index.js; no second construction |
| T-08-08-03 | Elevation of Privilege | premature gate wiring bypassing Phase 9 design | medium | mitigate | assertComplianceGate injected but not invoked; comment + acceptance forbid wiring a call site this phase |
</threat_model>

<verification>
- routes/index.js imports resolve; all 5 mounts present.
- commerceModulesMount test passes (401-not-404 for protected commerce endpoints).
- `npm test` for dgfy-api is green.
</verification>

<success_criteria>
- All 5 commerce modules composed and mounted with correct cross-module injection and shared singletons.
- Compliance gate available but unwired (Phase 9 owns call-site wiring).
- Full dgfy-api suite green; zero backend/ writes.
</success_criteria>

<output>
Create `.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-08-SUMMARY.md` when done, listing the final mounted endpoint map for the whole phase.
</output>
