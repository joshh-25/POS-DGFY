---
phase: 08-commerce-foundation-product-catalog-booking-shift-cash-drawe
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/dgfy-api/src/modules/inventory/index.js
  - apps/dgfy-api/src/modules/inventory/routes.js
  - apps/dgfy-api/src/modules/inventory/controllers/inventoryMovementController.js
  - apps/dgfy-api/src/modules/inventory/usecases/inventoryMovementUseCases.js
  - apps/dgfy-api/src/modules/inventory/usecases/inventoryEffectContracts.js
  - apps/dgfy-api/src/modules/inventory/repositories/inventoryMovementRepository.js
  - apps/dgfy-api/src/modules/inventory/entities/inventoryMovementEntity.js
  - apps/dgfy-api/tests/unit/modules/inventory/inventoryMovementUseCases.test.js
autonomous: true
requirements: [PRD-04]
must_haves:
  truths:
    - "Staff/owner can record a restock, loss, or adjustment movement, creating an append-only inventory_movements row and updating the product's cached stock_count in the same transaction (for basic_inventory products)."
    - "modules/inventory is the sole writer of inventory_movements; the repository exposes only create/bulkCreate/findAll/findOne (never update/delete)."
    - "The reserved sale/booking effect-type contract shapes are defined (unwired) so Phase 9 and the booking module plug into an existing shape."
  artifacts:
    - "modules/inventory/ with DI factory, routes, controller, usecases, repository, entity"
    - "inventoryEffectContracts.js defining recordSaleEffect/recordBookingEffect contract shapes"
    - "inventoryMovementUseCases unit test"
  key_links:
    - "inventoryMovementRepository resolves InventoryMovement + Product via TenantConnector.getModels(); movement insert + product stock_count update share one sequelize.transaction()."
  prohibitions:
    - "No backend/ writes; no reference to legacy stock_movements/items tables."
    - "The repository must NOT expose update or delete on inventory_movements (append-only, ADR 0029 single-writer)."
    - "No other module writes inventory_movements directly (booking/availment request effects through this module)."
---

<objective>
Build the inventory module as the sole writer of the append-only inventory_movements ledger (ADR 0029), shipping manual restock/loss/adjustment endpoints now and defining the reserved (unwired) sale/booking effect-type contract shapes that Phase 9 (availment) and the booking module will call into later.

Purpose: Every stock-count change must flow through one auditable, append-only ledger with a single writer. Establishing the sole-writer contract and the reserved effect shapes now means Phase 9 plugs into an existing contract rather than extending the ledger later (D-06).

Output: modules/inventory with buildInventoryModule DI factory + the effect-contract module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-PATTERNS.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-CONTEXT.md
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append-only movement repository + entity + effect contracts</name>
  <files>apps/dgfy-api/src/modules/inventory/repositories/inventoryMovementRepository.js, apps/dgfy-api/src/modules/inventory/entities/inventoryMovementEntity.js, apps/dgfy-api/src/modules/inventory/usecases/inventoryEffectContracts.js, apps/dgfy-api/tests/unit/modules/inventory/inventoryMovementUseCases.test.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/businesses/repositories/locationRepository.js (EXACT: TenantDatabaseUnavailableError, resolveDatabaseName, withModel; tenant-DB resolution scaffold)
    - apps/dgfy-api/src/modules/businesses/entities/businessEntity.js (entity shape)
    - backend/src/models/StockMovement.js (READ-ONLY: append-only mindset, quantity DECIMAL(24,12), movement_type/reference_type/reference_id)
    - docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md (single-writer rule: only Inventory records stock effects; POS/Storefront request effects)
    - .planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-RESEARCH.md (Pattern C append-only, D-06 reserved effect stubs)
  </read_first>
  <behavior>
    - recordMovement create path inserts an inventory_movements row and returns it; the repository object has no update/delete methods.
    - For a basic_inventory product, a restock of +N increases product.stock_count by N inside one transaction; a loss of N decreases it; the movement quantity sign matches the effect.
    - Calling any update path against a movement is impossible via the repository API (only create/bulkCreate/findAll/findOne exist).
    - inventoryEffectContracts exports recordSaleEffect and recordBookingEffect as callable contract functions that currently throw a NotImplemented/reserved DomainError (unwired) but validate/return the agreed input shape descriptor.
  </behavior>
  <action>
    Build inventoryMovementRepository on the copied tenant-DB scaffold, resolving InventoryMovement and Product via tenantConnector.getModels(). Expose ONLY create, bulkCreate, findAll, findOne — deliberately no update/delete. Provide a recordMovementWithStockSync(businessId, {productId, movementType, quantity, ...}) that, inside a single sequelize.transaction(), inserts the movement and (when the product is basic_inventory) applies the guarded stock_count update on products (using the atomic-guarded UPDATE style so a loss cannot drive stock negative unless allowed). inventoryMovementEntity mirrors businessEntity with toPlain() exposing the movement's public shape. In inventoryEffectContracts.js define the reserved shapes recordSaleEffect({ businessId, productId, quantity, referenceType:'availment', referenceId }) and recordBookingEffect({ businessId, productId, quantity, referenceType:'booking', referenceId }) — document that these are the Phase 9 / booking-fulfillment entry points, keep modules/inventory the sole writer, and have them raise a clear reserved DomainError when invoked this phase (they exist to fix the contract shape, not to run). Write the inventoryMovementUseCases unit test covering restock/loss stock sync and the append-only surface.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/unit/modules/inventory/inventoryMovementUseCases.test.js 2>&1 | tail -20</automated>
    <automated>grep -Eq "update|destroy|delete" apps/dgfy-api/src/modules/inventory/repositories/inventoryMovementRepository.js && echo "REVIEW: repo mentions update/delete — ensure not exposed on movements" || echo "no update/delete surface OK"</automated>
  </verify>
  <acceptance_criteria>
    - inventoryMovementRepository exposes only create/bulkCreate/findAll/findOne for movements; no update/destroy method on the movement surface.
    - restock/loss adjust product.stock_count transactionally for basic_inventory products; unit test proves sign handling.
    - inventoryEffectContracts defines recordSaleEffect/recordBookingEffect with the agreed input shape and reserved behavior.
    - No backend/ writes; no legacy stock_movements/items reference.
  </acceptance_criteria>
  <done>The append-only ledger has a single-writer repository, transactional stock sync, and reserved effect-contract shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Manual-movement usecases, controller, routes, DI factory</name>
  <files>apps/dgfy-api/src/modules/inventory/usecases/inventoryMovementUseCases.js, apps/dgfy-api/src/modules/inventory/controllers/inventoryMovementController.js, apps/dgfy-api/src/modules/inventory/routes.js, apps/dgfy-api/src/modules/inventory/index.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/businesses/usecases/locationUseCases.js (DomainError helpers, requireMembership/guardBusinessAccess, tenant-error mapping, ApplicationResult)
    - apps/dgfy-api/src/modules/businesses/controllers/locationController.js (transport-only controller)
    - apps/dgfy-api/src/modules/accounts/routes.js (createXRoutes with injected authenticateAccount + .catch(next))
    - apps/dgfy-api/src/modules/businesses/index.js (buildXModule DI factory shape)
  </read_first>
  <action>
    Build recordRestock, recordLoss, recordAdjustment usecases (each requireMembership staff-or-owner, validate quantity/productId inline, call recordMovementWithStockSync, return ApplicationResult) plus listMovements(productId). Build the transport-only inventoryMovementController and createInventoryRoutes(useCases,{authenticateAccount}) with POST /movements (type in body: restock|loss|adjustment) or discrete POST /restock, /loss, /adjustment routes, plus GET /movements — all behind authenticateAccount with .catch(next). Build buildInventoryModule({ tenantConnector, businessDatabaseRegistryRepository, businessRepository }) returning { repository, useCases, effectContracts } and re-export createInventoryRoutes + the effectContracts so 08-08 and Phase 9 can reach them. Do NOT edit routes/index.js here.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node -e "import('./src/modules/inventory/index.js').then(m=>{const mod=m.buildInventoryModule({tenantConnector:{getModels(){return{}}},businessDatabaseRegistryRepository:{},businessRepository:{}});if(!mod.useCases||typeof m.createInventoryRoutes!=='function'||!mod.effectContracts)throw new Error('factory/route/effect export missing');console.log('inventory module OK')})" 2>&1 | tail -5</automated>
    <automated>cd apps/dgfy-api && node -e "import('./src/modules/inventory/index.js').then(m=>{try{m.createInventoryRoutes({},{})}catch(e){if(/authenticateAccount/.test(e.message)){console.log('auth guard OK');return}}throw new Error('missing auth guard')})" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - buildInventoryModule returns { repository, useCases, effectContracts }; createInventoryRoutes exported and throws without authenticateAccount.
    - restock/loss/adjustment usecases are membership-gated and return ApplicationResult.
    - Controllers are transport-only; routes behind authenticateAccount with .catch(next).
  </acceptance_criteria>
  <done>Manual movement endpoints exist and the module exposes the sole-writer repository + reserved effect contracts for downstream wiring.</done>
</task>

</tasks>

<artifacts_produced>
- Module factory: `buildInventoryModule({...}) → { repository, useCases, effectContracts }`
- Route factory: `createInventoryRoutes(useCases, { authenticateAccount })`
- Usecases: recordRestock, recordLoss, recordAdjustment, listMovements
- Repository method: `recordMovementWithStockSync(businessId, {...})` (transactional; sole writer)
- Reserved effect contracts: `recordSaleEffect({...})`, `recordBookingEffect({...})` (unwired, Phase 9 / booking entry points)
- Entity: InventoryMovementEntity
- Endpoints (mounted under /inventory in 08-08): POST /inventory/restock|loss|adjustment (or /movements), GET /inventory/movements
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → inventory API | Untrusted movement quantities; staff/owner authz required |
| module → ledger | Single-writer boundary — only this module inserts inventory_movements |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Tampering/Repudiation | inventory_movements | high | mitigate | Append-only repo (no update/delete) + 08-01 DB triggers + 08-02 insert-only model hooks |
| T-08-04-02 | Tampering | stock_count double-write race | high | mitigate | Guarded UPDATE on products.stock_count inside one sequelize.transaction() with the movement insert |
| T-08-04-03 | Elevation of Privilege | manual movements | high | mitigate | requireMembership staff/owner on every movement usecase |
| T-08-04-04 | Repudiation | who recorded the movement | medium | mitigate | actor_account_id / actor_staff_account_id captured on each row |
</threat_model>

<verification>
- inventoryMovementUseCases unit test passes (restock/loss stock sync; append-only surface).
- Module factory + route + effect-contract export smoke checks pass.
</verification>

<success_criteria>
- Append-only ledger with single-writer repository and transactional stock sync (PRD-04).
- Reserved sale/booking effect contracts defined for Phase 9 / booking without ledger redesign (D-06).
- Zero backend/ writes; ledger is inventory_movements only.
</success_criteria>

<output>
Create `.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-04-SUMMARY.md` when done, documenting the exact recordBookingEffect input shape the booking module (08-07) will reference.
</output>
