---
phase: 08-commerce-foundation-product-catalog-booking-shift-cash-drawe
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/dgfy-api/src/models/Tenant/Product.js
  - apps/dgfy-api/src/models/Tenant/ProductFolder.js
  - apps/dgfy-api/src/models/Tenant/InventoryMovement.js
  - apps/dgfy-api/src/models/Tenant/Booking.js
  - apps/dgfy-api/src/models/Tenant/BookingCapacity.js
  - apps/dgfy-api/src/models/Tenant/Shift.js
  - apps/dgfy-api/src/models/Tenant/CashDrawerEvent.js
  - apps/dgfy-api/src/models/Tenant/ComplianceModeState.js
  - apps/dgfy-api/src/infra/tenantConnector.js
autonomous: true
requirements: [PRD-01, PRD-02, PRD-04, SFT-01, SFT-02, FSC-01]
must_haves:
  truths:
    - "TenantConnector.getModels(databaseName) returns a registry that includes Product, ProductFolder, InventoryMovement, Booking, BookingCapacity, Shift, CashDrawerEvent, and ComplianceModeState alongside the existing tenant models."
    - "The InventoryMovement and CashDrawerEvent models throw on beforeUpdate/beforeBulkUpdate and define no updated_at column (insert-only)."
    - "Each model's column definitions match the columns created by the 08-01 migration exactly (same names, ENUM values, types)."
  artifacts:
    - "8 new files under apps/dgfy-api/src/models/Tenant/"
    - "Updated apps/dgfy-api/src/infra/tenantConnector.js modelDefiners map"
  key_links:
    - "tenantConnector.getModels() modelDefiners must import and register all 8 model definers; repositories resolve models by these registry keys."
  prohibitions:
    - "No file under backend/ is written; StockMovement.js/ItemFolder.js/PosTerminalShift.js are read-only field-list sources."
    - "No model imports a backend/src/models model or references legacy items/stock_movements/PosTransactionLine tables."
    - "InventoryMovement/CashDrawerEvent models must NOT expose or allow update paths (no updated_at, throw hooks)."
---

<objective>
Create the 8 Sequelize Tenant model factories for the commerce tables and register them in the shared TenantConnector model registry so every module's repositories can resolve them.

Purpose: Repositories in the 5 new modules (waves 3-4) resolve their models through `TenantConnector.getModels()`. Models must exist and match the 08-01 migration columns before any repository is built. Insert-only models add the application-layer half of the append-only guarantee (the DB triggers from 08-01 are the hard backstop).

Output: Product, ProductFolder, InventoryMovement, Booking, BookingCapacity, Shift, CashDrawerEvent, ComplianceModeState models + an extended modelDefiners map.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-PATTERNS.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-01-SUMMARY.md
@apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 8 Tenant model factories matching the migration columns</name>
  <files>apps/dgfy-api/src/models/Tenant/Product.js, apps/dgfy-api/src/models/Tenant/ProductFolder.js, apps/dgfy-api/src/models/Tenant/InventoryMovement.js, apps/dgfy-api/src/models/Tenant/Booking.js, apps/dgfy-api/src/models/Tenant/BookingCapacity.js, apps/dgfy-api/src/models/Tenant/Shift.js, apps/dgfy-api/src/models/Tenant/CashDrawerEvent.js, apps/dgfy-api/src/models/Tenant/ComplianceModeState.js</files>
  <read_first>
    - apps/dgfy-api/src/models/Tenant/TerminalIdentity.js (EXACT structure: export default (sequelize) => class extends Model with static associate(models); X.init({...}, { sequelize, modelName, tableName, underscored:true, timestamps:true, createdAt:'created_at', updatedAt:'updated_at', indexes:[...] }); return X; same-DB FK via references:{model:'table',key:'id'})
    - apps/dgfy-api/src/models/Tenant/Location.js (doc-comment convention: model matches the real migration exactly)
    - apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs (authoritative column list — model columns MUST match this)
    - backend/src/models/StockMovement.js (READ-ONLY: updatedAt:false; quantity DECIMAL(24,12); movement_type/reference_type/reference_id shapes)
    - backend/src/models/ItemFolder.js (READ-ONLY: field list for product_folders — name/description/show_in_pos_filter/is_active; do NOT port parent_id, per D-14 flat)
    - backend/src/models/PosTerminalShift.js (READ-ONLY: DECIMAL(14,4) money-column shapes for Shift)
    - backend/src/modules/compliance/policy/complianceConstants.js (READ-ONLY: exact ENUM values for ComplianceModeState — COMPLIANCE_MODE_STATE, COMPLIANCE_VERIFICATION_STATUS, COMPLIANCE_VERIFIER_ACTOR_TYPE, COMPLIANCE_PROFILE_DEFAULT)
  </read_first>
  <action>
    Create one model factory per table following the TerminalIdentity.js class-extends-Model + init + associate shape, underscored:true, timestamps:true with explicit created_at/updated_at EXCEPT the two insert-only models. Column names, ENUM member values, and types MUST match the 08-01 migration exactly. Declare same-DB associations in associate(): Product belongsTo ProductFolder; InventoryMovement belongsTo Product; Booking belongsTo Product and Location; BookingCapacity belongsTo Product and Location; Shift belongsTo TerminalIdentity and StaffAccount; CashDrawerEvent belongsTo Shift; ComplianceModeState belongsTo Location.

    Insert-only models (InventoryMovement.js, CashDrawerEvent.js): set updatedAt:false (no updated_at column, matching migration) and add hooks: beforeUpdate() throws Error('<table> is insert-only'), beforeBulkUpdate() throws Error('<table> is insert-only').

    ComplianceModeState.js: state ENUM = (non_compliant_active, compliant_pending, compliant_active); verification_status ENUM = (pending_review, verified, rejected, revoked); verified_by_actor_type ENUM = (tenant_master_admin, platform_admin); compliance_profile is JSON. Add a domain helper allowsFiscalChoice() returning true when state === compliant_active. Do not embed policy-engine logic here (that lives in modules/compliance).

    Shift.js: money columns DECIMAL(14,4); status ENUM (open, closed); the generated column active_terminal_cashier_key is DB-managed — do NOT declare it as a writable attribute (omit it). Add domain helper isOpen().

    Product.js: include is_bookable/slot_duration_minutes/concurrent_capacity (BOK-01 config) and inventory_mode ENUM (basic_inventory, non_stock); do NOT add stock_effect_type (D-07).
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node -e "import('sequelize').then(async ({Sequelize})=>{const s=new Sequelize('sqlite::memory:',{logging:false});const files=['Product','ProductFolder','InventoryMovement','Booking','BookingCapacity','Shift','CashDrawerEvent','ComplianceModeState'];for(const f of files){const m=(await import('./src/models/Tenant/'+f+'.js')).default(s);if(!m.tableName)throw new Error('no tableName '+f);}console.log('all models define OK')})" 2>&1 | tail -5</automated>
    <automated>grep -L "backend/src/models" apps/dgfy-api/src/models/Tenant/InventoryMovement.js apps/dgfy-api/src/models/Tenant/CashDrawerEvent.js >/dev/null && grep -q "updatedAt: false" apps/dgfy-api/src/models/Tenant/InventoryMovement.js && grep -q "updatedAt: false" apps/dgfy-api/src/models/Tenant/CashDrawerEvent.js && echo "insert-only OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 model files export a factory that defines a model with the correct tableName under an in-memory Sequelize.
    - InventoryMovement and CashDrawerEvent set updatedAt:false and throw on beforeUpdate/beforeBulkUpdate.
    - ENUM values and column names match the 08-01 migration; product_folders model has no parent_id; Product model has no stock_effect_type.
    - No model imports from backend/ and no reference to legacy items/stock_movements/PosTransactionLine.
  </acceptance_criteria>
  <done>The 8 commerce Tenant models exist, match the migration, and enforce insert-only at the application layer for the two ledgers.</done>
</task>

<task type="auto">
  <name>Task 2: Register all 8 models in the TenantConnector modelDefiners registry</name>
  <files>apps/dgfy-api/src/infra/tenantConnector.js</files>
  <read_first>
    - apps/dgfy-api/src/infra/tenantConnector.js (lines 2-6 import block; lines ~118-145 getModels() modelDefiners map with existing Location/StaffAccount/StaffInvitation/AccountStaffAssignment/TerminalIdentity, the idempotent `connection.models[name] || define(connection)` resolution, and the associate() wiring loop)
  </read_first>
  <action>
    Add 8 import lines at the top mirroring the existing `import defineXModel from '../models/Tenant/X.js';` style, then add 8 keys to the modelDefiners object inside getModels(): Product, ProductFolder, InventoryMovement, Booking, BookingCapacity, Shift, CashDrawerEvent, ComplianceModeState. Do not change the existing idempotent resolution or the associate() wiring loop — they already handle any number of models. Update the getModels() JSDoc return-type comment to list the new models.
  </action>
  <verify>
    <automated>grep -c "defineProductModel\|defineProductFolderModel\|defineInventoryMovementModel\|defineBookingModel\|defineBookingCapacityModel\|defineShiftModel\|defineCashDrawerEventModel\|defineComplianceModeStateModel" apps/dgfy-api/src/infra/tenantConnector.js</automated>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/unit/infra/tenantConnector.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 8 new model definers are imported and present in the modelDefiners map.
    - The existing tenantConnector unit test still passes.
    - getModels() resolves the new models without altering the existing idempotent resolution or associate() loop.
  </acceptance_criteria>
  <done>TenantConnector.getModels() exposes all 8 commerce models so tenant-scoped repositories can resolve them.</done>
</task>

</tasks>

<artifacts_produced>
- Tenant model factories: `Product`, `ProductFolder`, `InventoryMovement`, `Booking`, `BookingCapacity`, `Shift`, `CashDrawerEvent`, `ComplianceModeState` (all under apps/dgfy-api/src/models/Tenant/)
- Model domain helpers: `ComplianceModeState.allowsFiscalChoice()`, `Shift.isOpen()`
- Registry keys added to `TenantConnector.getModels()` modelDefiners: the 8 model names above
- Insert-only enforcement (app layer): beforeUpdate/beforeBulkUpdate throw hooks on InventoryMovement + CashDrawerEvent
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → tenant model | Models are the typed write surface; insert-only hooks guard ledger tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02-01 | Tampering | InventoryMovement/CashDrawerEvent models | high | mitigate | updatedAt:false + throwing beforeUpdate/beforeBulkUpdate hooks block application-layer mutation (DB triggers from 08-01 are the backstop) |
| T-08-02-02 | Tampering | model/migration drift | medium | mitigate | Column/ENUM parity with the authoritative 08-01 migration asserted in read_first + acceptance |
| T-08-02-03 | Info Disclosure | cross-tenant model reuse | medium | accept | getModels() caches per databaseName; isolation is TenantConnector's existing responsibility, unchanged here |
</threat_model>

<verification>
- In-memory Sequelize define check passes for all 8 models.
- Insert-only grep confirms updatedAt:false on both ledger models.
- tenantConnector unit test passes with the extended registry.
</verification>

<success_criteria>
- 8 commerce models created, matching the 08-01 migration, with insert-only enforcement on the two ledgers.
- All 8 registered in getModels(); existing tenant tests unaffected.
- Zero backend/ writes; no legacy-table references.
</success_criteria>

<output>
Create `.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-02-SUMMARY.md` when done.
</output>
