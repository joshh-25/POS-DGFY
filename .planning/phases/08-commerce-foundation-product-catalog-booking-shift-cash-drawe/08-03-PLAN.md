---
phase: 08-commerce-foundation-product-catalog-booking-shift-cash-drawe
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/dgfy-api/src/modules/products/index.js
  - apps/dgfy-api/src/modules/products/routes.js
  - apps/dgfy-api/src/modules/products/controllers/productController.js
  - apps/dgfy-api/src/modules/products/controllers/productFolderController.js
  - apps/dgfy-api/src/modules/products/usecases/productUseCases.js
  - apps/dgfy-api/src/modules/products/usecases/productFolderUseCases.js
  - apps/dgfy-api/src/modules/products/repositories/productRepository.js
  - apps/dgfy-api/src/modules/products/repositories/productFolderRepository.js
  - apps/dgfy-api/src/modules/products/entities/productEntity.js
  - apps/dgfy-api/tests/unit/modules/products/productUseCases.test.js
autonomous: true
requirements: [PRD-01, PRD-02, PRD-03, PRD-05, BOK-01]
must_haves:
  truths:
    - "A business owner can create a Product with category in {food,service,retail} and inventory_mode in {basic_inventory,non_stock}, persisted to the tenant products table."
    - "A business owner can create a flat product folder (per-tenant-unique name) and assign products to it."
    - "A business owner can mark a service Product bookable with slot_duration_minutes and concurrent_capacity (BOK-01 config)."
    - "Product and folder writes are gated by owner membership via requireMembership/guardBusinessAccess."
  artifacts:
    - "modules/products/ with index.js DI factory, routes.js, controllers, usecases, repositories, entities"
    - "productUseCases unit test"
  key_links:
    - "productRepository resolves the Product model via TenantConnector.getModels(); usecases return ApplicationResult and throw DomainError."
  prohibitions:
    - "No backend/ writes; no FK or model reference into legacy items/PosTransactionLine."
    - "No stock_effect_type field is added to Product (D-07 — it lives on AvailmentItem in Phase 9)."
    - "product_folders remain flat — no parent_id / nesting logic."
---

<objective>
Build the products module (products + product_folders) following the exact modules/businesses shape, delivering Product CRUD with Food/Service/Retail category, Basic-Inventory-vs-non-stock inventory_mode, flat folder grouping, and the mark-bookable configuration that Booking (08-07) consumes.

Purpose: Products are the root dependency of the whole Commerce Domain — every downstream line item, booking, and stock movement references a Product. This module owns the products table and all writes to it (including bookable config).

Output: A complete products module wired as a buildProductsModule DI factory returning { repository, useCases }.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-PATTERNS.md
@.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Product & folder repositories + entity + usecases</name>
  <files>apps/dgfy-api/src/modules/products/repositories/productRepository.js, apps/dgfy-api/src/modules/products/repositories/productFolderRepository.js, apps/dgfy-api/src/modules/products/entities/productEntity.js, apps/dgfy-api/src/modules/products/usecases/productUseCases.js, apps/dgfy-api/src/modules/products/usecases/productFolderUseCases.js, apps/dgfy-api/tests/unit/modules/products/productUseCases.test.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/businesses/repositories/locationRepository.js (EXACT: TenantDatabaseUnavailableError class, resolveDatabaseName requiring active+verified_at, withModel wrapper, CRUD + toPlain)
    - apps/dgfy-api/src/modules/businesses/usecases/locationUseCases.js (EXACT: validationError/conflictError/notFoundError/forbiddenError helpers; requireMembership/guardBusinessAccess at lines ~97-123; tenant-error mapping at lines ~57-78; builder-per-usecase returning ApplicationResult)
    - apps/dgfy-api/src/modules/businesses/entities/businessEntity.js (EXACT: class + toPlain() + factory fn)
    - apps/dgfy-api/src/shared/contracts/applicationResult.js, apps/dgfy-api/src/shared/contracts/domainErrors.js (reuse as-is)
    - apps/dgfy-api/tests/unit/modules/businesses/locationUseCases.test.js (test style: inject fake repository/businessRepository, assert ApplicationResult success/failure)
  </read_first>
  <behavior>
    - createProduct with valid owner membership + category food + inventory_mode non_stock returns ApplicationResult.success with the product; missing/invalid category returns validationError (400).
    - createProduct by a non-owner (membership role !== owner) returns forbiddenError (403).
    - createProductFolder with a name already used in that tenant returns conflictError (409) [per-tenant-unique name].
    - setProductBookable on a service product sets is_bookable=true with slot_duration_minutes and concurrent_capacity; on a non-service product returns validationError.
    - Tenant DB unavailable maps to 404 (missing/not_configured) or 503 per the copied mapping.
  </behavior>
  <action>
    Copy locationRepository's tenant-DB scaffold into productRepository and productFolderRepository, resolving the Product / ProductFolder models via the injected tenantConnector.getModels(). Expose create/findById/findAll/update (and soft is_active toggle) through withModel; add a folder-name uniqueness check in productFolderRepository. productEntity mirrors businessEntity with toPlain() exposing the public product shape (id, name, category, inventory_mode, folder_id, is_bookable, slot_duration_minutes, concurrent_capacity, base_price, is_active). Build usecases with the copied DomainError helpers + requireMembership({role:'owner'}) for writes: createProduct, updateProduct, listProducts, createProductFolder, listProductFolders, and setProductBookable (validates category==='service', sets bookable config). Use inline DomainError validation (no joi — follow existing convention). Write the productUseCases unit test covering the behaviors above with injected fakes.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/unit/modules/products/productUseCases.test.js 2>&1 | tail -20</automated>
    <automated>grep -Lq "stock_effect_type" apps/dgfy-api/src/modules/products/entities/productEntity.js && grep -Lq "parent_id" apps/dgfy-api/src/modules/products/repositories/productFolderRepository.js && echo "prohibitions OK"</automated>
  </verify>
  <acceptance_criteria>
    - productUseCases unit test passes covering create (valid/invalid category), non-owner 403, folder name conflict 409, setProductBookable service-only rule.
    - Repositories use the copied TenantDatabaseUnavailableError/withModel scaffold; no direct model imports in usecases/controllers.
    - No stock_effect_type on Product; no parent_id handling for folders.
  </acceptance_criteria>
  <done>Products and folders have working, tested, owner-gated usecases over the tenant DB, including bookable config.</done>
</task>

<task type="auto">
  <name>Task 2: Controllers, routes, and module DI factory</name>
  <files>apps/dgfy-api/src/modules/products/controllers/productController.js, apps/dgfy-api/src/modules/products/controllers/productFolderController.js, apps/dgfy-api/src/modules/products/routes.js, apps/dgfy-api/src/modules/products/index.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/businesses/controllers/locationController.js (EXACT: buildXController(useCases) transport-only, req.account.id as actor, sendUseCaseResult(res,result,201|200))
    - apps/dgfy-api/src/modules/accounts/routes.js (EXACT: createXRoutes(useCases,{authenticateAccount}) throws if middleware missing, one thin route per handler with authenticateAccount + .catch(next))
    - apps/dgfy-api/src/modules/businesses/index.js (EXACT: buildXModule factory constructing tenant repos with {tenantConnector, businessDatabaseRegistryRepository} and closing usecases over them; barrel re-exports)
    - apps/dgfy-api/src/shared/controllers/useCaseResponder.js (sendUseCaseResult signature)
  </read_first>
  <action>
    Build transport-only productController and productFolderController mirroring locationController (parse req.body/params/query, actor = req.account.id passed as requestingAccountId, sendUseCaseResult with 201 on create / 200 on read/update). Build createProductRoutes(useCases,{authenticateAccount}) mounting: POST/GET/PATCH product routes, POST/GET folder routes, and PATCH /:id/bookable for setProductBookable, each behind authenticateAccount with .catch(next). Build buildProductsModule({ tenantConnector, businessDatabaseRegistryRepository, businessRepository }) constructing the two repositories and closing all usecases over them, returning { repository: productRepository, folderRepository, useCases }; re-export createProductRoutes from index.js. Do NOT edit routes/index.js here (composition wiring is 08-08).
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node -e "import('./src/modules/products/index.js').then(m=>{const mod=m.buildProductsModule({tenantConnector:{getModels(){return{}}},businessDatabaseRegistryRepository:{},businessRepository:{}});if(!mod.useCases||typeof m.createProductRoutes!=='function')throw new Error('factory/route export missing');console.log('products module OK')})" 2>&1 | tail -5</automated>
    <automated>cd apps/dgfy-api && node -e "import('./src/modules/products/index.js').then(m=>{try{m.createProductRoutes({},{})}catch(e){if(/authenticateAccount/.test(e.message)){console.log('auth guard OK');return}}throw new Error('missing auth guard')})" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - buildProductsModule returns { repository, useCases }; createProductRoutes is exported and throws without authenticateAccount.
    - Controllers contain no model/repository imports (transport-only).
    - Routes cover product create/list/update, folder create/list, and set-bookable, all behind authenticateAccount with .catch(next).
  </acceptance_criteria>
  <done>The products module is a self-contained DI factory with routes ready to mount in 08-08.</done>
</task>

</tasks>

<artifacts_produced>
- Module factory: `buildProductsModule({ tenantConnector, businessDatabaseRegistryRepository, businessRepository }) → { repository, folderRepository, useCases }`
- Route factory: `createProductRoutes(useCases, { authenticateAccount })`
- Usecases: createProduct, updateProduct, listProducts, createProductFolder, listProductFolders, setProductBookable
- Repositories: productRepository, productFolderRepository (tenant-scoped, withModel scaffold)
- Entity: ProductEntity + createProductEntity
- Endpoints (mounted under /products in 08-08): POST/GET/PATCH /products, POST/GET /products/folders, PATCH /products/:id/bookable
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → products API | Untrusted product/folder payloads; owner authorization enforced in usecase |
| usecase → tenant DB | Writes gated by active+verified tenant + membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Elevation of Privilege | product/folder writes | high | mitigate | requireMembership({role:'owner'}) + guardBusinessAccess on every write usecase |
| T-08-03-02 | Tampering | category/inventory_mode injection | medium | mitigate | Inline DomainError validation restricts category to {food,service,retail}, inventory_mode to {basic_inventory,non_stock} |
| T-08-03-03 | Info Disclosure | cross-tenant product reads | high | mitigate | productRepository resolves DB via resolveDatabaseName (active+verified); business scoping in queries |
</threat_model>

<verification>
- productUseCases unit test passes (create, authz, folder conflict, bookable rule).
- Module factory + route export + auth-guard smoke checks pass.
- Prohibition greps confirm no stock_effect_type and no parent_id.
</verification>

<success_criteria>
- Products module delivers PRD-01/PRD-02/PRD-03/BOK-01 config with owner authorization and tenant isolation.
- New tables only; no legacy items/PosTransactionLine coupling (PRD-05).
- DI factory + routes ready for composition-root wiring.
</success_criteria>

<output>
Create `.planning/phases/08-commerce-foundation-product-catalog-booking-shift-cash-drawe/08-03-SUMMARY.md` when done, documenting the productRepository read API (findById/findAll) that the booking module (08-07) depends on.
</output>
