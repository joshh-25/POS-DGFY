---
phase: 01-architecture-and-migration-runner-contract
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - apps/dgfy-migration-runner/src/migrations/schema/00000000000000-runner-contract-placeholder.cjs
  - apps/dgfy-migration-runner/src/commands/schema.js
  - apps/dgfy-migration-runner/src/commands/data.js
  - apps/dgfy-migration-runner/src/commands/verify.js
  - apps/dgfy-migration-runner/src/commands/status.js
  - apps/dgfy-migration-runner/src/commands/rollbackPlan.js
  - apps/dgfy-migration-runner/src/cli.js
  - apps/dgfy-migration-runner/tests/schemaCommand.test.js
  - apps/dgfy-migration-runner/tests/dataCommand.test.js
  - apps/dgfy-migration-runner/tests/reportCommands.test.js
  - apps/dgfy-migration-runner/tests/cliContract.test.js
autonomous: true
requirements: [RUN-02]

must_haves:
  truths:
    - "Operator can run `schema migrate`, `data dry-run`, `data apply --confirm-destructive`, `verify`, `status`, and `rollback-plan` as six explicit, --help-discoverable commands from one CLI entry"
    - "`data apply` without --confirm-destructive exits non-zero and never calls a target DB connection factory"
    - "`rollback-plan` never calls a migration's down() function — it only reads meta.rollbackDescription/meta.estimatedRisk"
  artifacts:
    - path: apps/dgfy-migration-runner/src/cli.js
      provides: "Commander program wiring all 6 subcommands to commands/*.js run functions"
    - path: apps/dgfy-migration-runner/src/commands/schema.js
      provides: "runSchemaMigrate() driving Umzug against the placeholder migration"
    - path: apps/dgfy-migration-runner/src/commands/data.js
      provides: "runDataDryRun(), runDataApply()"
    - path: apps/dgfy-migration-runner/src/commands/verify.js
      provides: "runVerify()"
    - path: apps/dgfy-migration-runner/src/commands/status.js
      provides: "runStatus()"
    - path: apps/dgfy-migration-runner/src/commands/rollbackPlan.js
      provides: "runRollbackPlan()"
  key_links:
    - from: "src/cli.js"
      to: "src/commands/schema.js, data.js, verify.js, status.js, rollbackPlan.js"
      via: "Commander .action() handlers call the exported run*() functions"
      pattern: "runSchemaMigrate|runDataDryRun|runDataApply|runVerify|runStatus|runRollbackPlan"
    - from: "src/commands/data.js (runDataApply)"
      to: "src/safety/destructiveGate.js"
      via: "assertDestructiveAllowed({isDestructive:true,...}) called before any DB factory call"
      pattern: "assertDestructiveAllowed"
    - from: "src/commands/schema.js"
      to: "src/metadata/storage.js"
      via: "new Umzug({storage: new MetaSequelizeStorage({sequelize: metaSequelize})})"
      pattern: "MetaSequelizeStorage"
---

<objective>
Implement RUN-02's full command surface — `schema migrate`, `data dry-run`, `data apply`, `verify`, `status`, `rollback-plan` — as independently-testable handler functions wired into one Commander CLI entry (`src/cli.js`), each following the validate→guard→connect→bootstrap→record→report contract established in Plans 01-02.

Purpose: RUN-02 requires operators to choose all six commands "from the same image." D-02/D-04 lock a custom Commander CLI over `sequelize-cli` with explicit, `--help`-discoverable subcommands. This plan is the harness proof: `data`/`verify`/`status`/`rollback-plan` are Phase 1 contract stubs (no real transformation logic — that ships in Phase 3), while `schema migrate` drives a real (additive, non-destructive) placeholder migration through Umzug end-to-end.
Output: `src/commands/schema.js`, `src/commands/data.js`, `src/commands/verify.js`, `src/commands/status.js`, `src/commands/rollbackPlan.js`, `src/cli.js`, the placeholder migration file, and their tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architecture-and-migration-runner-contract/01-CONTEXT.md
@.planning/phases/01-architecture-and-migration-runner-contract/01-RESEARCH.md
@.planning/phases/01-architecture-and-migration-runner-contract/01-PATTERNS.md
</context>

<artifacts_produced>
Files: `apps/dgfy-migration-runner/src/migrations/schema/00000000000000-runner-contract-placeholder.cjs`, `apps/dgfy-migration-runner/src/commands/schema.js`, `apps/dgfy-migration-runner/src/commands/data.js`, `apps/dgfy-migration-runner/src/commands/verify.js`, `apps/dgfy-migration-runner/src/commands/status.js`, `apps/dgfy-migration-runner/src/commands/rollbackPlan.js`, `apps/dgfy-migration-runner/src/cli.js`.
Exports: `runSchemaMigrate()` (schema.js); `runDataDryRun()`, `runDataApply()` (data.js); `runVerify()` (verify.js); `runStatus()` (status.js); `runRollbackPlan()` (rollbackPlan.js). Migration file exports a `meta` object: `{ destructive, rollbackDescription, estimatedRisk }` alongside `up`/`down`.
CLI surface (Commander subcommands defined in cli.js): `schema migrate [--confirm-destructive]`, `data dry-run`, `data apply [--confirm-destructive]`, `verify`, `status`, `rollback-plan`.
DB table created by this plan: `runner_contract_placeholder` (in the TARGET database, via the placeholder migration) — columns `id`, `note`, `created_at`, `updated_at`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Placeholder schema migration + schema-migrate command (Umzug wiring)</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/00000000000000-runner-contract-placeholder.cjs, apps/dgfy-migration-runner/src/commands/schema.js, apps/dgfy-migration-runner/tests/schemaCommand.test.js</files>
  <read_first>
    backend/migrations/20240101000003-create-suppliers.cjs (full file — up/down(queryInterface,Sequelize) signature + idempotent createTable-if-missing shape)
    .planning/phases/01-architecture-and-migration-runner-contract/01-RESEARCH.md "Modifying Migration Parameters with Resolve" / Umzug Sequelize-integration excerpt (glob + `context: sequelize.getQueryInterface()` + resolver calling `migration.up(context, Sequelize)`)
    apps/dgfy-migration-runner/src/metadata/storage.js (Plan 02 Task 2 — MetaSequelizeStorage)
    apps/dgfy-migration-runner/src/metadata/bootstrap.js (Plan 02 Task 1 — ensureMetadataSchema/recordCommandStart/recordCommandComplete)
    apps/dgfy-migration-runner/src/safety/destructiveGate.js, apps/dgfy-migration-runner/src/safety/targetGuard.js (Plan 01 Task 2 — call before any DB factory)
    apps/dgfy-migration-runner/src/config/db.js, apps/dgfy-migration-runner/src/config/env.js (Plan 01 Tasks 1 and 3 — validateEnv(), createTargetConnection/createMetaConnection)
    .planning/phases/01-architecture-and-migration-runner-contract/01-CONTEXT.md decisions D-09, D-14
  </read_first>
  <behavior>
    - Test 1: `runSchemaMigrate({})` (no `confirmDestructive`) succeeds against the placeholder migration because `meta.destructive === false`.
    - Test 2: the returned report's `command` field equals `'schema:migrate'` and `migrations_executed` includes the placeholder migration's name.
    - Test 3: `assertTargetDbNameAllowed` and `assertDestructiveAllowed` are both called before `createTargetConnection`/`createMetaConnection` (mock call-order assertion).
  </behavior>
  <action>
    Create `src/migrations/schema/00000000000000-runner-contract-placeholder.cjs` following `backend/migrations/20240101000003-create-suppliers.cjs`'s exact `up(queryInterface, Sequelize)`/`down(queryInterface, Sequelize)` export shape (so Umzug's resolver, which calls `migration.up(context, Sequelize)`, works unmodified), PLUS a `meta` object: `module.exports = { meta: { destructive: false, rollbackDescription: 'Drops runner_contract_placeholder (Phase 1 contract-proof table only — carries no production data)', estimatedRisk: 'low' }, async up(queryInterface, Sequelize) { ... }, async down(queryInterface, Sequelize) { ... } };`. `up` follows the suppliers migration's idempotent "check `showAllTables()`, only `createTable` if missing" pattern (lines 4-7, 67-78) creating a `runner_contract_placeholder` table: `id` (INTEGER, primaryKey, autoIncrement), `note` (STRING(255), allowNull:false, defaultValue:'Phase 1 migration runner contract proof'), `created_at`/`updated_at` (DATE, defaultValue: `Sequelize.literal('CURRENT_TIMESTAMP')`). `down` calls `queryInterface.dropTable('runner_contract_placeholder')`. Per D-14/phase scope: this file proves the contract only — it must NOT contain any real `dgfy_*` landlord/tenant schema DDL (Phase 2 owns that).
    Create `src/commands/schema.js` exporting `export async function runSchemaMigrate({ confirmDestructive = false } = {})` following the "options in, report object out" contract from `backend/scripts/sync-tenant-schemas.js`'s `runTenantSchemaSync` (lines 438-457, 622-636): (1) `const { valid, errors, config } = validateEnv(); if (!valid) throw new EnvValidationError(errors.join('; '));`; (2) `assertTargetDbNameAllowed(config.targetDb.name, config.runtimeMode);`; (3) build the migrations glob path via `join(dirname(fileURLToPath(import.meta.url)), '..', 'migrations', 'schema', '*.cjs')`; (4) use `createRequire(import.meta.url)` (from Node's `module` package) to `require()` each `.cjs` migration file from this ESM module — the standard Node ESM-importing-CJS interop pattern — inside the Umzug `resolve` callback: `resolve: ({ name, path, context }) => { const migration = requireCjs(path); return { name, up: async () => migration.up(context, Sequelize), down: async () => migration.down(context, Sequelize) }; }`; (5) `const pendingMigrations = await umzug.pending();` then compute `isDestructive = pendingMigrations.some((m) => requireCjs(m.path).meta?.destructive === true)` and call `assertDestructiveAllowed({ isDestructive, confirmDestructive, runtimeMode: config.runtimeMode })` BEFORE calling `umzug.up()` — this is the per-migration-file destructive check D-09 describes; (6) connect lazily: `const targetSequelize = createTargetConnection(config); const metaSequelize = createMetaConnection(config); await ensureMetadataSchema(metaSequelize);` (only after steps 1-5 pass); (7) `const executionId = await recordCommandStart(metaSequelize, { command: 'schema:migrate', mode: confirmDestructive ? 'apply-destructive' : 'apply', argsJson: JSON.stringify({ confirmDestructive }), actor: config.actor, runtimeMode: config.runtimeMode });`; (8) `const executed = await umzug.up();`; (9) build `const report = { generated_at: new Date().toISOString(), command: 'schema:migrate', mode: confirmDestructive ? 'apply-destructive' : 'apply', migrations_executed: executed.map((m) => m.name), summary: { total_pending: pendingMigrations.length, executed: executed.length } };`; (10) write reports via `writeJsonReport`/`writeSummaryReport` (Plan 02 Task 3), call `recordCommandComplete(metaSequelize, executionId, { exitStatus: 'success', reportJsonPath, reportSummaryPath })`, return `report`. Wrap steps 6-10 in `try/catch` — on error, if `executionId` was already obtained, call `recordCommandComplete(metaSequelize, executionId, { exitStatus: 'failed', errorMessage: error.message })` before re-throwing.
    Write `apps/dgfy-migration-runner/tests/schemaCommand.test.js` mocking `config/db.js`'s factories, `metadata/bootstrap.js`'s functions, and `metadata/storage.js`, covering the three behaviors above.
  </action>
  <acceptance_criteria>
    - The placeholder migration file exports a `meta` object with `destructive: false`.
    - `runSchemaMigrate` calls `assertTargetDbNameAllowed` and `assertDestructiveAllowed` before any `createTargetConnection`/`createMetaConnection` call, in that source order.
    - `npm --prefix apps/dgfy-migration-runner test -- schemaCommand.test.js` passes with 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix apps/dgfy-migration-runner test -- schemaCommand.test.js</automated>
  </verify>
  <done>`runSchemaMigrate()` runs the additive placeholder migration through Umzug without requiring `--confirm-destructive`, records a `command_executions` row, and writes a JSON+summary report pair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: data / verify / status / rollback-plan command handlers</name>
  <files>apps/dgfy-migration-runner/src/commands/data.js, apps/dgfy-migration-runner/src/commands/verify.js, apps/dgfy-migration-runner/src/commands/status.js, apps/dgfy-migration-runner/src/commands/rollbackPlan.js, apps/dgfy-migration-runner/tests/dataCommand.test.js, apps/dgfy-migration-runner/tests/reportCommands.test.js</files>
  <read_first>
    apps/dgfy-migration-runner/src/commands/schema.js (Task 1 of this plan — replicate its exact "validate env → guard → connect lazily → bootstrap metadata → recordCommandStart → do work → write reports → recordCommandComplete" skeleton for these four handlers)
    apps/dgfy-migration-runner/src/metadata/storage.js (Plan 02 Task 2 — `executed()` lets status.js/rollbackPlan.js read which migrations have run)
    .planning/phases/01-architecture-and-migration-runner-contract/01-CONTEXT.md decisions D-09, D-14
    .planning/phases/01-architecture-and-migration-runner-contract/01-RESEARCH.md "Command contract to plan" table (data dry-run/data apply/verify/status/rollback-plan Phase 1 behaviors)
  </read_first>
  <behavior>
    - Test 1: `runDataApply({confirmDestructive:false})` rejects with `DestructiveOperationError` and NEVER calls `createTargetConnection` (mock proves zero calls).
    - Test 2: `runDataApply({confirmDestructive:true})` succeeds and its report's `summary.rows_written` is `0` (Phase 1 writes no real data).
    - Test 3: `runDataDryRun({})` succeeds without requiring `confirmDestructive`.
    - Test 4: `runVerify({})`'s report has `summary.metadata_schema_ok` and `summary.target_db_reachable` boolean fields.
    - Test 5: `runRollbackPlan({})` NEVER calls any mocked migration's `down` function (assert 0 calls) while still returning a non-empty `results` array when at least one migration is `executed()`.
  </behavior>
  <action>
    Create `src/commands/data.js` exporting `export async function runDataDryRun({} = {})` — validates env (both `sourceDb` and `targetDb` required per D-03), `assertTargetDbNameAllowed`, does NOT call `assertDestructiveAllowed` (dry-run is never destructive per D-09), connects target+meta lazily, `ensureMetadataSchema`, `recordCommandStart({command:'data:dry-run', mode:'dry-run', ...})`, builds `report = { generated_at, command:'data:dry-run', mode:'dry-run', summary: { planned_inserts:0, planned_updates:0, planned_skips:0, planned_conflicts:0, orphan_records:0, tenant_coverage: [] }, results: [], note: 'Phase 1 contract stub proving dry-run gating/reporting only — real source-to-target transformation logic ships in Phase 3' }`, writes reports, `recordCommandComplete`, returns report.
    Export `export async function runDataApply({ confirmDestructive = false } = {})` — validates env, `assertTargetDbNameAllowed`, THEN `assertDestructiveAllowed({ isDestructive: true, confirmDestructive, runtimeMode: config.runtimeMode })` (data apply is ALWAYS destructive per D-09 — no per-file check needed here, unlike `schema.js`), connects, bootstraps metadata, `recordCommandStart({command:'data:apply', mode:'apply', ...})`, builds `report = { generated_at, command:'data:apply', mode:'apply', summary: { rows_written: 0 }, results: [], note: 'Phase 1 contract stub proving apply gating/reporting only — real data writes ship in Phase 3' }` (`rows_written` stays 0 — Phase 1 must not write real data), writes reports, `recordCommandComplete`, returns report.
    Create `src/commands/verify.js` exporting `export async function runVerify({} = {})` — validates env, `assertTargetDbNameAllowed`, connects meta+target, calls `ensureMetadataSchema(metaSequelize)` (this IS the contract check per D-08 — if it doesn't throw, the metadata schema is verified valid) and `targetSequelize.authenticate()` to prove target DB connectivity, `recordCommandStart`/`recordCommandComplete`, builds `report = { generated_at, command:'verify', summary: { metadata_schema_ok: true, target_db_reachable: true, target_db_name: config.targetDb.name } }`, writes reports, returns report; on a caught `MetadataSchemaError` or connection failure, set the corresponding boolean to `false` in `summary` INSTEAD of throwing, so `verify` always completes and reports findings (contrast with the other handlers, which throw on failure).
    Create `src/commands/status.js` exporting `export async function runStatus({} = {})` — validates env, connects meta, `ensureMetadataSchema`, reads the most recent 20 rows from `command_executions` via `metaSequelize.query('SELECT command, mode, actor, runtime_mode, started_at, completed_at, exit_status FROM command_executions ORDER BY started_at DESC LIMIT 20')`, reads Umzug migration status via `new MetaSequelizeStorage({sequelize: metaSequelize}).executed()`, builds `report = { generated_at, command:'status', summary: { recent_commands: rows.length, schema_migrations_executed: executedNames.length }, results: rows }`, writes reports, `recordCommandStart`/`recordCommandComplete`, returns report.
    Create `src/commands/rollbackPlan.js` exporting `export async function runRollbackPlan({} = {})` — validates env, connects meta, `ensureMetadataSchema`, reads executed migration names via `MetaSequelizeStorage.executed()`, for each executed name uses `createRequire(import.meta.url)` to `require()` the corresponding `.cjs` file under `src/migrations/schema/` and reads its `meta.rollbackDescription`/`meta.estimatedRisk` fields — per D-14, this function must NEVER call any migration's `down()` — builds `report = { generated_at, command:'rollback-plan', summary: { migrations_covered: entries.length }, results: entries.map((e) => ({ migration: e.name, rollback_description: e.rollbackDescription, estimated_risk: e.estimatedRisk })) }`, writes reports, `recordCommandStart`/`recordCommandComplete`, returns report.
    Write `apps/dgfy-migration-runner/tests/dataCommand.test.js` covering behaviors 1-3 above. Write `apps/dgfy-migration-runner/tests/reportCommands.test.js` covering `verify`/`status`/`rollback-plan` behaviors 4-5 above.
  </action>
  <acceptance_criteria>
    - `runDataApply` throws `DestructiveOperationError` before any connection factory call when `confirmDestructive` is false.
    - `runDataApply`'s stub report always has `summary.rows_written === 0`.
    - `grep -c "\.down(" apps/dgfy-migration-runner/src/commands/rollbackPlan.js` returns `0` (never invokes a migration's down function).
    - `npm --prefix apps/dgfy-migration-runner test -- dataCommand.test.js reportCommands.test.js` passes with 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix apps/dgfy-migration-runner test -- dataCommand.test.js reportCommands.test.js</automated>
  </verify>
  <done>All four handlers (`data dry-run`, `data apply`, `verify`, `status`, `rollback-plan`) follow the validate→guard→connect→bootstrap→record→report contract; `data apply` is destructive-gated and writes zero real rows in Phase 1; `rollback-plan` is report-only and never executes an undo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commander CLI dispatch wiring all six commands</name>
  <files>apps/dgfy-migration-runner/src/cli.js, apps/dgfy-migration-runner/tests/cliContract.test.js</files>
  <read_first>
    Context7 `/tj/commander.js` subcommand-with-options-and-action-handler excerpt (`program.command('split').option(...).action((opts) => {...})`, async handlers require `.parseAsync()`)
    backend/scripts/sync-tenant-schemas.js lines 638-645 (isMainModule catch-and-exit-1 error translation pattern)
    apps/dgfy-migration-runner/src/commands/schema.js, data.js, verify.js, status.js, rollbackPlan.js (this plan's Tasks 1-2 — cli.js imports and wires all five)
    .planning/phases/01-architecture-and-migration-runner-contract/01-CONTEXT.md decisions D-02, D-04, D-10
  </read_first>
  <behavior>
    - Test 1: `--help` output lists all six commands `schema`, `data`, `verify`, `status`, `rollback-plan` (nested `migrate`/`dry-run`/`apply` visible under `schema --help`/`data --help`).
    - Test 2: invoking the parsed CLI with argv `['node','cli.js','data','apply']` (no `--confirm-destructive`) calls `runDataApply` with `confirmDestructive: false`.
    - Test 3: invoking with `['node','cli.js','data','apply','--confirm-destructive']` calls it with `confirmDestructive: true`.
  </behavior>
  <action>
    Create `src/cli.js` as the package's `main` entry (matches `package.json`'s `"main": "src/cli.js"` from Plan 01 Task 1). Import `Command` from `commander` and all five `run*` functions from `./commands/schema.js`, `./commands/data.js`, `./commands/verify.js`, `./commands/status.js`, `./commands/rollbackPlan.js`. Build `const program = new Command(); program.name('dgfy-migration-runner').description('DGFY database-first migration runner: schema, data, verify, status, rollback-plan commands').version('1.0.0');`. Per D-04's exact subcommand shape (`node runner.js schema migrate`, `node runner.js data dry-run`): create a `schema` subcommand group — `const schemaCmd = new Command('schema').description('Schema migration commands'); schemaCmd.command('migrate').description('Run pending schema migrations through Umzug').option('--confirm-destructive', 'Required when pending migrations include destructive DDL (per D-09/D-10)').action(async (options) => { await runSchemaMigrate({ confirmDestructive: Boolean(options.confirmDestructive) }); }); program.addCommand(schemaCmd);`. Same nested pattern for `data`: `const dataCmd = new Command('data').description('Data migration commands'); dataCmd.command('dry-run').description('Report planned data migration changes without mutating target data').action(async () => { await runDataDryRun({}); }); dataCmd.command('apply').description('Apply data migration changes to the target database').option('--confirm-destructive', 'Required — data apply is always destructive per D-09').action(async (options) => { await runDataApply({ confirmDestructive: Boolean(options.confirmDestructive) }); }); program.addCommand(dataCmd);`. Top-level (non-grouped) commands: `program.command('verify').description('Run metadata schema and target DB connectivity checks').action(async () => { await runVerify({}); });`, `program.command('status').description('Show recent command execution history and schema migration status').action(async () => { await runStatus({}); });`, `program.command('rollback-plan').description('Generate a rollback-plan report artifact (does not execute any rollback)').action(async () => { await runRollbackPlan({}); });`. At the bottom, define `async function main() { await program.parseAsync(process.argv); }` (Commander requires `parseAsync` for async action handlers, per Context7 docs) and `main().catch((error) => { console.error(\`[dgfy-migration-runner] fatal: ${error.message}\`); process.exit(1); });` mirroring `sync-tenant-schemas.js`'s catch-and-exit pattern (lines 638-645) — unlike that analog, `cli.js` is ALWAYS the main entry (no `isMainModule` guard needed since this file's only job is CLI dispatch).
    Write `apps/dgfy-migration-runner/tests/cliContract.test.js` using `jest.mock('../src/commands/schema.js', ...)` / `jest.mock('../src/commands/data.js', ...)` etc. to spy on the imported `run*` functions, plus a child-process invocation of `node src/cli.js --help` (and `schema --help`, `data --help`) to assert on real stdout. Cover the three behaviors above.
  </action>
  <acceptance_criteria>
    - `node apps/dgfy-migration-runner/src/cli.js --help` exits 0 and its stdout contains all of: `schema`, `data`, `verify`, `status`, `rollback-plan`.
    - `node apps/dgfy-migration-runner/src/cli.js schema --help` stdout contains `migrate`; `node apps/dgfy-migration-runner/src/cli.js data --help` stdout contains `dry-run` and `apply`.
    - `npm --prefix apps/dgfy-migration-runner test -- cliContract.test.js` passes with 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>node apps/dgfy-migration-runner/src/cli.js --help && npm --prefix apps/dgfy-migration-runner test -- cliContract.test.js</automated>
  </verify>
  <done>All six RUN-02 commands are dispatchable, `--help`-discoverable subcommands of one CLI entry point; `--confirm-destructive` is correctly threaded from the CLI flag to `runDataApply`/`runSchemaMigrate`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → Commander action handlers | Untrusted CLI flags (`--confirm-destructive`) cross into command handler options |
| Command handlers → target/source/meta DB | Handlers connect only after env validation and safety-gate checks pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Elevation of Privilege | src/commands/data.js (runDataApply) | high | mitigate | `assertDestructiveAllowed({isDestructive:true,...})` is unconditional (data apply is always destructive per D-09) and runs before any DB factory call — verified by Task 2's test asserting zero `createTargetConnection` calls when the flag is absent. |
| T-01-08 | Repudiation | src/cli.js → commands/*.js | medium | mitigate | Every command action handler calls straight into a `run*()` function that itself calls `recordCommandStart`/`recordCommandComplete` (Plan 02) — there is no code path from CLI dispatch to DB work that skips the audit row. |
| T-01-09 | Tampering | src/commands/rollbackPlan.js | medium | mitigate | `rollbackPlan.js` statically reads `meta.rollbackDescription`/`meta.estimatedRisk` off each migration module and never invokes `down()`; Task 2's test asserts the mocked `down()` has zero calls, closing off any accidental-execution regression as later phases extend this file. |

</threat_model>

<verification>
Run `npm --prefix apps/dgfy-migration-runner test` — all four new test files pass with 0 failures. Run `node apps/dgfy-migration-runner/src/cli.js --help` and confirm all six commands are listed.
</verification>

<success_criteria>
`node src/cli.js schema migrate`, `data dry-run`, `data apply --confirm-destructive`, `verify`, `status`, and `rollback-plan` are all reachable, `--help`-discoverable Commander subcommands; `data apply` without the confirm flag is rejected before any DB connection; `schema migrate` runs the real placeholder migration through Umzug end-to-end using the Plan 02 metadata store and report writers.
</success_criteria>

<output>
Create `.planning/phases/01-architecture-and-migration-runner-contract/01-03-SUMMARY.md` when done
</output>
