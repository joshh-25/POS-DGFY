---
phase: 11-order-fulfillment-delivery-coordination
plan: 04
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - apps/dgfy-api/tests/integration/availments/fulfillmentFinalizeLive.test.js
autonomous: false
requirements: [FUL-01, FUL-02, FUL-03]
user_setup:
  - service: live-tenant-mysql
    why: "Applying the additive fulfillment migration and proving the stage-event auto-write requires a reachable MySQL 8.0 tenant (dgfy_business_*) database — Sequelize model/type checks pass WITHOUT the tables existing, so a live apply is the only proof the schema is real."
    env_vars:
      - name: TARGET_DB_NAME
        source: "The dgfy_business_* tenant schema to migrate (e.g. dgfy_business_aa1fb840807e198b6448), per the migration-runner env contract"
      - name: DB_HOST / DB_PORT / DB_USER / DB_PASSWORD
        source: "MySQL connection creds for the migration-runner and dgfy-api (same server used in Phase 9 09-08 live apply; e.g. 127.0.0.1:3306 via the lima-dgfy-dev docker context)"
    dashboard_config: []
must_haves:
  truths:
    - "The additive fulfillment migration is APPLIED against at least one live dgfy_business_* tenant schema (migration-runner reports executed>=1)."
    - "availment_stage_events and courier_assignments tables exist in the live tenant DB, plus the three new availments columns."
    - "availment_stage_events has BEFORE UPDATE/DELETE append-only triggers; courier_assignments does NOT (its payout is updatable)."
    - "A live POS finalize writes the full 5-row stage sequence; a live online finalize persists fulfillment_mode + one placed event (D-05/D-06)."
  artifacts:
    - apps/dgfy-api/tests/integration/availments/fulfillmentFinalizeLive.test.js
  key_links:
    - "migration-runner cli schema migrate -> live dgfy_business_* apply (BLOCKING, before any DB-dependent verification)"
    - "migration-runner cli verify -> dgfyBusinessContract covers the two new tables + columns"
---

<objective>
Apply the additive fulfillment migration against a live tenant (`dgfy_business_*`) MySQL schema, verify the new tables/columns/triggers exist, and prove end-to-end that a finalize writes the stage-event history. This closes the [BLOCKING] live-DB schema-application gate: Sequelize model definitions and `node --check`/unit tests all pass WITHOUT the tables existing in the real database, so without an explicit "run the migration" step the earlier plans' verification would false-positive.

Purpose: Make the schema real in the live DB and prove the D-05/D-06 auto-write works against real MySQL (append-only triggers, mutable payout, denormalized columns). Mirrors Phase 9's 09-08 live-apply approach.
Output: Live migration applied + verified; one ENV-gated live integration test added.

This plan is `autonomous: false` — it requires a reachable live tenant MySQL (operator-provided env / docker context). The migrate command itself is non-interactive for an additive (`destructive: false`) migration, but the live-DB environment must be confirmed by a human, and the apply is gated by a blocking human-verify checkpoint.
Locked decisions proven here: D-05 (dine-in full sequence), D-06 (single-transaction finalize extension). Landmine L1 (fulfillment_status column present, distinct from status), L3 (courier_assignments has NO append-only trigger).
</objective>

<execution_context>
@/home/user/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-order-fulfillment-delivery-coordination/11-CONTEXT.md
@.planning/phases/11-order-fulfillment-delivery-coordination/11-RESEARCH.md
@.planning/phases/09-pos-checkout-payment/09-08-SUMMARY.md
@.planning/phases/11-order-fulfillment-delivery-coordination/11-01-SUMMARY.md
@.planning/phases/11-order-fulfillment-delivery-coordination/11-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- Live-applied schema (the migration from Plan 01) in one or more `dgfy_business_*` tenant databases.
- One new ENV-gated live integration test: `apps/dgfy-api/tests/integration/availments/fulfillmentFinalizeLive.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Apply the fulfillment migration to the live tenant DB and verify</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260715120000-create-availment-fulfillment.cjs (applied, not modified), apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js (read)</files>
  <read_first>
    - .planning/phases/09-pos-checkout-payment/09-08-SUMMARY.md (the exact live-apply invocation: `node apps/dgfy-migration-runner/src/cli.js verify` then `node apps/dgfy-migration-runner/src/cli.js schema migrate`, the TARGET_DB_NAME env, and the post-apply direct-SQL SHOW TABLES/COLUMNS/TRIGGERS checks)
    - apps/dgfy-migration-runner/package.json (start script = node src/cli.js; commander CLI)
    - apps/dgfy-migration-runner/src/migrations/schema/20260715120000-create-availment-fulfillment.cjs (the migration being applied — from Plan 01)
    - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js (the verify contract — from Plan 01 Task 3)
  </read_first>
  <action>
    With the operator-provided live MySQL env vars set (TARGET_DB_NAME pointing at a real dgfy_business_* schema, plus DB_HOST/DB_PORT/DB_USER/DB_PASSWORD), first run `node apps/dgfy-migration-runner/src/cli.js verify` to confirm the target DB is reachable, then run `node apps/dgfy-migration-runner/src/cli.js schema migrate` to APPLY the additive fulfillment migration (destructive:false, targetKind:'business'). Capture the reporter output (expect status=success and executed>=1; if the migration was already applied on a re-run, total_pending=0 is acceptable as idempotent proof). Then confirm via direct SQL against the tenant DB: `SHOW TABLES LIKE 'availment_stage_events'` and `... 'courier_assignments'` return the tables; `SHOW COLUMNS FROM availments LIKE 'fulfillment_%'` returns the three new columns; `SHOW TRIGGERS` shows `trg_availment_stage_events_append_only_update`/`..._delete` present AND NO append-only triggers on `courier_assignments` (L3 — payout mutable). This task MUST run BEFORE Task 3's live finalize proof (the [BLOCKING] ordering).
  </action>
  <verify>
    <automated>node apps/dgfy-migration-runner/src/cli.js verify</automated>
  </verify>
  <acceptance_criteria>
    - `schema migrate` reports status=success with executed>=1 on first apply (or total_pending=0 on an idempotent re-run).
    - Direct SQL confirms `availment_stage_events` and `courier_assignments` exist in the tenant DB and `availments` has `fulfillment_mode`/`fulfillment_status`/`fulfillment_stage`.
    - `availment_stage_events` has BEFORE UPDATE and BEFORE DELETE append-only triggers; `courier_assignments` has NO such triggers.
    - `cli.js verify` passes (contract covers the new tables/columns).
    - No file under `backend/` is created or modified.
  </acceptance_criteria>
  <done>The fulfillment migration is live-applied and verified against a real tenant schema, with append-only triggers scoped to the stage-event table only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the live migration apply</name>
  <action>Pause for human verification of the live schema apply from Task 1 before running the DB-dependent finalize proof. Do not proceed until the operator confirms the tables/columns/triggers exist in the intended tenant schema.</action>
  <what-built>The additive fulfillment migration was applied to the live tenant DB (Task 1): three new availments columns, availment_stage_events (append-only, triggers present), and courier_assignments (mutable payout, no triggers).</what-built>
  <how-to-verify>
    1. Confirm the migration-runner output showed `status=success` and `executed>=1` (or `total_pending=0` on a clean re-run) for `schema migrate` against the intended `dgfy_business_*` schema.
    2. In the tenant DB, run `SHOW TABLES LIKE 'availment_stage_events'`, `SHOW TABLES LIKE 'courier_assignments'`, and `SHOW COLUMNS FROM availments LIKE 'fulfillment_%'` — expect both tables and the three columns.
    3. Run `SHOW TRIGGERS` and confirm append-only triggers exist on `availment_stage_events` and NOT on `courier_assignments`.
    4. Confirm this was applied to the correct tenant schema(s) for your environment.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to the live finalize proof, or describe what is missing.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: ENV-gated live end-to-end finalize proof</name>
  <files>apps/dgfy-api/tests/integration/availments/fulfillmentFinalizeLive.test.js</files>
  <read_first>
    - apps/dgfy-api/tests/integration/availments/finalizeLive.test.js (the ENV-gated live integration pattern: LIVE_TENANT_DB gate + BUSINESS_IT_DB_* env, throwaway landlord+tenant DB pair created/dropped in-test, self-skips without env; the finalize assertions to mirror)
    - apps/dgfy-api/tests/helpers/tenantSchemaProvisioning.js (applyAndVerifyBusinessSchema — dynamically loads every meta.targetKind==='business' migration by filename order, so it already includes the new fulfillment migration automatically)
    - apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js (the finalize seams modified in Plan 03)
    - apps/dgfy-api/src/modules/fulfillment/index.js (recordStageEvents port used by the seam)
  </read_first>
  <action>
    Create apps/dgfy-api/tests/integration/availments/fulfillmentFinalizeLive.test.js mirroring finalizeLive.test.js's ENV-gated harness (self-skips when LIVE_TENANT_DB is unset; provisions a throwaway tenant via applyAndVerifyBusinessSchema so the new migration is applied to the test DB). Assert two behaviors against real MySQL: (1) a POS/dine-in finalize writes exactly 5 availment_stage_events rows in the sequence placed->confirmed->preparing->ready->completed for the availment, sets the availment's fulfillment_status/fulfillment_stage to 'completed', and defaults fulfillment_mode to 'dine_in' (A1); (2) a storefront/online finalize persists fulfillment_mode (from the threaded value, not dropped) and writes exactly one 'placed' stage event with fulfillment_status='placed'. Optionally assert that an UPDATE on an availment_stage_events row is rejected by the append-only trigger (SIGNAL 45000) while an UPDATE of courier_assignments.payout_status succeeds. This task runs AFTER Task 1's live apply.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs tests/integration/availments/fulfillmentFinalizeLive.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and SELF-SKIPS cleanly when `LIVE_TENANT_DB` is unset (CI stays green, matching finalizeLive.test.js behavior).
    - With live env set, the POS finalize assertion (5-row sequence + denormalized 'completed' + default dine_in) passes.
    - With live env set, the online finalize assertion (fulfillment_mode persisted + one 'placed' event) passes.
    - The append-only-vs-mutable divergence is exercised (stage-event UPDATE rejected; courier payout UPDATE allowed), or documented if deferred.
    - No file under `backend/` is created or modified.
  </acceptance_criteria>
  <done>A live, ENV-gated integration test proves the D-05/D-06 stage-event auto-write end-to-end against real MySQL and self-skips without a live DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live tenant DB DDL | Migration applies schema changes to a real tenant; must be additive/idempotent and scoped to the intended schema. |
| test harness -> throwaway DB | Live test creates/drops its own DB pair; must not touch production tenants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04-01 | Denial of Service | wrong-schema migrate | medium | mitigate | Operator confirms TARGET_DB_NAME; migration is additive (destructive:false) + idempotent; blocking human-verify checkpoint before proceeding. |
| T-11-04-02 | Tampering | append-only bypass | high | mitigate | Live test asserts the SIGNAL 45000 trigger rejects UPDATE/DELETE on availment_stage_events. |
| T-11-04-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase. |
</threat_model>

<verification>
- `cli.js verify` passes and `schema migrate` applied the migration (executed>=1 / idempotent re-run).
- Both new tables + three columns exist in the live tenant DB; append-only triggers scoped to stage events only.
- The live finalize test proves the 5-row dine-in sequence and the online placed event, and self-skips without a live DB.
- No writes under `backend/`.
</verification>

<success_criteria>
The fulfillment schema is real in a live `dgfy_business_*` tenant (not just model-defined), verified by the migration-runner contract and direct SQL, and the D-05/D-06 stage-event auto-write is proven end-to-end against real MySQL — closing the [BLOCKING] live-DB schema-application gate.
</success_criteria>

<output>
Create `.planning/phases/11-order-fulfillment-delivery-coordination/11-04-SUMMARY.md` when done.
</output>
