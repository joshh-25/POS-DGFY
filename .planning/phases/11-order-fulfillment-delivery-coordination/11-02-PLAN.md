---
phase: 11-order-fulfillment-delivery-coordination
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - apps/dgfy-api/src/modules/fulfillment/repositories/stageEventRepository.js
  - apps/dgfy-api/src/modules/fulfillment/repositories/courierAssignmentRepository.js
  - apps/dgfy-api/src/modules/fulfillment/repositories/availmentReadRepository.js
  - apps/dgfy-api/src/modules/fulfillment/usecases/fulfillmentUseCases.js
  - apps/dgfy-api/src/modules/fulfillment/controllers/fulfillmentController.js
  - apps/dgfy-api/src/modules/fulfillment/routes.js
  - apps/dgfy-api/src/modules/fulfillment/index.js
  - apps/dgfy-api/tests/unit/modules/fulfillment/fulfillmentUseCases.test.js
  - apps/dgfy-api/tests/unit/modules/fulfillment/fulfillmentRepositories.test.js
autonomous: true
requirements: [FUL-01, FUL-02, FUL-03]
must_haves:
  truths:
    - "Staff can list incoming ONLINE orders (fulfillment_mode in pickup/delivery AND fulfillment_status in placed/confirmed/preparing), scoped to their business (FUL-01, D-08)."
    - "Staff can progress an availment to the NEXT legal stage for its fulfillment_mode; an illegal/skipped transition is rejected with a 409-class conflict (FUL-02, D-15)."
    - "A delivery order in out_for_delivery can be force-completed, recorded with is_forced=true + reason (D-10)."
    - "Staff can assign a free-text courier (name/contact) as a new append-only row and mark payout owed->paid (FUL-03, D-01/D-02/D-04)."
    - "Every /fulfillment usecase gates on any-active business membership before reading/writing tenant data (A3)."
  artifacts:
    - apps/dgfy-api/src/modules/fulfillment/usecases/fulfillmentUseCases.js
    - apps/dgfy-api/src/modules/fulfillment/repositories/stageEventRepository.js
    - apps/dgfy-api/src/modules/fulfillment/repositories/courierAssignmentRepository.js
    - apps/dgfy-api/src/modules/fulfillment/repositories/availmentReadRepository.js
    - apps/dgfy-api/src/modules/fulfillment/index.js
  key_links:
    - "STAGE_SEQUENCES frozen map -> nextLegalStage() validation gates every progression (D-12/D-13/D-14/D-15)"
    - "availmentReadRepository.findIncomingAvailments() (resolves getModels(db).Availment) -> injected into buildListIncomingOrdersUseCase; this is the concrete FUL-01 read path (D-08)"
    - "buildFulfillmentModule() exposes recordStageEvents port for the availments finalize seam (consumed in Plan 03)"
    - "requireMembership(businessRepository) -> landlord dgfy_core membership gate on every usecase (A3)"
---

<objective>
Build the new `apps/dgfy-api/src/modules/fulfillment` Clean-Architecture module mirroring `modules/inventory`: three repositories (append-only stage events; courier assignments with mutable payout; a read-only availment reader that backs the FUL-01 incoming-orders queue), the usecases layer carrying the `STAGE_SEQUENCES` frozen map + per-mode next-legal-stage validation (D-15 app-logic) + membership gating, a transport-only controller, the routes, and the DI wiring factory. Includes mock-Sequelize unit tests. The module's `buildFulfillmentModule()` also exposes a `recordStageEvents` port that the availments finalize seam consumes in Plan 03.

Purpose: This is the staff-facing surface for FUL-01 (view/process incoming orders), FUL-02 (progress the shared pipeline), and FUL-03 (assign courier + track payout). The linchpin is that per-mode valid-sequence enforcement lives in application logic, never the DB (D-15).
Output: 7 new module files + 2 unit test files.

Locked decisions implemented here: D-01 (free-text courier), D-02 (payout owed->paid lifecycle), D-04 (append-only courier identity, reassignment supersedes not overwrites), D-08 (queue scoped to online orders only), D-09/D-11 (staff marks completed; no customer-facing endpoint), D-10 (force-complete override), D-12/D-13/D-14 (exact per-mode stage names), D-15 (shared enum + app-logic sequence).

**Resolved research forks (dispositions locked in this plan, no cross-read of 11-RESEARCH.md needed):**
- **A3 (staff gate) → RESOLVED: any-active-membership gate, not owner-only** — every usecase calls `requireMembership` (inventory precedent).
- **Open Q4 (FUL-01 filter shape) → RESOLVED: optional `branch_id`/`fulfillment_mode` filters, default = all active online orders** — implemented in `availmentReadRepository.findIncomingAvailments` + `buildListIncomingOrdersUseCase`.
- **Open Q2 / D-10 (force-complete distinctness) → RESOLVED: force-complete writes `is_forced=true` + `reason`** (not `reason` alone) in `buildProgressStageUseCase`'s force branch.
- **A5 (courier payout mutability) → RESOLVED: mutable** — `courierAssignmentRepository.updatePayout` performs a real UPDATE (this table is intentionally NOT append-only).
</objective>

<execution_context>
@/home/user/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/user/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-order-fulfillment-delivery-coordination/11-CONTEXT.md
@.planning/phases/11-order-fulfillment-delivery-coordination/11-RESEARCH.md
@.planning/phases/11-order-fulfillment-delivery-coordination/11-PATTERNS.md
@.planning/phases/11-order-fulfillment-delivery-coordination/11-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- Module dir `apps/dgfy-api/src/modules/fulfillment/` with `index.js`, `routes.js`, `controllers/fulfillmentController.js`, `usecases/fulfillmentUseCases.js`, `repositories/stageEventRepository.js`, `repositories/courierAssignmentRepository.js`, `repositories/availmentReadRepository.js`.
- The `availmentReadRepository` (read-only) with `findIncomingAvailments(businessId, { branchId, fulfillmentMode })` resolving `getModels(db).Availment` — the concrete FUL-01 read path injected into `buildListIncomingOrdersUseCase`.
- The `STAGE_SEQUENCES` exported frozen constant.
- The `buildFulfillmentModule()` factory and its exported `recordStageEvents` port.
- Endpoint paths (mounted under `/fulfillment` in Plan 03): `GET /fulfillment/incoming-orders`, `POST /fulfillment/stage`, `POST /fulfillment/courier`, `POST /fulfillment/payout`.
- Unit tests under `apps/dgfy-api/tests/unit/modules/fulfillment/`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stageEventRepository + courierAssignmentRepository</name>
  <files>apps/dgfy-api/src/modules/fulfillment/repositories/stageEventRepository.js, apps/dgfy-api/src/modules/fulfillment/repositories/courierAssignmentRepository.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/inventory/repositories/inventoryMovementRepository.js (copy verbatim: `TenantDatabaseUnavailableError` class, constructor guard, `resolveDatabaseName` requiring registry status active + verified_at, `resolveModel`, `withModel`, and the transaction-aware `options.transaction` pattern)
    - apps/dgfy-api/src/models/Tenant/InventoryMovement.js (the append-only model shape the stage-event repo writes against)
    - .planning/phases/11-order-fulfillment-delivery-coordination/11-PATTERNS.md (stageEventRepository + courierAssignmentRepository adaptation sections)
  </read_first>
  <behavior>
    - stageEventRepository.create(businessId, input, { transaction }) inserts one availment_stage_events row via getModels(db).AvailmentStageEvent and returns the plain row.
    - stageEventRepository.bulkCreate(businessId, rows, { transaction }) inserts many rows in one call (used by the dine-in full-sequence auto-write).
    - stageEventRepository exposes create/bulkCreate/findAll/findOne ONLY — no update/delete (append-only).
    - When an outer `{ transaction }` is passed, the write uses it directly (does not open a new transaction).
    - courierAssignmentRepository.create inserts a new courier_assignments row (is_active=true).
    - courierAssignmentRepository.updatePayout(businessId, assignmentId, { payoutStatus, paidAt }) performs a real UPDATE (this table is NOT append-only).
    - courierAssignmentRepository.markSuperseded(businessId, assignmentId) sets is_active=false + superseded_at on a prior attempt (reassignment history, D-04).
    - A registry lookup that is missing/inactive/unverified surfaces as TenantDatabaseUnavailableError.
  </behavior>
  <action>
    Create apps/dgfy-api/src/modules/fulfillment/repositories/stageEventRepository.js. Copy the `TenantDatabaseUnavailableError` class, constructor `{ tenantConnector, businessDatabaseRegistryRepository }` guard, `resolveDatabaseName`, `resolveModel` (resolving `getModels(databaseName).AvailmentStageEvent`), and `withModel` from inventoryMovementRepository.js. Expose `create(businessId, input, options)`, `bulkCreate(businessId, rows, options)`, `findAll`, `findOne` — mapping camelCase input to snake_case columns and passing `options.transaction` straight through to `Model.create`/`Model.bulkCreate` when present (mirror the inventory repo's transaction-aware write). Expose NO update/delete methods. Provide a `toPlain` helper.

    Create apps/dgfy-api/src/modules/fulfillment/repositories/courierAssignmentRepository.js from the same scaffold (same error class + `resolveDatabaseName` + `withModel`, resolving `getModels(databaseName).CourierAssignment`). Add `create(businessId, input)` (new assignment row); `updatePayout(businessId, assignmentId, { payoutStatus, paidAt })` performing a real `Model.update({ payout_status, paid_at }, { where: { id, business_id } })`; `markSuperseded(businessId, assignmentId)` performing `Model.update({ is_active: false, superseded_at: new Date() }, { where })`; and `findAll`/`findActiveForAvailment`. This repo intentionally supports UPDATE (payout mutability, L3/A5) — it does not mirror the inventory repo's append-only-only surface.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs tests/unit/modules/fulfillment/fulfillmentRepositories.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `stageEventRepository.js` exposes create/bulkCreate/findAll/findOne and NO update/delete; passes an injected `{ transaction }` straight through to the model write.
    - `courierAssignmentRepository.js` exposes create, updatePayout (real UPDATE), markSuperseded, and a find method.
    - Both repositories reuse the `TenantDatabaseUnavailableError` + `resolveDatabaseName` (active + verified_at) + `withModel` pattern from the inventory repository.
    - `fulfillmentRepositories.test.js` passes (mock-Sequelize: create returns a row, updatePayout issues Model.update, missing/unverified registry surfaces TenantDatabaseUnavailableError).
    - No file under `backend/` is created or modified.
  </acceptance_criteria>
  <done>Both repositories exist with the correct method surfaces (append-only for stage events, mutable payout for courier), transaction pass-through works, and repository unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: availmentReadRepository (FUL-01 read path) + fulfillmentUseCases (STAGE_SEQUENCES + membership + FUL-01/02/03 usecases) + controller + routes + index</name>
  <files>apps/dgfy-api/src/modules/fulfillment/repositories/availmentReadRepository.js, apps/dgfy-api/src/modules/fulfillment/usecases/fulfillmentUseCases.js, apps/dgfy-api/src/modules/fulfillment/controllers/fulfillmentController.js, apps/dgfy-api/src/modules/fulfillment/routes.js, apps/dgfy-api/src/modules/fulfillment/index.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/inventory/repositories/inventoryMovementRepository.js (the repository scaffold to reuse for availmentReadRepository: `TenantDatabaseUnavailableError`, constructor `{ tenantConnector, businessDatabaseRegistryRepository }` guard, `resolveDatabaseName` (registry active + verified_at), `resolveModel`, `withModel`, and the `Op`-based `where`/`findAll` read style)
    - apps/dgfy-api/src/models/Tenant/Availment.js (the Availment column shape the reader queries: `fulfillment_mode`, `fulfillment_status`, and `branch_id` — added/confirmed in Plan 01)
    - apps/dgfy-api/src/modules/inventory/usecases/inventoryMovementUseCases.js (copy verbatim: the local error-helper block L16-50, `isTenantDatabaseUnavailableError`/`mapTenantDatabaseError` L58-81, `requireMembership`/`guardBusinessAccess` L96-119, and the `MOVEMENT_TYPES = Object.freeze([...])` constant style L14)
    - apps/dgfy-api/src/modules/inventory/controllers/inventoryMovementController.js (transport-only controller: businessId from req.body(POST)/req.query(GET), req.account.id, sendUseCaseResult — NO model/repository imports)
    - apps/dgfy-api/src/modules/inventory/routes.js (createInventoryRoutes shape: authenticateAccount guard + `.catch(next)` per route, top-level mount)
    - apps/dgfy-api/src/modules/inventory/index.js (buildInventoryModule DI factory + reservationPorts export style — the model exposes a port object; mirror for recordStageEvents)
    - apps/dgfy-api/src/shared/contracts/applicationResult.js and apps/dgfy-api/src/shared/controllers/useCaseResponder.js (ApplicationResult.success/failure + sendUseCaseResult contracts)
    - .planning/phases/11-order-fulfillment-delivery-coordination/11-PATTERNS.md (usecases + controller + routes + index sections; STAGE_SEQUENCES and nextLegalStage snippet)
  </read_first>
  <behavior>
    - availmentReadRepository.findIncomingAvailments(businessId, { branchId, fulfillmentMode }) resolves getModels(db).Availment and returns ONLY rows where fulfillment_mode IN ('pickup','delivery') AND fulfillment_status IN ('placed','confirmed','preparing') (D-08 online-in-progress queue); a completed/ready row or a dine_in/POS row is excluded.
    - findIncomingAvailments narrows to a single mode when a valid `fulfillmentMode` is passed, and to a single branch when `branchId` is passed; with neither it returns all active online orders.
    - availmentReadRepository is READ-ONLY (no create/update/delete) and surfaces a missing/inactive/unverified registry as TenantDatabaseUnavailableError (same scaffold as the other repos).
    - STAGE_SEQUENCES is a frozen map: pickup and dine_in = [placed, confirmed, preparing, ready, completed]; delivery = [placed, confirmed, preparing, out_for_delivery, completed].
    - progressStage advances an availment from its current fulfillment_status to the next legal stage for its fulfillment_mode; a non-successor target returns a conflict (409-class) DomainError.
    - progressStage with force=true on a delivery order in out_for_delivery jumps to completed, writing is_forced=true + reason (D-10).
    - listIncomingOrders returns only availments with fulfillment_mode in (pickup,delivery) AND fulfillment_status in (placed,confirmed,preparing); optional branch_id / fulfillment_mode filters narrow it (D-08, Open Q4).
    - assignCourier writes a new courier_assignments row (free-text name/contact) and supersedes any prior active assignment for that availment (D-04).
    - markPayout updates payout_status owed->paid and sets paid_at (D-02).
    - Every usecase returns forbiddenError when the requester is not an active member (A3).
    - payout_amount is rejected when negative or non-numeric (V5).
  </behavior>
  <action>
    Create apps/dgfy-api/src/modules/fulfillment/repositories/availmentReadRepository.js — the concrete FUL-01 read component (this is the named availment-read path; the usecase must NOT invent one). Reuse the inventoryMovementRepository scaffold verbatim: the `TenantDatabaseUnavailableError` class, the constructor `{ tenantConnector, businessDatabaseRegistryRepository }` guard, `resolveDatabaseName` (registry `status==='active'` AND `verified_at`), `resolveModel` (resolving `getModels(databaseName).Availment` — NOT AvailmentStageEvent; the stage-event repo is hard-bound to a different model and cannot serve this query), and `withModel`. Expose ONE read method `findIncomingAvailments(businessId, { branchId, fulfillmentMode } = {})` that, inside `withModel`, calls `Model.findAll({ where, order: [['created_at','ASC']] })` where `where` is built as: `fulfillment_status: { [Op.in]: ['placed','confirmed','preparing'] }` AND `fulfillment_mode: { [Op.in]: ['pickup','delivery'] }` (D-08 online-in-progress scope) — narrowed to `fulfillment_mode: fulfillmentMode` when a valid `fulfillmentMode` ('pickup'|'delivery') is passed, and adding `branch_id: branchId` only when `branchId` is provided (Open Q4). Map rows via a `toPlain` helper. Expose NO create/update/delete (read-only). Import `Op` from sequelize.

    Create apps/dgfy-api/src/modules/fulfillment/usecases/fulfillmentUseCases.js. Copy the local error helpers, tenant-DB error mapping, and `requireMembership`/`guardBusinessAccess` (any-active-member gate, A3) from inventoryMovementUseCases.js. Export `STAGE_SEQUENCES = Object.freeze({ pickup:[...], dine_in:[...], delivery:[...] })` with the exact D-12/D-13/D-14 sequences, plus a private `nextLegalStage(mode, current)` returning the successor or null. Build:
    - `buildListIncomingOrdersUseCase({ availmentReadRepository, businessRepository })` — FUL-01/D-08: guard membership, then delegate the read to `availmentReadRepository.findIncomingAvailments(businessId, { branchId, fulfillmentMode })` (the concrete reader created above), and return `ApplicationResult.success` with the returned list. Do NOT import a model into the usecase and do NOT reuse the stage-event repo (it resolves AvailmentStageEvent, not Availment) — the availment query lives solely in `availmentReadRepository`.
    - `buildProgressStageUseCase({ stageEventRepository, availmentRepository | repository, businessRepository })` — FUL-02: guard membership, load the availment, compute `nextLegalStage(fulfillment_mode, fulfillment_status)`; if the requested next stage is not the legal successor (and not a valid D-10 force to completed), return `conflictError`; otherwise write an append-only stage event and update the availment's denormalized `fulfillment_status`/`fulfillment_stage`. Reject progressing an already-`completed` availment (D-07 immutability).
    - `buildForceCompleteUseCase` (or a `force` branch inside progressStage) — D-10: only a delivery order in `out_for_delivery` may jump to `completed` with `is_forced: true` + a `reason`; record it distinctly.
    - `buildAssignCourierUseCase({ courierAssignmentRepository, businessRepository })` — FUL-03/D-01/D-04: guard membership, validate `courier_name` non-empty and length-bounded and `payout_amount` non-negative if present, mark any prior active assignment superseded, then insert a new active row (`assigned_by_staff_account_id` from the requester's staff account).
    - `buildMarkPayoutUseCase({ courierAssignmentRepository, businessRepository })` — FUL-03/D-02: guard membership, update `payout_status` to `paid` and set `paid_at`.

    Create apps/dgfy-api/src/modules/fulfillment/controllers/fulfillmentController.js (transport-only, mirror inventory controller): `listIncoming` (GET, businessId + filters from req.query, req.account.id), `progressStage`/`assignCourier`/`markPayout` (POST, businessId from req.body, req.account.id); each calls the injected usecase and returns `sendUseCaseResult(res, result, 200|201)`. No model/repository imports (eslint no-restricted-imports).

    Create apps/dgfy-api/src/modules/fulfillment/routes.js: `createFulfillmentRoutes(useCases, { authenticateAccount })` that throws if authenticateAccount is not a function, builds the controller, and wires `GET /incoming-orders`, `POST /stage`, `POST /courier`, `POST /payout` each with `authenticateAccount` + `.catch(next)`.

    Create apps/dgfy-api/src/modules/fulfillment/index.js: `buildFulfillmentModule({ tenantConnector, businessDatabaseRegistryRepository, businessRepository })` that constructs all three repositories (stageEventRepository, courierAssignmentRepository, and availmentReadRepository — the last built from the SAME `{ tenantConnector, businessDatabaseRegistryRepository }`), builds every usecase closed over them (inject `availmentReadRepository` into `buildListIncomingOrdersUseCase`), and returns `{ repositories, useCases, createFulfillmentRoutes, recordStageEvents }`. `recordStageEvents` is the injectable PORT the availments module consumes (Plan 03): a function `(businessId, events, { transaction }) => stageEventRepository.bulkCreate(businessId, events, { transaction })` so the finalize seam can write inside the existing transaction. Re-export `STAGE_SEQUENCES` and `createFulfillmentRoutes`.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs tests/unit/modules/fulfillment/fulfillmentUseCases.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `availmentReadRepository.js` exists, resolves `getModels(db).Availment` via the shared `resolveDatabaseName`/`withModel` scaffold, and exposes ONLY `findIncomingAvailments` (no write methods).
    - `findIncomingAvailments` returns only in-progress ONLINE availments — `fulfillment_mode IN ('pickup','delivery')` AND `fulfillment_status IN ('placed','confirmed','preparing')` — and applies optional `branchId`/`fulfillmentMode` narrowing; ready/completed rows and dine_in/POS rows are excluded.
    - `buildListIncomingOrdersUseCase` reads exclusively through the injected `availmentReadRepository.findIncomingAvailments` (no model import in the usecase, no reuse of the stage-event repo).
    - `buildFulfillmentModule()` constructs `availmentReadRepository` from the shared `{ tenantConnector, businessDatabaseRegistryRepository }` and injects it into `buildListIncomingOrdersUseCase`.
    - `fulfillmentUseCases.js` exports `STAGE_SEQUENCES` with pickup/dine_in = [placed,confirmed,preparing,ready,completed] and delivery = [placed,confirmed,preparing,out_for_delivery,completed].
    - progressStage rejects an illegal/skipped transition with a conflict (409) DomainError and accepts only the legal successor.
    - A delivery `out_for_delivery` order force-completes with `is_forced: true` + reason; a non-delivery or wrong-stage force is rejected.
    - listIncomingOrders returns only pickup/delivery availments in placed/confirmed/preparing (dine-in/POS and completed orders excluded) and honors optional branch/mode filters.
    - assignCourier supersedes the prior active assignment and inserts a new active row; markPayout updates payout to paid with paid_at.
    - Every usecase returns a forbidden (403) result for a non-active-member requester.
    - The controller imports no model/repository; routes mount `incoming-orders`/`stage`/`courier`/`payout` behind authenticateAccount.
    - `buildFulfillmentModule()` returns a `recordStageEvents` port bound to stageEventRepository.bulkCreate.
    - `fulfillmentUseCases.test.js` passes.
    - No file under `backend/` is created or modified.
  </acceptance_criteria>
  <done>The fulfillment module (repositories already built in Task 1) has usecases with app-logic stage validation, membership gating, courier + payout flows, a transport-only controller, routes, and a DI factory exposing recordStageEvents — with usecase unit tests passing.</done>
</task>

<task type="auto">
  <name>Task 3: Mock-Sequelize unit tests for the module</name>
  <files>apps/dgfy-api/tests/unit/modules/fulfillment/fulfillmentUseCases.test.js, apps/dgfy-api/tests/unit/modules/fulfillment/fulfillmentRepositories.test.js</files>
  <read_first>
    - apps/dgfy-api/tests/unit/modules/inventory/inventoryMovementUseCases.test.js (Jest ESM `import { jest } from '@jest/globals'`; makeModels() mock-Sequelize with create/findAll/findOne/bulkCreate + sequelize.transaction; makeTenantConnector; makeRegistryRepository returning { database_name, status:'active', verified_at })
    - apps/dgfy-api/src/modules/fulfillment/usecases/fulfillmentUseCases.js (the code under test — from Task 2)
    - apps/dgfy-api/src/modules/fulfillment/repositories/stageEventRepository.js (from Task 1)
    - apps/dgfy-api/src/modules/fulfillment/repositories/availmentReadRepository.js (from Task 2 — the FUL-01 reader; its findIncomingAvailments is mocked in the listIncomingOrders usecase test and exercised directly in the repository test)
  </read_first>
  <action>
    Create apps/dgfy-api/tests/unit/modules/fulfillment/fulfillmentUseCases.test.js and apps/dgfy-api/tests/unit/modules/fulfillment/fulfillmentRepositories.test.js (the latter is the target of Task 1's verify). Use the inventory test's mock-Sequelize helpers. Cover: (1) STAGE_SEQUENCES legal successor accepted, illegal skip rejected with 409, per each mode; (2) delivery force-complete from out_for_delivery writes is_forced=true, and force rejected for pickup/dine_in or wrong stage; (3) listIncomingOrders delegates to a mocked `availmentReadRepository.findIncomingAvailments` (assert it is called with businessId + the branch/mode filter inputs and the usecase returns its rows); (4) availmentReadRepository.findIncomingAvailments (mock-Sequelize Availment model) builds a `where` with fulfillment_mode IN (pickup,delivery) AND fulfillment_status IN (placed,confirmed,preparing), narrows on branchId/fulfillmentMode, and returns only online in-progress rows; (5) assignCourier supersedes prior + inserts new; markPayout flips owed->paid with paid_at; (6) non-active-member requester -> forbidden (403) for each usecase; (7) negative payout_amount rejected; (8) repository transaction pass-through: bulkCreate receives the injected transaction object.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs tests/unit/modules/fulfillment</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist under `tests/unit/modules/fulfillment/` and run green.
    - Tests assert: legal vs illegal stage transitions (per mode), D-10 force-complete rules, FUL-01 (listIncomingOrders delegates to a mocked availmentReadRepository.findIncomingAvailments AND the reader itself builds the online-in-progress where-clause with branch/mode narrowing), courier supersede+insert, payout owed->paid, membership-forbidden paths, negative-payout rejection, and transaction pass-through.
    - The full `tests/unit/modules/fulfillment` run passes with 0 failures.
    - No file under `backend/` is created or modified.
  </acceptance_criteria>
  <done>The module has passing mock-Sequelize unit tests covering every FUL-01/02/03 behavior and the D-10/D-15 rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff client -> /fulfillment/* | Authenticated staff input crosses here; business_id + stage/courier/payout fields are attacker-controllable. |
| usecase -> tenant DB | Reads/writes scoped by resolveDatabaseName(business_id) + WHERE business_id. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02-01 | Elevation of Privilege | cross-tenant availment access | high | mitigate | `requireMembership(businessId)` on every usecase + `WHERE business_id = ?` scoping; opaque cross-DB business_id, never a joinable FK. |
| T-11-02-02 | Tampering | illegal/skipped stage transition | high | mitigate | App-logic `nextLegalStage` check; only D-10 force may jump to completed, recorded as is_forced + reason. |
| T-11-02-03 | Tampering | client-supplied payout_amount | medium | mitigate | Server validates payout_amount is non-negative numeric; records assigned_by_staff_account_id. |
| T-11-02-04 | Repudiation | courier reassignment overwrites history | medium | mitigate | Reassignment supersedes (is_active=false + superseded_at) and inserts a new row; prior attempts never deleted (D-04). |
| T-11-02-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (RESEARCH: no install). |
</threat_model>

<verification>
- `tests/unit/modules/fulfillment` passes (usecases + repositories).
- STAGE_SEQUENCES matches D-12/D-13/D-14 exactly; illegal transitions rejected (D-15 app-logic).
- FUL-01 queue is online-only (D-08); courier + payout flows satisfy FUL-03; force-complete satisfies D-10.
- Controller has no model/repository imports; routes gate on authenticateAccount.
- No writes under `backend/`.
</verification>

<success_criteria>
The `/fulfillment` module implements FUL-01 (list/process incoming online orders), FUL-02 (progress the shared pipeline with per-mode handoff steps), and FUL-03 (assign courier + track payout), with per-mode sequence validation in application logic and a `recordStageEvents` port ready for the finalize seam — all proven by passing unit tests.
</success_criteria>

<output>
Create `.planning/phases/11-order-fulfillment-delivery-coordination/11-02-SUMMARY.md` when done.
</output>
