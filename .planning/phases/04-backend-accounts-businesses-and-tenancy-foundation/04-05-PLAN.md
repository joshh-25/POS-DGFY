---
phase: 04-backend-accounts-businesses-and-tenancy-foundation
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified: [
  "tests/integration/accounts/accountFlows.test.js",
  "tests/integration/accounts/accountValidation.test.js",
  "tests/integration/accounts/accountPersistence.test.js",
  "tests/integration/businesses/businessFlows.test.js",
  "tests/integration/businesses/businessValidation.test.js",
  "tests/integration/tenancy/tenantSessionFlows.test.js",
  "tests/integration/tenancy/tenantSessionValidation.test.js",
  "tests/e2e/phase4FullFlow.test.js"
]
autonomous: false
requirements: ["API-06", "API-05"]
user_setup: []

must_haves:
  truths:
    - "Account registration success path: create account → account is persisted unverified"
    - "Account login success path: valid credentials → session + businesses list returned"
    - "Account profile update success: modify email/password → changes persisted"
    - "Business creation success: create business → creator is owner → membership persisted"
    - "Staff onboarding both paths: invitation and direct add → staff persisted"
    - "Tenant session success: activate session → context bound if membership + assignment exist"
    - "Tenant session rejection: missing membership → HTTP 403"
    - "Tenant session rejection: missing assignment → HTTP 403"
    - "Conflict scenarios: concurrent registrations, duplicate handles, race conditions → handled correctly"
    - "Replay rejection: duplicate registration → fails; accept invitation → idempotent"
    - "Persistence validated: end-to-end flow → all data durable in landlord and tenant DBs"
    - "Architecture compliance: npm run check:architecture passes; no controller-model shortcuts"
    - "All tests pass; coverage >80% for accounts, businesses, tenancy"
  
  artifacts:
    - "tests/integration/accounts/accountFlows.test.js"
    - "tests/integration/accounts/accountValidation.test.js"
    - "tests/integration/accounts/accountPersistence.test.js"
    - "tests/integration/businesses/businessFlows.test.js"
    - "tests/integration/businesses/businessValidation.test.js"
    - "tests/integration/tenancy/tenantSessionFlows.test.js"
    - "tests/integration/tenancy/tenantSessionValidation.test.js"
    - "tests/e2e/phase4FullFlow.test.js"
  
  key_links:
    - "Architecture Pattern Enforcement: all tests verify routes→controllers→usecases→repositories→models"
    - "API-06 (Testing): comprehensive coverage of success, validation, conflicts, replay, logout, persistence"
    - "D-01 through D-14: all user decisions validated via integration and E2E tests"
    - "Phase 2 Schema: tenant and landlord data verified durable via re-read tests"
---

## Objective

**Wave 5: Comprehensive Integration & E2E Tests** validates all Phase 4 APIs against success criteria, validates architecture compliance, and provides comprehensive test coverage for the entire module.

**Output:** 8 comprehensive test suites covering success flows, validation errors, conflicts, replay rejection, persistence, and E2E user journeys. Architecture compliance verified.

---

## Context

@.planning/04-PLAN.md — Phase 4 master plan and must-haves
@.planning/phases/04-backend-accounts-businesses-and-tenancy-foundation/04-04-PLAN.md — Wave 4 (Tenant Session) complete

---

<tasks>

<task type="auto">
  <name>Task 1: Account Success & Validation Tests — accountFlows.test.js</name>
  <files>tests/integration/accounts/accountFlows.test.js</files>
  <action>
Create accountFlows.test.js with end-to-end account flows:

**Test Suite 1: Registration Flow**
- Test 1: New user registration → HTTP 201 + account created unverified
- Test 2: Re-read account → verify all fields persisted (first_name, last_name, email, phone, status='active', email_verified_at=null)
- Test 3: Login immediately after registration → works (unverified users can log in per D-01)
- Test 4: Concurrent registrations with same email → first succeeds, second fails with 409

**Test Suite 2: Login & Session Flow**
- Test 1: Login with correct credentials → HTTP 200 + sessionToken + empty businesses list (no businesses yet)
- Test 2: Login updates last_login_at timestamp
- Test 3: Re-read account → verify last_login_at changed
- Test 4: Logout is stateless → token still valid on server (verify with mock check)
- Test 5: Multiple concurrent logins → each returns unique session (or same token, depending on backend design)

**Test Suite 3: Profile Update Flow**
- Test 1: Update name and phone → HTTP 200 + updated account
- Test 2: Re-read account → verify name and phone changed
- Test 3: Update email to existing email → HTTP 409 Conflict
- Test 4: Update password, login again → succeeds with new password
- Test 5: Partial update (only name) → only name changes, other fields untouched

**Test Suite 4: Account Lookup Flow**
- Test 1: User looks up own account → HTTP 200 + full details
- Test 2: Admin looks up other account → HTTP 200 + full details
- Test 3: Non-admin looks up other account → HTTP 403 Forbidden
- Test 4: Look up non-existent account → HTTP 404 Not Found

Coverage >80%. All tests use real test database; verify data persists via re-read.
  </action>
  <verify>
    <automated>cd /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api && npm test -- tests/integration/accounts/accountFlows.test.js --coverage 2>&1 | grep "% Statements"</automated>
  </verify>
  <done>
accountFlows.test.js exists; success paths tested; persistence verified; coverage >80%; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Account Validation & Error Tests — accountValidation.test.js</name>
  <files>tests/integration/accounts/accountValidation.test.js</files>
  <action>
Create accountValidation.test.js testing validation errors and edge cases:

**Test Suite 1: Registration Validation**
- Missing email → HTTP 400
- Missing password → HTTP 400
- Invalid email format → HTTP 400
- Password <8 chars → HTTP 400
- Duplicate email → HTTP 409
- Very long name (>255 chars) → HTTP 400 or truncated (depending on validation)

**Test Suite 2: Login Validation**
- Missing email → HTTP 400
- Missing password → HTTP 400
- Wrong password → HTTP 401 Unauthorized
- Non-existent email → HTTP 401 Unauthorized
- Case-sensitivity: login with 'Test@Example.COM' after registering with 'test@example.com' → should work (case-insensitive)

**Test Suite 3: Profile Update Validation**
- Update with invalid email format → HTTP 400
- Update password to <8 chars → HTTP 400
- Update email to duplicate → HTTP 409
- Update non-existent account → HTTP 404
- Update with missing required fields → HTTP 400 (if applicable)

**Test Suite 4: Replay & Idempotency**
- Register user → succeeds
- Register same email again → fails with 409
- Register different email → succeeds (new account)
- Update profile with same data twice → both succeed; second is idempotent
- Login, logout, login again → both succeed; stateless

Coverage >80%.
  </action>
  <verify>
    <automated>cd /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api && npm test -- tests/integration/accounts/accountValidation.test.js --coverage 2>&1 | grep "% Statements"</automated>
  </verify>
  <done>
accountValidation.test.js exists; validation and error scenarios tested; replay tested; coverage >80%; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Account Persistence Tests — accountPersistence.test.js</name>
  <files>tests/integration/accounts/accountPersistence.test.js</files>
  <action>
Create accountPersistence.test.js validating durable persistence:

**Test Suite 1: Registration Persistence**
- Register account
- Close DB connection
- Reconnect
- Query account → data intact (proves transaction committed)

**Test Suite 2: Profile Update Persistence**
- Update account fields
- Force DB sync
- Query account from separate connection → updated values visible

**Test Suite 3: Transaction Rollback**
- Attempt to update email to duplicate (should fail)
- Original account should be unchanged
- Verify via re-read

**Test Suite 4: Concurrent Modifications**
- Two requests modify same account concurrently
- One succeeds, one fails (or both succeed with last-write-wins depending on design)
- Data remains consistent

**Test Suite 5: Password Hash Verification**
- Register account with password
- Query account directly → password_hash is NOT the plaintext password
- Hash is bcrypt-generated (verify format)
- Plaintext never stored

Coverage >80%.
  </action>
  <verify>
    <automated>cd /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api && npm test -- tests/integration/accounts/accountPersistence.test.js --coverage 2>&1 | grep "% Statements"</automated>
  </verify>
  <done>
accountPersistence.test.js exists; transaction isolation tested; concurrency handled; password hash verified; coverage >80%; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Business Success & Validation Tests — businessFlows.test.js & businessValidation.test.js</name>
  <files>
    tests/integration/businesses/businessFlows.test.js
    tests/integration/businesses/businessValidation.test.js
  </files>
  <action>
Create businessFlows.test.js and businessValidation.test.js:

**businessFlows.test.js:**

Test Suite 1: Business Creation
- User creates business → HTTP 201 + business created
- Creator is automatically owner (verify membership record exists with role='owner')
- Re-read business → all fields persisted
- Re-read membership → creator is owner

Test Suite 2: Business Selection & Switching
- User with 1 business logs in → session auto-bound (Wave 2 behavior)
- User with 2+ businesses logs in → unbound session, business list returned
- User activates Business A → context bound to A
- User activates Business B → context switches to B
- User can access data scoped to active business

Test Suite 3: Staff Onboarding (Invitation)
- Owner sends invitation: email, name → HTTP 202
- Email sent (verify via mock)
- Staff receives email with token
- Staff clicks link → POST /invitations/:token/accept → HTTP 200
- Staff account created; assignment exists

Test Suite 4: Staff Onboarding (Direct Add)
- Owner directly adds staff: email, name → HTTP 201
- Staff account created immediately
- No email sent (or notification only sent, no invitation)
- Staff can log in immediately

Test Suite 5: Business Member Access
- Owner views business → can see all members + roles
- Non-member tries to view → HTTP 403
- Member views business → can see business details, own membership

**businessValidation.test.js:**

Test Suite 1: Business Creation Validation
- Duplicate handle → HTTP 409
- Missing legal_name → HTTP 400
- Missing business_handle → HTTP 400
- Invalid handle format (if any validation) → HTTP 400

Test Suite 2: Staff Onboarding Validation
- Duplicate staff email in same business → HTTP 409
- Invalid email format → HTTP 400
- Non-owner trying to onboard staff → HTTP 403

Test Suite 3: Access Control
- Non-owner updates business → HTTP 403
- Non-member views business → HTTP 403
- Owner updates business → HTTP 200

Test Suite 4: Replay & Idempotency
- Create business twice with same handle → first succeeds, second fails 409
- Accept same invitation twice → first succeeds, second fails (invitation already accepted)
- Onboard same staff twice (direct add) → first succeeds, second fails 409

Coverage >80%.
  </action>
  <verify>
    <automated>cd /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api && npm test -- tests/integration/businesses/ --coverage 2>&1 | grep "% Statements"</automated>
  </verify>
  <done>
businessFlows.test.js and businessValidation.test.js exist; creation, staff onboarding, switching tested; validation and access control tested; replay tested; coverage >80%; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: Tenant Session Validation & Flows — tenantSessionFlows.test.js & tenantSessionValidation.test.js</name>
  <files>
    tests/integration/tenancy/tenantSessionFlows.test.js
    tests/integration/tenancy/tenantSessionValidation.test.js
  </files>
  <action>
Create tenantSessionFlows.test.js and tenantSessionValidation.test.js:

**tenantSessionFlows.test.js:**

Test Suite 1: Tenant Session Creation (D-04 Enforcement)
- Owner with membership + no assignment requirement (owner role) creates session → HTTP 200 + context
- Staff with membership + assignment creates session → HTTP 200 + context
- Session includes business_id, tenant_database, dgfy_account_id, active_assignment (if staff)

Test Suite 2: Mid-Session Business Switching
- User logs in (unbound session)
- User activates Business A → context = A
- User activates Business B → context = B (same session token)
- Verify no re-login required

Test Suite 3: Tenant Isolation
- User A activates Business A → accesses Location, StaffAccount data from A
- User B activates Business B → accesses only B's data
- User A queries Business B's data → returns empty or error (no cross-tenant leakage)

Test Suite 4: Staff Assignment Verification
- Create staff in Business A
- Staff tries to access Business B → HTTP 403 (no assignment)
- Assign staff to Business B
- Staff activates Business B → HTTP 200 + context
- Staff can now access B's data

Test Suite 5: Terminal Identity Verification
- Create location and terminal in Business A
- Query terminal in Business B → not found (isolation)
- Query terminal in Business A → found

**tenantSessionValidation.test.js:**

Test Suite 1: D-04 Enforcement (API-04)
- No membership: activate session → HTTP 403 + NO_MEMBERSHIP error
- No assignment (staff): activate session → HTTP 403 + NO_TENANT_ASSIGNMENT error
- Owner (no assignment requirement): activate session → HTTP 200 (bypasses assignment check)
- Both requirements met: activate session → HTTP 200

Test Suite 2: Edge Cases
- Activate non-existent business → HTTP 404
- Activate business without database registry → HTTP 404 (or error)
- Activate business with invalid database → HTTP 500 (DB connection error)
- Unauthenticated activation → HTTP 401

Test Suite 3: Concurrent Session Activations
- User makes 2 concurrent activation requests (Business A and B)
- Last one wins (or both succeed, depending on design)
- Subsequent requests use active context correctly

Test Suite 4: Replay & Idempotency
- Activate Business A twice → both succeed; second is idempotent
- Activate Business A, then Business B, then Business A again → all succeed

Coverage >80%.
  </action>
  <verify>
    <automated>cd /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api && npm test -- tests/integration/tenancy/ --coverage 2>&1 | grep "% Statements"</automated>
  </verify>
  <done>
tenantSessionFlows.test.js and tenantSessionValidation.test.js exist; D-04 enforcement tested; isolation verified; replay tested; coverage >80%; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 6: Full End-to-End Flow Test — phase4FullFlow.test.js</name>
  <files>tests/e2e/phase4FullFlow.test.js</files>
  <action>
Create phase4FullFlow.test.js with complete user journey tests:

**Journey 1: Business Owner Registration to Tenant Session**
1. POST /accounts/register {email, password, name, phone} → HTTP 201 + account
2. POST /accounts/login {email, password} → HTTP 200 + sessionToken + empty businesses list
3. POST /businesses {legal_name, display_name, business_handle} → HTTP 201 + business (owner auto-assigned)
4. POST /accounts/login again → HTTP 200 + session auto-bound to business (single business)
5. GET /businesses → HTTP 200 + [single business]
6. POST /businesses/:id/activate-session → HTTP 200 + tenant context bound

**Journey 2: Staff Onboarding via Email Invitation + Location Management**
1. Owner creates business (from Journey 1)
2. POST /businesses/:id/staff {email: staff@example.com, name: "Alice"} → HTTP 202 (invitation sent)
3. Staff receives email with acceptance token
4. POST /invitations/:token/accept → HTTP 200 + staff_account created
5. Owner creates location: POST /businesses/:id/locations {name, address} → HTTP 201 (first location auto-marked primary)
6. Owner views locations: GET /businesses/:id/locations → HTTP 200 + [location created in step 5]
7. Owner creates second location: POST /businesses/:id/locations {name, address} → HTTP 201
8. Owner sets second location as primary: POST /businesses/:id/locations/:locationId2/set-primary → HTTP 200
9. Verify only one primary: GET /businesses/:id/locations → check only location 2 has is_primary=true
10. Owner assigns staff: PATCH /businesses/:id/staff/:staffId {location_id} → HTTP 200
11. Staff logs in: POST /accounts/login {email: staff@example.com, password: ...} → HTTP 200 (if password was provided)
12. Staff activates business: POST /businesses/:id/activate-session → HTTP 200 + tenant context
13. Staff queries location data → sees assigned location

**Journey 3: Multi-Business Owner Mid-Session Switching**
1. Owner creates Business A and Business B
2. POST /accounts/login → HTTP 200 + unbound session + [Business A, Business B]
3. POST /businesses/A/activate-session → HTTP 200 + context = A
4. Access A's data → succeeds
5. POST /businesses/B/activate-session → HTTP 200 + context = B (same session token)
6. Access B's data → succeeds
7. Access A's data → forbidden (different context)
8. POST /businesses/A/activate-session again → back to A

**Journey 4: Permission Checks & Security**
1. User A creates Business A
2. User B tries to access Business A → 403 (not member)
3. User A adds User B as manager
4. User B accesses Business A → 200 (member)
5. User B tries to update Business A → 403 (manager, not owner)
6. User A updates Business A → 200 (owner)
7. User B logs in; tries to activate Business A with no assignment → 403
8. User A creates location, assigns User B
9. User B activates Business A again → 200 (now has assignment)

**Verification Points in Each Journey:**
- HTTP status codes correct
- Response bodies contain expected fields
- Timestamps (created_at, updated_at, last_login_at) updated correctly
- Database state changes verified via re-read
- No cross-tenant data leakage
- Access control enforced at all steps

All 4 journeys should pass completely (end-to-end proof).

**Test Isolation (Per WARNING #5):**
- Each journey uses isolated test data (separate accounts/businesses/locations per journey)
- Journey 1: account = "owner1@phase4test.com", business = "test-business-1"
- Journey 2: account = "owner2@phase4test.com", business = "test-business-2" (separate from Journey 1)
- Journey 3: account = "owner3@phase4test.com", businesses = "test-business-3a" and "test-business-3b"
- Journey 4: account_a = "user-a@phase4test.com", account_b = "user-b@phase4test.com", business = "test-business-4"
- Use unique UUIDs or timestamps in names/emails to prevent collisions
- Each journey teardown (or per-journey isolation) ensures no interference
  </action>
  <verify>
    <automated>cd /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api && npm test -- tests/e2e/phase4FullFlow.test.js 2>&1 | grep -E "PASS|FAIL|passing|failing"</automated>
  </verify>
  <done>
phase4FullFlow.test.js exists; 4 complete user journeys tested; all business flows end-to-end; location management verified; test isolation enforced; tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 7: Architecture Compliance Verification & Coverage Report</name>
  <files>tests/integration (implicit)</files>
  <action>
Run architecture and coverage verification:

**Step 1: Controller Boundaries Check**
```bash
cd /Users/pat/Projects/Sieitz/dgfy-platform && CONTROLLER_BOUNDARY_TARGETS=$PWD/apps/dgfy-api/src/modules node backend/scripts/check-controller-boundaries.js
```
Expected: PASS (no models imported in controllers)
If FAIL: Review accounts and businesses controllers; remove model imports if any.

**Step 2: Architecture Guardrails Check**
```bash
cd /Users/pat/Projects/Sieitz/dgfy-platform && ARCH_GUARDRAIL_MODULES_ROOT=$PWD/apps/dgfy-api/src/modules node backend/scripts/check-architecture-guardrails.js
```
Expected: PASS (all use cases follow pattern; repositories own data access)
If FAIL: Review violations and fix violations (likely: use case calling repository instead of use case method).

**Step 3: Full Test Coverage**
```bash
cd /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api && npm test -- --coverage tests/integration/accounts tests/integration/businesses tests/integration/tenancy tests/e2e/phase4FullFlow.test.js
```
Expected output should show:
- Accounts module: >80% coverage
- Businesses module: >80% coverage
- Tenancy module: >80% coverage
- Overall: >85% coverage for Phase 4 code

Create a coverage report summary showing lines, statements, branches, functions.

**Step 4: Lint Pass**
```bash
cd /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api && npm run lint
```
Expected: No ESLint violations

**Step 5: No Controller Allowlist Expansion**
```bash
test -f /Users/pat/Projects/Sieitz/dgfy-platform/apps/dgfy-api/src/config/controllerModelImportAllowlist.js && echo "UNEXPECTED: dgfy-api allowlist file exists" || echo "OK: no allowlist file — zero exceptions carved out"
```
Expected: "OK" (apps/dgfy-api has no allowlist file at all, so Step 1 passing is proof of zero violations, not a pre-approved exception)

If any checks fail, identify root cause and fix before proceeding.
  </action>
  <verify>
    <automated>cd /Users/pat/Projects/Sieitz/dgfy-platform && npm run check:architecture:dgfy-api && cd apps/dgfy-api && npm run lint 2>&1 | grep -E "PASS|FAIL|error|success"</automated>
  </verify>
  <done>
Architecture checks pass; controller boundaries enforced; no model imports in controllers; eslint clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Wave 5 Comprehensive Testing: 8 test suites covering account flows, validation, persistence; business flows, validation; tenant session flows, validation; full end-to-end user journeys. Architecture compliance verified (controller boundaries, architecture guardrails, lint). Coverage >80% for all modules.
  </what-built>
  <how-to-verify>
1. Run all tests: `cd apps/dgfy-api && npm test -- tests/integration/accounts tests/integration/businesses tests/integration/tenancy tests/e2e/phase4FullFlow.test.js`
   - Expect: ALL TESTS PASS
2. Check coverage: Verify >80% for accounts, businesses, tenancy
3. Run architecture checks: `npm run check:architecture:dgfy-api`
   - Expect: PASS (controller boundaries + architecture guardrails, both scoped to apps/dgfy-api)
4. Run lint: `cd apps/dgfy-api && npm run lint`
   - Expect: No violations
5. Verify no allowlist exceptions: confirm apps/dgfy-api/src/config/controllerModelImportAllowlist.js does not exist
   - Expect: File absent (Step 1 passing is the only guarantee needed)
6. Spot-check E2E test: Review phase4FullFlow.test.js; verify all 4 journeys complete
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass; describe any failures for fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| End User Request → Phase 4 API | Unverified HTTP input from client; validated by use cases |
| Phase 4 API → Database | Repository ensures data consistency; transactions protect concurrent modifications |
| Tenant Database Access → Landlord Database | Per-tenant connection via TenantConnector; isolated access (no cross-tenant queries) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Information Disclosure | Cross-Tenant Data Leakage | high | mitigate | Integration tests verify tenant isolation; tenantContextResolver enforces business_id validation; separate DB connections per tenant |
| T-04-11 | Denial of Service | Concurrent Registration Spam | medium | accept | Rate limiting deferred to infrastructure layer; no per-request limit in Wave 5 |
| T-04-12 | Tampering | Transaction Rollback | medium | mitigate | Tests verify transaction isolation; concurrent modifications handled correctly (e2e Journey 4 validates permission checks) |
| T-04-13 | Elevation of Privilege | Role-Based Access Control | high | mitigate | Tests verify owner/manager/staff roles enforced; non-owner cannot update business; staff without assignment cannot activate tenant (Journey 4 validates) |
| T-04-SC | Supply Chain | npm/pip installs | high | mitigate | No new dependencies in Wave 5; existing packages vetted; slopcheck passed in Phase 1-3 |

</threat_model>

<verification>
**Wave 5 Completion Gate (Phase 4 Final Gate):**

- [ ] All integration tests pass (accounts, businesses, tenancy): `cd apps/dgfy-api && npm test -- tests/integration/`
- [ ] All E2E tests pass: `cd apps/dgfy-api && npm test -- tests/e2e/phase4FullFlow.test.js`
- [ ] Coverage report: >80% for accounts, businesses, tenancy modules
- [ ] Architecture check passes: `npm run check:architecture:dgfy-api` (guardrails + controller boundaries, scoped to apps/dgfy-api)
- [ ] Lint passes: `cd apps/dgfy-api && npm run lint`
- [ ] apps/dgfy-api has no controllerModelImportAllowlist.js (no exceptions carved out)
- [ ] E2E Journey 1 (Business Owner): Registration → Business Creation → Tenant Session ✓
- [ ] E2E Journey 2 (Staff Invitation): Email Onboarding → Accept → Location Management → Assignment → Tenant Access ✓
  - [ ] Location creation endpoints tested (POST /businesses/:id/locations)
  - [ ] Location listing verified (GET /businesses/:id/locations)
  - [ ] Primary location auto-assignment verified (first location auto-primary)
  - [ ] Set primary endpoint tested (POST /businesses/:id/locations/:id/set-primary)
- [ ] E2E Journey 3 (Multi-Business Switching): Mid-session activation, context isolation ✓
- [ ] E2E Journey 4 (Security): Permission checks, cross-tenant prevention ✓
- [ ] Persistence verified: All data durable in landlord and tenant databases
- [ ] Replay rejection verified: Duplicate registration fails; accept invitation idempotent
- [ ] Tenant isolation verified: No cross-tenant data leakage

**Phase 4 Success Criteria (All Waves):**

✓ API-01: Accounts APIs (registration, login, profile, lookup)
✓ API-02: Businesses APIs (creation, selection, staff onboarding, location/branch management per D-12)
✓ API-03: Tenancy APIs (registry lookup, context selection)
✓ API-04: Tenant Security (membership + assignment validation)
✓ API-05: Architecture Pattern (routes→controllers→usecases→repositories→models)
✓ API-06: Comprehensive Testing (success, validation, conflicts, replay, persistence, location endpoints)
✓ All requirements (API-01 through API-06) covered
✓ All decisions (D-01 through D-14) validated via tests, including D-12 (location endpoints)
✓ All models exist and match Phase 2 schema contracts (including Location model)
✓ All endpoints functional and tested (including location CRUD)
✓ Architecture guardrails enforced
✓ Coverage >80%, including location module

**Phase 4 is COMPLETE.**

Ready for Phase 5 planning (Compatibility and Backend-First Cutover Seam).
</verification>

<success_criteria>
**Wave 5 Success = all of the following:**

1. All integration test suites pass (accounts flows, validation, persistence; business flows, validation; tenancy flows, validation)
2. All E2E test journeys pass (4 complete user stories)
3. Coverage >80% for accounts, businesses, tenancy modules
4. Architecture check passes (controller boundaries, architecture guardrails)
5. Lint passes; no violations
6. No controller-model shortcuts; pattern fully enforced
7. Tenant isolation tested and verified; no cross-tenant data leakage
8. Persistence tested; all data durable in both landlord and tenant databases
9. Replay rejection tested; duplicate operations fail appropriately
10. All decisions D-01 through D-14 validated via tests
11. All requirements API-01 through API-06 satisfied and verified

**When all criteria met, Phase 4 is complete. Proceed to Phase 5 planning.**
</success_criteria>

<output>
Create `.planning/phases/04-backend-accounts-businesses-and-tenancy-foundation/04-SUMMARY.md` when done (Phase 4 overall summary, not just Wave 5)
</output>

---

*Wave 5 of Phase 4: Backend Accounts, Businesses, and Tenancy Foundation*
*Prerequisite: Wave 4 Complete (04-04-SUMMARY.md exists)*
*Status: Ready for Execution*

---

## Phase 4 Completion Summary

When Wave 5 is complete:
- 6 waves executed sequentially (Wave 3 → 3.5 → 4 → 5)
- All 6 requirements (API-01 through API-06) satisfied
- All 14 decisions (D-01 through D-14) implemented and validated, including D-12 (location/branch management)
- 8+ comprehensive test suites; >80% coverage (including location tests)
- Architecture pattern fully enforced
- Models match Phase 2 schema contracts exactly (including Location model from Wave 3.5)
- APIs ready for Phase 5 (compatibility seams, including location endpoints)

Archive final SUMMARY: `.planning/phases/04-backend-accounts-businesses-and-tenancy-foundation/04-SUMMARY.md`

This will contain:
- Phase 4 completion date
- All 5 wave summaries (link to individual SUMMARY files)
- Test coverage report
- Architecture compliance report
- Known issues / technical debt (if any)
- Ready-for-Phase-5 checklist
