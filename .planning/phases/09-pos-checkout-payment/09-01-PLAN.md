---
phase: 09-pos-checkout-payment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-migration-runner/src/migrations/schema/20260713120000-create-availment-checkout.cjs
  - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js
  - apps/dgfy-api/src/models/Tenant/Availment.js
  - apps/dgfy-api/src/models/Tenant/AvailmentItem.js
  - apps/dgfy-api/src/models/Tenant/Payment.js
  - apps/dgfy-api/src/models/Tenant/Receipt.js
  - apps/dgfy-api/src/models/Tenant/ComplianceEvidence.js
  - apps/dgfy-api/src/infra/tenantConnector.js
autonomous: true
requirements: [CHK-01, CHK-04, CHK-05, CHK-06, FSC-03]

must_haves:
  truths:
    - "The migration creates availments, availment_items, payments, receipts, and compliance_evidence tables in a dgfy_business_* schema without touching dgfy_core or legacy items/PosTransactionLine."
    - "receipts and payments are append-only at the DB layer (BEFORE UPDATE / BEFORE DELETE SIGNAL 45000 triggers); availment_items carry a nullable cancelled_at soft-delete column."
    - "tenantConnector.getModels(db) resolves Availment, AvailmentItem, Payment, Receipt, and ComplianceEvidence to defined models (never undefined)."
    - "dgfyBusinessContract.tables lists all five new tables so migration-runner verify passes after apply."
  artifacts:
    - "apps/dgfy-migration-runner/src/migrations/schema/20260713120000-create-availment-checkout.cjs"
    - "apps/dgfy-api/src/models/Tenant/{Availment,AvailmentItem,Payment,Receipt,ComplianceEvidence}.js"
    - "dgfyBusinessContract.tables entries for availments/availment_items/payments/receipts/compliance_evidence"
  key_links:
    - "tenantConnector.getModels() modelDefiners map -> each new Tenant model (Pitfall 2: unregistered model resolves to undefined)."
    - "migration table/column shapes <-> Tenant model attribute shapes <-> dgfyBusinessContract table contracts (all three must agree)."
---

<objective>
Create the Phase 9 schema foundation: one idempotent business-target umzug migration adding five tenant tables (availments, availment_items, payments, receipts, compliance_evidence), their append-only triggers and generated-column uniqueness, the matching five Sequelize Tenant models, their registration in tenantConnector, and the schema-contract entries so migration-runner verify passes.

Purpose: Every downstream availment/payment/receipt/evidence read and write is unreachable until these tables exist, the models are registered (Pitfall 2), and the contract lists them. This plan lands all three in lockstep. HARD CONSTRAINT: zero writes under backend/; all code under apps/dgfy-api and apps/dgfy-migration-runner. No table foreign-keys into legacy items/PosTransactionLine.

Output: The migration file, five model files, tenantConnector registration edits, and dgfyBusinessContract table contracts. The live apply of this migration against real MySQL is deliberately deferred to the [BLOCKING] plan 09-08.

Artifacts this plan produces:
- Migration: 20260713120000-create-availment-checkout.cjs (meta.targetKind 'business')
- Tables: availments, availment_items, payments, receipts, compliance_evidence
- Models: Availment, AvailmentItem, Payment, Receipt, ComplianceEvidence
- Contract entries: dgfyBusinessContract.tables.{availments,availment_items,payments,receipts,compliance_evidence}
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pos-checkout-payment/09-CONTEXT.md
@.planning/phases/09-pos-checkout-payment/09-RESEARCH.md
@.planning/phases/09-pos-checkout-payment/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the availment-checkout tenant migration</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260713120000-create-availment-checkout.cjs</files>
  <read_first>
    - apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs (analog — copy meta/helper/createTable/append-only-trigger/generated-column/down shape VERBATIM; lines 56-140 helpers + createTable, 288-360 generated-column + RESTRICT FK, 456-536 triggers + down)
    - apps/dgfy-api/src/models/Tenant/ComplianceModeState.js (branch_scope_key generated-column + RESTRICT branch_id precedent)
    - 09-PATTERNS.md section "2026XXXX-create-availment-checkout.cjs (migration)"
  </read_first>
  <action>
    Author a new migration exporting module.exports with meta { destructive:false, targetKind:'business', rollbackDescription, estimatedRisk:'low' }, an async up(queryInterface, Sequelize), and an async down(queryInterface). Copy the inline idempotent helpers tableExists, hasIndex, addIndexIfMissing, timestampColumns verbatim from the commerce-foundation analog. Guard every createTable with if (!await tableExists(name)) and every index with addIndexIfMissing.

    Create tables (all same-DB integer PKs; cross-DB refs are CHAR(36) with NO references; same-DB FKs are INTEGER with references):
    - availments: id INT PK autoincrement; business_id CHAR(36) not null; branch_id INT null references locations(id) onDelete SET NULL onUpdate CASCADE; customer_account_id CHAR(36) null; shift_id INT null references shifts(id) onDelete RESTRICT onUpdate RESTRICT; terminal_id INT null references terminal_identities(id) onDelete RESTRICT onUpdate RESTRICT; cashier_account_id INT null references staff_accounts(id) onDelete RESTRICT onUpdate RESTRICT; cashier_dgfy_account_id CHAR(36) null; status ENUM('draft','finalized','voided') not null default 'draft'; document_context ENUM('fiscal','non_fiscal') null; subtotal_amount/discount_amount/vat_amount/vat_exempt_amount/total_amount all DECIMAL(14,4) null; sc_pwd_id_number STRING(64) null; sc_pwd_metadata JSON null; finalized_at DATE null; plus timestampColumns(). Index on (business_id, status).
    - availment_items: id INT PK; business_id CHAR(36) not null; availment_id INT not null references availments(id) onDelete CASCADE onUpdate CASCADE; product_id INT not null references products(id) onDelete RESTRICT onUpdate CASCADE; product_name STRING(255) not null; quantity DECIMAL(24,12) not null; unit_price DECIMAL(14,4) not null; stock_effect_type ENUM('inventory_issue','stock_exempt') not null (D-03: per-line toggle, default derived from Product inventory_mode); tax_treatment ENUM('vatable','vat_exempt','zero_rated') not null default 'vatable'; tax_rate DECIMAL(5,4) not null default 0.1200 (D-13: per-item hierarchical structure, uniform 12% VAT in Phase 9); cancelled_at DATE null; plus timestampColumns(). Index on (business_id, availment_id).
    - payments: id INT PK; business_id CHAR(36) not null; availment_id INT not null references availments(id) onDelete CASCADE onUpdate CASCADE; payment_method ENUM('cash','gcash','credit_card') not null (D-08 single method per availment; Payment is a separate entity so split-tender can be added later without schema change); amount_received DECIMAL(14,4) not null; change_due DECIMAL(14,4) null; payment_handoff_mode STRING(32) null (D-10: records method + amount, never a live gateway charge). created_at only (no updated_at — append-only). Index on (business_id, availment_id).
    - receipts: id INT PK; business_id CHAR(36) not null; availment_id INT not null references availments(id) onDelete RESTRICT onUpdate CASCADE; receipt_number STRING(64) not null; document_type ENUM('fiscal_invoice','non_fiscal_slip') not null; compliance_mode STRING(32) null (D-14: Fiscal/Non-Fiscal on the receipt); payload JSON not null (D-12/D-14: stores all line items, discounts, tax breakdown, payment, change, staff id, receipt id); printed BOOLEAN not null default false; print_error STRING(255) null; created_at only (no updated_at — append-only immutable receipt record, D-12). Unique index on (business_id, receipt_number).
    - compliance_evidence: id INT PK; business_id CHAR(36) not null; branch_id INT null references locations(id) onDelete RESTRICT onUpdate RESTRICT (base column of a generated column — RESTRICT is mandatory, MySQL 8.0 forbids CASCADE/SET NULL on a STORED-generated base column); evidence_bundle JSON not null; attested_by_actor_type STRING(64) null; attested_at DATE null; plus timestampColumns(). After createTable, ALTER TABLE to add a STORED generated column branch_scope_key AS (COALESCE(branch_id, 0)) guarded by describeTable (mirror the shifts active_terminal_cashier_key / ComplianceModeState branch_scope_key idiom), then addIndexIfMissing unique on (business_id, branch_scope_key) named unique_compliance_evidence_business_branch_scope.

    Append-only enforcement: extend the appendOnlyTables loop to ['receipts','payments'] — DROP TRIGGER IF EXISTS then CREATE TRIGGER BEFORE UPDATE and BEFORE DELETE with SIGNAL SQLSTATE '45000' for each. down() drops the triggers, drops the branch_scope_key column + its index, drops tables in reverse dependency order (receipts, payments, availment_items, availments, compliance_evidence), and DROP TYPE IF EXISTS for each MySQL enum created (enum_availments_status, enum_availments_document_context, enum_availment_items_stock_effect_type, enum_availment_items_tax_treatment, enum_payments_payment_method, enum_receipts_document_type).

    Document in the header comment that this is an additive tenant migration and that every backend/ reference is read-only pattern porting.
  </action>
  <verify>
    <automated>node --check apps/dgfy-migration-runner/src/migrations/schema/20260713120000-create-availment-checkout.cjs && grep -c "createTable('availments'\|createTable('availment_items'\|createTable('payments'\|createTable('receipts'\|createTable('compliance_evidence'" apps/dgfy-migration-runner/src/migrations/schema/20260713120000-create-availment-checkout.cjs</automated>
  </verify>
  <acceptance_criteria>
    - node --check parses the migration with no syntax error.
    - grep confirms all five createTable calls are present (count 5).
    - grep confirms both BEFORE UPDATE and BEFORE DELETE triggers reference receipts and payments in the appendOnlyTables loop.
    - grep confirms branch_scope_key generated-column ALTER and the unique_compliance_evidence_business_branch_scope index exist.
  </acceptance_criteria>
  <done>The migration creates all five tenant tables idempotently with append-only triggers on receipts/payments, generated-column uniqueness on compliance_evidence, RESTRICT FKs on generated-column base columns, no cross-DB FKs, and a reverse-order down().</done>
</task>

<task type="auto">
  <name>Task 2: Register the five new tables in dgfyBusinessContract</name>
  <files>apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js (lines 31-478 — the tables map + existing product_folders/inventory_movements/shifts/compliance_mode_state contract entries to mirror; foreignKeys/indexes/uniqueConstraints shape)
    - apps/dgfy-migration-runner/src/commands/verify.js (lines 102-135 checkContractSchema — iterates contract.tables, so every new table MUST be listed here or verify under-reports coverage)
  </read_first>
  <action>
    Add one entry per new table to dgfyBusinessContract.tables, matching Task 1's migration exactly: column lists, the same-DB foreignKeys array ({ column, referencesTable, referencesColumn }) for availments (branch_id->locations, shift_id->shifts, terminal_id->terminal_identities, cashier_account_id->staff_accounts), availment_items (availment_id->availments, product_id->products), payments (availment_id->availments), receipts (availment_id->availments), compliance_evidence (branch_id->locations), and the indexes/uniqueConstraints entries (unique (business_id,receipt_number) for receipts, unique_compliance_evidence_business_branch_scope for compliance_evidence, plus the non-unique lookup indexes). Do NOT add anything to rejectedTables — these are legitimate new tenant tables. Follow the existing entries' exact key shape so checkTableAgainstContract can consume them.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner && node -e "const {dgfyBusinessContract}=require('./src/schemaContracts/dgfyBusinessContract.js'); const t=dgfyBusinessContract.tables; ['availments','availment_items','payments','receipts','compliance_evidence'].forEach(n=>{if(!t[n])throw new Error('missing '+n)}); console.log('ok')" 2>/dev/null || node --experimental-vm-modules -e "import('./apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js').then(m=>{const t=m.dgfyBusinessContract.tables;['availments','availment_items','payments','receipts','compliance_evidence'].forEach(n=>{if(!t[n])throw new Error('missing '+n)});console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - Loading dgfyBusinessContract resolves table entries for all five new tables.
    - Each entry's foreignKeys reference only same-DB tables (never legacy items/PosTransactionLine, never a cross-DB CHAR(36) column).
  </acceptance_criteria>
  <done>dgfyBusinessContract.tables lists availments, availment_items, payments, receipts, compliance_evidence with columns/FKs/indexes matching the migration, so migration-runner verify checks them after apply.</done>
</task>

<task type="auto">
  <name>Task 3: Create the five Tenant models and register them in tenantConnector</name>
  <files>apps/dgfy-api/src/models/Tenant/Availment.js, apps/dgfy-api/src/models/Tenant/AvailmentItem.js, apps/dgfy-api/src/models/Tenant/Payment.js, apps/dgfy-api/src/models/Tenant/Receipt.js, apps/dgfy-api/src/models/Tenant/ComplianceEvidence.js, apps/dgfy-api/src/infra/tenantConnector.js</files>
  <read_first>
    - apps/dgfy-api/src/models/Tenant/Shift.js (money/FK/CHAR(36)/RESTRICT-on-generated-base-column model pattern)
    - apps/dgfy-api/src/models/Tenant/InventoryMovement.js (append-only pattern: updatedAt:false + beforeUpdate/beforeBulkUpdate throw — apply to Receipt and Payment)
    - apps/dgfy-api/src/models/Tenant/ComplianceModeState.js (JSON column + generated-column-not-declared pattern — apply to ComplianceEvidence)
    - apps/dgfy-api/src/infra/tenantConnector.js (lines 1-14 imports, 123-165 getModels modelDefiners map + associate loop + @returns JSDoc line 121)
  </read_first>
  <action>
    Create five persistence-only Sequelize model factories (export default (sequelize) => { class X extends Model { static associate(models){...} } X.init({...}, { sequelize, modelName, tableName, underscored:true, timestamps:true, createdAt:'created_at', updatedAt:... }); return X; }), each attribute matching Task 1's migration columns EXACTLY:
    - Availment.js (tableName 'availments'): all columns; business_id/customer_account_id/cashier_dgfy_account_id as CHAR(36) no references; branch_id/shift_id/terminal_id/cashier_account_id as INTEGER with references matching the migration onDelete/onUpdate; status/document_context ENUMs; the five DECIMAL(14,4) amounts; sc_pwd_metadata JSON; timestamps true. associate(): belongsTo Location (branch), Shift, TerminalIdentity, StaffAccount, hasMany AvailmentItem/Payment, hasOne Receipt — each guarded with !X.associations?.foo.
    - AvailmentItem.js (tableName 'availment_items'): timestamps true (NOT append-only — updatable qty/stock_effect_type/cancelled_at per D-01/D-03); cancelled_at DATE nullable; stock_effect_type/tax_treatment ENUMs; tax_rate DECIMAL(5,4); product_name STRING(255) snapshot; quantity DECIMAL(24,12); unit_price DECIMAL(14,4); availment_id INTEGER references availments, product_id INTEGER references products. Domain helper isCancelledLine() => this.cancelled_at != null.
    - Payment.js (tableName 'payments'): append-only — updatedAt:false + throwing beforeUpdate/beforeBulkUpdate hooks; payment_method ENUM; amount_received/change_due DECIMAL(14,4); payment_handoff_mode STRING(32).
    - Receipt.js (tableName 'receipts'): append-only — updatedAt:false + throwing beforeUpdate/beforeBulkUpdate hooks; receipt_number STRING(64); document_type ENUM; payload JSON; printed BOOLEAN; print_error STRING(255); unique index (business_id, receipt_number) named unique_receipts_business_number.
    - ComplianceEvidence.js (tableName 'compliance_evidence'): business_id CHAR(36); branch_id INTEGER nullable references locations RESTRICT/RESTRICT (base of generated column); evidence_bundle JSON; attested_by_actor_type STRING(64); attested_at DATE; timestamps true; branch_scope_key intentionally NOT declared (DB-generated); unique index on (business_id, branch_scope_key) named unique_compliance_evidence_business_branch_scope. associate(): belongsTo Location (branch) guarded.

    In tenantConnector.js: add the five import defineXModel lines at the top (mirror lines 7-14), add the five entries to the modelDefiners object inside getModels() alongside ComplianceModeState (Availment, AvailmentItem, Payment, Receipt, ComplianceEvidence), and extend the getModels @returns JSDoc list. The existing Object.entries loop auto-defines and wires associate() — no other change needed.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/models/Tenant/Availment.js && node --experimental-vm-modules --check src/models/Tenant/AvailmentItem.js && node --experimental-vm-modules --check src/models/Tenant/Payment.js && node --experimental-vm-modules --check src/models/Tenant/Receipt.js && node --experimental-vm-modules --check src/models/Tenant/ComplianceEvidence.js && grep -c "Availment:\|AvailmentItem:\|Payment:\|Receipt:\|ComplianceEvidence:" src/infra/tenantConnector.js</automated>
  </verify>
  <acceptance_criteria>
    - All five model files parse with no syntax error.
    - tenantConnector modelDefiners map contains all five new entries (grep count 5) and the five import lines are present.
    - Receipt and Payment set updatedAt:false and throw on beforeUpdate/beforeBulkUpdate; AvailmentItem keeps timestamps:true with a nullable cancelled_at.
  </acceptance_criteria>
  <done>tenantConnector.getModels(db) exposes Availment, AvailmentItem, Payment, Receipt, ComplianceEvidence as defined models whose attributes match the migration and contract; Receipt/Payment are append-only, AvailmentItem is soft-deletable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration-runner -> tenant DB | DDL applied to dgfy_business_* schema |
| ORM model -> tenant DB rows | model attribute set defines what can be written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Tampering | receipts/payments rows | high | mitigate | DB BEFORE UPDATE/DELETE SIGNAL 45000 triggers + model updatedAt:false + throwing hooks make financial records immutable at both layers. |
| T-09-01-02 | Tampering | cross-database FK forgery | medium | mitigate | business_id and other cross-DB refs are opaque CHAR(36) with NO FK; only same-DB INTEGER FKs are enforced — no FK into legacy items/PosTransactionLine (ADR-0003/0029). |
| T-09-01-03 | Repudiation | compliance_evidence uniqueness for NULL branch | medium | mitigate | STORED generated branch_scope_key = COALESCE(branch_id,0) + unique index prevents duplicate per-business attestation rows (mirrors Phase 8 CR-01 fix). |
| T-09-01-SC | Tampering | npm installs | low | accept | This plan installs no new packages (RESEARCH: zero new runtime deps); no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- node --check / --experimental-vm-modules --check pass for the migration and all five models.
- dgfyBusinessContract loads with the five new table entries.
- tenantConnector modelDefiners registers all five models.
- No file under backend/ is modified (git diff --name-only shows only apps/ paths).
</verification>

<success_criteria>
Migration, contract, and models exist and agree on column/FK/index shape; all five models are registered in tenantConnector; receipts/payments are append-only; compliance_evidence has NULL-safe uniqueness. Live apply is deferred to 09-08.
</success_criteria>

<output>
Create `.planning/phases/09-pos-checkout-payment/09-01-SUMMARY.md` when done.
</output>
