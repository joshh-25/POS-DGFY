---
phase: 09-pos-checkout-payment
plan: 05
type: execute
wave: 3
depends_on: [09-04, 09-02]
files_modified:
  - apps/dgfy-api/src/modules/availments/usecases/availmentUseCases.js
  - apps/dgfy-api/src/modules/availments/controllers/availmentController.js
  - apps/dgfy-api/src/modules/availments/routes.js
  - apps/dgfy-api/src/modules/availments/index.js
  - apps/dgfy-api/tests/unit/modules/availments/availmentUseCases.test.js
autonomous: true
requirements: [CHK-01, CHK-03]

must_haves:
  truths:
    - "Staff can create an availment and add/update/remove/restore lines; every usecase returns an ApplicationResult and enforces active-member access via guardBusinessAccess."
    - "A manual discount is permission-gated and recorded with the applying staff account id and a reason; a discount without a reason or from an unauthorized member is rejected (CHK-03)."
    - "Controllers are transport-only (no model imports) and never read a client-supplied total, change, or discount-amount; server recomputes amounts."
    - "The finalize controller/route exists and delegates to the injected finalizeAvailment usecase (implemented in 09-06)."
  artifacts:
    - "availmentUseCases.js: createAvailment, addLine, updateLine, removeLine, restoreLine, applyDiscount"
    - "availmentController.js, routes.js, buildAvailmentsModule useCases wiring"
    - "availmentUseCases.test.js (CRUD + soft-delete + discount permission)"
  key_links:
    - "applyDiscount permission gate <-> requireMembership extension (CHK-03)."
    - "routes -> controller -> usecases -> availmentRepository (09-04)."
---

<objective>
Implement the availment line-editing usecases, the transport-only controller, the routes, and the buildAvailmentsModule useCases wiring, plus a CRUD/discount unit test. This delivers CHK-01 (add/adjust/remove/restore lines before finalize) and CHK-03 (permission-gated manual discount recorded with staff id + reason). The finalize route/controller are wired here but the finalize usecase body lands in 09-06.

Purpose: Give staff the full pre-finalize editing surface with server-side authority and access control, cloning the modules/inventory usecase/controller/routes patterns.

Output: availmentUseCases.js (CRUD + discount), availmentController.js, routes.js, index.js useCases wiring, and availmentUseCases.test.js.

Artifacts this plan produces:
- Usecases: buildCreateAvailmentUseCase, buildAddLineUseCase, buildUpdateLineUseCase, buildRemoveLineUseCase, buildRestoreLineUseCase, buildApplyDiscountUseCase
- Controller: buildAvailmentController (create/addLine/updateLine/removeLine/restoreLine/applyDiscount/finalize/getById)
- Routes: createAvailmentRoutes
- Endpoints: POST /v1/availments, GET /:id, POST /:id/lines, PATCH /:id/lines/:lineId, DELETE /:id/lines/:lineId, POST /:id/lines/:lineId/restore, POST /:id/discounts, POST /:id/finalize
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-pos-checkout-payment/09-CONTEXT.md
@.planning/phases/09-pos-checkout-payment/09-RESEARCH.md
@.planning/phases/09-pos-checkout-payment/09-PATTERNS.md
@.planning/phases/09-pos-checkout-payment/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Availment line-editing and discount usecases</name>
  <files>apps/dgfy-api/src/modules/availments/usecases/availmentUseCases.js, apps/dgfy-api/tests/unit/modules/availments/availmentUseCases.test.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/inventory/usecases/inventoryMovementUseCases.js (error factories lines 16-81, requireMembership/guardBusinessAccess lines 96-119, ApplicationResult return convention lines 128-177 — copy verbatim and extend)
    - apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js (09-04 — method signatures + error class names to duck-type)
    - apps/dgfy-api/src/modules/products/repositories/productRepository.js (findById(businessId,id) — used by addLine to snapshot product_name/unit_price/inventory_mode for the stock_effect_type default)
    - 09-PATTERNS.md section "availmentUseCases.js"
  </read_first>
  <behavior>
    - createAvailment: active member required; returns a draft availment; non-member -> 403.
    - addLine: reads the product via productRepository.findById to snapshot product_name + unit_price + derive default stock_effect_type (basic_inventory->inventory_issue, non_stock->stock_exempt, D-03); staff may override stock_effect_type; missing product -> 404; finalized availment -> 409.
    - updateLine: adjusts quantity and/or stock_effect_type on an active line; unknown line -> 404.
    - removeLine (the usecase/route verb) soft-deletes by calling repository.cancelLine (WARNING-3: the repository soft-delete method is named cancelLine; the usecase/route verb is removeLine — one method, mapped); restoreLine clears cancelled_at; both idempotent-safe.
    - applyDiscount: all three discount types stack on one availment (D-04: promo code(s) + one manual + one SC/PWD), each persisted as its own availment_discounts row via repository.recordDiscount (the concrete 09-01 shape: discount_type, code, amount/percent, applied_by_staff_account_id, reason, sc_pwd_id_number/sc_pwd_customer_name) — never a JSON blob. Manual discount (discount_type 'manual') requires the applying staff account id AND a non-empty reason AND a manual-discount permission; missing reason -> 400; unauthorized member -> 403; the recorded row carries applied_by_staff_account_id + reason (CHK-03). Promo (discount_type 'promo_code') records the free-text code (stored-not-validated). SC/PWD (discount_type 'sc_pwd') stores the provided id number (+ optional name) without format validation (D-06); the statutory 20%-off-VAT-exclusive computation is server-side only (D-07). Discount peso amounts are computed server-side by money.js at finalize — applyDiscount records the discount rows' intent/metadata (type + percent and/or provided amount + audit fields), not a client-computed applied total.
  </behavior>
  <action>
    Copy the error factories (validationError 400, notFoundError 404, forbiddenError 403, conflictError 409, noTenantDatabaseError, mapTenantDatabaseError) and the requireMembership/guardBusinessAccess helpers from inventoryMovementUseCases.js. Extend requireMembership with an additional manual-discount permission check for applyDiscount (CHK-03) — reject if the member lacks the permission. Implement the six builders returning ApplicationResult, duck-typing repository errors on error.name (AvailmentNotFoundError->404, AvailmentFinalizedError->409, AvailmentLineNotFoundError->404, TenantDatabaseUnavailableError->mapTenantDatabaseError). buildRemoveLineUseCase calls repository.cancelLine (the soft-delete method — WARNING-3 mapping). buildApplyDiscountUseCase calls repository.recordDiscount with the concrete availment_discounts fields (discount_type + code/amount/percent + applied_by_staff_account_id + reason + sc_pwd_id_number/sc_pwd_customer_name), one row per applied discount. Never trust a client-supplied line total or discount amount — the usecase records quantities/unit-price snapshots and discount-row metadata only; monetary computation is money.js at finalize.

    Write availmentUseCases.test.js with a mocked repository + mocked productRepository + mocked businessRepository: cover create (member vs non-member), addLine (product snapshot + default stock_effect_type + finalized rejection), updateLine, removeLine/restoreLine (cancelled_at toggling via repository.cancelLine), and applyDiscount (reason required, permission gate, recordDiscount called with a 'manual' row carrying applied_by_staff_account_id + reason).
  </action>
  <verify>
    <automated>cd apps/dgfy-api && npm test -- tests/unit/modules/availments/availmentUseCases.test.js</automated>
  </verify>
  <acceptance_criteria>
    - availmentUseCases.test.js passes all cases including the applyDiscount reason-required + permission-gate + staff-id-recorded assertions.
    - Every usecase returns an ApplicationResult (never throws for expected failures).
  </acceptance_criteria>
  <done>The six line-editing/discount usecases exist, are access-controlled, record manual discounts with staff id + reason, and are unit-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Controller, routes, and module useCases wiring</name>
  <files>apps/dgfy-api/src/modules/availments/controllers/availmentController.js, apps/dgfy-api/src/modules/availments/routes.js, apps/dgfy-api/src/modules/availments/index.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/inventory/controllers/inventoryMovementController.js (transport-only shape; sendUseCaseResult usage)
    - apps/dgfy-api/src/modules/inventory/routes.js (createInventoryRoutes: authenticateAccount + .catch(next))
    - apps/dgfy-api/src/modules/products/routes.js (specific paths before /:id ordering)
    - apps/dgfy-api/src/modules/availments/index.js (09-04 shell — fill the useCases map)
    - 09-PATTERNS.md sections "availmentController.js" and "routes.js"
  </read_first>
  <action>
    Create buildAvailmentController(useCases) with transport-only methods create, getById, addLine, updateLine, removeLine, restoreLine, applyDiscount, finalize — each reads from req.body/req.params/req.query and req.account.id, calls the matching usecase, and returns sendUseCaseResult(res, result, code). The finalize method reads only business_id, payment_method, cash_received, requested_document_context, terminal_id, branch_id from the body and the availment id from params — it must not read any client-provided order total, change, or discount amount. No model/repository imports (eslint no-restricted-imports on **/models).

    Create createAvailmentRoutes(useCases, { authenticateAccount }) that throws if authenticateAccount is not a function, builds the controller, and registers each route with authenticateAccount + .catch(next), specific paths before /:id: POST /, GET /:id, POST /:id/lines, PATCH /:id/lines/:lineId, DELETE /:id/lines/:lineId, POST /:id/lines/:lineId/restore, POST /:id/discounts, POST /:id/finalize.

    In index.js fill buildAvailmentsModule's useCases map with the six builders from Task 1 (createAvailment, addLine, updateLine, removeLine, restoreLine, applyDiscount), each closed over repository/businessRepository/productRepository as needed. Leave finalizeAvailment to be added in 09-06 (a placeholder key wired to the 09-06 builder import). Export createAvailmentRoutes and buildAvailmentController.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/modules/availments/controllers/availmentController.js && node --experimental-vm-modules --check src/modules/availments/routes.js && node --experimental-vm-modules --check src/modules/availments/index.js && grep -c "authenticateAccount" src/modules/availments/routes.js</automated>
  </verify>
  <acceptance_criteria>
    - All three files parse.
    - Every route registers authenticateAccount (grep count matches route count) and uses .catch(next).
    - The finalize controller reads only the whitelisted request fields (no client total/change/discount amount).
  </acceptance_criteria>
  <done>The availments transport surface exists behind auth, wired to the CRUD/discount usecases; finalize route is present for 09-06 to complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> availment API | untrusted line/discount/finalize input |
| member -> manual discount | privilege to reduce a total |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05-01 | Tampering | client-supplied total/change/discount | high | mitigate | controllers read only whitelisted fields; discount amounts and totals are computed server-side (money.js at finalize), never accepted from the client. |
| T-09-05-02 | Elevation of Privilege | unauthorized manual discount | high | mitigate | applyDiscount is gated by a manual-discount permission check and requires a recorded staff id + reason (CHK-03). |
| T-09-05-03 | Repudiation | untraceable discount | medium | mitigate | every manual discount persists applied_by_staff_account_id + reason. |
| T-09-05-04 | Info Disclosure | cross-tenant availment access | high | mitigate | guardBusinessAccess (active membership) + business_id-scoped repository resolution on every usecase. |
| T-09-05-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

<verification>
- availmentUseCases.test.js passes.
- Controller/routes/index parse; routes are auth-gated; finalize reads only whitelisted fields.
</verification>

<success_criteria>
CHK-01 line editing and CHK-03 permission-gated manual discount recording are implemented, access-controlled, transport-clean, and unit-tested; the finalize route is wired for 09-06.
</success_criteria>

<output>
Create `.planning/phases/09-pos-checkout-payment/09-05-SUMMARY.md` when done.
</output>
