---
phase: 09-pos-checkout-payment
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/dgfy-api/src/modules/availments/entities/availmentEntity.js
  - apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js
  - apps/dgfy-api/src/modules/availments/index.js
autonomous: true
requirements: [CHK-01, CHK-03]

must_haves:
  truths:
    - "availmentRepository resolves the tenant DB via the active+verified registry scaffold (copied from inventoryMovementRepository) and never a direct model-factory import."
    - "Line editing is append-friendly: the removeLine verb sets cancelled_at (repository method cancelLine), restoreLine clears it; no hard delete exists for lines, payments, or receipts."
    - "recordDiscount writes one concrete availment_discounts row per applied discount (discount_type promo_code | manual | sc_pwd) with amount/percent, applied_by_staff_account_id, and reason — the SC/PWD path also writes a discount_type 'sc_pwd' row so every discount renders uniformly on the receipt (CHK-03 / D-14)."
    - "finalizePersist writes availments (status finalized) + payments + receipts in ONE sequelize.transaction, running the injected recordSaleEffect for each inventory_issue line inside that same transaction, and THROWS on any non-success sale-effect ApplicationResult so an insufficient-stock (or other) failure rolls the whole sale back; it row-locks the availment to reject double-finalize."
  artifacts:
    - "apps/dgfy-api/src/modules/availments/entities/availmentEntity.js"
    - "apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js"
    - "apps/dgfy-api/src/modules/availments/index.js (buildAvailmentsModule shell + exports)"
  key_links:
    - "availmentRepository.finalizePersist <-> injected recordSaleEffect (transaction-threaded, throw-on-failure) — Pitfall 7 all-or-nothing."
    - "recordDiscount <-> availment_discounts rows (09-01) — CHK-03 staff id + reason, D-14 per-discount receipt lines."
    - "availment_items.cancelled_at <-> removeLine verb / cancelLine repository method / restoreLine (D-02/D-18 soft delete)."
---

<objective>
Build the availments module's persistence layer: the availment entity and the tenant-scoped repository (scaffold, line CRUD, soft-delete/restore, and the atomic finalize-persist write path), plus the module index shell exporting buildAvailmentsModule. This is a verbatim clone of the modules/inventory + modules/shifts repository scaffolds adapted to the availment tables from 09-01.

Purpose: The repository owns all Sequelize access and the one-transaction finalize write (availment + payments + receipts + per-line sale effects), so the usecase layer (09-05/09-06) stays pure orchestration. Row-locking the availment on finalize prevents double-finalize/replay.

Output: availmentEntity.js, availmentRepository.js, and index.js (module shell). Usecases/controllers/routes are 09-05; finalize orchestration is 09-06.

Artifacts this plan produces:
- createAvailmentEntity / AvailmentEntity (isFinalized, isCancelledLine helpers)
- AvailmentRepository with: createAvailment, findById, addLine, updateLine, cancelLine (the soft-delete method the removeLine usecase/route verb maps to — see WARNING-3 reconciliation), restoreLine, recordDiscount (writes availment_discounts rows), finalizePersist; error classes AvailmentNotFoundError, AvailmentFinalizedError, AvailmentLineNotFoundError, NoOpenShiftError (exported for duck-typing)
- buildAvailmentsModule shell (index.js)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-pos-checkout-payment/09-CONTEXT.md
@.planning/phases/09-pos-checkout-payment/09-RESEARCH.md
@.planning/phases/09-pos-checkout-payment/09-PATTERNS.md
@.planning/phases/09-pos-checkout-payment/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Availment entity</name>
  <files>apps/dgfy-api/src/modules/availments/entities/availmentEntity.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/inventory/entities/inventoryMovementEntity.js (whole file — constructor + toPlain + factory shape to mirror)
    - 09-PATTERNS.md section "entities/availmentEntity.js"
  </read_first>
  <action>
    Create a plain-constructor entity with defaulted fields mirroring inventoryMovementEntity: an AvailmentEntity class carrying availment header fields plus its items array, domain helpers isFinalized() (status === 'finalized') and isCancelledLine(item) (item.cancelled_at != null), a toPlain() returning the stable public field set, and a createAvailmentEntity(attrs) factory. No Sequelize or HTTP concern.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/modules/availments/entities/availmentEntity.js</automated>
  </verify>
  <acceptance_criteria>
    - Parses; exports createAvailmentEntity and AvailmentEntity with isFinalized()/isCancelledLine()/toPlain().
  </acceptance_criteria>
  <done>availmentEntity mirrors the inventory entity role with finalize/soft-delete domain helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Availment repository — scaffold, line CRUD, soft-delete/restore, discount persistence</name>
  <files>apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/inventory/repositories/inventoryMovementRepository.js (lines 27-148 scaffold: TenantDatabaseUnavailableError, error subclasses, constructor, resolveDatabaseName active+verified, resolveModel, withModel)
    - apps/dgfy-api/src/modules/shifts/repositories/shiftRepository.js (lines 164-238 multi-table transaction; 264-271 row-lock guard)
    - 09-PATTERNS.md section "availmentRepository.js"
    - apps/dgfy-api/src/models/Tenant/Availment.js (09-01 — attribute names) and AvailmentItem.js / AvailmentDiscount.js / Payment.js / Receipt.js
  </read_first>
  <action>
    Copy the tenant-DB scaffold verbatim: TenantDatabaseUnavailableError (reason enum), domain error subclasses AvailmentNotFoundError / AvailmentFinalizedError / AvailmentLineNotFoundError / NoOpenShiftError (each sets this.name for duck-typing), constructor requiring tenantConnector and optional businessDatabaseRegistryRepository (fail-closed at op time), resolveDatabaseName requiring registry status 'active' AND verified_at, resolveModel returning getModels(databaseName).Availment, and withModel normalizing unknown errors to TenantDatabaseUnavailableError('unreachable') without double-wrapping the known domain errors.

    Implement write/read methods (all reject if the parent availment isFinalized where mutation is attempted):
    - createAvailment(businessId, { branchId, customerAccountId, cashierAccountId, cashierDgfyAccountId, terminalId }) -> inserts a status 'draft' availment; returns the plain row.
    - findById(businessId, availmentId) -> availment header plus its non-cancelled AND cancelled items (full history) AND its availment_discounts rows so the usecase can present current state, recompute totals from the recorded discounts (09-06), and audit.
    - addLine(businessId, availmentId, { productId, productName, quantity, unitPrice, stockEffectType, taxTreatment, taxRate }) -> inserts an availment_items row; reject on a finalized availment.
    - updateLine(businessId, availmentId, lineId, { quantity?, stockEffectType? }) -> updates an active (non-cancelled) line; AvailmentLineNotFoundError if missing.
    - cancelLine(businessId, availmentId, lineId) -> sets cancelled_at = new Date() (soft delete, never destroy). NOTE (WARNING-3 reconciliation): this repository method keeps the name cancelLine; the usecase/route verb in 09-05 is removeLine and maps to this method. Do NOT also expose a separate removeLine on the repository — one soft-delete method only.
    - restoreLine(businessId, availmentId, lineId) -> sets cancelled_at = null.
    - recordDiscount(businessId, availmentId, discount) -> inserts one concrete availment_discounts row (via getModels(db).AvailmentDiscount) with the exact 09-01 columns: discount_type ('promo_code' | 'manual' | 'sc_pwd'), code (promo only), amount and/or percent, applied_by_staff_account_id (required for 'manual'), reason (required for 'manual'), sc_pwd_id_number + sc_pwd_customer_name (sc_pwd only). One call inserts one discount row; a multi-discount availment accrues multiple rows (D-04 stacking). The SC/PWD path additionally writes its own discount_type 'sc_pwd' row (in addition to any sc_pwd metadata the availment header still carries) so every discount renders uniformly as a receipt line (D-14). Reject on a finalized availment.
    Use getModels(databaseName) to resolve Availment/AvailmentItem/AvailmentDiscount/Payment/Receipt; never a direct model-factory import. Expose NO hard-delete method for lines, payments, or receipts.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/modules/availments/repositories/availmentRepository.js && grep -c "cancelled_at" src/modules/availments/repositories/availmentRepository.js && grep -cE "AvailmentDiscount|availment_discounts|discount_type" src/modules/availments/repositories/availmentRepository.js</automated>
  </verify>
  <acceptance_criteria>
    - Parses; exports AvailmentRepository plus the four duck-typable error classes.
    - cancelLine/restoreLine manipulate cancelled_at only (grep confirms cancelled_at usage; no destroy()/DELETE for lines/payments/receipts); the repository exposes cancelLine (not removeLine) as the single soft-delete method.
    - recordDiscount writes an availment_discounts row via getModels(db).AvailmentDiscount (grep confirms AvailmentDiscount / availment_discounts / discount_type usage).
    - resolveDatabaseName requires status 'active' AND verified_at (copied guard).
  </acceptance_criteria>
  <done>The repository provides the tenant scaffold plus line CRUD, soft-delete/restore, and per-discount availment_discounts persistence, single-owner of availment/item/discount/payment/receipt writes.</done>
</task>

<task type="auto">
  <name>Task 3: Repository finalizePersist (one-transaction write) and module index shell</name>
  <files>apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js, apps/dgfy-api/src/modules/availments/index.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/shifts/repositories/shiftRepository.js (openShift lines 194-238 multi-table single-transaction; closeShift lines 256-289 row-locked guard read)
    - apps/dgfy-api/src/modules/inventory/index.js (lines 17-72 export list + buildInventoryModule shape to mirror for buildAvailmentsModule)
    - 09-PATTERNS.md section "index.js (wiring, DI factory)" (buildAvailmentsModule signature)
  </read_first>
  <action>
    Add finalizePersist(businessId, availmentId, { header, payment, receipt, saleEffectLines, recordSaleEffect }) to availmentRepository.js: obtain sequelize from the resolved Availment model, open ONE sequelize.transaction(async (transaction) => { ... }): (1) findOne the availment with lock: transaction.LOCK.UPDATE and reject with AvailmentFinalizedError if status !== 'draft' (prevents double-finalize/replay); (2) update the availment row to status 'finalized' with the server-computed amounts, document_context, shift_id, sc_pwd fields, finalized_at; (3) for each saleEffectLines entry call the injected recordSaleEffect({ businessId, productId, quantity, referenceType:'availment', referenceId: availmentId, actorAccountId, actorStaffAccountId, transaction }) so the sale movement writes inside the SAME transaction (single-writer + atomic). CRITICAL (WARNING-1 / A5 atomicity): recordSaleEffect is the inventory recordSale usecase, which RETURNS an ApplicationResult and does NOT throw on insufficient-stock (it returns a failure). A returned failure inside sequelize.transaction will NOT trigger rollback on its own, so inspect each result and, if it is not a success (e.g. result.isSuccess === false / result.success === false — match the ApplicationResult shape), THROW an Error carrying the failure so the whole transaction rolls back; the availment/payment/receipt must never commit while a stock decrement silently failed. (4) create the Payment row; (5) create the Receipt row. Return the plain finalized availment + payment + receipt. Any thrown error rolls back all of it (Pitfall 7). Do not print here — printing is best-effort AFTER commit in the usecase (09-06).

    Create index.js exporting the repository, entity, error classes, and a buildAvailmentsModule({ tenantConnector, businessDatabaseRegistryRepository, businessRepository, productRepository, assertComplianceGate, recordSaleEffect, shiftRepository, deviceBridgeClient }) shell that constructs one AvailmentRepository and returns { repository, useCases: {} } — the useCases map is filled by 09-05/09-06. Mirror buildInventoryModule's export/return shape.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/modules/availments/repositories/availmentRepository.js && node --experimental-vm-modules --check src/modules/availments/index.js && grep -c "LOCK.UPDATE\|sequelize.transaction\|recordSaleEffect" src/modules/availments/repositories/availmentRepository.js</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse.
    - finalizePersist opens one sequelize.transaction, row-locks the availment, invokes recordSaleEffect with a threaded transaction, and throws on a non-success sale-effect ApplicationResult so a failed decrement rolls back the whole sale (grep confirms LOCK.UPDATE, sequelize.transaction, recordSaleEffect).
    - buildAvailmentsModule constructs one AvailmentRepository and returns { repository, useCases }.
  </acceptance_criteria>
  <done>finalizePersist is an all-or-nothing multi-table write that runs sale effects inside its transaction and rejects double-finalize; the module shell is ready for usecase wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usecase -> repository | only the repository touches Sequelize/transactions |
| finalize write | multi-table financial commit must be atomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Tampering | double-finalize / replay | high | mitigate | finalizePersist row-locks the availment (LOCK.UPDATE) and rejects status !== 'draft', so a concurrent/replayed finalize cannot double-charge or double-decrement stock. |
| T-09-04-02 | Tampering | partial commit on multi-line finalize | high | mitigate | availment + payment + receipt + all sale effects commit in ONE transaction; a non-success recordSaleEffect result is re-thrown (not silently swallowed) so an insufficient-stock decrement rolls back the whole sale (Pitfall 7 / WARNING-1). |
| T-09-04-03 | Info Disclosure | cross-tenant read/write | high | mitigate | every method scopes on business_id and resolves the tenant DB via the active+verified registry guard; no cross-tenant model access. |
| T-09-04-04 | Repudiation | silent line deletion | medium | mitigate | lines are soft-deleted via cancelled_at (never destroyed); full add/remove history is preserved for audit (D-02/D-18). |
| T-09-04-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

<verification>
- All three availments files parse.
- Repository exposes no hard-delete for lines/payments/receipts; finalize is single-transaction and row-locked.
</verification>

<success_criteria>
The availments persistence layer exists: entity, tenant-scoped repository with line CRUD + soft-delete/restore + atomic finalizePersist, and a buildAvailmentsmodule shell — ready for usecases.
</success_criteria>

<output>
Create `.planning/phases/09-pos-checkout-payment/09-04-SUMMARY.md` when done.
</output>
