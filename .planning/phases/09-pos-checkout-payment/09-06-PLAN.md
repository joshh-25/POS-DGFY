---
phase: 09-pos-checkout-payment
plan: 06
type: execute
wave: 4
depends_on: [09-05, 09-03]
files_modified:
  - apps/dgfy-api/src/modules/availments/repositories/complianceEvidenceRepository.js
  - apps/dgfy-api/src/modules/availments/usecases/complianceEvidenceUseCases.js
  - apps/dgfy-api/src/modules/availments/usecases/availmentUseCases.js
  - apps/dgfy-api/src/modules/availments/controllers/availmentController.js
  - apps/dgfy-api/src/modules/availments/routes.js
  - apps/dgfy-api/src/modules/availments/index.js
  - apps/dgfy-api/tests/integration/availments/finalize.test.js
autonomous: true
requirements: [CHK-02, CHK-03, CHK-04, CHK-05, CHK-06, FSC-03]

must_haves:
  truths:
    - "finalizeAvailment recomputes all totals/VAT/discounts/change server-side via money.js from stored line data AND the recorded availment_discounts rows — no client total, discount amount, or change is ever used."
    - "Finalize binds an open shift (shiftRepository.findOpenShift) for (cashier, terminal) or rejects with a no-open-shift conflict (CHK-06)."
    - "Finalize calls assertComplianceGate with operation 'pos.checkout' and the requestedDocumentContext; for a compliant_active business it assembles and passes the evidence bundle from the ComplianceEvidence attestation store (D-21/D-23)."
    - "A fiscal (or compliant_active) checkout with incomplete evidence hard-fails (gate throws 409 REQUIRES_SETUP / 403 DENY) and finalize surfaces the missing checklist signals — the sale is rejected, never silently downgraded to non_fiscal (D-24)."
    - "Availment/Payment/Receipt persist in one transaction with per-line sale effects, THEN the device-bridge print is attempted best-effort; a print failure returns success-with-warning, never a rolled-back sale (D-22)."
  artifacts:
    - "complianceEvidenceRepository.js (read + upsert compliance_evidence), complianceEvidenceUseCases.js (attest + assemble bundle)"
    - "buildFinalizeAvailmentUseCase in availmentUseCases.js"
    - "attestComplianceEvidence controller method + route; finalize wired in buildAvailmentsModule"
    - "tests/integration/availments/finalize.test.js"
  key_links:
    - "money.js (09-02) -> finalize totals; findOpenShift (09-03) -> shift_id; recordSale (09-03) -> per-line sale effect; assertComplianceGate + ComplianceEvidence store -> receipt document_type; finalizePersist (09-04) -> atomic write; deviceBridgeClient (09-03) -> best-effort print."
---

<objective>
Complete the checkout: the finalizeAvailment orchestration usecase plus the interim compliance-evidence attestation store (D-23) it reads for fiscal / compliant_active checkout, wired into the controller/routes/module, and an integration test proving the full finalize path with mocked ports. This closes CHK-02, CHK-04, CHK-05, CHK-06, FSC-03, and the fiscal-evidence half of D-21/D-24.

Purpose: finalize is the one orchestration usecase with no exact analog. It recomputes money server-side (money.js), calls the shared compliance gate with the correct document context (D-15) and (for compliant_active) the assembled evidence bundle, binds the open shift (D-16), records per-line sale effects via the inventory single-writer (D-17) and the availment/payment/receipt atomically, then best-effort prints (D-22 refining D-11). Fiscal checkout hard-fails on incomplete evidence and surfaces exactly which signals are missing (D-24). Single payment method per availment with server-side change (D-08/D-09).

Output: complianceEvidenceRepository.js, complianceEvidenceUseCases.js, the finalize usecase, controller/route/module wiring, and finalize.test.js.

Artifacts this plan produces:
- ComplianceEvidenceRepository (getForBusinessBranch, upsert), buildAttestComplianceEvidenceUseCase, assembleEvidenceBundle
- buildFinalizeAvailmentUseCase
- POST /v1/availments/compliance-evidence (operator attestation), finalize usecase wired
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-pos-checkout-payment/09-CONTEXT.md
@.planning/phases/09-pos-checkout-payment/09-RESEARCH.md
@.planning/phases/09-pos-checkout-payment/09-PATTERNS.md
@.planning/phases/09-pos-checkout-payment/09-05-SUMMARY.md
@.planning/phases/09-pos-checkout-payment/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compliance-evidence attestation store (read/upsert) and attest usecase</name>
  <files>apps/dgfy-api/src/modules/availments/repositories/complianceEvidenceRepository.js, apps/dgfy-api/src/modules/availments/usecases/complianceEvidenceUseCases.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/compliance/repositories/complianceModeStateRepository.js (getForBusinessBranch + upsertState shape to mirror; tenant scaffold)
    - apps/dgfy-api/src/models/Tenant/ComplianceEvidence.js (09-01 — evidence_bundle JSON, branch_scope_key uniqueness)
    - 09-RESEARCH.md section "Fiscal Checkout Evidence Path (D-21)" parts 1-2 (the exact artifacts/peripherals/settings/evidence bundle shape) and "Shared Patterns > Fiscal Evidence Bundle"
    - apps/dgfy-api/src/modules/inventory/usecases/inventoryMovementUseCases.js (guardBusinessAccess/requireMembership for the operator attestation access control)
  </read_first>
  <action>
    Create complianceEvidenceRepository.js reusing the tenant-DB scaffold (resolveDatabaseName active+verified, resolveModel -> getModels(db).ComplianceEvidence, withModel): getForBusinessBranch(businessId, branchId) returns the attested evidence_bundle row (or null), and upsert(businessId, branchId, { evidenceBundle, attestedByActorType, attestedAt }) inserts-or-updates the single per-(business, branch) row (the generated branch_scope_key unique index makes this deterministic).

    Create complianceEvidenceUseCases.js: buildAttestComplianceEvidenceUseCase({ repository, businessRepository }) — owner/authorized-member gated operator endpoint returning ApplicationResult, persisting the operator-attested { settings, artifacts, peripherals, evidence } bundle. Also export a pure assembleEvidenceBundle(evidenceRow) that maps the stored bundle into the { artifacts, peripherals, settings, evidence } arguments assertComplianceGate expects (defaulting to empty arrays/objects when no attestation exists — which is correct for a non_compliant_active business).
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/modules/availments/repositories/complianceEvidenceRepository.js && node --experimental-vm-modules --check src/modules/availments/usecases/complianceEvidenceUseCases.js</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse.
    - getForBusinessBranch/upsert are business_id-scoped and go through the active+verified tenant guard.
    - assembleEvidenceBundle returns empty defaults when no attestation row exists.
  </acceptance_criteria>
  <done>The interim per-business attestation store and its attest/assemble helpers exist, giving finalize the evidence bundle inputs for compliant_active/fiscal checkout.</done>
</task>

<task type="auto">
  <name>Task 2: finalizeAvailment usecase and wiring</name>
  <files>apps/dgfy-api/src/modules/availments/usecases/availmentUseCases.js, apps/dgfy-api/src/modules/availments/controllers/availmentController.js, apps/dgfy-api/src/modules/availments/routes.js, apps/dgfy-api/src/modules/availments/index.js</files>
  <read_first>
    - apps/dgfy-api/src/modules/compliance/usecases/complianceGate.js (throw/return semantics; 09-03 context passthrough)
    - apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js (09-04 finalizePersist signature)
    - apps/dgfy-api/src/modules/availments/usecases/money.js (09-02 computeAvailmentTotals/computeChange)
    - 09-PATTERNS.md sections "Finalize orchestration", "Compliance Gate", "Fiscal Evidence Bundle", "Inventory Sale Effect"
    - 09-RESEARCH.md section "Fiscal Checkout Evidence Path (D-21)" parts 2-3 (fiscal vs non_fiscal call; REQUIRES_SETUP/DENY handling; receipt_contract.document_type)
  </read_first>
  <action>
    Add buildFinalizeAvailmentUseCase({ repository, businessRepository, productRepository, money, assertComplianceGate, recordSaleEffect, shiftRepository, complianceEvidenceRepository, deviceBridgeClient }) to availmentUseCases.js. In order: (1) guardBusinessAccess (active member); (2) load the availment + non-cancelled lines + recorded availment_discounts rows via repository.findById; reject if not draft or empty. (3) Recompute subtotal/VAT/SC-PWD/total/change via money.js from the stored line data AND the recorded availment_discounts rows (each row's type + percent and/or amount, computed independently against the base subtotal per D-05) — never a client amount; for Cash reject cash_received < total. (4) Load the compliance state; for a compliant_active business assemble the evidence bundle via complianceEvidenceRepository + assembleEvidenceBundle; for non-cash payment set context.payment_handoff_mode 'external'. (5) Call assertComplianceGate({ businessId, branchId, operation:'pos.checkout', requestedDocumentContext, artifacts, peripherals, settings, evidence, context }); wrap the thrown DomainError into ApplicationResult.failure on REQUIRES_SETUP (409) / DENY (403), surfacing details.decision.checklist.missing_* and activation_blockers so staff see the missing signals — do NOT downgrade a fiscal request to non_fiscal (D-24). (6) findOpenShift(businessId, { terminalId, cashierAccountId }); reject with a no-open-shift conflict if none (CHK-06). (7) Build the receipt payload (D-14 fields + decision.receipt_contract.document_type), rendering each recorded availment_discounts row as its own discount line (promo / manual / SC-PWD listed separately with its server-computed amount, D-14). (8) Call repository.finalizePersist(...) passing the server-computed header amounts, the Payment, the Receipt, the inventory_issue lines as saleEffectLines, and recordSaleEffect (transaction-threaded). (9) AFTER the DB commit, best-effort deviceBridgeClient.printReceipt(...); on a non-ok/timeout result, mark the receipt print warning and return ApplicationResult.success with a print_warning field — never roll back (D-22). Return the finalized availment + payment + receipt.

    Wire finalizeAvailment into buildAvailmentsModule's useCases map (closed over all injected ports). Add the attestComplianceEvidence controller method + POST /:? route: mount POST /compliance-evidence (operator attestation) in createAvailmentRoutes behind authenticateAccount, and add getById already wired in 09-05.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/modules/availments/usecases/availmentUseCases.js && node --experimental-vm-modules --check src/modules/availments/index.js && grep -c "findOpenShift\|assertComplianceGate\|finalizePersist\|printReceipt" src/modules/availments/usecases/availmentUseCases.js</automated>
  </verify>
  <acceptance_criteria>
    - Files parse; finalize references findOpenShift, assertComplianceGate, finalizePersist, and printReceipt (grep count >= 4).
    - finalizeAvailment is present in buildAvailmentsModule's useCases map.
  </acceptance_criteria>
  <done>finalizeAvailment orchestrates server-side money, compliance gating with evidence assembly, open-shift binding, atomic persist with sale effects, and best-effort print, honoring D-22 and D-24.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize integration test (mocked ports)</name>
  <files>apps/dgfy-api/tests/integration/availments/finalize.test.js</files>
  <read_first>
    - apps/dgfy-api/tests/integration/commerce/commerceModulesMount.test.js (jest/supertest convention; ESM)
    - apps/dgfy-api/tests/helpers/tenantSchemaProvisioning.js (helper reuse for any tenant-scoped fixtures)
    - 09-RESEARCH.md section "Validation Architecture" (finalize.test.js scope: mocked assertComplianceGate, inventory sale usecase, deviceBridgeClient)
  </read_first>
  <action>
    Write finalize.test.js exercising buildFinalizeAvailmentUseCase with mocked ports (no live MySQL): (a) non_fiscal happy path — non_compliant_active gate returns ALLOW with empty evidence, open shift found, totals/change recomputed server-side, availment+payment+receipt persisted, print succeeds; assert the client cannot influence the total (pass a bogus client total and prove the response uses the server-computed value). (b) CHK-06 — no open shift -> conflict, no persist. (c) Cash with cash_received < total -> rejected. (d) D-24 fiscal hard-fail — compliant_active + fiscal with incomplete evidence: mocked gate throws REQUIRES_SETUP; assert finalize returns a failure surfacing the missing signals and does NOT persist a receipt and does NOT downgrade to non_fiscal. (e) D-22 print failure — deviceBridgeClient.printReceipt resolves not-ok; assert the sale still succeeds with a print warning and the receipt row is persisted. (f) D-14 recorded manual discount — findById returns an availment_discounts row (discount_type 'manual', a percent/amount + applied_by_staff_account_id + reason); assert the server-computed total is reduced by that discount AND the receipt payload contains a distinct discount line for it (its own line, not folded into an aggregate). (g) WARNING-1 insufficient-stock rollback — one saleEffectLines entry's recordSaleEffect resolves a non-success ApplicationResult (insufficient stock); assert finalizePersist throws so the transaction rolls back and NOTHING is persisted (no finalized availment, no payment, no receipt) — a returned failure must not leave a partial commit.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && npm test -- tests/integration/availments/finalize.test.js</automated>
  </verify>
  <acceptance_criteria>
    - finalize.test.js passes all seven cases including the client-total-ignored assertion, the CHK-06 rejection, the D-24 fiscal hard-fail without downgrade, the D-14 recorded-manual-discount total-reduction + distinct-receipt-line assertion, and the WARNING-1 insufficient-stock full-rollback (nothing persisted).
  </acceptance_criteria>
  <done>The finalize path is proven with mocked ports across happy path, no-shift, insufficient cash, fiscal-evidence hard-fail, print-failure fail-open, recorded-discount total reduction + receipt line, and insufficient-stock rollback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> finalize | untrusted totals/change/document-context |
| finalize -> compliance gate | fiscal receipt issuance decision |
| finalize -> device-bridge | best-effort print after commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06-01 | Tampering | client-dictated total/change | high | mitigate | finalize recomputes everything via money.js from stored lines; a client-supplied total is ignored (asserted in finalize.test.js). |
| T-09-06-02 | Elevation of Privilege | fiscal receipt without evidence (gate bypass) | high | mitigate | assertComplianceGate is called inside finalize with operation 'pos.checkout' + assembled evidence; REQUIRES_SETUP/DENY hard-fails the sale (D-24), never a silent non_fiscal downgrade. |
| T-09-06-03 | Repudiation | selling with no open shift | high | mitigate | findOpenShift binds shift_id or the sale is rejected (CHK-06); DB one-open-shift invariant backs it. |
| T-09-06-04 | Tampering | double-charge on print retry | medium | mitigate | DB commit precedes best-effort print; print failure never re-runs the sale; finalizePersist row-lock rejects re-finalize (09-04). |
| T-09-06-05 | Info Disclosure | cross-tenant finalize | high | mitigate | guardBusinessAccess + business_id-scoped repository/gate/evidence resolution. |
| T-09-06-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

<verification>
- finalize.test.js is green (all seven cases).
- availmentUseCases.js/index.js parse; finalize wired into the module.
- npm run check:compat-boundary passes (no legacy/compat imports) — confirmed in 09-07 full-suite gate.
</verification>

<success_criteria>
CHK-02/03/04/05/06 and FSC-03 are delivered end-to-end at the usecase layer: server-side money, permission-gated discounts, payment recording, open-shift binding, compliance-gated receipt, and fiscal-evidence hard-fail — proven with mocked-port integration tests.
</success_criteria>

<output>
Create `.planning/phases/09-pos-checkout-payment/09-06-SUMMARY.md` when done.
</output>
