---
phase: 09-pos-checkout-payment
plan: 08
type: execute
wave: 6
depends_on: [09-07]
files_modified:
  - apps/dgfy-api/tests/integration/availments/finalizeLive.test.js
autonomous: false
requirements: [CHK-01, CHK-05, CHK-06]

must_haves:
  truths:
    - "The 09-01 availment-checkout migration is applied to at least one real dgfy_business_* tenant database via the migration-runner (umzug), and migration-runner verify reports the five new tables + append-only triggers present against the schema contract."
    - "A live end-to-end finalize against that tenant DB records an availment + payment + immutable receipt, decrements stock for inventory_issue lines, and rejects a finalize with no open shift."
  artifacts:
    - "tests/integration/availments/finalizeLive.test.js (live-MySQL-gated)"
    - "A migration-runner apply + verify evidence run recorded in the SUMMARY"
  key_links:
    - "migration (09-01) -> live schema apply (migration-runner) -> verify contract check -> live finalize."
---

<objective>
Apply the Phase 9 schema to a real tenant database and prove the checkout works end-to-end against live MySQL. Build/type/unit checks all pass WITHOUT the schema applied (types are not DB-derived here), so this [BLOCKING] step is required to avoid a false-positive verification state — it must run AFTER all migration/model code exists (09-01..09-07) and BEFORE the phase is verified.

This plan is NON-AUTONOMOUS: applying the migration requires live MySQL (dev: lima-dgfy-dev), which mirrors the Phase 8 UAT live-MySQL blocker and is not guaranteed in the current environment. The apply + live smoke are gated behind a human checkpoint so an operator can run them against real infrastructure when available.

Purpose: Close the schema-application gap the schema-gate contribution flags, and produce real apply/verify/finalize evidence.

Output: a live-gated finalize test and recorded apply/verify evidence in the SUMMARY.

Artifacts this plan produces:
- finalizeLive.test.js (skipped unless a live tenant DB env is present)
- migration-runner apply + verify evidence
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-pos-checkout-payment/09-RESEARCH.md
@.planning/phases/09-pos-checkout-payment/09-01-SUMMARY.md
@.planning/phases/09-pos-checkout-payment/09-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-gated end-to-end finalize test</name>
  <files>apps/dgfy-api/tests/integration/availments/finalizeLive.test.js</files>
  <read_first>
    - apps/dgfy-api/tests/helpers/tenantSchemaProvisioning.js (provisions a real tenant schema for live-DB integration — Phase 8 precedent)
    - apps/dgfy-api/tests/integration/commerce/commerceModulesMount.test.js (describe/it convention)
    - 09-RESEARCH.md sections "Environment Availability" and "Validation Architecture" (live-MySQL fallback: skip unless a live tenant DB env is present)
  </read_first>
  <action>
    Write finalizeLive.test.js that is SKIPPED (describe.skip or an env guard, e.g. process.env.LIVE_TENANT_DB) unless a live tenant database is configured — mirroring the Phase 8 live-DB test convention so CI without MySQL stays green. When enabled, it provisions/uses a real tenant schema (tenantSchemaProvisioning helper), applies the Phase 9 migration if not already applied, seeds a product + open shift, then drives a real create -> addLine -> finalize and asserts: an availment row is finalized, a payment row is written, an immutable receipt row exists (and a second UPDATE against it is rejected by the append-only trigger), stock decremented for an inventory_issue line, and a finalize with no open shift is rejected (CHK-06).
  </action>
  <verify>
    <automated>cd apps/dgfy-api && npm test -- tests/integration/availments/finalizeLive.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Without a live tenant DB env, the test suite runs and the live test is skipped (no failure).
    - With a live tenant DB env, the end-to-end finalize + append-only-immutability + no-open-shift assertions pass.
  </acceptance_criteria>
  <done>A live-gated finalize test exists that proves the full checkout against real MySQL when infrastructure is available and is safely skipped otherwise.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply the Phase 9 schema to a live tenant DB and record verify evidence</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260713120000-create-availment-checkout.cjs</files>
  <action>Operator applies the 09-01 migration to a real dgfy_business_* tenant via the migration-runner and records the apply + contract-verify report as evidence; this is a live-MySQL action, not code to author.</action>
  <what-built>The Phase 9 availment-checkout migration (09-01), models, module, and composition wiring are complete. Applying the migration to a real dgfy_business_* tenant database requires live MySQL (dev: lima-dgfy-dev), which is not guaranteed in this environment.</what-built>
  <how-to-verify>
    1. With live MySQL reachable and env configured, apply the Phase 9 migration to a target tenant: `node apps/dgfy-migration-runner/src/cli.js activate-tenant --database-name dgfy_business_<x>` (or the `schema migrate` command for pending business migrations) — this applies AND verifies against the schema contract.
    2. Confirm the migration-runner verify/report shows availments, availment_items, payments, receipts, and compliance_evidence present with their indexes and the append-only triggers, and no rejected tables.
    3. Optionally run the live finalize proof: `LIVE_TENANT_DB=dgfy_business_<x> npm test -- tests/integration/availments/finalizeLive.test.js` from apps/dgfy-api.
    4. Paste the apply + verify report summary into the SUMMARY as evidence.
  </how-to-verify>
  <resume-signal>Type "approved" with the apply/verify evidence, or describe the live-MySQL blocker to defer the live apply to phase UAT (mirroring the Phase 8 live-MySQL deferral).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration-runner -> live tenant DB | destructive-capable DDL apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08-01 | Tampering | false-positive verification (schema not applied) | high | mitigate | this [BLOCKING] step forces a real apply + contract verify before the phase is considered done, so passing build/unit checks cannot mask an unapplied schema. |
| T-09-08-02 | Denial of Service | migration failure on live tenant | medium | mitigate | migration is additive/idempotent (meta.destructive:false, targetKind 'business', guarded createTable/addIndex); human checkpoint runs it against real infra with report evidence. |
| T-09-08-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

<verification>
- finalizeLive.test.js is skipped without live DB and passes with it.
- Migration-runner apply + verify report shows the five new tables + triggers present (recorded in SUMMARY).
</verification>

<success_criteria>
The Phase 9 schema is applied and verified against a real tenant database (or the live-MySQL blocker is explicitly deferred to UAT with evidence), and a live finalize proves the checkout end-to-end.
</success_criteria>

<output>
Create `.planning/phases/09-pos-checkout-payment/09-08-SUMMARY.md` when done.
</output>
