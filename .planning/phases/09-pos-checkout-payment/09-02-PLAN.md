---
phase: 09-pos-checkout-payment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-api/src/modules/availments/usecases/money.js
  - apps/dgfy-api/tests/unit/modules/availments/money.test.js
autonomous: true
requirements: [CHK-02, FSC-03]

must_haves:
  truths:
    - "money.js computes line VAT-inclusive decomposition (vat = round(L - L/1.12), net = L - vat) in integer centavos with one round-half-up convention."
    - "For an SC/PWD-qualified availment, money.js zeroes the 12% VAT AND applies 20% off the net (VAT-exclusive) base — the BIR VAT-exempt branch (D-20)."
    - "Discounts stack independently: each discount term is computed against the original base subtotal, summed, then subtracted (never cascading) (D-05)."
    - "change_due = cash_received - total (integer centavos, server-side); computeChange rejects cash_received < total for Cash and yields no change for GCash/Credit Card."
  artifacts:
    - "apps/dgfy-api/src/modules/availments/usecases/money.js"
    - "apps/dgfy-api/tests/unit/modules/availments/money.test.js (golden cases)"
  key_links:
    - "money.js is the single server-side authority consumed by finalizeAvailment (09-06) — no toFixed math is scattered elsewhere."
---

<objective>
Build money.js, the pure integer-centavo money/tax/discount engine that is the server-side authority for CHK-02 and FSC-03, and a golden-case unit test locking its behavior. No money utility exists anywhere in the repo (grep-confirmed), so this is built fresh from the RESEARCH spec, not a codebase analog.

Purpose: All availment totals, VAT decomposition, independent discount stacking, SC/PWD VAT-exempt treatment, and cash change are computed here in integer centavos so the client can never dictate a total or change amount. This file is imported by finalizeAvailment (09-06) and is the Wave-0 golden-test target for FSC-03.

Output: money.js (pure functions) + money.test.js (golden cases covering VAT-inclusive decomposition, independent stacking, SC/PWD VAT-exempt, change, and rounding).

Artifacts this plan produces:
- money.js exports: parseAmountToCentavos, formatCentavos, roundHalfUp, decomposeVatInclusiveLine, sumLineSubtotals, computeManualAndCodeDiscounts, computeScPwdDiscount, computeAvailmentTotals, computeChange
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-pos-checkout-payment/09-CONTEXT.md
@.planning/phases/09-pos-checkout-payment/09-RESEARCH.md
@.planning/phases/09-pos-checkout-payment/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement money.js integer-centavo engine</name>
  <files>apps/dgfy-api/src/modules/availments/usecases/money.js</files>
  <read_first>
    - 09-RESEARCH.md section "Money & Tax Computation" (integer-centavo approach, VAT-inclusive decomposition, SC/PWD VAT-exempt formula, change)
    - 09-PATTERNS.md section "usecases/money.js (utility, pure transform) — NO ANALOG"
    - apps/dgfy-api/src/modules/products/usecases/productUseCases.js (Number(...) coercion convention for DECIMAL-as-string values; import ApplicationResult/DomainError style — money.js itself stays pure and throws no ApplicationResult, but follow the module's numeric coercion idiom)
  </read_first>
  <behavior>
    - parseAmountToCentavos('112.0000') === 11200; parseAmountToCentavos(112) === 11200; handles mysql2 DECIMAL-as-string.
    - formatCentavos(11200) === '112.0000' (DECIMAL(14,4) string for persistence/printing).
    - roundHalfUp is the single rounding convention reused everywhere (no scattered toFixed).
    - decomposeVatInclusiveLine(L_centavos): vat = roundHalfUp(L*12/112), net = L - vat, gross = L. For L=11200 -> vat=1200, net=10000.
    - computeScPwdDiscount(baseNetCentavos): 20% of the VAT-exclusive (net) base = roundHalfUp(net*20/100). For a VAT-exempt SC/PWD availment the 12% VAT is zeroed entirely (vat=0) and the 20% is taken on net.
    - Independent stacking (D-05): given base subtotal S (sum of line grosses or nets per rule), each discount term (each promo code amount, one manual discount, one SC/PWD) computes against the ORIGINAL S, then total_discount = sum(terms); never discount-on-discount.
    - computeChange(cashReceivedCentavos, totalCentavos, method): for 'cash' returns cashReceived - total and signals an error/negative when cashReceived < total; for 'gcash'/'credit_card' returns null (no change).
    - computeAvailmentTotals(lines, { scPwd, manualDiscount, codeDiscounts }) returns an object with subtotal_amount, vat_amount, vat_exempt_amount, discount_amount, total_amount as centavos plus formatted DECIMAL strings, applying the SC/PWD VAT-exempt branch when scPwd is present.
  </behavior>
  <action>
    Implement money.js as pure exported functions operating in integer centavos. VAT is uniform 12% per D-13 (per-item structure, single rate in Phase 9), prices are VAT-inclusive per D-19, and SC/PWD sales are VAT-exempt per D-20. Parse every incoming DECIMAL-as-string / number once via parseAmountToCentavos, do ALL arithmetic in integer centavos, and only formatCentavos back to a DECIMAL(14,4) string at the boundary. Define exactly one roundHalfUp(numerator, denominator) helper and route every VAT and discount rounding through it. Implement the VAT-inclusive decomposition, the SC/PWD VAT-exempt branch (zero the 12% VAT for the whole availment when SC/PWD applies AND take 20% off the net base), independent (non-cascading) discount stacking per D-05, and computeChange per D-09. computeAvailmentTotals orchestrates the per-line decomposition, sums, discount composition, and returns both centavo integers and formatted strings. Keep the file free of any I/O, Sequelize, or HTTP concern. Do not embed fenced code samples of the tokens the receipt later prints; document behavior in prose.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/modules/availments/usecases/money.js</automated>
  </verify>
  <acceptance_criteria>
    - node --check parses money.js.
    - money.test.js (Task 2) passes with the golden values in <behavior>.
  </acceptance_criteria>
  <done>money.js exports the full integer-centavo engine; VAT-inclusive decomposition, SC/PWD VAT-exempt 20%, independent stacking, and server-side change are all implemented with one rounding convention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden-case unit tests for money.js</name>
  <files>apps/dgfy-api/tests/unit/modules/availments/money.test.js</files>
  <read_first>
    - apps/dgfy-api/jest.config.cjs (roots ['<rootDir>/tests']; ESM via --experimental-vm-modules)
    - apps/dgfy-api/tests/integration/commerce/commerceModulesMount.test.js (import + describe/it convention)
    - 09-RESEARCH.md section "Validation Architecture" (Wave 0 money.test.js golden cases)
  </read_first>
  <action>
    Write jest tests importing money.js. Cover: (a) VAT-inclusive decomposition — a 112.00 line yields vat 12.00, net 100.00; (b) a multi-line subtotal sums per-line then decomposes; (c) SC/PWD VAT-exempt — an availment with SC/PWD applied zeroes VAT and takes 20% off the net base, producing a separate discount amount; (d) independent stacking — a manual discount AND an SC/PWD discount each compute on the original base subtotal, summed (not cascaded), and the combined discount is less-than-or-equal-to subtotal; (e) computeChange — cash 200.00 on total 112.00 yields 88.00; cash below total is rejected; GCash yields null change; (f) rounding — an odd amount (e.g. 99.99) round-trips through roundHalfUp deterministically with no float drift. Assert on formatted DECIMAL strings AND centavo integers.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && npm test -- tests/unit/modules/availments/money.test.js</automated>
  </verify>
  <acceptance_criteria>
    - npm test on money.test.js passes all golden cases including the SC/PWD VAT-exempt branch and the cash-change rejection.
  </acceptance_criteria>
  <done>money.test.js locks the FSC-03 / CHK-02 golden behavior and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-submitted amounts -> server money math | client may attempt to submit totals/change/discount amounts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02-01 | Tampering | order total / change amount | high | mitigate | money.js recomputes every total and change server-side in integer centavos from stored line data; callers never accept a client total/change (enforced downstream in 09-05/09-06). |
| T-09-02-02 | Tampering | float money drift | medium | mitigate | integer-centavo math + one roundHalfUp convention; golden tests assert no float drift. |
| T-09-02-03 | Repudiation | SC/PWD statutory miscalculation | high | mitigate | FSC-03 golden test locks the BIR VAT-exempt 20%-of-net branch so the statutory discount cannot silently drift. |
| T-09-02-SC | Tampering | npm installs | low | accept | No new packages (zero-dep integer math chosen over decimal.js per RESEARCH). |
</threat_model>

<verification>
- npm test on money.test.js is green.
- money.js contains no Sequelize/HTTP import and no scattered toFixed (grep).
</verification>

<success_criteria>
money.js is a pure, tested integer-centavo authority for VAT-inclusive decomposition, SC/PWD VAT-exempt statutory discount, independent discount stacking, and server-side change; golden tests pass.
</success_criteria>

<output>
Create `.planning/phases/09-pos-checkout-payment/09-02-SUMMARY.md` when done.
</output>
