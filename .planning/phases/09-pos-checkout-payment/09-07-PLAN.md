---
phase: 09-pos-checkout-payment
plan: 07
type: execute
wave: 5
depends_on: [09-06]
files_modified:
  - apps/dgfy-api/src/routes/index.js
  - apps/dgfy-api/tests/integration/commerce/commerceModulesMount.test.js
autonomous: true
requirements: [CHK-04, CHK-05]

must_haves:
  truths:
    - "buildAvailmentsModule is composed in the single composition root reusing the SAME tenantConnector/businessDatabaseRegistryRepository/businessRepository/productRepository/assertComplianceGate instances, plus the newly-exposed recordSale, shiftRepository, and a constructed deviceBridgeClient — never a second divergent instance."
    - "/v1/availments/* is mounted behind authenticateAccount and returns 401 (not 404) when unauthenticated."
  artifacts:
    - "routes/index.js availments wiring + router.use('/availments', ...)"
    - "commerceModulesMount.test.js extended with an /v1/availments assertion"
  key_links:
    - "inventoryUseCases.recordSale + shiftsModule.repository + deviceBridgeClient -> buildAvailmentsModule injection."
---

<objective>
Wire the availments module into the composition root and prove it mounts. This is the only place concrete instances are composed: destructure the newly-exposed recordSale from the inventory module and repository from the shifts module, construct the device-bridge client from env, build the availments module with all shared instances, and mount /availments (and its compliance-evidence attestation route). Extend the commerce mount test to assert /v1/availments returns 401 (not 404), which needs no live MySQL.

Purpose: Make the whole Phase 9 surface reachable through the real app without constructing any second divergent tenantConnector/repository instance.

Output: routes/index.js edits + commerceModulesMount.test.js extension.

Artifacts this plan produces:
- Composition-root wiring for availments; /v1/availments and /v1/availments/compliance-evidence mounts
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-pos-checkout-payment/09-PATTERNS.md
@.planning/phases/09-pos-checkout-payment/09-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose and mount the availments module</name>
  <files>apps/dgfy-api/src/routes/index.js</files>
  <read_first>
    - apps/dgfy-api/src/routes/index.js (lines 75-127 — commerce module composition + mounts; the existing inventory/shifts/compliance builds whose returns are reused)
    - apps/dgfy-api/src/modules/inventory/index.js (recordSale now exported from buildInventoryModule — 09-03)
    - apps/dgfy-api/src/modules/shifts/index.js (buildShiftsModule now returns repository — 09-03)
    - apps/dgfy-api/src/infra/deviceBridgeClient.js (buildDeviceBridgeClient — 09-03)
    - 09-PATTERNS.md section "Composition Root Wiring"
  </read_first>
  <action>
    Import buildAvailmentsModule and createAvailmentRoutes from ../modules/availments/index.js and buildDeviceBridgeClient from ../infra/deviceBridgeClient.js. Where the shifts module is built, also destructure its repository as shiftRepository. Where the inventory module is built, also destructure recordSale from its useCases. Construct one deviceBridgeClient = buildDeviceBridgeClient() from env. After the booking wiring, build the availments module reusing the SAME tenantConnector, businessDatabaseRegistryRepository, businessRepository, productRepository, and assertComplianceGate instances already constructed above, injecting recordSaleEffect: inventoryUseCases.recordSale, shiftRepository, and deviceBridgeClient — never construct a second tenantConnector or repository set. Mount router.use('/availments', createAvailmentRoutes(availmentUseCases, { authenticateAccount })) after the bookings mount.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --experimental-vm-modules --check src/routes/index.js && grep -c "buildAvailmentsModule\|'/availments'\|deviceBridgeClient\|recordSale\|shiftRepository" src/routes/index.js</automated>
  </verify>
  <acceptance_criteria>
    - routes/index.js parses.
    - grep confirms buildAvailmentsModule, the /availments mount, deviceBridgeClient, recordSale, and shiftRepository are all present.
    - No second tenantConnector/TenantConnector is constructed (the availments build reuses the existing instances).
  </acceptance_criteria>
  <done>The availments module is composed once from shared instances and mounted at /v1/availments behind auth.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the commerce mount test for availments</name>
  <files>apps/dgfy-api/tests/integration/commerce/commerceModulesMount.test.js</files>
  <read_first>
    - apps/dgfy-api/tests/integration/commerce/commerceModulesMount.test.js (whole file — the commerceEndpoints table + 401-not-404 assertion pattern)
  </read_first>
  <action>
    Add an availments entry to the commerceEndpoints table (e.g. { module: 'availments', method: 'get', path: '/v1/availments/1' } or a POST that hits auth first) so the existing 401-not-404 assertion also covers the availments mount, proving it is reachable through the real composition root without a live tenant database.
  </action>
  <verify>
    <automated>cd apps/dgfy-api && npm test -- tests/integration/commerce/commerceModulesMount.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The mount test passes and now includes an availments endpoint returning 401 (auth required), not 404.
  </acceptance_criteria>
  <done>The availments mount is proven by the transport test with no live MySQL dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composition root | single place concrete instances are wired |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07-01 | Tampering | divergent tenant/repository instance | medium | mitigate | availments reuses the single tenantConnector/registry/business/product/gate instances; no second divergent set is constructed. |
| T-09-07-02 | Authentication bypass | unauthenticated availment access | high | mitigate | every availments route is mounted behind authenticateAccount; mount test asserts 401-not-404. |
| T-09-07-SC | Tampering | npm installs | low | accept | No new packages. |
</threat_model>

<verification>
- routes/index.js parses; availments composed from shared instances and mounted.
- commerceModulesMount.test.js green with the availments endpoint.
- Full suite + npm run check:compat-boundary / check:architecture expected green (run at phase verification).
</verification>

<success_criteria>
The availments module is reachable at /v1/availments behind auth, composed once from shared instances, and proven by the mount test without live MySQL.
</success_criteria>

<output>
Create `.planning/phases/09-pos-checkout-payment/09-07-SUMMARY.md` when done.
</output>
