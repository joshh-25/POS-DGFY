---
phase: 12-scope-unblock-schema-extension
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-migration-runner/src/migrations/schema/20260716100000-extend-schema-for-legacy-migration.cjs
  - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js
  - apps/dgfy-api/src/models/Tenant/Product.js
  - apps/dgfy-api/src/models/Tenant/InventoryMovement.js
  - apps/dgfy-api/src/models/Tenant/ProductEmbedding.js
  - apps/dgfy-api/src/infra/tenantConnector.js
  - docs/database/legacy-product-attributes-folding-design.md
autonomous: true
requirements: [LDM-02, LDM-03, LDM-04]
must_haves:
  truths:
    - "The additive migration exports { meta, up, down } with meta.targetKind === 'business' and meta.destructive === false"
    - "dgfyBusinessContract.js declares the 7 new products columns, the new product_embeddings table, and the inventory_movements natural-key unique index (so verify checks them)"
    - "A committed design doc records the namespaced 1:1-vs-1:many satellite-folding shape (incl. BOM) before any mapper code"
  artifacts:
    - "apps/dgfy-migration-runner/src/migrations/schema/20260716100000-extend-schema-for-legacy-migration.cjs (NEW)"
    - "apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js (products + inventory_movements + new product_embeddings entries)"
    - "apps/dgfy-api/src/models/Tenant/ProductEmbedding.js (NEW model)"
    - "docs/database/legacy-product-attributes-folding-design.md (NEW design doc)"
  key_links:
    - "migration ↔ dgfyBusinessContract.js: one atomic unit — a column/table/index absent from the contract is never checked by verify (RESEARCH top insight)"
    - "ProductEmbedding model ↔ tenantConnector.modelDefiners: unregistered model resolves undefined at runtime"
    - "products.attributes JSON ↔ Phase 13 satellite-folding design doc: the container this phase reserves, the doc Phase 13 mappers read"
---

<objective>
Create the extended target schema (LDM-02, LDM-03, LDM-04) — verified-ready — before any mapper code starts: one additive migration adding 6 typed columns + an `attributes` JSON column to `products`, a new `product_embeddings` table (1:1 per product), and a natural-key unique index on `inventory_movements`. Update `dgfyBusinessContract.js` in lockstep so `verify` can actually confirm the new shapes (this is the phase's single most important cross-file invariant). Update the three dgfy-api Tenant persistence models for drift parity (explicit decision: update now, per RESEARCH A3 — no feature reads these yet, but every sibling model matches its migration and read-path parity is wanted for Phase 13). Commit the satellite-folding design doc (LDM-02 deliverable, D-01..D-04).

Purpose: Success criteria #2-#4 all say "confirmed via schema verification," and `verify` only checks shapes declared in the contract — so migration and contract entry are one atomic unit of work.

Output: NEW migration file, contract entries, 3 model updates + 1 new model + connector registration, and a committed folding-design doc.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-scope-unblock-schema-extension/12-CONTEXT.md
@.planning/phases/12-scope-unblock-schema-extension/12-RESEARCH.md
@.planning/phases/12-scope-unblock-schema-extension/12-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts This Plan Produces
- **Migration file:** `20260716100000-extend-schema-for-legacy-migration.cjs` (targetKind `business`, additive).
- **`products` columns (6 typed + 1 JSON):** `sku_code` STRING(50) nullable, `description` TEXT nullable, `unit_of_measure` STRING(50) nullable, `cost_per_unit` DECIMAL(14,4) nullable, `vat_type` ENUM('vatable','vat_exempt','zero_rated') NOT NULL default 'vatable', `senior_pwd_discount_eligible` BOOLEAN NOT NULL default false, `attributes` JSON nullable.
- **`products` index:** `idx_products_sku_code` (non-unique, D-05).
- **`product_embeddings` table** (columns: `id`, `business_id` CHAR(36), `product_id` INTEGER FK→products.id, `vector` TEXT, `legacy_embedding_id` INTEGER nullable, `created_at`, `updated_at`) + unique index `unique_product_embeddings_product` on `product_id` (1:1, D-09).
- **`inventory_movements` index:** `unique_inventory_movements_natural_key` unique on (`business_id`, `reference_type`, `reference_id`) (D-10).
- **Contract declarations** in `dgfyBusinessContract.js` for all of the above.
- **New model** `ProductEmbedding.js` + `modelDefiners` registration in `tenantConnector.js`.
- **Design doc** `docs/database/legacy-product-attributes-folding-design.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Additive migration + dgfyBusinessContract.js in lockstep</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260716100000-extend-schema-for-legacy-migration.cjs, apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/migrations/schema/20260712100000-create-commerce-foundation.cjs (meta block lines 57-68; idempotent guards lines 71-104; createTable + unique-index lines 120-160, 283-286; append-only triggers 456-488; down() ENUM style 524-534)
    - apps/dgfy-migration-runner/src/migrations/schema/20260714103000-add-availment-source-reference.cjs (canonical addColumn + describeTable guard + down() shape, lines 57-127)
    - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js (product_folders entry ~line 230; products ~line 250; inventory_movements ~line 278; rejectedTables ~line 623 — DO NOT TOUCH)
    - backend/src/models/Item.js (legacy source types: sku_code 10-14, description 59-62, unit_of_measure 82-85, cost_per_unit 86-92, vat_type 101-106, senior_pwd 107-111)
    - backend/src/models/ItemEmbedding.js (vector TEXT + unique item_id 1:1, lines 10-25)
    - .planning/phases/12-scope-unblock-schema-extension/12-PATTERNS.md (§migration + §dgfyBusinessContract.js — copy-ready excerpts)
    - .planning/phases/12-scope-unblock-schema-extension/12-RESEARCH.md (§Code Examples — combined migration skeleton + contract update)
  </read_first>
  <action>
    Create the migration file with timestamp `20260716100000` (lexically greater than the current max `20260715120000` so Umzug treats it as pending; Pitfall: a lower timestamp never applies). Export `{ meta, up, down }`. `meta` MUST set `targetKind: 'business'` (load-bearing — omitting defaults to 'core' and the migration silently never runs against tenant DBs, Pitfall 2), `destructive: false`, `rollbackDescription`, `estimatedRisk: 'low'`. Copy the idempotent guard helpers (`tableExists`, `hasIndex`, `addIndexIfMissing`, `timestampColumns`) verbatim from `20260712100000`.
    In `up()`: (LDM-02) `describeTable('products')` then guarded `addColumn` for the 6 typed columns using legacy-ground-truth types (`sku_code` STRING(50) nullable; `description` TEXT nullable; `unit_of_measure` STRING(50) nullable; `cost_per_unit` DECIMAL(14,4) nullable per D-06 widening legacy's DECIMAL(10,4) to the `base_price` convention; `vat_type` ENUM('vatable','vat_exempt','zero_rated') NOT NULL default 'vatable' per D-07; `senior_pwd_discount_eligible` BOOLEAN NOT NULL default false per D-08), then `attributes` JSON nullable per D-03 (the satellite-fold container — this phase only reserves it, no mapper logic here), then `addIndexIfMissing('products', ['sku_code'], { name: 'idx_products_sku_code' })` NON-unique per D-05 (legacy allows duplicate SKUs; do NOT add a unique constraint). (LDM-03) guarded `createTable('product_embeddings')` with `id` autoincrement PK, `business_id` CHAR(36) NOT NULL (opaque UUID, NO cross-DB FK), `product_id` INTEGER NOT NULL with same-DB `references: { model: 'products', key: 'id' }` onDelete/onUpdate CASCADE, `vector` TEXT NOT NULL (legacy JSON-stringified float array shape, D-09 discretion resolved to TEXT), `legacy_embedding_id` INTEGER nullable (traceability pointer, recommended per A2), timestamp columns; then `addIndexIfMissing('product_embeddings', ['product_id'], { name: 'unique_product_embeddings_product', unique: true })` for strict 1:1 per D-09. (LDM-04) `addIndexIfMissing('inventory_movements', ['business_id','reference_type','reference_id'], { name: 'unique_inventory_movements_natural_key', unique: true })` per D-10 — DDL does not trip the append-only BEFORE UPDATE/DELETE triggers, existing organic rows leave NULL tuples (distinct under MySQL unique index) so NO backfill.
    In `down()`: guarded reversal in reverse dependency order (drop `product_embeddings` table first, then remove `products` columns and indexes, then remove the `inventory_movements` index), each wrapped in try/catch no-ops. For the `vat_type` ENUM, use the defensive `DROP TYPE IF EXISTS enum_products_vat_type` `.catch(()=>{})` MySQL-harmless style from `20260712100000` (Pitfall 5 — do not over-engineer enum teardown).
    In `dgfyBusinessContract.js` IN THE SAME COMMIT (the atomic invariant): append the 7 new column names to the `products` entry `columns` array and add `'idx_products_sku_code'` to its `indexes` (leave `uniqueConstraints` empty — D-05 index is non-unique); add `'unique_inventory_movements_natural_key'` to BOTH the `inventory_movements` `indexes` and `uniqueConstraints`; add a NEW `product_embeddings` entry modeled on `product_folders` shape with `columns` (id, business_id, product_id, vector, legacy_embedding_id, created_at, updated_at), `indexes: ['unique_product_embeddings_product']`, `uniqueConstraints: ['unique_product_embeddings_product']`, `foreignKeys: [{ column: 'product_id', referencesTable: 'products', referencesColumn: 'id' }]`, `projectionOnly: false`. Do NOT touch `rejectedTables` (orthogonal target guard). Do NOT use `sync({ alter: true })` anywhere.
  </action>
  <verify>
    <automated>node -e "const m=require('./apps/dgfy-migration-runner/src/migrations/schema/20260716100000-extend-schema-for-legacy-migration.cjs'); const ok = m && m.meta && m.meta.targetKind==='business' && m.meta.destructive===false && typeof m.up==='function' && typeof m.down==='function'; if(!ok){console.error('migration shape FAIL',m&&m.meta);process.exit(1)} console.log('migration shape ok')" && grep -q "product_embeddings" apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js && grep -q "unique_inventory_movements_natural_key" apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js && grep -q "'attributes'" apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js && echo "contract entries ok"</automated>
  </verify>
  <acceptance_criteria>
    - The migration module `require()`s cleanly, exporting `meta.targetKind === 'business'`, `meta.destructive === false`, and function `up`/`down`.
    - The migration filename is `20260716100000-extend-schema-for-legacy-migration.cjs` (timestamp strictly greater than `20260715120000`).
    - `dgfyBusinessContract.js` `products.columns` includes `sku_code`, `description`, `unit_of_measure`, `cost_per_unit`, `vat_type`, `senior_pwd_discount_eligible`, `attributes`; `products.indexes` includes `idx_products_sku_code`; `products.uniqueConstraints` does NOT include `idx_products_sku_code`.
    - `dgfyBusinessContract.js` has a `product_embeddings` entry with `unique_product_embeddings_product` in both `indexes` and `uniqueConstraints` and a `product_id → products.id` foreignKey.
    - `dgfyBusinessContract.js` `inventory_movements` lists `unique_inventory_movements_natural_key` in both `indexes` and `uniqueConstraints`.
    - `rejectedTables` block is unchanged.
  </acceptance_criteria>
  <done>The additive migration and its contract declarations exist as one atomic unit; the migration parses with the correct meta; every new column/table/index is declared in the contract so `verify` (Plan 04) will check them.</done>
</task>

<task type="auto">
  <name>Task 2: Update dgfy-api Tenant models + register ProductEmbedding (drift parity)</name>
  <files>apps/dgfy-api/src/models/Tenant/Product.js, apps/dgfy-api/src/models/Tenant/InventoryMovement.js, apps/dgfy-api/src/models/Tenant/ProductEmbedding.js, apps/dgfy-api/src/infra/tenantConnector.js</files>
  <read_first>
    - apps/dgfy-api/src/models/Tenant/Product.js (init block lines 43-114; base_price DECIMAL(14,4) lines 79-82; indexes 109-113; header "match the migration exactly" contract)
    - apps/dgfy-api/src/models/Tenant/InventoryMovement.js (indexes array 103-106; reference_type/reference_id columns 65-72; append-only updatedAt:false + beforeUpdate/beforeBulkUpdate hooks 100-114; associate() 20-33)
    - apps/dgfy-api/src/infra/tenantConnector.js (imports 2-22; modelDefiners registration 140-174; auto-instantiate/associate loop 176-188; unregistered-model-undefined comment 158-160)
    - backend/src/models/ItemEmbedding.js (field ground truth 10-25)
    - .planning/phases/12-scope-unblock-schema-extension/12-PATTERNS.md (§Product.js, §InventoryMovement.js, §ProductEmbedding.js, §tenantConnector.js)
  </read_first>
  <action>
    Decision recorded here (RESEARCH A3 / Open Question 2): update the models NOW for read-path parity and drift avoidance — no API feature reads these columns yet, but every sibling Tenant model matches its migration and Phase 13's verification benefits.
    `Product.js`: add the 7 new fields to the `Product.init` block using `DataTypes.*` (matching legacy types exactly, mirroring how `base_price` uses `DataTypes.DECIMAL(14, 4)`; `vat_type` as `DataTypes.ENUM('vatable','vat_exempt','zero_rated')` with `allowNull:false, defaultValue:'vatable'`; `attributes` as `DataTypes.JSON, allowNull:true`), and append `{ fields: ['sku_code'], name: 'idx_products_sku_code' }` to the `indexes` array (non-unique). Keep `underscored: true`, `timestamps: true` and the "matches migration" header contract.
    `InventoryMovement.js`: append `{ fields: ['business_id','reference_type','reference_id'], name: 'unique_inventory_movements_natural_key', unique: true }` to the existing `indexes` array. Preserve the append-only `updatedAt: false` and the throwing `beforeUpdate`/`beforeBulkUpdate` hooks — do not weaken them.
    `ProductEmbedding.js` (NEW): follow the modern Tenant-model shape from `Product.js` (`(sequelize) => { class ProductEmbedding extends Model {...} ProductEmbedding.init({...}, { sequelize, tableName: 'product_embeddings', underscored: true, timestamps: true }) }`). Fields: `business_id` CHAR(36) NOT NULL (opaque UUID, no FK), `product_id` INTEGER NOT NULL with `references: { model: 'products', key: 'id' }`, `vector` TEXT NOT NULL, `legacy_embedding_id` INTEGER nullable. Add `associate(models)` → `belongsTo(models.Product, { foreignKey: 'product_id', as: 'product' })` following `InventoryMovement.js`'s associate pattern. Include a header comment stating it matches migration `20260716100000` exactly (D-09 1:1 shape).
    `tenantConnector.js`: add `import defineProductEmbeddingModel from '../models/Tenant/ProductEmbedding.js';` and register `ProductEmbedding: defineProductEmbeddingModel,` in the `modelDefiners` object so the auto-instantiate/associate loop wires it (an unregistered model resolves `undefined` at runtime — the exact Phase 9 bug the comment warns about).
  </action>
  <verify>
    <automated>cd apps/dgfy-api && node --input-type=module -e "import('./src/models/Tenant/ProductEmbedding.js').then(m=>{if(typeof m.default!=='function'){console.error('ProductEmbedding default export FAIL');process.exit(1)} console.log('model loads ok')})" && grep -q "ProductEmbedding" src/infra/tenantConnector.js && echo "registered ok"</automated>
  </verify>
  <acceptance_criteria>
    - `ProductEmbedding.js` default-exports a model-definer function and declares `tableName: 'product_embeddings'` with a unique/1:1 `product_id`.
    - `tenantConnector.js` `modelDefiners` includes a `ProductEmbedding` entry and imports it.
    - `Product.js` init block contains all 7 new fields and an `idx_products_sku_code` (non-unique) index entry.
    - `InventoryMovement.js` indexes array contains `unique_inventory_movements_natural_key` with `unique: true`, and the append-only hooks are intact.
    - dgfy-api architecture check (or the model-load fallback) passes.
  </acceptance_criteria>
  <done>The three Tenant models match the migration exactly and `ProductEmbedding` is registered so repositories resolve it at runtime; no drift between migration and models.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the satellite-folding design doc (LDM-02 deliverable)</name>
  <files>docs/database/legacy-product-attributes-folding-design.md</files>
  <read_first>
    - backend/src/models/index.js (Item associations lines 242-260 — the satellite hasOne/hasMany enumeration)
    - .planning/phases/12-scope-unblock-schema-extension/12-CONTEXT.md (D-01..D-04)
    - .planning/phases/12-scope-unblock-schema-extension/12-RESEARCH.md (§"Satellite-folding shape" — cardinality → namespace table)
  </read_first>
  <action>
    Write a committed design doc that records the `products.attributes` JSON folding contract BEFORE any Phase 13 mapper code (LDM-02 explicitly requires this design committed first). Capture: (1) D-01 — `product_composition`/BOM data IS in scope for migration; (2) D-03 namespace→cardinality table with one key per legacy satellite table: 1:1 objects `nutrition`, `physicalProperties`, `shelfLife`, `packaging`, `qualityControl`, `compliance` (from legacy regulatoryCompliance), `costBreakdown`; 1:many arrays `allergens`, `barcodes`, `composition`; (3) D-04 — when a legacy item has no row in a satellite table, the mapper OMITS that key entirely (not `null`/`{}`), so "key present" unambiguously means "legacy had this satellite row"; (4) D-02 — `attributes.composition` entries resolve their ingredient to the migrated `products.id` via `legacy_id_map` at Phase 13 mapper time (unmigrated ingredient → a `finding` is logged), and note explicitly that THIS phase only reserves the `attributes` JSON container and contains NO resolution logic; (5) that `attributes.barcodes` is always an array (matching the Phase 13 criterion for multi-`ItemBarcode` items). Reference the legacy source associations (`backend/src/models/index.js` lines 242-260) and note the doc is the written contract Phase 13's `items → products` mapper (PIM-02) reads.
  </action>
  <verify>
    <automated>test -f docs/database/legacy-product-attributes-folding-design.md && grep -q "legacy_id_map" docs/database/legacy-product-attributes-folding-design.md && grep -q "composition" docs/database/legacy-product-attributes-folding-design.md && grep -q "barcodes" docs/database/legacy-product-attributes-folding-design.md && grep -q "costBreakdown" docs/database/legacy-product-attributes-folding-design.md && echo "design doc ok"</automated>
  </verify>
  <acceptance_criteria>
    - The doc exists and documents all 10 namespace keys (7 objects: nutrition, physicalProperties, shelfLife, packaging, qualityControl, compliance, costBreakdown; 3 arrays: allergens, barcodes, composition).
    - The doc states the omit-key-when-absent rule (D-04) and the BOM-in-scope decision (D-01).
    - The doc states composition ingredient resolution via `legacy_id_map` is a Phase 13 concern and that this phase only reserves the container (D-02).
  </acceptance_criteria>
  <done>A committed, reviewed design doc captures the 1:1-vs-1:many satellite-folding shape (incl. BOM) before any mapper code — satisfying LDM-02's design-doc requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 13 mapper → products.attributes JSON | The new `attributes` column will be a data sink populated from legacy satellite tables in Phase 13 (NOT this phase) — a future untrusted-data surface |
| migration DDL → tenant DB | additive ALTER/CREATE against `dgfy_business_*`; must not mutate legacy or drift unrelated tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering (data integrity) | duplicate migrated inventory_movements on retry | high | mitigate | `unique_inventory_movements_natural_key` on (business_id, reference_type, reference_id) makes Phase 13 apply retries idempotent (LDM-04) |
| T-12-04 | Tampering | migration adds shape but verify can't see it (false-positive verify) | high | mitigate | Contract updated in the SAME task as the migration; Plan 04 verify asserts business_schemas_ok against declared shapes |
| T-12-05 | Information Disclosure | products.attributes as a future untrusted-data sink | low | accept | This phase only reserves a nullable JSON container; no parsing/eval/mapper logic lands here — Phase 13 owns input handling for it. Documented in the folding-design doc |
| T-12-06 | Tampering | migration silently targets dgfy_core (targetKind omitted) | medium | mitigate | `meta.targetKind: 'business'` asserted in Task 1 verify; Plan 04 confirms it runs against tenant targets |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed; only existing sequelize/umzug used and existing source files edited |
</threat_model>

<verification>
- Task 1: migration `require()` shape assertion + contract grep assertions pass.
- Task 2: dgfy-api architecture check / ProductEmbedding model-load passes; models declare the new shapes.
- Task 3: design-doc existence + required-key grep passes.
- Full schema apply + `verify` against a real tenant DB is Plan 04 (the [BLOCKING] gate) — this plan produces the files; Plan 04 proves them against a DB.
</verification>

<success_criteria>
LDM-02/03/04 schema artifacts exist and are declared in the verification contract; the committed folding-design doc exists; models match the migration. The shapes are ready for the Plan 04 apply+verify gate.
</success_criteria>

<output>
Create `.planning/phases/12-scope-unblock-schema-extension/12-02-SUMMARY.md` when done.
</output>
