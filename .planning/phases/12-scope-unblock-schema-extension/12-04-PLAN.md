---
phase: 12-scope-unblock-schema-extension
plan: 04
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - apps/dgfy-migration-runner/reports/
autonomous: false
requirements: [LDM-02, LDM-03, LDM-04]
user_setup:
  - service: tenant-mysql
    why: "Success criteria #2-#4 require applying + verifying the additive migration against a real dgfy_business_* tenant database"
    env_vars:
      - name: DGFY_BUSINESS_DB_NAMES
        source: "Existing migration-runner env — the tenant DB target(s) schema migrate/verify run against (e.g. lima-dgfy-dev configured tenant)"
must_haves:
  truths:
    - "schema migrate applies migration 20260716100000 to every configured tenant target with no error"
    - "verify reports business_schemas_ok === true with the 7 new products columns, product_embeddings table + unique index, and inventory_movements natural-key unique index all confirmed present"
    - "Re-running schema migrate is a pure no-op: pending_migrations empty, no duplicate-index error, legacy_non_mutation.unchanged === true"
  artifacts:
    - "migration-runner JSON + summary report for the schema migrate run"
    - "migration-runner verify report showing business_schemas_ok and the new shapes"
  key_links:
    - "schema migrate (targetKind=business) → dgfy_business_* tenant DBs"
    - "verify checkContractSchema(dgfyBusinessContract) → confirms the Plan 02 contract shapes actually exist in the DB"
    - "idempotency section (umzug pending) + within-migration existence guards → success criterion #4 re-run safety"
---

<objective>
[BLOCKING] Prove the Plan 02 schema against a real tenant database (success criteria #2-#4 explicitly say "confirmed via schema verification"). Apply the additive migration via the migration-runner's `schema migrate`, run `verify` to confirm the new columns/table/index exist per the contract, then re-run `schema migrate` to prove idempotency (no duplicate-index error, no data loss, no drift on unrelated tables).

Purpose: A migration that builds and type-checks but is never applied + verified is a false-positive — this gate closes that. `verify` only checks shapes declared in `dgfyBusinessContract.js`, which Plan 02 updated in lockstep, so this run is the actual proof.

Output: A schema-migrate report + a verify report showing `business_schemas_ok === true`, plus an idempotency re-run confirming a clean no-op. Requires a live tenant DB (`autonomous: false`).
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-scope-unblock-schema-extension/12-RESEARCH.md
@.planning/phases/12-scope-unblock-schema-extension/12-02-PLAN.md
</context>

<artifacts_produced>
## Artifacts This Plan Produces
- A `schema migrate` JSON + human-readable summary report (in the migration-runner reports dir) listing migration `20260716100000` under the business targets' `migrations_executed`.
- A `verify` report with `report.summary.business_schemas_ok === true`, the new `products` columns / `product_embeddings` table + `unique_product_embeddings_product` / `unique_inventory_movements_natural_key` confirmed, `idempotency[].pending_migrations === []`, and `legacy_non_mutation.unchanged === true`.
- No new source symbols (this plan runs commands; it does not edit schema code).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Apply the migration, verify, and prove idempotency against a tenant DB</name>
  <files>apps/dgfy-migration-runner/reports/</files>
  <read_first>
    - apps/dgfy-migration-runner/src/commands/schema.js (runSchemaMigrate flow: env validation, target-db allow-list, fingerprint baseline, Umzug per target, report writing — lines 178-304)
    - apps/dgfy-migration-runner/src/commands/verify.js (runVerify + checkContractSchema; idempotency + legacy_non_mutation derivation — lines 46-137, 159-174, 225-281, 337-469)
    - apps/dgfy-migration-runner/src/cli.js (command names: schema migrate, verify)
    - .planning/phases/12-scope-unblock-schema-extension/12-RESEARCH.md (§"How migrations are applied", §"How verify confirms", §"Idempotency proof", §Environment Availability)
  </read_first>
  <action>
    Ensure the migration-runner env is configured for at least one tenant target (`DGFY_BUSINESS_DB_NAMES` plus DB connection env). Run `node src/cli.js schema migrate` from `apps/dgfy-migration-runner` — confirm migration `20260716100000-extend-schema-for-legacy-migration.cjs` appears under the business targets' executed list (NOT the core target — if it lands on core, `meta.targetKind` was wrong, Pitfall 2). Then run `node src/cli.js verify` and inspect the report: `report.summary.business_schemas_ok` must be `true`, with `products` showing the 7 new columns present (empty `missing_columns`), `product_embeddings` present with `unique_product_embeddings_product`, and `inventory_movements` carrying `unique_inventory_movements_natural_key` (empty `missing_indexes`/`missing_unique_constraints`). Then re-run `node src/cli.js schema migrate` a SECOND time to prove idempotency (success criterion #4): the run must be a clean no-op — no duplicate-index/duplicate-column error — and a subsequent `verify` must show `idempotency[].pending_migrations === []` and `legacy_non_mutation.unchanged === true` (no drift on unrelated tables). Do NOT pass any destructive-confirmation flag (the migration is additive, `destructive: false`). Capture the JSON + summary reports as the evidence artifacts. If the tenant DB / env cannot be reached in this environment, do NOT fabricate success — stop and route to the human checkpoint below with the exact command output.
  </action>
  <verify>
    <human-check>Operator runs `schema migrate` → `verify` → `schema migrate` (again) → `verify` against a configured tenant DB and confirms the verify report shows business_schemas_ok true and a clean idempotent re-run. See checkpoint task below.</human-check>
  </verify>
  <acceptance_criteria>
    - First `schema migrate` executes `20260716100000` against the business target(s) with no error; the report lists it under the business (not core) target.
    - `verify` report `summary.business_schemas_ok === true`; `products` new columns, `product_embeddings` + `unique_product_embeddings_product`, and `unique_inventory_movements_natural_key` all confirmed present (no `missing_*` entries for them).
    - Second `schema migrate` is a clean no-op (no duplicate-index error); post-run `verify` shows `pending_migrations` empty and `legacy_non_mutation.unchanged === true`.
  </acceptance_criteria>
  <done>The additive schema is applied and verified against a real tenant DB, and the idempotency re-run is a clean no-op with no drift — success criteria #2, #3, #4 are proven, not merely built.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator confirms apply + verify + idempotent re-run against a tenant DB</name>
  <action>Operator-run verification gate — the executor cannot self-approve. The operator runs the migrate/verify/re-migrate/verify sequence against a live tenant DB and confirms the reported results per the steps below, then approves or reports the failing check.</action>
  <what-built>The Plan 02 additive migration (`20260716100000`) was applied to the configured tenant DB(s) via `schema migrate`, verified via `verify`, and re-applied to prove idempotency.</what-built>
  <how-to-verify>
    1. From `apps/dgfy-migration-runner`, with tenant env configured (`DGFY_BUSINESS_DB_NAMES` + DB connection), run `node src/cli.js schema migrate`. Confirm the summary lists `20260716100000-extend-schema-for-legacy-migration.cjs` as executed under the business target(s).
    2. Run `node src/cli.js verify`. Confirm the report shows `business_schemas_ok: true`, and that `products` (7 new columns), `product_embeddings` (with `unique_product_embeddings_product`), and `inventory_movements` (`unique_inventory_movements_natural_key`) are confirmed with no `missing_*` entries.
    3. Run `node src/cli.js schema migrate` a second time. Confirm it is a clean no-op — NO duplicate-index/column error.
    4. Run `node src/cli.js verify` again. Confirm `idempotency[].pending_migrations` is empty and `legacy_non_mutation.unchanged` is true.
  </how-to-verify>
  <resume-signal>Type "approved" once the verify report shows business_schemas_ok true and the second migrate run was a clean idempotent no-op, or describe the failing check (e.g. migration landed on core target, a missing column, or a duplicate-index error).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → schema migrate → tenant DB | Destructive-operation gate; this run must stay additive (no --confirm-destructive) |
| verify contract → live tenant schema | The declared contract is trusted as the source of truth for "what must exist"; a shape missing from the contract is invisible to verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-10 | Tampering (data integrity) | non-idempotent re-run corrupts schema (duplicate index / lost data) | high | mitigate | Within-migration existence guards + unique index names; the second `schema migrate` run is explicitly asserted to be a clean no-op with `legacy_non_mutation.unchanged` true |
| T-12-11 | Repudiation | migration applied without reviewable evidence | low | mitigate | `schema migrate`/`verify` emit DB-backed metadata + JSON + summary reports captured as artifacts |
| T-12-12 | Tampering | migration silently applied to dgfy_core instead of tenant DBs | medium | mitigate | Task 1 + checkpoint assert the file appears under the business target's executed list, not core |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed; runs existing CLI commands only |
</threat_model>

<verification>
- Human checkpoint confirms: `business_schemas_ok === true`, all new shapes present, idempotent second run is a clean no-op, `legacy_non_mutation.unchanged === true`.
- Evidence: the captured `schema migrate` + `verify` JSON/summary reports.
</verification>

<success_criteria>
Success criteria #2-#4 proven against a real tenant DB: the extended `products` columns + `attributes`, the `product_embeddings` table (1:1), and the `inventory_movements` natural-key unique index are confirmed via schema verification, and re-running the migration is idempotent with no drift.
</success_criteria>

<output>
Create `.planning/phases/12-scope-unblock-schema-extension/12-04-SUMMARY.md` when done.
</output>
