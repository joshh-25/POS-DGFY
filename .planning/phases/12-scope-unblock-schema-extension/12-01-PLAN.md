---
phase: 12-scope-unblock-schema-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-migration-runner/src/data/mappings.js
  - docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
  - docs/database/dgfy-data-migration-map.md
autonomous: true
requirements: [LDM-01]
must_haves:
  truths:
    - "isInScopeLegacyTable('items'), ('stock_movements'), ('pos_transactions') all return true after the edit"
    - "isInScopeLegacyTable('pos_transaction_lines') still returns false (Phase 14 owns it)"
    - "ADR 0029 and migration-map §10 no longer read as permanently excluding items/stock_movements/pos_transactions"
  artifacts:
    - "apps/dgfy-migration-runner/src/data/mappings.js (OUT_OF_SCOPE_LEGACY_TABLES minus 3 names)"
    - "docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md (dated v2.1 amendment)"
    - "docs/database/dgfy-data-migration-map.md (§10 exclusion bullets amended)"
  key_links:
    - "isInScopeLegacyTable() reads OUT_OF_SCOPE_LEGACY_TABLES — the literal gate every future mapper calls through"
    - "migration-map §10 header states it mirrors OUT_OF_SCOPE_LEGACY_TABLES — must move in lockstep or immediately stale"
---

<objective>
Remove the ADR-level and scope-gate blocker (LDM-01, D-11) that prevents any legacy `items`/`stock_movements`/`pos_transactions` mapper from being written in Phase 13/14. Perform the surgical triple amendment: delete exactly three entries from `OUT_OF_SCOPE_LEGACY_TABLES` in `mappings.js`, and amend the two prose docs that mirror it (ADR 0029 + migration-map §10).

Purpose: `isInScopeLegacyTable()` is the pure-function gate every downstream mapper/dry-run/apply calls through; while the three names are in the array, every attempt to migrate them short-circuits to an `out_of_scope` skip finding. This plan flips that.

Output: Amended `mappings.js` array + ADR 0029 prose amendment + migration-map §10 amendment. No DB, no runtime, no schema.
</objective>

<execution_context>
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/Users/pat/Projects/Sieitz/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-scope-unblock-schema-extension/12-CONTEXT.md
@.planning/phases/12-scope-unblock-schema-extension/12-RESEARCH.md
@.planning/phases/12-scope-unblock-schema-extension/12-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts This Plan Produces
- `OUT_OF_SCOPE_LEGACY_TABLES` array in `mappings.js` with `'items'`, `'stock_movements'`, `'pos_transactions'` removed (3 fewer entries; `'pos_transaction_lines'` retained).
- A dated `## Amendment (v2.1 Legacy Data Migration, 2026-07)` section in ADR 0029, plus inline pointers on Compatibility Decisions #1/#3 and Rollout Policy #3.
- Amended §10 "Explicit Exclusions (ADR 0029)" bullets in `docs/database/dgfy-data-migration-map.md`.
- No new symbols, tables, columns, indexes, or methods.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Remove the three unblocked legacy tables from the scope gate</name>
  <files>apps/dgfy-migration-runner/src/data/mappings.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/mappings.js (lines 54-81 OUT_OF_SCOPE_LEGACY_TABLES array; lines 129-161 isInScopeLegacyTable/classifyOutOfScopeRecord consumers)
    - .planning/phases/12-scope-unblock-schema-extension/12-PATTERNS.md (§"mappings.js" — exact lines to delete: 55, 62, 67; leave 68)
    - .planning/phases/12-scope-unblock-schema-extension/12-RESEARCH.md (§"Scope-Check Enforcement" — item_folders no-op verdict; pos_transaction_lines retention; rejectedTables orthogonality)
  </read_first>
  <action>
    Delete exactly three array entries from `OUT_OF_SCOPE_LEGACY_TABLES` (`Object.freeze([...])`, currently lines 54-81): `'items'` (line 55), `'stock_movements'` (line 62), and `'pos_transactions'` (line 67). Per LDM-01 and D-11. Do NOT touch `'pos_transaction_lines'` (line 68) — Phase 14 / SHM-02 owns its unblock (Pitfall 4). Do NOT add or edit `item_folders`: it is not present in the array (verified no-op, Pitfall 3) and `isInScopeLegacyTable('item_folders')` already returns true. Do NOT touch `classifyOutOfScopeRecord()` or `isInScopeLegacyTable()` bodies — they read the array and need no change. Do NOT touch `rejectedTables` in either schema contract: that is an orthogonal target-side guard (legacy names must never exist as tenant tables) and stays intact (Anti-Pattern: editing rejectedTables). Leave a one-line comment near the array noting the three tables are now in scope starting Phase 13 (v2.1 milestone).
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./apps/dgfy-migration-runner/src/data/mappings.js').then(m=>{const f=m.isInScopeLegacyTable; const inScope=['items','stock_movements','pos_transactions'].every(f); const stillGated=f('pos_transaction_lines')===false; const folderOk=f('item_folders')===true; if(inScope&&stillGated&&folderOk){console.log('scope gate ok')}else{console.error('FAIL',{inScope,stillGated,folderOk});process.exit(1)}})"</automated>
  </verify>
  <acceptance_criteria>
    - `isInScopeLegacyTable('items')`, `('stock_movements')`, `('pos_transactions')` each return `true`.
    - `isInScopeLegacyTable('pos_transaction_lines')` returns `false` (unchanged).
    - `isInScopeLegacyTable('item_folders')` returns `true` (already, no code change).
    - `rejectedTables` in `dgfyBusinessContract.js` is byte-identical to before.
  </acceptance_criteria>
  <done>The scope gate returns in-scope for the three unblocked tables, still gates `pos_transaction_lines`, and `rejectedTables` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Amend ADR 0029 prose and migration-map §10 in lockstep</name>
  <files>docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md, docs/database/dgfy-data-migration-map.md</files>
  <read_first>
    - docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md (Compatibility Decisions #1 ~line 48, #3 ~line 55; Rollout Policy #3 ~line 95; front-matter status/last_reviewed; the ADR 0029 Validation checklist section)
    - docs/database/dgfy-data-migration-map.md (§10 "Explicit Exclusions (ADR 0029)" ~lines 268-280)
    - .planning/phases/12-scope-unblock-schema-extension/12-PATTERNS.md (§"ADR 0029 + migration-map §10" — exact passages + recommended amendment mechanism)
  </read_first>
  <action>
    ADR 0029: add a dated `## Amendment (v2.1 Legacy Data Migration, 2026-07)` section at the end recording that the v2.1 milestone now migrates legacy `items` data into `dgfy_business_*.products` (Phase 13), `stock_movements` into `inventory_movements` (Phase 13), and `pos_transactions` sales history into `availments` (Phase 14) — additively, into new `dgfy_*` tables, not by mutating legacy. Add short inline pointers to that amendment at Compatibility Decisions #1 ("items table remains unchanged in this phase"), #3 ("Storefront orders remain stored in pos_transactions"), and Rollout Policy #3 ("Do not introduce destructive migrations..."), clarifying that "in this phase" framing is superseded by the v2.1 milestone and that Rollout Policy #3 remains literally true because v2.1 changes are additive to NEW tables (no destructive migration, rename, or split). Preserve `status: accepted`; bump `last_reviewed` to the current date. Do NOT rewrite decision history — amend, do not delete.
    Migration-map §10: in the "Explicit Exclusions (ADR 0029)" bullets, move `items` out of the products/items bullet (SKUs/variants/categories STAY excluded), move `stock_movements` out of the stock-movements bullet (item/location stock, FIFO batches STAY), and move `pos_transactions` out of the POS bullet (`pos_transaction_lines`, shifts, cashier sessions STAY). Add a note that these three are covered starting Phase 13 (v2.1). Leave `item_folders` implicit (it was never listed). Because the §10 header states it mirrors `OUT_OF_SCOPE_LEGACY_TABLES`, this must land in the same plan as Task 1 (D-11).
    Do NOT introduce the literal source-array identifier from `mappings.js` into the ADR as if the ADR contained it — the ADR is prose; the array lives only in `mappings.js` (success-criterion #1 phrasing discrepancy, see RESEARCH Open Question 1).
  </action>
  <verify>
    <automated>npm run lint:docs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint:docs` exits 0 (ADR 0029 validation passes).
    - ADR 0029 contains a dated `## Amendment (v2.1 Legacy Data Migration` section and retains `status: accepted`.
    - `docs/database/dgfy-data-migration-map.md` §10 no longer lists `items`, `stock_movements`, or `pos_transactions` as permanent exclusions but still lists `pos_transaction_lines`, SKUs, categories, and FIFO batches.
    - `grep -n "pos_transaction_lines" docs/database/dgfy-data-migration-map.md` still returns the retained-exclusion bullet.
  </acceptance_criteria>
  <done>ADR 0029 and migration-map §10 read consistently with the amended `mappings.js` array; `lint:docs` passes; the three tables are documented as in-scope from Phase 13 while the retained exclusions are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → migration-runner scope gate | `isInScopeLegacyTable()` decides whether a legacy table's rows are eligible for migration; widening it wrongly could admit an unintended table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | OUT_OF_SCOPE_LEGACY_TABLES over-broad edit | medium | mitigate | Remove exactly 3 named entries; functional assertion proves `pos_transaction_lines` still gated and only the 3 flipped |
| T-12-02 | Tampering | rejectedTables accidentally weakened | high | mitigate | Do not edit `rejectedTables`; acceptance criterion asserts it is byte-identical, so no legacy table name can appear as a tenant table |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages and edits only existing source + docs; no supply-chain surface |
</threat_model>

<verification>
- `node --input-type=module` scope assertion passes (Task 1).
- `npm run lint:docs` passes (Task 2).
- Manual grep confirms `pos_transaction_lines` retained in both `mappings.js` and migration-map §10; `rejectedTables` unchanged.
</verification>

<success_criteria>
LDM-01 satisfied: the three legacy tables are no longer scope-gated (verified functionally via `isInScopeLegacyTable`), and both prose docs are amended in lockstep with the array, with `pos_transaction_lines` and `rejectedTables` correctly untouched.
</success_criteria>

<output>
Create `.planning/phases/12-scope-unblock-schema-extension/12-01-SUMMARY.md` when done.
</output>
