---
phase: 10-storefront-discovery-online-ordering
plan: 04
type: execute
wave: 3
depends_on: ["10-03", "10-01"]
files_modified:
  - apps/dgfy-api/src/infra/emailOtp.js
  - apps/dgfy-api/src/modules/storefront/repositories/guestIdentityRepository.js
  - apps/dgfy-api/src/modules/storefront/usecases/guestCheckoutUseCases.js
  - apps/dgfy-api/src/modules/storefront/controllers/guestCheckoutController.js
  - apps/dgfy-api/src/modules/storefront/routes.js
  - apps/dgfy-api/src/modules/storefront/index.js
  - apps/dgfy-api/tests/storefront/guestCheckout.test.js
autonomous: true
requirements: [STF-03]
must_haves:
  truths:
    - "A guest can verify their email via OTP and check out without creating a DGFY Account (STF-03)."
    - "A repeat guest with the same verified email maps to one persistent landlord guest identity across orders (D-06)."
    - "A logged-in DGFY Account can check out via the existing account auth path; account is optional, never forced."
    - "checkout identity resolves to either a customer_account_id (account) or a guest_identity_id (guest) — an application-level cross-DB reference, not a DB FK."
  artifacts:
    - "apps/dgfy-api/src/modules/storefront/usecases/guestCheckoutUseCases.js"
    - "apps/dgfy-api/src/modules/storefront/repositories/guestIdentityRepository.js"
  key_links:
    - "requestGuestOtp/verifyGuestOtp -> existing emailOtp.js (new STOREFRONT_GUEST_CHECKOUT purpose)"
    - "verified email -> upsert persistent storefront_guest_identities row (D-06)"
    - "resolveCheckoutIdentity -> { customer_account_id | guest_identity_id } consumed by placeOrder (10-06)"
---

<objective>
Deliver the identity half of checkout (STF-03): guest email-OTP verification reusing the existing `emailOtp.js`, a persistent landlord-side guest identity keyed by verified email (D-06), and identity resolution that lets a logged-in DGFY Account check out without forcing guests to create one.

Purpose: Establish the "who is ordering" resolution that `placeOrder` (10-06) consumes, keyed on a verified email for guests and on the existing account auth for members — with phone captured as unverified contact only (D-05).
Output: A new email-OTP purpose, a guest identity repository (upsert-by-email), guest OTP request/verify + identity-resolution usecases, routes, and tests.
</objective>

<execution_context>
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-CONTEXT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-RESEARCH.md

# Existing OTP infra + account auth to reuse
@apps/dgfy-api/src/infra/emailOtp.js
@apps/dgfy-api/src/models/Landlord/StorefrontGuestIdentity.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add storefront guest-checkout OTP purpose + guest identity repository</name>
  <files>apps/dgfy-api/src/infra/emailOtp.js, apps/dgfy-api/src/modules/storefront/repositories/guestIdentityRepository.js</files>
  <action>
In `emailOtp.js`, add a new value to `EMAIL_OTP_PURPOSES` — `STOREFRONT_GUEST_CHECKOUT: 'storefront_guest_checkout'` — and a matching `PURPOSE_LABELS` entry ("storefront guest checkout"). Do not change the hashing/TTL/attempt-limit/enforcement logic; only extend the purpose enum + label map (the existing `isValidPurpose` picks it up automatically).

Create `guestIdentityRepository` (landlord dgfy_core connection, `StorefrontGuestIdentity` model from 10-01): `upsertByVerifiedEmail({ email, phone, displayName })` — finds the existing identity by the UNIQUE verified_email or inserts a new one, updating phone/display_name/last_order_at; returns the identity id. `findByEmail(email)`. The upsert must be race-safe against the unique index (catch the unique-violation and re-select), so two concurrent first-orders from the same guest collapse to one identity.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; node -e "import('./src/infra/emailOtp.js').then(m=>{if(!m.EMAIL_OTP_PURPOSES.STOREFRONT_GUEST_CHECKOUT) process.exit(1); console.log('purpose ok')})"</automated>
  </verify>
  <done>New OTP purpose available and validated by isValidPurpose; guestIdentityRepository upserts one persistent identity per verified email, race-safe against the unique index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guest OTP request/verify + checkout identity resolution usecases + routes</name>
  <files>apps/dgfy-api/src/modules/storefront/usecases/guestCheckoutUseCases.js, apps/dgfy-api/src/modules/storefront/controllers/guestCheckoutController.js, apps/dgfy-api/src/modules/storefront/routes.js, apps/dgfy-api/src/modules/storefront/index.js, apps/dgfy-api/tests/storefront/guestCheckout.test.js</files>
  <behavior>
    - requestGuestOtp({email}): validates email, calls emailOtp.requestEmailOtp with the STOREFRONT_GUEST_CHECKOUT purpose; returns 503 EMAIL_OTP_DELIVERY_UNAVAILABLE if SMTP unconfigured (existing behavior); rate-limited.
    - verifyGuestOtp({email, code, phone, displayName}): calls emailOtp.verifyEmailOtp (consume) with the purpose; on success upserts the persistent guest identity (D-06) and returns { guest_identity_id, verified: true }; on wrong/expired code returns the OTP error.
    - resolveCheckoutIdentity({ authenticatedAccountId, guestIdentityId }): returns { customer_account_id: authenticatedAccountId } when an account is present, else { guest_identity_id: guestIdentityId } when a verified guest identity is present, else rejects "verification required". Never forces account creation. The returned value is an application-level cross-DB reference (no FK) consumed by placeOrder.
  </behavior>
  <action>
Implement the usecases and wire them into `buildStorefrontModule` (inject `guestIdentityRepository`, `emailOtp`, and the existing account lookup). Add routes to the storefront routes.js: `POST /storefront/guest/otp/request`, `POST /storefront/guest/otp/verify` (both public, rate-limited via express-rate-limit — OTP brute-force/spam guard). Mount the checkout routes so the account path can pass through the existing `authenticateAccount` middleware optionally (account attaches identity if a valid bearer is present, otherwise the guest path applies). Export `resolveCheckoutIdentity` for 10-06. Phone is stored as unverified contact only (D-05) — never OTP-challenged.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/storefront/guestCheckout.test.js 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Guest can request+verify an email OTP and receive a persistent guest_identity_id; repeat email reuses the same identity; identity resolution yields customer_account_id OR guest_identity_id and never forces account creation; OTP routes rate-limited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → OTP request/verify | Unauthenticated email verification |
| guest identity | Verified email is not a login session — no elevated capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04-01 | Elevation / DoS | OTP brute force / spam | high | mitigate | Existing emailOtp hashing + EMAIL_OTP_MAX_ATTEMPTS + single-active-OTP, plus express-rate-limit on request/verify routes |
| T-10-04-02 | Spoofing | unverified email used for checkout | high | mitigate | Guest checkout requires a consumed OTP verification before an identity is issued; phone stays unverified contact-only (D-05) |
| T-10-04-03 | Tampering | concurrent first-order duplicates identity | medium | mitigate | Upsert race-safe against UNIQUE verified_email (catch-and-reselect) |
| T-10-04-04 | Session Management | guest identity mistaken for a login | low | accept | Guest identity confers no capability beyond reading its own orders; not a bearer session |
| T-10-04-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- Guest OTP + identity tests pass.
- emailOtp.js change is additive (new purpose only); no change to hashing/TTL/enforcement.
- No `backend/` writes; no new dependency.
</verification>

<success_criteria>
Guest can verify email and check out without an account; repeat guests are recognized; account path works and is never forced. Covers STF-03.
</success_criteria>

<output>
Create `.planning/phases/10-storefront-discovery-online-ordering/10-04-SUMMARY.md` when done.
</output>
