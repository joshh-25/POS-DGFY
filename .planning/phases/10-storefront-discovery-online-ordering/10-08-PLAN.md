---
phase: 10-storefront-discovery-online-ordering
plan: 08
type: execute
wave: 5
depends_on: ["10-06", "10-07", "10-05"]
autonomous: true
files_modified:
  - apps/dgfy-api/src/app.js
  - apps/dgfy-api/src/routes/index.js
  - apps/dgfy-api/src/modules/commercePayments/routes.js
  - apps/dgfy-api/src/modules/commercePayments/controllers/webhookController.js
  - apps/dgfy-api/src/modules/commercePayments/usecases/handleWebhookUseCases.js
  - apps/dgfy-api/src/modules/commercePayments/usecases/finalizePaidOrderUseCases.js
  - apps/dgfy-api/src/modules/commercePayments/usecases/retryFinalizationUseCases.js
  - apps/dgfy-api/src/modules/commercePayments/index.js
  - apps/dgfy-api/tests/commercePayments/webhookFinalize.test.js
  - apps/dgfy-api/tests/storefront/storefrontE2E.test.js
requirements: [STF-05]
must_haves:
  truths:
    - "A PayMongo payment.paid webhook (verified by HMAC over the raw body) finalizes the order into the correct tenant Availment idempotently (STF-05, D-04)."
    - "A duplicate or replayed webhook never produces a second Availment or a second stock deduction."
    - "payment.failed / qrph.expired releases the reservation and moves the order to expired/failed (D-09); account.* and refund.* are ignored (D-02/D-03)."
    - "Auto-release does not depend on the qrph.expired webhook: a wired sweep (scheduled + on-read) reclaims expired holds even if the webhook is never delivered (D-09 no-manual-step)."
    - "A paid-but-unfinalizable order lands in finalize_failed_manual_resolution_required and is retryable — never silently dropped (STF-05)."
    - "The storefront and commercePayments modules are mounted and wired into the running app with the raw-body webhook route."
  artifacts:
    - "apps/dgfy-api/src/modules/commercePayments/usecases/handleWebhookUseCases.js"
    - "apps/dgfy-api/src/modules/commercePayments/usecases/finalizePaidOrderUseCases.js"
    - "apps/dgfy-api/src/modules/commercePayments/usecases/retryFinalizationUseCases.js"
    - "apps/dgfy-api/src/routes/index.js (composition wiring)"
  key_links:
    - "raw-body capture (app.js express.json verify) -> HMAC signature verify (10-05) BEFORE any DB work"
    - "payment.paid -> resolve session/tenant -> finalizeStorefrontOrder (10-07) -> write availment_id back to landlord order+session"
    - "finalize throw after paid -> finalize_failed_manual_resolution_required -> operator retry endpoint"
---

<objective>
Close STF-05: the async, webhook-driven cross-database finalization (D-04). A verified `payment.paid` event resolves the landlord session/tenant, invokes the storefront finalize path (10-07), and writes the tenant `availment_id` back to the durable landlord record — idempotently. Any paid-but-unfinalizable order enters an explicit manual-resolution state with an operator retry endpoint, never a silent failure. This plan also wires the two new modules into the running app, including the raw-body webhook route.

Purpose: The final seam that makes an interrupted or retried cross-DB write safe, and the composition that turns the isolated modules from 10-01..10-07 into a live storefront ordering flow.
Output: Raw-body webhook route + controller, webhook event router, finalizePaidOrder orchestration, retryFinalization operator endpoint, expiry auto-release, composition wiring in routes/index.js + app.js, and webhook/E2E tests.

Assumption to confirm during execution: [ASSUMED A6] PayMongo event names (payment.paid/payment.failed/qrph.expired) and payload shape are read from legacy code, not re-verified against live PayMongo — sandbox-validate before go-live. Surface in the SUMMARY.
</objective>

<execution_context>
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-CONTEXT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-RESEARCH.md
@apps/dgfy-api/src/app.js
@apps/dgfy-api/src/routes/index.js

# Legacy webhook + finalize + retry pattern (READ-ONLY reference)
@backend/src/modules/commercePayments/usecases/handlePayMongoCommerceWebhookUseCase.js
@backend/src/modules/commercePayments/usecases/finalizePaidCommerceSession.js
@backend/src/modules/commercePayments/usecases/commercePaymentAdminUseCases.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raw-body webhook route + signature-verified event router (paid / failed / expired)</name>
  <files>apps/dgfy-api/src/app.js, apps/dgfy-api/src/modules/commercePayments/routes.js, apps/dgfy-api/src/modules/commercePayments/controllers/webhookController.js, apps/dgfy-api/src/modules/commercePayments/usecases/handleWebhookUseCases.js, apps/dgfy-api/tests/commercePayments/webhookFinalize.test.js</files>
  <behavior>
    - The webhook route receives the RAW request body; the controller reads req.rawBody and the paymongo-signature header and calls verifyWebhookSignature (10-05) BEFORE any parsing/DB work; an invalid or stale signature returns 401 and does no DB work (Pitfall 1).
    - handleWebhook(event): routes by type — payment.paid → finalizePaidOrder (Task 2); payment.failed → release reservation + order/session → failed; qrph.expired → release reservation + order/session → expired (D-09); account.* and payment.refunded/payment.refund.updated → acknowledged and ignored (D-02/D-03).
    - Session resolution order: metadata commerce_payment_session reference → provider_payment_intent_id → provider_payment_id (10-05 helpers).
    - Always returns 200 for a verified event it chose to ignore; a duplicate delivery of payment.paid is safe (idempotent finalize).
  </behavior>
  <action>
Capture the raw body without breaking the existing global JSON parser: in `app.js`, pass a `verify: (req, _res, buf) => { req.rawBody = buf }` option to the existing `express.json()` so `req.rawBody` is available for the webhook while parsed `req.body` still works for every other route (least-invasive; editing apps/dgfy-api is permitted — only backend/ is off-limits). Mount `POST /v1/commerce-payments/paymongo/webhook` via the commercePayments routes. Implement the controller (signature gate first) and the event router. Do NOT trust any event that fails signature verification. Test: valid payment.paid triggers finalize; tampered body → 401 no-DB; stale timestamp → 401; qrph.expired releases reservation; refund/account events are ignored with 200; duplicate payment.paid is a no-op.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/commercePayments/webhookFinalize.test.js -t "signature|ignore|expired" 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Webhook verifies HMAC over raw body before any DB work; routes paid/failed/expired correctly; ignores refund/account events; raw-body capture does not break other routes' parsed body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: finalizePaidOrder orchestration + manual-resolution state + operator retry + expiry release</name>
  <files>apps/dgfy-api/src/modules/commercePayments/usecases/finalizePaidOrderUseCases.js, apps/dgfy-api/src/modules/commercePayments/usecases/retryFinalizationUseCases.js, apps/dgfy-api/src/modules/commercePayments/routes.js, apps/dgfy-api/src/modules/commercePayments/index.js, apps/dgfy-api/tests/commercePayments/webhookFinalize.test.js</files>
  <behavior>
    - finalizePaidOrder({ session }): natural idempotency guard first — if the landlord order/session already has availment_id or status finalized, return no-op. Else: mark session paid/paid_at; resolve tenant by session.tenant_id (UUID); call finalizeStorefrontOrder (10-07) with the order's source_reference (= order public_reference), customer/guest ref, lines, total, mapped payment_method, and the commitReservation port; on success write the returned availment_id back onto BOTH the landlord order and session and set status finalized/finalized_at.
    - If finalizeStorefrontOrder throws after the payment is paid: set order AND session status to finalize_failed_manual_resolution_required with a manual_resolution_reason — NEVER silently mark finalized or drop it (STF-05, ADR 0027 #8).
    - retryFinalization({ sessionReference }): operator endpoint that re-runs finalizePaidOrder idempotently for a session in paid or finalize_failed_manual_resolution_required state (mirror the ~20-line legacy retry usecase). Guarded to owner/operator access.
    - expireDueSessions(now): releases reservations (via the 10-02 releaseReservation/expireDueReservations port) + moves awaiting_payment sessions/orders past expires_at to expired (D-09) — the auto-release sweep.
    - Auto-release must NOT depend solely on the `qrph.expired` webhook (a missed/never-delivered webhook would otherwise strand stock). D-09 "no manual step" is satisfied by two independent mechanisms: (1) this sweep, invoked on a schedule AND opportunistically on-read; and (2) the on-read availability exclusion in 10-02 (expired-active holds don't count against availability regardless of sweep timing).
  </behavior>
  <action>
Implement the orchestration, retry usecase, and expiry-release sweep. finalizePaidOrder is landlord-durable-first then idempotent-tenant-finalize (Pattern 1/4). Mount `POST /commerce-payments/sessions/:reference/retry-finalization` behind the existing account/owner auth. Wire finalizePaidOrder, retryFinalization, expireDueSessions, and the webhook usecases into `buildCommercePaymentsModule` (inject finalizeStorefrontOrder from 10-07, the reservation release/commit ports from 10-02, and the storefront/commerce repositories).

Wire the sweep so it actually runs — do NOT leave expireDueSessions as an unreachable export: invoke it (a) on a lightweight recurring interval (a setInterval/timer started at composition, cadence via STOREFRONT_EXPIRY_SWEEP_INTERVAL_SECONDS, default 300; guarded so it no-ops when commerce payments are disabled) AND (b) opportunistically from getOrderStatus/webhook read paths for the referenced order. This closes the missed-`qrph.expired`-webhook gap so both the happy path and the missed-webhook path satisfy D-09 without any manual operator step. Document that a production scheduler/cron can replace the in-process timer later without changing the usecase.

Test: finalize writes availment_id back to order+session; a finalize throw lands the order+session in finalize_failed_manual_resolution_required (asserted, not silent); retry re-runs and finalizes idempotently; expireDueSessions releases and expires; and an expired session with NO qrph.expired webhook delivered is still reclaimed by the sweep (missed-webhook path asserted).
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/commercePayments/webhookFinalize.test.js -t "finalize|manual|retry|expire" 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Paid webhook finalizes idempotently and writes availment_id back; a finalize failure lands an explicit manual-resolution state (never silent); operator retry re-finalizes; expired sessions auto-release stock via a wired sweep (recurring interval + on-read) that does not depend on the qrph.expired webhook — missed-webhook path asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Composition wiring (mount storefront + commercePayments) + end-to-end test</name>
  <files>apps/dgfy-api/src/routes/index.js, apps/dgfy-api/tests/storefront/storefrontE2E.test.js</files>
  <action>
In `routes/index.js`, build the two new modules reusing the SAME shared instances the existing composition root already constructs (tenantConnector, businessDatabaseRegistryRepository, businessRepository, productRepository, availmentUseCases, inventory reservation ports, sequelize) — never a second divergent set (matches the Phase 8 P08 / Phase 9 composition-root convention). Build order: `buildCommercePaymentsModule` (payMongoClient, commerce repo, finalizePaidOrder wired with availments' finalizeStorefrontOrder + inventory reservation ports) then `buildStorefrontModule` (discovery, guest identity, placeOrder wired with validateCart, resolveCheckoutIdentity, reserveStock, createQrphSession, and the storefront finalize for the cash branch). Mount `router.use('/storefront', createStorefrontRoutes(...))` and `router.use('/commerce-payments', createCommercePaymentRoutes(...))`. Confirm `finalizeStorefrontOrder` is exported from the availments module composition for reuse.

Add `storefrontE2E.test.js`: with PayMongo + tenant DB mocked, drive discovery → store page → validate cart → guest OTP verify → placeOrder (QR path) → simulate a verified payment.paid webhook → assert one Availment finalized, availment_id written back, reservation committed, and a duplicate webhook produces no second Availment.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/storefront/storefrontE2E.test.js 2>&amp;1 | tail -25 &amp;&amp; npx jest --listTests 2>&amp;1 | grep -c storefront</automated>
  </verify>
  <done>Both modules mount under /storefront and /commerce-payments reusing shared instances; the end-to-end test drives browse→checkout→webhook-finalize and proves single-Availment idempotency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PayMongo → webhook | Untrusted signed inbound event; verified before any DB work |
| landlord order → tenant Availment | The interrupted-write-safe cross-DB seam |
| operator → retry endpoint | Privileged re-finalization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08-01 | Spoofing / Tampering | forged/replayed webhook | high | mitigate | HMAC over raw body + timestamp tolerance verified before any DB work; unsigned rejected in prod |
| T-10-08-02 | Tampering | duplicate webhook → double Availment/stock | high | mitigate | Natural guard (availment_id/status) + row-lock + unique source_reference (10-07) |
| T-10-08-03 | Repudiation | paid-but-unfinalized silently dropped | high | mitigate | Explicit finalize_failed_manual_resolution_required state + operator retry (never silent) |
| T-10-08-04 | Tampering | raw-body capture breaks other routes' parsing | medium | mitigate | verify-callback stashes rawBody while express.json still parses req.body for all routes |
| T-10-08-05 | Elevation | unauthorized retry-finalization | medium | mitigate | Retry endpoint behind existing account/owner auth |
| T-10-08-06 | Elevation | refund/split event acted upon | low | mitigate | account.*/refund.* explicitly ignored (D-02/D-03) |
| T-10-08-07 | Denial of Service (integrity) | missed qrph.expired webhook strands stock | medium | mitigate | Sweep wired to a recurring interval + on-read invocation, independent of the webhook; plus 10-02 on-read availability exclusion (D-09) |
| T-10-08-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- Webhook + E2E tests pass, including duplicate-webhook single-Availment and manual-resolution assertions.
- Raw-body capture verified non-breaking for existing routes.
- SUMMARY flags A6 (sandbox-validate PayMongo events before go-live).
- No `backend/` writes; no new dependency.
</verification>

<success_criteria>
A verified payment.paid webhook idempotently finalizes the order into the correct tenant Availment; interrupted/retried cross-DB writes never duplicate or lose an order; unresolved cases land in an explicit, retryable manual-resolution state. Completes STF-05 and mounts the full flow. Covers all four phase success criteria end-to-end.
</success_criteria>

<output>
Create `.planning/phases/10-storefront-discovery-online-ordering/10-08-SUMMARY.md` when done.
</output>
