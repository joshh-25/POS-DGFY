---
phase: 10-storefront-discovery-online-ordering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dgfy-migration-runner/src/migrations/schema/20260714100000-create-storefront-commerce-landlord.cjs
  - apps/dgfy-migration-runner/src/migrations/schema/20260714101000-enable-storefront-discovery-geo-search.cjs
  - apps/dgfy-migration-runner/src/schemaContracts/dgfyCoreContract.js
  - apps/dgfy-migration-runner/tests/phase10StorefrontCommerceSchema.test.js
  - apps/dgfy-api/src/models/Landlord/StorefrontGuestIdentity.js
  - apps/dgfy-api/src/models/Landlord/StorefrontOrder.js
  - apps/dgfy-api/src/models/Landlord/CommercePaymentSession.js
autonomous: true
requirements: [STF-03, STF-04, STF-05, STF-01]
must_haves:
  truths:
    - "A dgfy_core landlord order can be durably recorded before any tenant write or payment session exists."
    - "A landlord payment session row resolves its tenant by UUID and holds a single shared expires_at."
    - "A repeat guest is recognized by verified email across orders (persistent guest identity)."
    - "The storefront_discovery_index projection can be spatially and text-searched."
  artifacts:
    - "apps/dgfy-migration-runner/src/migrations/schema/20260714100000-create-storefront-commerce-landlord.cjs"
    - "apps/dgfy-migration-runner/src/migrations/schema/20260714101000-enable-storefront-discovery-geo-search.cjs"
    - "apps/dgfy-api/src/models/Landlord/StorefrontOrder.js"
    - "apps/dgfy-api/src/models/Landlord/CommercePaymentSession.js"
    - "apps/dgfy-api/src/models/Landlord/StorefrontGuestIdentity.js"
  key_links:
    - "commerce_payment_sessions.tenant_id (UUID) -> businesses/tenants resolution (never integer-coerced)"
    - "storefront_orders.public_reference UNIQUE -> idempotent lookup + Availment source_reference cross-DB guard"
    - "storefront_orders(tenant_id, target_type, idempotency_key) UNIQUE -> client idempotency"
---

<objective>
Create the Landlord-side (`dgfy_core`) schema and Sequelize models that every later Phase 10 plan writes to, plus enable spatial + full-text search on the existing `storefront_discovery_index` projection.

This plan lays the durable-first foundation for STF-05 (landlord order recorded before any tenant write), STF-04 (payment sessions), STF-03 (persistent guest identity), and STF-01 (searchable discovery projection). No behavior wires up here — this is schema + models + contract only.

Purpose: Establish the landlord commerce tables with the exact idempotency and cross-DB-reference shapes the finalize path (10-07/10-08) depends on, following the legacy `CommercePaymentSession` shape (read-only reference) minus split/refund columns.
Output: Two additive `dgfy_core` migrations, three Landlord models, an updated core schema contract, and a schema verification test.
</objective>

<execution_context>
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-storefront-discovery-online-ordering/10-CONTEXT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

# Existing migration + contract patterns to mirror (read-only for structure)
@apps/dgfy-migration-runner/src/migrations/schema/20260713120000-create-availment-checkout.cjs
@apps/dgfy-migration-runner/src/migrations/schema/20260710020000-create-dgfy-core-foundation.cjs
@apps/dgfy-migration-runner/src/schemaContracts/dgfyCoreContract.js

# Legacy landlord shape reference (READ-ONLY — never import or edit)
@backend/src/models/Landlord/CommercePaymentSession.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landlord commerce schema migration (orders, sessions, guest identities)</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260714100000-create-storefront-commerce-landlord.cjs</files>
  <action>
Create an additive `meta.targetKind: 'core'` migration (mirror the idempotent helper shape — tableExists/hasIndex/addIndexIfMissing/timestampColumns — from `20260710020000-create-dgfy-core-foundation.cjs`). Create three dgfy_core tables:

`storefront_guest_identities` (D-06): id UUID PK (default UUIDV4), verified_email STRING(255) NOT NULL, phone STRING(32) NULL (contact only, unverified — D-05), display_name STRING(255) NULL, last_order_at DATE NULL, timestamps. UNIQUE index on verified_email (`unique_storefront_guest_identities_email`) so a repeat guest maps to one persistent identity.

`storefront_orders` (STF-05 durable landlord record): id UUID PK, public_reference STRING(40) NOT NULL (opaque, e.g. `SFO-...`) UNIQUE (`unique_storefront_orders_public_reference`), tenant_id CHAR(36)/UUID NOT NULL (never integer-coerce — ADR 0027 #17), target_type STRING(40) NOT NULL default `storefront_checkout`, idempotency_key STRING(120) NOT NULL, request_hash CHAR(64) NOT NULL, status STRING(48) NOT NULL default `pending_payment` (lifecycle values documented in Task 2 model: pending_payment, awaiting_payment, finalized, expired, failed, finalize_failed_manual_resolution_required), customer_account_id CHAR(36) NULL (application-level cross-DB ref, NO FK — Domain_02 line 473/499), guest_identity_id UUID NULL (FK -> storefront_guest_identities.id, SET NULL), fulfillment_mode STRING(16) NOT NULL (`pickup`|`delivery`), fulfillment_timing STRING(16) NOT NULL (`immediate`|`scheduled`), requested_for DATE NULL, payment_method STRING(24) NOT NULL (`cash`|`gcash`|`credit_card`), checkout_payload JSON NOT NULL (validated server-side cart snapshot + contact), total_centavos INTEGER NOT NULL, availment_id INTEGER NULL (tenant Availment id written back on finalize — cross-DB ref, NO FK), expires_at DATE NULL, timestamps. Composite UNIQUE index (`unique_storefront_orders_idempotency`) on (tenant_id, target_type, idempotency_key). Index on status.

`commerce_payment_sessions` (STF-04/D-01): id UUID PK, public_reference STRING(40) NOT NULL UNIQUE (`unique_commerce_payment_sessions_reference`, e.g. `CPS-...`), storefront_order_id UUID NOT NULL (FK -> storefront_orders.id), tenant_id CHAR(36)/UUID NOT NULL, status STRING(40) NOT NULL default `awaiting_payment` (awaiting_payment, paid, finalized, expired, failed, finalize_failed_manual_resolution_required), provider STRING(24) NOT NULL default `paymongo`, provider_payment_intent_id STRING(80) NULL, provider_payment_id STRING(80) NULL, qr_code_image_url TEXT NULL, amount_centavos INTEGER NOT NULL, expires_at DATE NULL (the ONE shared clock, D-08), paid_at DATE NULL, finalized_at DATE NULL, manual_resolution_reason TEXT NULL, split_payload JSON NULL, platform_fee_centavos INTEGER NULL, timestamps. Add indexes on provider_payment_intent_id and provider_payment_id (session-resolution lookups). The split_payload/platform_fee_centavos columns are nullable and unpopulated in Phase 10 — they exist ONLY so a future phase can add per-tenant split without a migration (D-02 "keep adjustable"); nothing in Phase 10 requires or writes them.

Cross-database reference columns (tenant_id, customer_account_id, availment_id) carry NO foreign key — MySQL cannot enforce a FK across two databases. Document this in the file header comment by concept (do not embed any legacy directory path substring in comments — see the Phase 8 P02 note in STATE.md). Provide a `down()` that drops the three tables in FK-safe order.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner &amp;&amp; npx jest tests/phase10StorefrontCommerceSchema.test.js -t "landlord commerce tables" 2>&amp;1 | tail -20</automated>
  </verify>
  <done>Migration creates storefront_guest_identities, storefront_orders, commerce_payment_sessions with the unique idempotency index, unique public references, UUID tenant_id, and nullable split columns; `meta.targetKind` is `core`; down() drops all three.</done>
</task>

<task type="auto">
  <name>Task 2: Discovery geo/search enablement migration + Landlord models + core contract update</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260714101000-enable-storefront-discovery-geo-search.cjs, apps/dgfy-api/src/models/Landlord/StorefrontGuestIdentity.js, apps/dgfy-api/src/models/Landlord/StorefrontOrder.js, apps/dgfy-api/src/models/Landlord/CommercePaymentSession.js, apps/dgfy-migration-runner/src/schemaContracts/dgfyCoreContract.js</files>
  <action>
(a) Discovery geo/search migration (`meta.targetKind: 'core'`, additive, idempotent): the REAL `storefront_discovery_index` (created Phase 2) stores geo inside `location_snapshot` JSON and searchable text inside `search_snapshot` JSON — there is NO `geo_store_items` table and NO lat/lng columns (resolves RESEARCH Open Q3). Add MySQL GENERATED ALWAYS AS ... STORED columns extracted from the JSON: `latitude` DECIMAL(10,7) AS (JSON_EXTRACT(location_snapshot,'$.latitude')), `longitude` DECIMAL(10,7) AS (JSON_EXTRACT(location_snapshot,'$.longitude')), and `search_text` TEXT AS (JSON_UNQUOTE(JSON_EXTRACT(search_snapshot,'$.text'))). Add a composite btree index on (latitude, longitude) and a FULLTEXT index on search_text (`ftx_storefront_discovery_search_text`). Guard each ADD with an existence check so re-runs are safe. down() drops the added indexes and generated columns. If the running MySQL version rejects a generated-column expression, document the exact JSON path used so the discovery repository (10-03) can fall back to reading location_snapshot directly.

(b) Create three Landlord Sequelize models (`StorefrontGuestIdentity`, `StorefrontOrder`, `CommercePaymentSession`) following the existing `apps/dgfy-api/src/models/Landlord/*.js` define(sequelize) export convention (see Business.js for the tableName/underscored/timestamps pattern). Each model documents its status-lifecycle enum values as JSDoc and its cross-DB (no-FK) columns. Do not register them yet — composition wiring is 10-08.

(c) Update `dgfyCoreContract.js`: remove `storefront_orders` and `storefront_carts` from `rejectedTables` (Phase 10 legitimately introduces landlord operational commerce tables now), and add `storefront_guest_identities`, `storefront_orders`, `commerce_payment_sessions` plus the discovery generated columns/indexes to the contract `tables` map so the runner's own schema verification recognizes them.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner &amp;&amp; npx jest tests/phase10StorefrontCommerceSchema.test.js 2>&amp;1 | tail -25 &amp;&amp; cd ../.. &amp;&amp; node -e "import('./apps/dgfy-api/src/models/Landlord/StorefrontOrder.js').then(m=>console.log(typeof m.default))"</automated>
  </verify>
  <done>Discovery index gains latitude/longitude/search_text generated columns + spatial and FULLTEXT indexes; three Landlord models load and export a define function; dgfyCoreContract no longer rejects storefront_orders and recognizes the three new tables. Schema test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Schema verification test scaffold</name>
  <files>apps/dgfy-migration-runner/tests/phase10StorefrontCommerceSchema.test.js</files>
  <action>
Create the verification test mirroring `tests/phase08CommerceFoundationSchema.test.js`. Assert: the two Phase 10 migrations declare `meta.targetKind: 'core'`; running up() against a test dgfy_core creates the three tables with the documented columns; the unique idempotency index (tenant_id, target_type, idempotency_key), unique public_reference indexes, and UUID tenant_id are present; commerce_payment_sessions has nullable split_payload/platform_fee_centavos; storefront_discovery_index exposes latitude/longitude/search_text plus the spatial and FULLTEXT indexes; and dgfyCoreContract no longer lists storefront_orders/storefront_carts in rejectedTables. Gate any live-DB portions behind the same env flag the existing phase08 schema test uses; the structural (metadata/contract) assertions must run without a DB.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner &amp;&amp; npx jest tests/phase10StorefrontCommerceSchema.test.js 2>&amp;1 | tail -15</automated>
  </verify>
  <done>Test file exists and its non-DB structural assertions pass; contract reject-list assertion passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration-runner → dgfy_core | Schema DDL applied to the shared landlord database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-01 | Tampering | commerce_payment_sessions.tenant_id | high | mitigate | UUID column, never integer-coerced (ADR 0027 #17); resolution keyed on UUID so a coerced/wrong tenant cannot be selected |
| T-10-01-02 | Tampering | storefront_orders idempotency | high | mitigate | UNIQUE (tenant_id, target_type, idempotency_key) + UNIQUE public_reference make duplicate-order collisions a DB-enforced constraint, not app-only |
| T-10-01-03 | Elevation | split_payload columns present but unused | low | accept | Columns nullable and never populated in Phase 10 (D-02); no code path reads them, so no split can be silently applied |
| T-10-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced (RESEARCH Package Legitimacy Audit: none) — no install step |
</threat_model>

<verification>
- Both migrations declare `meta.targetKind: 'core'` and are additive (destructive:false).
- `phase10StorefrontCommerceSchema.test.js` passes.
- No file under `backend/` is modified (git diff shows only apps/dgfy-migration-runner and apps/dgfy-api paths).
- No new dependency added to any package.json.
</verification>

<success_criteria>
Landlord commerce tables + discovery geo/search exist as additive dgfy_core migrations; three Landlord models load; core contract updated; schema test green.
</success_criteria>

<output>
Create `.planning/phases/10-storefront-discovery-online-ordering/10-01-SUMMARY.md` when done.
</output>
