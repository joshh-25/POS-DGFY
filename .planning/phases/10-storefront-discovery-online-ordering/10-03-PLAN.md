---
phase: 10-storefront-discovery-online-ordering
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/dgfy-api/src/modules/storefront/index.js
  - apps/dgfy-api/src/modules/storefront/routes.js
  - apps/dgfy-api/src/modules/storefront/controllers/discoveryController.js
  - apps/dgfy-api/src/modules/storefront/usecases/searchDiscoveryUseCases.js
  - apps/dgfy-api/src/modules/storefront/usecases/getStorePageUseCases.js
  - apps/dgfy-api/src/modules/storefront/usecases/cartValidation.js
  - apps/dgfy-api/src/modules/storefront/repositories/storefrontDiscoveryRepository.js
  - apps/dgfy-api/tests/storefront/discovery.test.js
  - apps/dgfy-api/tests/storefront/cartValidation.test.js
autonomous: true
requirements: [STF-01, STF-02]
must_haves:
  truths:
    - "A consumer can search nearby stores by location and text and get distance-ranked results (STF-01)."
    - "A consumer can retrieve a specific store's page and product listing (STF-02)."
    - "A cart payload is validated server-side against the live catalog with money recomputed in centavos, never trusting client totals."
  artifacts:
    - "apps/dgfy-api/src/modules/storefront/usecases/searchDiscoveryUseCases.js"
    - "apps/dgfy-api/src/modules/storefront/usecases/getStorePageUseCases.js"
    - "apps/dgfy-api/src/modules/storefront/usecases/cartValidation.js"
  key_links:
    - "discovery search -> storefront_discovery_index generated geo/search columns (from 10-01) with Redis cache + DB fallback"
    - "store page -> products module catalog (Phase 8) for the listing"
    - "cartValidation -> products catalog + availments money.js for centavo recompute"
---

<objective>
Create the public storefront discovery/search surface (STF-01), the store-page + product-listing read (STF-02), and the server-side cart validator that every checkout path reuses. This plan also scaffolds the new `apps/dgfy-api/src/modules/storefront` module that 10-04 and 10-06 extend.

Purpose: Deliver the browse → store page → cart half of the phase against the REAL `storefront_discovery_index` shape (geo in generated columns from 10-01, catalog from Phase 8 products), with Redis caching and a hard rule that money is recomputed server-side.
Output: `storefront` module skeleton (routes/index/controllers), discovery search + store page usecases, a reusable cart validator, discovery repository with Redis-cached geo/text search, and tests.
</objective>

<execution_context>
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-storefront-discovery-online-ordering/10-CONTEXT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

# Existing module conventions to mirror + assets to reuse
@apps/dgfy-api/src/modules/products/index.js
@apps/dgfy-api/src/config/redis.js
@apps/dgfy-api/src/modules/availments/usecases/money.js

# Legacy geo/search + cart pattern (READ-ONLY reference)
@backend/src/modules/geoSearch/repositories/geoSearchRepository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storefront module skeleton + discovery repository (geo/text search + Redis cache)</name>
  <files>apps/dgfy-api/src/modules/storefront/index.js, apps/dgfy-api/src/modules/storefront/routes.js, apps/dgfy-api/src/modules/storefront/controllers/discoveryController.js, apps/dgfy-api/src/modules/storefront/repositories/storefrontDiscoveryRepository.js</files>
  <action>
Create the storefront module following the `products` module composition convention (index.js `buildStorefrontModule({...deps})` returning { useCases, ... }; routes.js `createStorefrontRoutes(useCases, { authenticateAccount })`). The module builds against the landlord dgfy_core connection (config/db.js sequelize) — discovery reads `storefront_discovery_index`.

Implement `storefrontDiscoveryRepository`:
- `searchNearby({ lat, lng, radiusKm, query, category, openNow, limit, offset })`: raw SQL over `storefront_discovery_index` using `ST_Distance_Sphere(POINT(longitude, latitude), POINT(:lng, :lat))/1000 AS distance_km` against the GENERATED latitude/longitude columns added in 10-01, `WHERE is_visible = 1` and distance <= radiusKm, ordered by distance. When `query` is present, add `MATCH(search_text) AGAINST(:q IN BOOLEAN MODE)` (sanitize boolean operators like the legacy repo). `category`/`openNow` filter on the projected snapshot fields. Parameterize every value (no string interpolation of user input — V5).
- Redis cache: wrap results with the existing `config/redis.js` client and a TTL from `GEO_SEARCH_REDIS_CACHE_TTL_SECONDS` when `GEO_SEARCH_REDIS_CACHE_ENABLED`/`STOREFRONT_DISCOVERY_REDIS_CACHE_ENABLED` is set; on cache miss OR any Redis error, fall back to the direct DB query (never fail the request on cache).
- `getStoreByHandle(handle)`: returns the visible discovery row (or null) for the store page.

Mount public read-only routes in routes.js: `GET /storefront/discovery/search`, `GET /storefront/stores/:handle`. Apply `express-rate-limit` to the discovery endpoints (public scraping/DoS guard).
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/storefront/discovery.test.js 2>&amp;1 | tail -20</automated>
  </verify>
  <done>storefront module builds; discovery search returns distance-ranked visible stores with optional text/category/open-now filters; Redis cache degrades to DB on miss/error; routes are rate-limited and public.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store page + product listing usecase, and server-side cart validator</name>
  <files>apps/dgfy-api/src/modules/storefront/usecases/searchDiscoveryUseCases.js, apps/dgfy-api/src/modules/storefront/usecases/getStorePageUseCases.js, apps/dgfy-api/src/modules/storefront/usecases/cartValidation.js, apps/dgfy-api/tests/storefront/discovery.test.js, apps/dgfy-api/tests/storefront/cartValidation.test.js</files>
  <behavior>
    - searchDiscovery({lat,lng,...}): validates inputs (lat/lng ranges, radius/limit bounds), delegates to the repository, returns opaque store references (handle/display_name/distance) — never raw sequential landlord ids (IDOR guard, V4).
    - getStorePage({handle}): returns the store discovery row + its product listing pulled from the Phase 8 products module (tenant catalog resolved via the store's tenant_id); returns 404 for an unknown/invisible handle.
    - validateCart({tenantId, lines}): for each line resolves the product from the tenant catalog, rejects unknown/unavailable/inactive products, snapshots name + unit price from the catalog (never the client), computes line and cart totals in centavos via availments money.js, returns { normalizedLines, totalCentavos }. Rejects empty carts and non-positive quantities. This is the single validator both cash and PayMongo checkout reuse in 10-06.
  </behavior>
  <action>
Implement the two read usecases and the cart validator. `getStorePage` reads product listings through the existing products module repository (inject `productRepository`), resolving the store's tenant via its discovery row's business_id/tenant_id. `validateCart` recomputes ALL money server-side in centavos using `availments/usecases/money.js` (reuse `roundHalfUp`) — the client-supplied total is ignored/validated-against, never trusted (V5, Pitfall: client-tampered cart total). Wire all three into `buildStorefrontModule`. Expose `validateCart` from the module so 10-06 imports the same function.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/storefront/cartValidation.test.js tests/storefront/discovery.test.js 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Store page returns catalog listing (404 for unknown handle); validateCart snapshots price from catalog, recomputes centavo totals, rejects tampered/empty/invalid carts; validateCart exported for reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer (unauthenticated) → discovery/store API | Public read-only endpoints |
| consumer → cart validation | Untrusted cart payload crosses into pricing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-01 | Tampering | client-supplied cart total | high | mitigate | validateCart recomputes every amount in centavos from catalog snapshots; client total never persisted |
| T-10-03-02 | Information Disclosure | IDOR on store identity | medium | mitigate | Expose opaque handles only; never raw sequential discovery ids; tenant-scoped catalog reads |
| T-10-03-03 | Denial of Service | discovery endpoint scraping | medium | mitigate | Redis cache + express-rate-limit on public discovery routes |
| T-10-03-04 | Tampering | SQL injection via search/geo params | high | mitigate | Fully parameterized raw SQL; boolean-mode search operators sanitized (legacy pattern) |
| T-10-03-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- Discovery + cart tests pass.
- Public routes are rate-limited; cache miss falls back to DB.
- No `backend/` writes; no new dependency.
</verification>

<success_criteria>
Consumer can search stores by map/location + text and open a store page with its product listing; every cart is validated and re-priced server-side. Covers STF-01, STF-02.
</success_criteria>

<output>
Create `.planning/phases/10-storefront-discovery-online-ordering/10-03-SUMMARY.md` when done.
</output>
