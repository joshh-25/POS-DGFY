---
phase: 10-storefront-discovery-online-ordering
plan: 07
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - apps/dgfy-migration-runner/src/migrations/schema/20260714103000-add-availment-source-reference.cjs
  - apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js
  - apps/dgfy-api/src/models/Tenant/Availment.js
  - apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js
  - apps/dgfy-api/src/modules/availments/usecases/storefrontFinalizeUseCases.js
  - apps/dgfy-api/src/modules/availments/index.js
  - apps/dgfy-api/tests/availments/storefrontFinalize.test.js
autonomous: true
requirements: [STF-05]
must_haves:
  truths:
    - "A storefront order finalizes into a tenant Availment WITHOUT an open shift, terminal, or cashier (the new non-POS finalize path)."
    - "Finalize creates the Availment with a customer_account_id application-level cross-DB reference (no FK) and a unique source_reference keyed to the landlord order."
    - "Finalize converts the stock reservation into a sale effect through modules/inventory in the SAME tenant transaction (ADR 0029), never a direct stock write."
    - "Finalize is idempotent: a second call for the same landlord order reference is a no-op returning the existing Availment (row-lock + unique source_reference)."
  artifacts:
    - "apps/dgfy-api/src/modules/availments/usecases/storefrontFinalizeUseCases.js"
    - "apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js (source_reference + storefront finalize method)"
  key_links:
    - "finalizeStorefrontOrder -> one tenant transaction: Availment + Payment + reservation→commit (recordSale) + unique source_reference"
    - "source_reference UNIQUE per business -> cross-DB duplicate-webhook race collapses to one Availment"
    - "payment_method mapping -> tenant payments ENUM (gcash/credit_card/cash)"
---

<objective>
Build the genuinely-new seam of this phase: a storefront-order finalize path in the availments module that creates a tenant Availment from a paid (or cash) online order — with no shift/terminal/cashier, a cross-DB customer reference, and a reservation→sale conversion — all inside one idempotent tenant transaction. Phase 9's `finalizeAvailment` CANNOT be reused (it hard-requires open shift + terminal + cashier + the `pos.checkout` gate — RESEARCH structural gap #1).

Purpose: Give 10-06 (cash) and 10-08 (webhook) a single tenant-side finalize entry point that mirrors Phase 9's transaction discipline but drops POS-only preconditions and adds the cross-DB idempotency guard (unique source_reference).
Output: A `source_reference` column + unique index on the tenant Availment, a `finalizeStorefrontOrder` repository method + usecase, and tests.

Assumptions to confirm during execution: [ASSUMED A4] QR Ph rail maps to the tenant payments ENUM as gcash/credit_card (no ENUM migration); the PayMongo `pay_...` id is stored as a reference field/metadata. [ASSUMED A5] the online storefront checkout uses a compliance operation distinct from `pos.checkout` (or the gate is relaxed online) — confirm with the compliance-policy owner before finalize goes live (Open Q2). Surface both in the SUMMARY.
</objective>

<execution_context>
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-CONTEXT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-RESEARCH.md
@apps/dgfy-api/src/modules/availments/index.js
@apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js
@apps/dgfy-api/src/modules/availments/usecases/availmentUseCases.js
@apps/dgfy-api/src/modules/availments/usecases/money.js
@apps/dgfy-api/src/models/Tenant/Payment.js
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md

# Legacy finalize discipline (READ-ONLY reference)
@backend/src/modules/commercePayments/usecases/finalizePaidCommerceSession.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source_reference to tenant Availment (migration + model) for cross-DB idempotency</name>
  <files>apps/dgfy-migration-runner/src/migrations/schema/20260714103000-add-availment-source-reference.cjs, apps/dgfy-api/src/models/Tenant/Availment.js, apps/dgfy-migration-runner/src/schemaContracts/dgfyBusinessContract.js</files>
  <action>
Additive `meta.targetKind: 'business'` migration adding a nullable `source_reference` STRING(64) column to the tenant `availments` table plus a UNIQUE index (`unique_availments_source_reference`) — this is the belt-and-suspenders (c) guard from RESEARCH Pitfall 2 so even a lost-guard duplicate-webhook race collapses to one Availment row. POS/Phase-9 availments leave it NULL (unique index permits multiple NULLs in MySQL). Add the column to the `Availment` Tenant model and the `dgfyBusinessContract`. Idempotent up()/down() following the existing add-column migration style.
  </action>
  <verify>
    <automated>cd apps/dgfy-migration-runner &amp;&amp; npx jest -t "source_reference" 2>&amp;1 | tail -15</automated>
  </verify>
  <done>availments gains a nullable source_reference + unique index; model and business contract updated; migration is additive with targetKind business.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: finalizeStorefrontOrder repository method + usecase (non-POS, idempotent, reservation→sale)</name>
  <files>apps/dgfy-api/src/modules/availments/repositories/availmentRepository.js, apps/dgfy-api/src/modules/availments/usecases/storefrontFinalizeUseCases.js, apps/dgfy-api/src/modules/availments/index.js, apps/dgfy-api/tests/availments/storefrontFinalize.test.js</files>
  <behavior>
    - finalizeStorefrontOrder({ businessId, sourceReference, customerAccountId, lines, totalCentavos, paymentMethod, paymentReference, fulfillmentMode, requestedFor, commitReservation }): inside ONE tenant transaction — (a) row-locked lookup by (business_id, source_reference); if an Availment already exists for that source_reference, return it (idempotent no-op); (b) create a finalized Availment with customer_account_id (cross-DB ref, no FK), source_reference, fulfillment_mode/requested_for, NO shift_id/terminal_id/cashier fields; (c) create the Payment row with payment_method mapped to the tenant ENUM (gcash/credit_card/cash — A4) and the PayMongo pay-reference stored in a reference field/metadata; (d) commit the stock reservation → sale effect via the injected commitReservation port (10-02) threaded on the SAME transaction — a sale-effect failure rolls back the entire finalize (atomicity, mirroring Phase 9 P04/P06). Money is recomputed/verified in centavos.
    - Idempotent under concurrent duplicate calls: the unique source_reference makes a lost-guard race collapse to one row (the second insert fails the unique constraint → caught → return existing).
    - No open-shift / terminal / cashier requirement; does NOT invoke the pos.checkout gate (A5 — uses the storefront compliance operation or relaxed gate, confirmed with compliance owner).
  </behavior>
  <action>
Implement the repository method and usecase. Reuse `money.js` for centavo recompute and mirror `availmentRepository`'s existing `sequelize.transaction` + `LOCK.UPDATE` discipline. commitReservation (from 10-02) MUST be the sole stock-writer path (ADR 0029) — never write InventoryMovement/stock_count here directly. Map payment_method per A4 to avoid a tenant ENUM migration (Pitfall 6). Wire `finalizeStorefrontOrder` into `buildAvailmentsModule` (inject the reservation `commitReservation` port) and export it for 10-06 (cash) and 10-08 (webhook). Test: fresh finalize creates Availment+Payment and commits the reservation; a second call for the same source_reference returns the same Availment without a second stock deduction; a sale-effect failure rolls back (no Availment persisted). Add a code comment recording that the folded compliance-transaction todo is re-deferred here (see SUMMARY rationale) — it is Phase 8/9 compliance-state-write debt, not storefront finalize scope.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/availments/storefrontFinalize.test.js 2>&amp;1 | tail -25</automated>
  </verify>
  <done>finalizeStorefrontOrder creates a non-POS Availment with customer_account_id + unique source_reference, converts reservation→sale through Inventory in one atomic transaction, is idempotent under duplicates, and requires no shift/terminal/cashier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| landlord order → tenant Availment | The cross-DB finalize seam |
| finalize → inventory | Reservation-to-sale conversion (single-writer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07-01 | Tampering | duplicate/replayed finalize → two Availments | high | mitigate | Row-lock + natural guard + UNIQUE source_reference (belt-and-suspenders, Pitfall 2) |
| T-10-07-02 | Elevation | direct stock write bypassing Inventory | high | mitigate | Reservation→sale via injected commitReservation (recordSale) only; no direct stock write (ADR 0029) |
| T-10-07-03 | Tampering | partial finalize (Availment without sale) | high | mitigate | Single tenant transaction; sale-effect failure rolls the whole finalize back (atomicity) |
| T-10-07-04 | Tampering | payment_method ENUM violation | medium | mitigate | Map QR Ph rail to gcash/credit_card (A4); pay-reference stored separately, no ENUM migration |
| T-10-07-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- Storefront finalize tests pass (fresh, idempotent-duplicate, rollback-on-sale-failure).
- No direct stock write asserted (commit goes through the injected port).
- SUMMARY flags A4/A5 and records the folded-todo re-defer rationale.
- No `backend/` writes; no new dependency.
</verification>

<success_criteria>
A paid or cash storefront order can be finalized into the correct tenant Availment idempotently, with reservation→sale conversion and no POS preconditions — the tenant-side half of STF-05.
</success_criteria>

<output>
Create `.planning/phases/10-storefront-discovery-online-ordering/10-07-SUMMARY.md` when done.
</output>
