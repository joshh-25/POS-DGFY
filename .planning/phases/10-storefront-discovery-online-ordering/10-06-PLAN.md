---
phase: 10-storefront-discovery-online-ordering
plan: 06
type: execute
wave: 4
depends_on: ["10-04", "10-05", "10-02", "10-03"]
files_modified:
  - apps/dgfy-api/src/config/env.js
  - apps/dgfy-api/src/modules/storefront/usecases/schedulingValidation.js
  - apps/dgfy-api/src/modules/storefront/usecases/placeOrderUseCases.js
  - apps/dgfy-api/src/modules/storefront/usecases/getOrderStatusUseCases.js
  - apps/dgfy-api/src/modules/storefront/repositories/storefrontOrderRepository.js
  - apps/dgfy-api/src/modules/storefront/controllers/checkoutController.js
  - apps/dgfy-api/src/modules/storefront/routes.js
  - apps/dgfy-api/src/modules/storefront/index.js
  - apps/dgfy-api/tests/storefront/scheduling.test.js
  - apps/dgfy-api/tests/storefront/placeOrder.test.js
autonomous: true
requirements: [STF-04, STF-05]
must_haves:
  truths:
    - "A consumer can choose pickup/delivery and immediate/scheduled, and a scheduled time is validated against business hours + min lead time + max advance window (D-11..D-13)."
    - "Placement records a durable landlord order FIRST, then reserves stock (tenant write, fail-fast to 503 if unreachable), THEN creates the PayMongo session — never a payment session for stock that could not be reserved (D-10)."
    - "Reservation and session share ONE expires_at (D-08)."
    - "A duplicate placement with the same idempotency_key + identical payload safely replays; a reused key with a different payload is a 409 conflict (STF-05)."
    - "A cash order takes the no-session branch; a GCash/Card order returns a QR + order reference."
  artifacts:
    - "apps/dgfy-api/src/modules/storefront/usecases/placeOrderUseCases.js"
    - "apps/dgfy-api/src/modules/storefront/usecases/schedulingValidation.js"
    - "apps/dgfy-api/src/modules/storefront/repositories/storefrontOrderRepository.js"
  key_links:
    - "placeOrder -> validateCart (10-03) -> resolveCheckoutIdentity (10-04) -> reserveStock (10-02) -> createQrphSession (10-05)"
    - "idempotency_key + request_hash -> storefront_orders unique index (safe replay / 409 on mismatch)"
    - "shared expires_at -> setReservationExpiry (10-02) + session (10-05)"
---

<objective>
Assemble the order-placement flow (STF-04 fulfillment/scheduling/payment-method + STF-05 landlord-first durable record). This is the orchestration seam that enforces the strict ordering D-10 requires and stamps the single shared clock D-08 requires.

Purpose: One `placeOrder` usecase that composes the validated cart (10-03), resolved identity (10-04), atomic reservation (10-02), and PayMongo session (10-05) in the exact fail-fast order, plus scheduling validation and a consumer order-status read. Finalization is NOT here — it happens on webhook (10-08).
Output: Scheduling validator (env-configurable bounds), storefront order repository, placeOrder + getOrderStatus usecases, checkout routes, and tests.

Assumptions to confirm during execution: [ASSUMED A1] min lead time = 30 min (STOREFRONT_MIN_LEAD_MINUTES); [ASSUMED A2] max advance window = 14 days (STOREFRONT_MAX_ADVANCE_DAYS); [ASSUMED A3] cash online orders finalize immediately at placement (Open Q1). Surface all three in the SUMMARY for product confirmation.
</objective>

<execution_context>
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-CONTEXT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-RESEARCH.md
@apps/dgfy-api/src/models/Landlord/StorefrontOrder.js
@apps/dgfy-api/src/config/env.js

# Legacy scheduling + idempotency pattern (READ-ONLY reference)
@backend/src/modules/store/usecases/storeUseCases.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scheduling/fulfillment validator + storefront order repository (idempotency)</name>
  <files>apps/dgfy-api/src/modules/storefront/usecases/schedulingValidation.js, apps/dgfy-api/src/modules/storefront/repositories/storefrontOrderRepository.js, apps/dgfy-api/src/config/env.js, apps/dgfy-api/tests/storefront/scheduling.test.js</files>
  <behavior>
    - validateFulfillment({ fulfillmentMode, fulfillmentTiming, requestedFor, businessHours, now }): mode must be pickup|delivery; timing immediate|scheduled. For scheduled: requestedFor is a valid future ISO within business hours (D-12), at least STOREFRONT_MIN_LEAD_MINUTES ahead (default 30, A1), and at most STOREFRONT_MAX_ADVANCE_DAYS ahead (default 14, A2). No capacity check (D-11). Returns normalized requested_for or a typed validation error naming which bound failed.
    - storefrontOrderRepository.createOrder(...) inserts a pending_payment order; findByIdempotency({tenant_id, target_type, idempotency_key}) returns an existing order; updateOrder(id, patch); findByPublicReference(ref).
    - request_hash: sha256 over a stable-stringified normalized payload; on idempotency hit with a matching hash -> return existing (safe replay); mismatched hash -> 409 conflict.
  </behavior>
  <action>
Implement the scheduling validator and the landlord order repository. Add STOREFRONT_MIN_LEAD_MINUTES (default 30) and STOREFRONT_MAX_ADVANCE_DAYS (default 14) to `config/env.js` — tag both [ASSUMED] in code comments as product-confirmable (A1/A2). The idempotency lookup relies on the UNIQUE (tenant_id, target_type, idempotency_key) index from 10-01; on an insert unique-violation race, re-select and apply the same replay/conflict logic. Test scheduling bounds (in-hours ok, out-of-hours rejected, below-lead rejected, beyond-advance rejected) and idempotency (replay vs 409-on-different-payload).
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/storefront/scheduling.test.js 2>&amp;1 | tail -25</automated>
  </verify>
  <done>Scheduling validator enforces business-hours + lead + advance bounds (env-configurable, no capacity); order repo supports idempotent lookup with safe-replay / 409-on-mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: placeOrder orchestration (durable-first → reserve → session) + getOrderStatus + routes</name>
  <files>apps/dgfy-api/src/modules/storefront/usecases/placeOrderUseCases.js, apps/dgfy-api/src/modules/storefront/usecases/getOrderStatusUseCases.js, apps/dgfy-api/src/modules/storefront/controllers/checkoutController.js, apps/dgfy-api/src/modules/storefront/routes.js, apps/dgfy-api/src/modules/storefront/index.js, apps/dgfy-api/tests/storefront/placeOrder.test.js</files>
  <behavior>
    - placeOrder({ idempotencyKey, payload, authenticatedAccountId }): (0) idempotency short-circuit; (1) validateCart -> normalized lines + totalCentavos (server-side money, 10-03); (2) resolveCheckoutIdentity -> customer_account_id | guest_identity_id (10-04); (3) validateFulfillment; (4) DURABLE landlord order insert (status pending_payment, request_hash, checkout_payload, requested_for) FIRST; (5) reserveStock via inventory port (10-02) — on TenantDatabaseUnavailableError return 503 and do NOT proceed (D-10); (6a) if payment_method is gcash/credit_card: createQrphSession (10-05), stamp the session's shared expires_at onto the reservation via setReservationExpiry (D-08), set order status awaiting_payment, return { order_reference, qr_code_image_url, expires_at }; (6b) if cash: [ASSUMED A3] finalize immediately at placement via the storefront finalize path (10-07) — commit reservation → sale, create Availment, order status finalized — returning { order_reference, status: finalized }.
    - Strict ordering: no PayMongo session is ever created before the reservation succeeds (Pitfall 4).
    - getOrderStatus({ reference, requesterEmail? }): returns the order status by opaque public_reference, tenant-scoped; a guest may read only their own order (optional email binding) — never expose sequential ids (IDOR, V4).
  </behavior>
  <action>
Implement placeOrder composing the injected ports in the exact D-10 order. For the cash branch, call the storefront finalize usecase from 10-07 (injected — this plan depends on 10-07 being available at composition time in 10-08; for isolated testing, inject a mock finalize). Server recomputes all money; the client total is validated-against, never trusted. Add routes: `POST /storefront/checkout` (optional authenticateAccount), `GET /storefront/orders/:reference`. Rate-limit the checkout endpoint. Wire everything into `buildStorefrontModule`. Test the happy PayMongo path (order persists before session, shared expires_at on both), the fail-fast path (reserve throws -> 503, no session created), idempotent replay, and the cash immediate-finalize path with mocks.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/storefront/placeOrder.test.js 2>&amp;1 | tail -25</automated>
  </verify>
  <done>placeOrder records the landlord order before reserving, fails fast to 503 on tenant-DB-unreachable without creating a session, shares one expires_at across reservation+session, replays idempotently, and takes the cash-vs-QR branch correctly; getOrderStatus is IDOR-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → checkout | Untrusted cart/contact/schedule payload |
| api → tenant DB (reserve) | First tenant write; fail-fast boundary (D-10) |
| api → PayMongo (session) | Only after reservation succeeds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06-01 | Tampering | client cart total / amount | high | mitigate | Server recompute in centavos (validateCart); total_centavos > 0 asserted before session |
| T-10-06-02 | Tampering | duplicate placement double-charge | high | mitigate | idempotency_key + request_hash unique index; safe replay / 409 on payload mismatch |
| T-10-06-03 | Tampering (integrity) | orphaned payment for unreservable stock | high | mitigate | Strict order: durable order → reserve (fail-fast 503) → session; no session before reservation (D-10, Pitfall 4) |
| T-10-06-04 | Tampering | reservation/session clock drift | medium | mitigate | Single shared expires_at stamped on both (D-08) |
| T-10-06-05 | Information Disclosure | IDOR on order status | medium | mitigate | Opaque public_reference, tenant-scoped, optional guest email binding |
| T-10-06-06 | Input Validation | out-of-bounds scheduled time | medium | mitigate | Business-hours + lead + advance validation (D-11..D-13) |
| T-10-06-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- Placement + scheduling tests pass, including fail-fast-no-session and idempotent-replay.
- Shared expires_at asserted equal on reservation and session in the happy path.
- SUMMARY flags A1/A2/A3 for product confirmation.
- No `backend/` writes; no new dependency.
</verification>

<success_criteria>
Consumer can place a pickup/delivery, immediate/scheduled, cash-or-QR order that is durably recorded landlord-first with stock reserved on a shared clock. Covers STF-04 and the landlord-first half of STF-05.
</success_criteria>

<output>
Create `.planning/phases/10-storefront-discovery-online-ordering/10-06-SUMMARY.md` when done.
</output>
