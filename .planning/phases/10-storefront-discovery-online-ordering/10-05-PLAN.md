---
phase: 10-storefront-discovery-online-ordering
plan: 05
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/dgfy-api/src/config/env.js
  - apps/dgfy-api/src/modules/commercePayments/index.js
  - apps/dgfy-api/src/modules/commercePayments/services/payMongoClient.js
  - apps/dgfy-api/src/modules/commercePayments/repositories/commercePaymentRepository.js
  - apps/dgfy-api/src/modules/commercePayments/usecases/createQrphSessionUseCases.js
  - apps/dgfy-api/tests/commercePayments/payMongoClient.test.js
  - apps/dgfy-api/tests/commercePayments/createQrphSession.test.js
autonomous: true
requirements: [STF-04]
must_haves:
  truths:
    - "A QR Ph payment session is created via a real PayMongo Payment Intent (dynamic QR), returning a QR image and a shared expires_at (D-01, D-08)."
    - "No 1% platform-fee split is applied — a single DGFY account collects the full amount — but the design can add split later via a provider-side arg with no schema migration (D-02)."
    - "The webhook signature can be verified with HMAC-SHA256 over the raw body using a timing-safe comparison (V6)."
    - "If PayMongo is unconfigured, QR Ph session creation returns 503 rather than a broken session."
  artifacts:
    - "apps/dgfy-api/src/modules/commercePayments/services/payMongoClient.js"
    - "apps/dgfy-api/src/modules/commercePayments/usecases/createQrphSessionUseCases.js"
    - "apps/dgfy-api/src/modules/commercePayments/repositories/commercePaymentRepository.js"
  key_links:
    - "createQrphSession -> PayMongo 3-call dance (intent -> method -> attach) via native fetch"
    - "session persistence -> commerce_payment_sessions (10-01) with shared expires_at + provider ids"
    - "verifyWebhookSignature -> crypto HMAC over rawBody, timing-safe + timestamp tolerance"
---

<objective>
Build the landlord-owned PayMongo QR Ph payment layer (D-01): a native-`fetch` PayMongo client (Payment Intent 3-call dance + raw-body signature verification), a landlord session repository over `commerce_payment_sessions`, and the createQrphSession usecase. No webhook processing or finalize here — 10-08 consumes this. Split is deliberately excised (D-02) by simply omitting the provider `split_payment` arg.

Purpose: Isolate the real payment integration and its config gate so the placement flow (10-06) can call `createQrphSession` and the webhook (10-08) can call `verifyWebhookSignature` and session-resolution helpers, all against a single DGFY PayMongo account.
Output: PayMongo client service, commerce payment repository (create + resolve-by-reference/intent/payment), createQrphSession usecase, env config + 503 gate, and tests. [ASSUMED A6] PayMongo event names/response shapes are read from legacy code, not re-verified against live docs — flag for sandbox validation before go-live.
</objective>

<execution_context>
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/workflows/execute-plan.md
@/home/pat/Projects/all/dgfy-platform/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-CONTEXT.md
@.planning/phases/10-storefront-discovery-online-ordering/10-RESEARCH.md
@apps/dgfy-api/src/config/env.js
@apps/dgfy-api/src/models/Landlord/CommercePaymentSession.js

# Legacy PayMongo client + session pattern (READ-ONLY — re-implement with native fetch, never import)
@backend/src/services/paymongoService.js
@docs/architecture/adr/0027-paymongo-commerce-qrph-platform-split-settlement.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PayMongo client service (QR Ph 3-call dance + raw-body signature verify) via native fetch</name>
  <files>apps/dgfy-api/src/modules/commercePayments/services/payMongoClient.js, apps/dgfy-api/src/config/env.js, apps/dgfy-api/tests/commercePayments/payMongoClient.test.js</files>
  <behavior>
    - createQrphPaymentIntent({amount, currency, description, billing, metadata, returnUrl}): performs createPaymentIntent (paymentMethodAllowed ['qrph'], NO split_payment arg — D-02) -> createPaymentMethod({type:'qrph'}) -> attachPaymentIntent, returning { paymentIntentId, paymentMethodId, qrCodeImageUrl (from next_action.code.image_url), expiresAt (next_action.code.expires_at, fallback +30min per legacy) }.
    - HTTP: native fetch to base https://api.paymongo.com/v1 (override PAYMONGO_API_BASE_URL), HTTP Basic auth base64(SECRET_KEY + ':'). Non-2xx maps to a typed PayMongoError with the provider error surfaced.
    - verifyWebhookSignature({rawBody, signatureHeader, secret, now}): parse `t=,te=,li=`; compute HMAC-SHA256 over `${t}.${rawBody}`; compare with crypto.timingSafeEqual; reject if |now - t| > tolerance (PAYMONGO_WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS default 300). Returns true/false; never throws on mismatch.
    - When PayMongo secret is not configured, createQrphPaymentIntent throws a typed ServiceUnavailable (mapped to 503 by the controller).
  </behavior>
  <action>
Implement `payMongoClient.js` with native `fetch` and Node `crypto` (zero new packages — apps/dgfy-api has no axios). Re-implement the legacy `paymongoService` QR Ph flow and signature verify as NEW code; never import from `backend/`. The split excision is literally omitting the `split_payment` argument on createPaymentIntent — document by concept that re-adding split later is a provider-side arg needing no schema change (D-02). Add the PayMongo env keys to `config/env.js` (PAYMONGO_MODE, PAYMONGO_SECRET_KEY / mode-specific, PAYMONGO_WEBHOOK_SECRET / mode-specific, PAYMONGO_WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS, PAYMONGO_API_BASE_URL, COMMERCE_PAYMENTS_ENABLED, COMMERCE_QRPH_ENABLED) with safe defaults and a `requireCommerceQrphConfig()` helper that reports missing config. Do NOT add PAYMONGO_DGFY_MERCHANT_ID / COMMERCE_PAYMONGO_SPLIT_ENABLED (not needed, D-02). Unit-test the signature verify (valid, tampered body, stale timestamp) and the 3-call flow with fetch mocked.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/commercePayments/payMongoClient.test.js 2>&amp;1 | tail -25</automated>
  </verify>
  <done>QR Ph client performs the 3-call dance via fetch and returns qr image + shared expiresAt with no split arg; signature verify is timing-safe and rejects tampered/stale; missing config throws 503-mapped error. Zero new packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commerce payment repository + createQrphSession usecase</name>
  <files>apps/dgfy-api/src/modules/commercePayments/repositories/commercePaymentRepository.js, apps/dgfy-api/src/modules/commercePayments/usecases/createQrphSessionUseCases.js, apps/dgfy-api/src/modules/commercePayments/index.js, apps/dgfy-api/tests/commercePayments/createQrphSession.test.js</files>
  <behavior>
    - commercePaymentRepository (landlord dgfy_core): createSession({storefront_order_id, tenant_id, amount_centavos, provider ids, qr_code_image_url, expires_at}); findSessionByPublicReference(ref); findSessionByProviderPaymentIntent(id); findSessionByProviderPayment(id); updateSessionStatus(id, patch). tenant_id stays UUID, never integer-coerced (Pitfall 7).
    - createQrphSession({ order }): calls payMongoClient.createQrphPaymentIntent with metadata { commerce_payment_session: order.public_reference, tenant_id }, persists a commerce_payment_sessions row (status awaiting_payment, shared expires_at from the intent), returns { session_public_reference, qr_code_image_url, expires_at, amount_centavos }.
  </behavior>
  <action>
Implement the repository over the `CommercePaymentSession` model (10-01) and the createQrphSession usecase. Wire both into `buildCommercePaymentsModule({ payMongoClient, sequelize, ... })` in commercePayments/index.js, exposing `useCases.createQrphSession`, the repository, and the client's `verifyWebhookSignature` + session-resolution helpers for 10-08 to reuse. Session resolution order to expose (for 10-08): metadata reference -> payment_intent id -> payment id. Do NOT mount webhook routes here (10-08 owns them). Test createQrphSession with the client mocked: asserts metadata carries the order reference + tenant_id, the session persists with the shared expires_at, and no split payload is written.
  </action>
  <verify>
    <automated>cd apps/dgfy-api &amp;&amp; npx jest tests/commercePayments/createQrphSession.test.js 2>&amp;1 | tail -25</automated>
  </verify>
  <done>createQrphSession persists a landlord session with the PayMongo intent's shared expires_at, carries the order reference + UUID tenant_id in metadata, writes no split payload; repository resolves sessions by reference/intent/payment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api → PayMongo | Outbound payment-intent creation over TLS with secret key |
| PayMongo → webhook (consumed in 10-08) | Signed inbound event — verified via HMAC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-01 | Spoofing / Tampering | forged/replayed webhook | high | mitigate | verifyWebhookSignature: HMAC-SHA256 over raw body + timing-safe compare + timestamp tolerance (rejects stale) |
| T-10-05-02 | Information Disclosure | PayMongo secret leakage | high | mitigate | Secret read from env only, Basic-auth header built server-side; never logged or returned to client |
| T-10-05-03 | Elevation | accidental split/settlement | medium | mitigate | split_payment arg omitted entirely (D-02); schema split columns nullable + unwritten |
| T-10-05-04 | Tampering | tenant_id integer coercion breaks resolution | high | mitigate | UUID tenant_id preserved end-to-end (Pitfall 7) |
| T-10-05-SC | Tampering | npm installs | high | mitigate | No new packages — native fetch + crypto (RESEARCH audit: none) |
</threat_model>

<verification>
- PayMongo client + session tests pass with the network mocked.
- Signature verify rejects tampered body and stale timestamp.
- [ASSUMED A6] Flag in SUMMARY: sandbox-validate PayMongo event names + response shape before go-live.
- No `backend/` writes; no new dependency.
</verification>

<success_criteria>
Real PayMongo QR Ph sessions can be created (single DGFY account, no split) with a shared expiry, and their webhooks can be signature-verified — the payment substrate for STF-04.
</success_criteria>

<output>
Create `.planning/phases/10-storefront-discovery-online-ordering/10-05-SUMMARY.md` when done.
</output>
