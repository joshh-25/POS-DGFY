---
phase: 14-sales-history-migration-full-verification
plan: 09
type: execute
wave: 7
depends_on: ["14-07", "14-08"]
files_modified: []
autonomous: false
requirements: [VER-03]
user_setup:
  - service: production-parity rehearsal environment
    why: "The agent cannot choose or reset an operator-controlled destructive target or supply its secrets."
    env_vars:
      - name: RUN_PHASE14_MILESTONE_REHEARSAL
        source: "Set to true only after this checkpoint is approved"
      - name: DGFY_MIGRATION_TARGET_MANIFEST
        source: "Operator-reviewed absolute manifest path"
      - name: DOCKER_CONTEXT
        source: "Approved context, expected lima-dgfy-dev unless the runbook records another"
      - name: DGFY_BUSINESS_DB_NAMES
        source: "Exact dgfy_business_* targets matching the manifest"
must_haves:
  truths:
    - "An operator confirms the exact source, target, runtime context, credentials, volume, reset scope, and evidence directory before destructive execution."
    - "Phase 13 and 13.5 schema/checkpoint prerequisites are present on the selected target."
  artifacts:
    - path: ".planning/phases/14-sales-history-migration-full-verification/14-09-SUMMARY.md"
      provides: "Authorization checkpoint result without secrets"
  key_links:
    - from: "operator-reviewed manifest"
      to: "Plan 10 enabled rehearsal"
      via: "blocking resume signal"
      pattern: "rehearsal authorized"
---

<objective>
Gate destructive real-volume rehearsal on explicit operator target authorization.

Purpose: Prevent wrong-runtime, wrong-target, stale-metadata, or vacuous execution.
Output: A blocking authorization decision; no source change.
</objective>

<execution_context>
@/Users/pat/.codex/gsd-core/workflows/execute-plan.md
@/Users/pat/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/START_HERE.md
@docs/architecture/ARCHITECTURE_BOUNDARIES.md
@docs/architecture/ARCHITECTURE_GOVERNANCE.md
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
@docs/database/dgfy-migration-rehearsal.md
@.planning/phases/13-product-inventory-migration/13-06-PLAN.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-02-SUMMARY.md
@.planning/phases/14-sales-history-migration-full-verification/14-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Authorize the exact production-parity source and disposable target</name>
  <files>None (operator-managed environment and secrets only)</files>
  <read_first>
    - docs/database/dgfy-migration-rehearsal.md
    - apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js
    - apps/dgfy-migration-runner/src/data/targetManifest.js
    - apps/dgfy-migration-runner/src/config/env.js
    - .planning/phases/14-sales-history-migration-full-verification/14-08-SUMMARY.md
  </read_first>
  <action>Pause before any destructive command. The operator must supply/review the manifest and credentials, name the explicit Docker context, prove the source contains real product and sales volume, confirm the targets are disposable `dgfy_*` databases, apply/verify Phase 13.5 and Phase 14 schema migrations, and reset only the selected domain rows plus matching `legacy_id_map`, `data_checkpoints`, and `data_quality_findings` run-scope rows. Do not print or commit secrets. The agent performs the CLI run in Plan 10 after authorization.</action>
  <what-built>Automated schema/mapping/dry-run/apply/verify code and the strict milestone rehearsal harness.</what-built>
  <how-to-verify>
    1. Review manifest entries and confirm legacy source names cannot be mistaken for DGFY targets.
    2. Confirm the approved context explicitly and verify target DB names against `DGFY_BUSINESS_DB_NAMES`.
    3. Confirm source row counts are non-zero for products and POS sales/lines.
    4. Confirm target and run-scope metadata reset is limited to the disposable rehearsal scope.
    5. Confirm REPORT_DIR is outside the disposable DB lifecycle and contains no committed secrets.
  </how-to-verify>
  <acceptance_criteria>
    - Operator provides a clear authorization signal naming source tenant, target DB, and runtime context.
    - Automated preflight can reach the explicit context and read the reviewed manifest.
    - No destructive execution occurs before approval.
  </acceptance_criteria>
  <verify><automated>test -n "$DGFY_MIGRATION_TARGET_MANIFEST" &amp;&amp; test -f "$DGFY_MIGRATION_TARGET_MANIFEST" &amp;&amp; test -n "$DOCKER_CONTEXT" &amp;&amp; docker --context "$DOCKER_CONTEXT" info &gt;/dev/null</automated></verify>
  <done>The precise real-volume rehearsal scope is operator-approved and technically reachable.</done>
  <resume-signal>Type "rehearsal authorized" with the source tenant, target database, and Docker context, or describe the blocker.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- No code/schema artifact; sanitized authorization result is recorded in `14-09-SUMMARY.md`.

<threat_model>
## ASVS Level 1 Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-09-01 | Spoofing | source/target/runtime identity | high | mitigate | Blocking operator review plus explicit context and validated manifest. |
| T-14-09-02 | Tampering | destructive reset scope | high | mitigate | Named disposable targets and run-scope-limited metadata reset. |
| T-14-09-03 | Information Disclosure | environment secrets | high | mitigate | Env-only credentials; no values in summary, plans, reports, or commits. |
</threat_model>

<verification>
Authorization remains blocked until manifest/context preflight and operator approval both succeed.
</verification>

<success_criteria>
Plan 10 cannot begin against an implicit, shared, stale, empty, or unreviewed target.
</success_criteria>

<output>Create `.planning/phases/14-sales-history-migration-full-verification/14-09-SUMMARY.md` when done.</output>
