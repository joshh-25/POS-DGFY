---
phase: 14-sales-history-migration-full-verification
plan: 04
type: execute
wave: 2
depends_on: ["14-02", "14-03"]
files_modified:
  - apps/dgfy-migration-runner/src/data/legacySource.js
  - apps/dgfy-migration-runner/tests/dataSalesSource.test.js
  - apps/dgfy-migration-runner/src/data/dryRun.js
  - apps/dgfy-migration-runner/tests/dataSalesDryRun.test.js
autonomous: true
requirements: [SHM-01, SHM-02, VER-01]
must_haves:
  truths:
    - "Each dry-run full-scans exactly the two legacy sales tables without writes or cursors."
    - "A clean-target dry-run understands planned header/product dependencies and does not falsely orphan every line."
    - "Dry-run reports never disclose legacy snapshot values, and every opened pool closes on success or failure."
  artifacts:
    - path: "apps/dgfy-migration-runner/src/data/legacySource.js"
      provides: "Read-only sales snapshot adapter"
    - path: "apps/dgfy-migration-runner/src/data/dryRun.js"
      provides: "Six-entity sales-aware planning and report redaction"
  key_links:
    - from: "readLegacySalesSnapshot"
      to: "mapPosTransactionToAvailment/mapPosTransactionLineToAvailmentItem"
      via: "runDryRunTransformations dependency-ordered plan"
      pattern: "availment_item"
---

<objective>
Extend source reads and dry-run planning to sales history with safe reports and bounded connection ownership.

Purpose: Prove D-14-06 full scans and first-run dependency planning without writing target data or leaking audit snapshots.
Output: Sales source reader, sales-aware dry-run, redaction/cleanup behavior, and tests.
</objective>

<execution_context>
@/Users/pat/.codex/gsd-core/workflows/execute-plan.md
@/Users/pat/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/START_HERE.md
@docs/architecture/ARCHITECTURE_BOUNDARIES.md
@docs/architecture/ARCHITECTURE_GOVERNANCE.md
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
@docs/database/dgfy-data-migration-map.md
@.planning/phases/14-sales-history-migration-full-verification/14-RESEARCH.md
@.planning/phases/14-sales-history-migration-full-verification/14-02-SUMMARY.md
@.planning/phases/14-sales-history-migration-full-verification/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the two-table read-only sales snapshot</name>
  <files>apps/dgfy-migration-runner/src/data/legacySource.js, apps/dgfy-migration-runner/tests/dataSalesSource.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/legacySource.js
    - apps/dgfy-migration-runner/tests/dataSalesSource.test.js (new target; confirm absent before creation)
    - apps/dgfy-migration-runner/tests/dataProductSource.test.js
    - backend/src/models/PosTransaction.js
    - backend/src/models/PosTransactionLine.js
    - .planning/phases/14-sales-history-migration-full-verification/14-CONTEXT.md
  </read_first>
  <behavior>
    - `readLegacySalesSnapshot(connection)` runs one `SELECT * FROM pos_transactions` and one `SELECT * FROM pos_transaction_lines`.
    - It returns raw `{posTransactions, posTransactionLines}` arrays and performs no target lookup or write.
  </behavior>
  <action>Add exported `readLegacySalesSnapshot` following `readLegacyProductSnapshot`. D-14-06 requires a full unpaginated scan on every run; do not add watermark/checkpoint cursor SQL. Correct stale source comments that claim POS tables are never read while preserving the read-only boundary.</action>
  <acceptance_criteria>
    - Query spies prove each exact table is read once and returned verbatim.
    - Source guard rejects mutation SQL and no connection is opened inside the adapter.
  </acceptance_criteria>
  <verify><automated>cd apps/dgfy-migration-runner &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataSalesSource.test.js</automated></verify>
  <done>Sales source reading is explicit, read-only, full-scan, and mapper-agnostic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plan sales dependencies and redact snapshots in dry-run reports</name>
  <files>apps/dgfy-migration-runner/src/data/dryRun.js, apps/dgfy-migration-runner/tests/dataSalesDryRun.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/dryRun.js
    - apps/dgfy-migration-runner/tests/dataSalesDryRun.test.js (new target; confirm absent before creation)
    - apps/dgfy-migration-runner/tests/dataProductDryRun.test.js
    - apps/dgfy-migration-runner/tests/dataDryRun.test.js
    - apps/dgfy-migration-runner/src/metadata/dataState.js
    - apps/dgfy-migration-runner/src/data/mappings.js
    - apps/dgfy-migration-runner/src/data/legacySource.js
  </read_first>
  <behavior>
    - Dry-run orders four Phase 13 entity types before `availment`, then `availment_item`.
    - Planned valid parent/product source dependencies are represented as pending on a clean target; genuinely absent dependencies remain blocking findings.
    - `redactTargetPayload()` replaces every `legacy_snapshot` value with `has_legacy_snapshot` and sorted top-level key names only.
    - Landlord and per-tenant connections close in `finally` on success and injected failure.
  </behavior>
  <action>Wire `readLegacySalesSnapshot` and the Plan 02 mappers into `runDryRunTransformations`, preserving the complete six-entity plan. Track planned source IDs separately from resolved target IDs so clean-target lines are not false orphans. Adopt `syncDataQualityFindings` for current reasons. Extend report assembly redaction for both header/line `legacy_snapshot`; seed customer, payment, delivery, and fiscal sentinels in tests and prove none survive serialized output. Ensure every connection created by this orchestration is closed in `finally`.</action>
  <acceptance_criteria>
    - Tests prove entity order, planned-vs-resolved dependencies, blocking real orphans, current-state findings, and zero target mutations.
    - Serialized dry-run output contains snapshot presence/key evidence but none of the seeded values.
    - Connection close spies fire exactly once on success and failure.
  </acceptance_criteria>
  <verify><automated>cd apps/dgfy-migration-runner &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataSalesSource.test.js tests/dataSalesDryRun.test.js</automated></verify>
  <done>Dry-run is six-entity aware, dependency-honest, report-safe, and pool-bounded.</done>
</task>

</tasks>

## Artifacts this phase produces

- Adapter: `readLegacySalesSnapshot`.
- Redaction evidence fields: `has_legacy_snapshot`, `legacy_snapshot_keys`.
- Tests: `dataSalesSource.test.js`, `dataSalesDryRun.test.js`.

<threat_model>
## ASVS Level 1 Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-04-01 | Information Disclosure | dry-run report snapshots | high | mitigate | Whole-value redaction plus seeded-sentinel serialization tests. |
| T-14-04-02 | Denial of Service | tenant connection pools | medium | mitigate | Creator-owned `finally` closure tested on success and failure. |
| T-14-04-03 | Tampering | false orphan classification | medium | mitigate | Separate planned source dependencies from durable resolved target IDs. |
</threat_model>

<verification>
Focused source/dry-run tests pass and report serialization contains no snapshot sentinel.
</verification>

<success_criteria>
Dry-run sees all six entities, performs no writes, leaks no snapshots, and closes every pool.
</success_criteria>

<output>Create `.planning/phases/14-sales-history-migration-full-verification/14-04-SUMMARY.md` when done.</output>
