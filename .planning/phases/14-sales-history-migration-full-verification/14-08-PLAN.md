---
phase: 14-sales-history-migration-full-verification
plan: 08
type: execute
wave: 6
depends_on: ["14-07"]
files_modified:
  - apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js
autonomous: true
requirements: [LDM-05, SHM-01, SHM-02, SHM-03, SHM-04, VER-01, VER-02, VER-03]
must_haves:
  truths:
    - "The committed rehearsal harness drives schema proof plus dry-run, apply, retry, and verify for all six entities."
    - "The harness skips cleanly without operator env but cannot represent VER-03 as passed while skipped."
    - "All focused/full suites, architecture checks, and docs lint are green before destructive authorization."
  artifacts:
    - path: "apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js"
      provides: "ENV-gated milestone-wide production-parity proof harness"
  key_links:
    - from: "dataMilestoneRehearsal.test.js"
      to: "runDryRunTransformations/runApplyTransformations/buildDataVerificationSections"
      via: "real command/orchestration path"
      pattern: "dry-run|apply|verify"
---

<objective>
Build the milestone-wide rehearsal harness and close all automated gates before operator authorization.

Purpose: Make the real-volume proof reproducible while keeping CI safe and honest about skipped external evidence.
Output: One ENV-gated rehearsal test and a green automated gate inventory.
</objective>

<execution_context>
@/Users/pat/.codex/gsd-core/workflows/execute-plan.md
@/Users/pat/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/START_HERE.md
@docs/architecture/ARCHITECTURE_BOUNDARIES.md
@docs/architecture/ARCHITECTURE_GOVERNANCE.md
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
@docs/database/dgfy-migration-rehearsal.md
@.planning/phases/13-product-inventory-migration/13-06-PLAN.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-04-SUMMARY.md
@.planning/phases/14-sales-history-migration-full-verification/14-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the six-entity production-parity rehearsal harness</name>
  <files>apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js (new target; confirm absent before creation)
    - apps/dgfy-migration-runner/tests/dataProductRehearsal.test.js
    - apps/dgfy-migration-runner/tests/phase03Integration.test.js
    - apps/dgfy-migration-runner/src/commands/data.js
    - apps/dgfy-migration-runner/src/commands/verify.js
    - apps/dgfy-migration-runner/src/data/apply.js
    - apps/dgfy-migration-runner/src/data/verifyData.js
    - docs/database/dgfy-migration-rehearsal.md
  </read_first>
  <behavior>
    - Suite is enabled only by `RUN_PHASE14_MILESTONE_REHEARSAL=true` plus every required manifest/credential/runtime variable; otherwise it skips cleanly.
    - Enabled sequence proves schema apply/verify (including Phase 13.5 `staff_credentials` and Phase 14 additions), dry-run order/redaction, first apply writes, retry zero inserts, and final six-entity verify.
    - Evidence asserts product/inventory/embedding parity, header/line counts, exact sales totals, provenance/FKs, void fidelity, zero blocking findings, and no snapshot/credential sentinels.
  </behavior>
  <action>Create `dataMilestoneRehearsal.test.js` using existing orchestration exports and explicit target manifest filtering. Require `DOCKER_CONTEXT=lima-dgfy-dev` (or the operator-approved documented context) rather than inheriting the active context. Use `try/finally` for evidence-query connections. The test may commit skipped, but its assertions must fail if an enabled run lacks real rows, any of six entity types, exact totals, clean retry, redaction, or a green verifier.</action>
  <acceptance_criteria>
    - Unset env exits 0 with the suite skipped; enabled env cannot vacuously pass with an empty manifest or zero sales/product volume.
    - The harness runs the real dry-run/apply/retry/verify seams, not mapper-only substitutes.
    - It never auto-resets or applies against an unreviewed target.
  </acceptance_criteria>
  <verify><automated>cd apps/dgfy-migration-runner &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataMilestoneRehearsal.test.js</automated></verify>
  <done>The committed harness is safe in CI and strict when enabled against production-parity data.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete automated regression and architecture gate set</name>
  <files>None (verification-only task)</files>
  <read_first>
    - apps/dgfy-migration-runner/package.json
    - package.json
    - docs/architecture/ARCHITECTURE_GOVERNANCE.md
    - docs/ai/PR.md
  </read_first>
  <action>Run the full migration-runner suite, root architecture checks, and docs lint after the focused tests. Confirm no new architecture allowlist entry, no external package change, and no live checkout fee path. Record exact pass/fail commands in the SUMMARY; a skipped real-volume suite is recorded as pending, not proof of VER-03.</action>
  <acceptance_criteria>
    - `npm --prefix apps/dgfy-migration-runner test`, `npm run check:architecture`, and `npm run lint:docs` pass.
    - `git diff` shows no package manifest/lockfile or architecture allowlist change from this phase.
    - The SUMMARY explicitly separates automated readiness from external real-volume proof.
  </acceptance_criteria>
  <verify><automated>npm --prefix apps/dgfy-migration-runner test &amp;&amp; npm run check:architecture &amp;&amp; npm run lint:docs</automated></verify>
  <done>Every automated code, architecture, and documentation gate is green before target authorization.</done>
</task>

</tasks>

## Artifacts this phase produces

- Test: `dataMilestoneRehearsal.test.js`.
- Automated gate inventory in `14-08-SUMMARY.md`; no new package, allowlist, or live API artifact.

<threat_model>
## ASVS Level 1 Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-08-01 | Spoofing | runtime/target selection | high | mitigate | Explicit context, manifest validation, non-empty real-volume assertions; no inherited active context. |
| T-14-08-02 | Tampering | vacuous/skipped proof | high | mitigate | Skip is readiness only; enabled mode rejects empty datasets and Plan 10 owns VER-03 evidence. |
| T-14-08-03 | Information Disclosure | rehearsal assertions/reports | high | mitigate | Sentinel redaction assertions and sanitized summary output. |
| T-14-08-04 | Denial of Service | evidence connections | medium | mitigate | `finally` closure and bounded sequential test execution. |
</threat_model>

<verification>
Focused rehearsal test skips/passes safely, full runner suite passes, and architecture/docs gates are green.
</verification>

<success_criteria>
The repository is fully ready for an explicitly authorized real-volume run, with no false claim that a skip satisfies VER-03.
</success_criteria>

<output>Create `.planning/phases/14-sales-history-migration-full-verification/14-08-SUMMARY.md` when done.</output>
