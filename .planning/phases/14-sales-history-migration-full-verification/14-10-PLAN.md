---
phase: 14-sales-history-migration-full-verification
plan: 10
type: execute
wave: 8
depends_on: ["14-09"]
files_modified:
  - .planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json
autonomous: true
requirements: [VER-01, VER-02, VER-03]
must_haves:
  truths:
    - "The authorized real-volume tenant completes schema verification, dry-run, first apply, retry apply, and final verify through real runner paths."
    - "Retry writes zero new migrated rows and final verification proves exact six-entity counts/sums with no unresolved blocking issue."
    - "Sanitized, machine-checkable report inventory survives target cleanup."
  artifacts:
    - path: ".planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json"
      provides: "Sanitized real-volume commands, report hashes, counts, exact totals, and verdicts"
  key_links:
    - from: "Plan 09 authorization"
      to: "dataMilestoneRehearsal.test.js"
      via: "enabled explicit-context execution"
      pattern: "RUN_PHASE14_MILESTONE_REHEARSAL"
---

<objective>
Execute the authorized production-parity rehearsal and preserve machine-checkable milestone evidence.

Purpose: Satisfy VER-03 with real legacy volume rather than a skipped or fixture-only run.
Output: Preserved reports plus sanitized evidence JSON proving the complete sequence.
</objective>

<execution_context>
@/Users/pat/.codex/gsd-core/workflows/execute-plan.md
@/Users/pat/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/START_HERE.md
@docs/architecture/ARCHITECTURE_BOUNDARIES.md
@docs/architecture/ARCHITECTURE_GOVERNANCE.md
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
@docs/database/dgfy-migration-rehearsal.md
@.planning/phases/14-sales-history-migration-full-verification/14-09-SUMMARY.md
@apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run schema proof and the full real-volume migration sequence once</name>
  <files>.planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json (new target; confirm absent before creation)
    - apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js
    - apps/dgfy-migration-runner/src/cli.js
    - apps/dgfy-migration-runner/src/commands/data.js
    - apps/dgfy-migration-runner/src/commands/verify.js
    - docs/database/dgfy-migration-rehearsal.md
    - .planning/phases/14-sales-history-migration-full-verification/14-09-SUMMARY.md
  </read_first>
  <action>Using only the approved context/manifest/targets from Plan 09, run schema migrate/verify as required, then execute the enabled `dataMilestoneRehearsal.test.js` exactly once so its sequence performs dry-run, destructive apply, idempotency retry, and verify. Preserve machine-readable reports before cleanup. Create sanitized `14-REHEARSAL-EVIDENCE.json` with this exact top-level contract: `schema_version` is integer `1`; `data_migration_ok`, `exact_sales_totals_match`, `provenance_ok`, `relationships_ok`, `void_fidelity_ok`, and `connections_closed` are booleans; `source_volume`, `retry_rows_written`, and `blocking_findings` are non-negative integers; `commands` is a non-empty array of command-name strings without arguments or secret values; `runtime` is `{ docker_context: string, source_tenant: string, target_databases: non-empty string[] }`; `entity_counts` is an object with exactly `product_folder`, `product`, `inventory_movement`, `product_embedding`, `availment`, and `availment_item`, each mapped to `{ source_rows: non-negative integer, target_rows: non-negative integer, first_rows_written: non-negative integer, retry_rows_written: non-negative integer }`; `sales_totals_by_status` is a non-empty object whose values are `{ source_total: fixed-four-decimal string, target_total: fixed-four-decimal string }`; `open_findings` is an array of `{ reason_code: string, blocking: boolean, count: positive integer }`; `reports` is a non-empty array of `{ path: relative string, sha256: 64-character lowercase hexadecimal SHA-256 }`; and `final_verdict` is the string `pass`. Fail closed; do not create green evidence after any partial/failed stage.</action>
  <acceptance_criteria>
    - Evidence identifies all six entity types with non-vacuous source/target counts; first apply writes expected rows and retry writes zero new rows.
    - Exact source/target sales totals match by mapped status; header/line provenance, line parent/product FKs, and void fidelity are true.
    - `data_migration_ok=true`, `blocking_findings=0`, no open sales orphan/status/attribution issue remains unresolved, and any expected-lossy/staff-linkage reason is explicitly listed for operator review.
    - Evidence contains no credential, password, PIN, token, credential hash material, connection string, snapshot value, customer/payment/delivery sentinel, or raw environment dump; report-integrity SHA-256 hashes remain mandatory.
  </acceptance_criteria>
  <verify><automated>RUN_PHASE14_MILESTONE_REHEARSAL=true DOCKER_CONTEXT="$DOCKER_CONTEXT" npm --prefix apps/dgfy-migration-runner test -- dataMilestoneRehearsal.test.js</automated></verify>
  <done>The authorized real-volume sequence passes once and produces sanitized, hashed, non-vacuous six-entity evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Validate evidence schema, integrity hashes, and redaction</name>
  <files>.planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json
    - docs/database/dgfy-migration-rehearsal.md
    - .planning/phases/14-sales-history-migration-full-verification/14-09-SUMMARY.md
  </read_first>
  <action>Run an automated assertion over the exact Task 1 contract before accepting the evidence. Assert every required top-level key and type; the exact six `entity_counts` keys and all four integer counters; non-vacuous source/target product and sales volume; aggregate and per-entity retry writes equal zero; all fidelity booleans true; `blocking_findings` equals zero; `final_verdict` equals `pass`; every report path is relative, exists, and recomputes to its recorded SHA-256; and fixed-four-decimal source/target totals match for every status. Scan keys and serialized values for credentials, passwords, PINs, tokens, connection strings, raw environment dumps, snapshot payloads, and customer/payment/delivery sentinels. Do not reject the `sha256` key or lowercase hexadecimal integrity digests.</action>
  <acceptance_criteria>
    - The schema assertion fails on a missing/mistyped key, unknown/missing entity, vacuous required volume, nonzero retry/blocker count, false fidelity flag, mismatched total, missing report, or digest mismatch.
    - The redaction assertion fails on credential/password/PIN/token hash material or sensitive snapshot/environment values while allowing required report-integrity hashes.
    - The same exact key names and types are ready for Plan 11's automated predicate and operator review.
  </acceptance_criteria>
  <verify><automated>node -e "const fs=require('fs'),p=require('path'),c=require('crypto'),f='.planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json',raw=fs.readFileSync(f,'utf8'),e=JSON.parse(raw),top=['schema_version','data_migration_ok','source_volume','retry_rows_written','blocking_findings','exact_sales_totals_match','provenance_ok','relationships_ok','void_fidelity_ok','connections_closed','commands','runtime','entity_counts','sales_totals_by_status','open_findings','reports','final_verdict'],ints=['source_volume','retry_rows_written','blocking_findings'],bools=['data_migration_ok','exact_sales_totals_match','provenance_ok','relationships_ok','void_fidelity_ok','connections_closed'],entities=['product_folder','product','inventory_movement','product_embedding','availment','availment_item'],counters=['source_rows','target_rows','first_rows_written','retry_rows_written'],same=(a,b)=&gt;JSON.stringify([...a].sort())===JSON.stringify([...b].sort()); if(!same(Object.keys(e),top)||e.schema_version!==1||e.final_verdict!=='pass')throw Error('top-level contract'); for(const k of ints)if(!Number.isInteger(e[k])||e[k]&lt;0)throw Error(k); for(const k of bools)if(e[k]!==true)throw Error(k); if(!Array.isArray(e.commands)||!e.commands.length||!e.commands.every(v=&gt;typeof v==='string'&amp;&amp;v.length))throw Error('commands'); if(!e.runtime||!same(Object.keys(e.runtime),['docker_context','source_tenant','target_databases'])||typeof e.runtime.docker_context!=='string'||!e.runtime.docker_context||typeof e.runtime.source_tenant!=='string'||!e.runtime.source_tenant||!Array.isArray(e.runtime.target_databases)||!e.runtime.target_databases.length||!e.runtime.target_databases.every(v=&gt;typeof v==='string'&amp;&amp;v.length))throw Error('runtime'); if(!e.entity_counts||!same(Object.keys(e.entity_counts),entities))throw Error('entities'); for(const k of entities){const v=e.entity_counts[k]; if(!v||!same(Object.keys(v),counters))throw Error(k+'.shape'); for(const n of counters)if(!Number.isInteger(v[n])||v[n]&lt;0)throw Error(k+'.'+n); if(v.source_rows&lt;1||v.target_rows&lt;1||v.retry_rows_written!==0)throw Error(k+'.volume/retry');} if(e.source_volume!==entities.reduce((n,k)=&gt;n+e.entity_counts[k].source_rows,0)||e.retry_rows_written!==entities.reduce((n,k)=&gt;n+e.entity_counts[k].retry_rows_written,0)||e.retry_rows_written!==0)throw Error('aggregate counts'); const money=/^-?\\d+\\.\\d{4}$/; if(!e.sales_totals_by_status||!Object.keys(e.sales_totals_by_status).length)throw Error('totals'); for(const v of Object.values(e.sales_totals_by_status)){if(!v||!same(Object.keys(v),['source_total','target_total'])||!money.test(v.source_total)||v.source_total!==v.target_total)throw Error('money');} if(!Array.isArray(e.open_findings)||!e.open_findings.every(v=&gt;v&amp;&amp;same(Object.keys(v),['reason_code','blocking','count'])&amp;&amp;typeof v.reason_code==='string'&amp;&amp;v.reason_code.length&amp;&amp;typeof v.blocking==='boolean'&amp;&amp;Number.isInteger(v.count)&amp;&amp;v.count&gt;0))throw Error('findings'); if(e.blocking_findings!==e.open_findings.filter(v=&gt;v.blocking).reduce((n,v)=&gt;n+v.count,0)||e.blocking_findings!==0)throw Error('blockers'); if(!Array.isArray(e.reports)||!e.reports.length)throw Error('reports'); for(const r of e.reports){if(!r||!same(Object.keys(r),['path','sha256'])||typeof r.path!=='string'||!r.path||p.isAbsolute(r.path)||typeof r.sha256!=='string'||!/^[a-f0-9]{64}$/.test(r.sha256))throw Error('report metadata'); const q=p.resolve(p.dirname(f),r.path); if(!fs.existsSync(q)||c.createHash('sha256').update(fs.readFileSync(q)).digest('hex')!==r.sha256)throw Error('report integrity');} if(/password|pin|credential|(?:access|refresh)?[_-]?token|authorization|connection[_-]?string|legacy_snapshot|raw[_-]?env|cash_received|checkout_url/i.test(raw))throw Error('sensitive evidence');"</automated></verify>
  <done>The evidence is schema-exact, non-vacuous, digest-verified, fidelity-green, and free of credential/snapshot material.</done>
</task>

</tasks>

## Artifacts this phase produces

- Evidence: `14-REHEARSAL-EVIDENCE.json` with report hashes, six-entity counts, exact totals, retry delta, finding inventory, provenance/FK/void/cleanup booleans, and final verdict.
- External preserved JSON reports referenced by hash; no secrets or snapshot payloads.

<threat_model>
## ASVS Level 1 Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-10-01 | Tampering | real-volume retry evidence | high | mitigate | One authorized sequence records first/retry deltas and report hashes; zero-retry assertion is blocking. |
| T-14-10-02 | Tampering | exact monetary evidence | high | mitigate | String/BigInt exact totals by status, never floating-point summaries. |
| T-14-10-03 | Information Disclosure | reports/evidence JSON | high | mitigate | Sanitized schema and sentinel/secret scans before evidence acceptance. |
| T-14-10-04 | Denial of Service | full-volume pools | medium | mitigate | Sequential execution, connection-close assertions, and bounded explicit tenant manifest. |
</threat_model>

<verification>
The enabled real-volume Jest command passes once; evidence/report hashes and non-vacuous assertions are preserved before disposable target cleanup.
</verification>

<success_criteria>
Real legacy volume migrates end-to-end with exact six-entity fidelity, zero duplicate retry writes, and clean verification evidence.
</success_criteria>

<output>Create `.planning/phases/14-sales-history-migration-full-verification/14-10-SUMMARY.md` when done.</output>
