---
phase: 14-sales-history-migration-full-verification
plan: 10
type: execute
wave: 8
depends_on: ["14-09"]
files_modified:
  - apps/dgfy-migration-runner/scripts/validate-phase14-rehearsal-evidence.js
  - apps/dgfy-migration-runner/tests/validatePhase14RehearsalEvidence.test.js
  - .planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json
autonomous: true
requirements: [VER-01, VER-02, VER-03]
must_haves:
  truths:
    - "The authorized real-volume tenant completes schema verification, dry-run, first apply, retry apply, and final verify through real runner paths."
    - "Retry writes zero new migrated rows and final verification proves exact six-entity counts/sums with no unresolved blocking issue."
    - "Sanitized, machine-checkable report inventory survives target cleanup."
  artifacts:
    - path: "apps/dgfy-migration-runner/scripts/validate-phase14-rehearsal-evidence.js"
      provides: "Single executable contract validator reused by Plans 10 and 11"
    - path: "apps/dgfy-migration-runner/tests/validatePhase14RehearsalEvidence.test.js"
      provides: "Positive and negative contract/redaction fixtures for the shared validator"
    - path: ".planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json"
      provides: "Sanitized real-volume commands, report hashes, counts, exact totals, and verdicts"
  key_links:
    - from: "Plan 09 authorization"
      to: "dataMilestoneRehearsal.test.js"
      via: "enabled explicit-context execution"
      pattern: "RUN_PHASE14_MILESTONE_REHEARSAL"
    - from: "14-REHEARSAL-EVIDENCE.json"
      to: "validate-phase14-rehearsal-evidence.js"
      via: "the same executable validation command used before Plan 11's human checkpoint"
      pattern: "validatePhase14RehearsalEvidence"
---

<objective>
Execute the authorized production-parity rehearsal and preserve machine-checkable milestone evidence.

Purpose: Satisfy VER-03 with real legacy volume rather than a skipped or fixture-only run.
Output: Preserved reports plus sanitized evidence JSON proving the complete sequence.
</objective>

<execution_context>
@/Users/pat/.codex/gsd-core/workflows/execute-plan.md
@/Users/pat/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/START_HERE.md
@docs/architecture/ARCHITECTURE_BOUNDARIES.md
@docs/architecture/ARCHITECTURE_GOVERNANCE.md
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
@docs/database/dgfy-migration-rehearsal.md
@.planning/phases/14-sales-history-migration-full-verification/14-09-SUMMARY.md
@apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run schema proof and the full real-volume migration sequence once</name>
  <files>.planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json (new target; confirm absent before creation)
    - apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js
    - apps/dgfy-migration-runner/src/cli.js
    - apps/dgfy-migration-runner/src/commands/data.js
    - apps/dgfy-migration-runner/src/commands/verify.js
    - docs/database/dgfy-migration-rehearsal.md
    - .planning/phases/14-sales-history-migration-full-verification/14-09-SUMMARY.md
  </read_first>
  <action>Using only the approved context/manifest/targets from Plan 09, run schema migrate/verify as required, then execute the enabled `dataMilestoneRehearsal.test.js` exactly once so its sequence performs dry-run, destructive apply, idempotency retry, and verify. Preserve machine-readable reports before cleanup. Create sanitized `14-REHEARSAL-EVIDENCE.json` with this exact top-level contract: `schema_version` is integer `1`; `data_migration_ok`, `exact_sales_totals_match`, `provenance_ok`, `relationships_ok`, `void_fidelity_ok`, and `connections_closed` are booleans; `source_volume`, `retry_rows_written`, and `blocking_findings` are non-negative integers; `commands` is a non-empty array of command-name strings without arguments or secret values; `runtime` is `{ docker_context: string, source_tenant: string, target_databases: non-empty string[] }`; `entity_counts` is an object with exactly `product_folder`, `product`, `inventory_movement`, `product_embedding`, `availment`, and `availment_item`, each mapped to `{ source_rows: non-negative integer, target_rows: non-negative integer, first_rows_written: non-negative integer, retry_rows_written: non-negative integer }`; `sales_totals_by_status` is a non-empty object whose values are `{ source_total: fixed-four-decimal string, target_total: fixed-four-decimal string }`; `open_findings` is an array of `{ reason_code: string, blocking: boolean, count: positive integer }`; `reports` is a non-empty array of `{ path: relative string, sha256: 64-character lowercase hexadecimal SHA-256 }`; and `final_verdict` is the string `pass`. Fail closed; do not create green evidence after any partial/failed stage.</action>
  <acceptance_criteria>
    - Evidence identifies all six entity types with non-vacuous source/target counts; first apply writes expected rows and retry writes zero new rows.
    - Exact source/target sales totals match by mapped status; header/line provenance, line parent/product FKs, and void fidelity are true.
    - `data_migration_ok=true` and `blocking_findings=0`; blocking `UNSUPPORTED_SALE_STATUS`, `AVAILMENT_PARENT_NOT_MAPPED`, and `SALE_PRODUCT_NOT_MAPPED` findings are absent. Per D-14-02/D-14-03, truthful `SALE_LOCATION_NOT_MAPPED`, `SALE_TERMINAL_NOT_MAPPED`, and `SALE_CASHIER_NOT_MAPPED` findings may remain open only as `blocking=false`, with positive counts for operator review; other expected-lossy/staff-linkage reason codes remain listed under their governed non-blocking policy.
    - Evidence contains no credential, password, PIN, token, credential hash material, connection string, snapshot value, customer/payment/delivery sentinel, or raw environment dump; report-integrity SHA-256 hashes remain mandatory.
  </acceptance_criteria>
  <verify><automated>RUN_PHASE14_MILESTONE_REHEARSAL=true DOCKER_CONTEXT="$DOCKER_CONTEXT" npm --prefix apps/dgfy-migration-runner test -- dataMilestoneRehearsal.test.js</automated></verify>
  <done>The authorized real-volume sequence passes once and produces sanitized, hashed, non-vacuous six-entity evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Validate evidence schema, integrity hashes, and redaction</name>
  <files>apps/dgfy-migration-runner/scripts/validate-phase14-rehearsal-evidence.js, apps/dgfy-migration-runner/tests/validatePhase14RehearsalEvidence.test.js, .planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json
    - apps/dgfy-migration-runner/tests/dataMilestoneRehearsal.test.js
    - apps/dgfy-migration-runner/src/data/verifyData.js
    - docs/database/dgfy-migration-rehearsal.md
    - .planning/phases/14-sales-history-migration-full-verification/14-09-SUMMARY.md
  </read_first>
  <action>Create one zero-dependency ESM helper, `validate-phase14-rehearsal-evidence.js`, exporting `validatePhase14RehearsalEvidence` and providing a CLI that accepts the evidence path. This helper is the only executable evidence-contract implementation and must be invoked unchanged here and in Plan 11. Validate the exact top-level key set and types from Task 1; exact `runtime` shape; non-empty command-name-only strings; exact six `entity_counts` keys and exact four non-negative integer counters; source and target counts greater than zero for every entity; per-entity source/target equality, first-write/target equality, and zero retry writes; `source_volume` equal to the sum of all six source counts and to the derived target total; top-level retry count equal to the per-entity retry sum and zero; all fidelity booleans true; non-empty fixed-four-decimal sales totals with per-status and aggregate source/target equality; exact finding object shapes with `blocking_findings` equal to the sum of blocking counts and zero; and `final_verdict='pass'`. Require reason codes to use the uppercase underscore vocabulary but exempt the `open_findings[].reason_code` value path from secret-word matching so safe codes such as `CREDENTIAL_RESET_REQUIRED` remain legal. Require `SALE_LOCATION_NOT_MAPPED`, `SALE_TERMINAL_NOT_MAPPED`, and `SALE_CASHIER_NOT_MAPPED` to be non-blocking when present per D-14-02/D-14-03; reject the blocking orphan/status codes if present. Validate exact report object shapes, safe relative paths contained below the evidence directory, existing files, and recomputed lowercase SHA-256 digests.

Implement redaction as structured path/value checks, never a regex over the entire serialized JSON: recursively reject explicit sensitive value-bearing field names (credential/password/PIN/token/hash/authorization/connection-string/raw-environment/snapshot/customer-payment-delivery payload fields) while explicitly allowing `reports[].sha256`; restrict `commands` to names without arguments/assignments/URLs and `runtime` to identifier-only values rather than connection URIs; and reject exact synthetic credential/hash/snapshot/customer/payment/delivery sentinel leaf values seeded by `dataMilestoneRehearsal.test.js`. Keep those synthetic literals in a named `FORBIDDEN_SEEDED_VALUES` set inside the helper and never add real operator secrets. Add Jest fixtures proving every malformed schema/count/total/finding/report case fails, all six entities must be non-vacuous, traversal/report-hash tampering fails, sensitive field paths and seeded values fail, and `CREDENTIAL_RESET_REQUIRED`, the three permitted attribution codes, and valid `reports[].sha256` values pass.</action>
  <acceptance_criteria>
    - The shared validator fails on any missing/extra/mistyped top-level or nested key, unknown/missing/vacuous entity, aggregate mismatch, source/target or first-write mismatch, nonzero retry/blocker count, false fidelity flag, mismatched per-status/aggregate sales total, malformed finding, unsafe/missing report, or digest mismatch.
    - Structured redaction rejects sensitive value-bearing field paths and the rehearsal's exact seeded secret/snapshot values without scanning permitted reason-code vocabulary or rejecting `reports[].sha256` integrity digests.
    - Zero blocking orphan/status findings is mandatory, while the exact D-14-02/D-14-03 location/terminal/cashier reason codes may remain open, counted, non-blocking, and operator reviewed.
    - Plans 10 and 11 invoke the same helper with the same evidence path and test suite; Plan 11 does not carry a second predicate.
  </acceptance_criteria>
  <verify><automated>npm --prefix apps/dgfy-migration-runner test -- validatePhase14RehearsalEvidence.test.js &amp;&amp; node apps/dgfy-migration-runner/scripts/validate-phase14-rehearsal-evidence.js .planning/phases/14-sales-history-migration-full-verification/14-REHEARSAL-EVIDENCE.json</automated></verify>
  <done>The evidence is schema-exact, non-vacuous, digest-verified, fidelity-green, and free of credential/snapshot material.</done>
</task>

</tasks>

## Artifacts this phase produces

- Evidence: `14-REHEARSAL-EVIDENCE.json` with report hashes, six-entity counts, exact totals, retry delta, finding inventory, provenance/FK/void/cleanup booleans, and final verdict.
- Shared validator: `validate-phase14-rehearsal-evidence.js` plus focused contract/redaction tests, invoked unchanged by Plans 10 and 11.
- External preserved JSON reports referenced by hash; no secrets or snapshot payloads.

<threat_model>
## ASVS Level 1 Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-10-01 | Tampering | real-volume retry evidence | high | mitigate | One authorized sequence records first/retry deltas and report hashes; zero-retry assertion is blocking. |
| T-14-10-02 | Tampering | exact monetary evidence | high | mitigate | String/BigInt exact totals by status, never floating-point summaries. |
| T-14-10-03 | Information Disclosure | reports/evidence JSON | high | mitigate | Sanitized schema and sentinel/secret scans before evidence acceptance. |
| T-14-10-04 | Denial of Service | full-volume pools | medium | mitigate | Sequential execution, connection-close assertions, and bounded explicit tenant manifest. |
</threat_model>

<verification>
The enabled real-volume Jest command passes once; evidence/report hashes and non-vacuous assertions are preserved before disposable target cleanup.
</verification>

<success_criteria>
Real legacy volume migrates end-to-end with exact six-entity fidelity, zero duplicate retry writes, and clean verification evidence.
</success_criteria>

<output>Create `.planning/phases/14-sales-history-migration-full-verification/14-10-SUMMARY.md` when done.</output>
