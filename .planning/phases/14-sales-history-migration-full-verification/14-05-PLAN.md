---
phase: 14-sales-history-migration-full-verification
plan: 05
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03", "14-04"]
files_modified:
  - apps/dgfy-migration-runner/src/data/apply.js
  - apps/dgfy-migration-runner/tests/dataSalesApply.test.js
autonomous: true
requirements: [SHM-01, SHM-02, SHM-03, SHM-04, VER-01]
must_haves:
  truths:
    - "Sales apply refuses to start until all four Phase 13 product-domain checkpoints are complete for the tenant."
    - "Headers apply before lines; every line resolves both durable parent and product IDs."
    - "Retries and target-first recovery create no duplicates while persistent/new findings remain truthful."
    - "Historical timestamps survive and every opened connection closes."
  artifacts:
    - path: "apps/dgfy-migration-runner/src/data/apply.js"
      provides: "Checkpoint-gated dependency-ordered sales persistence"
    - path: "apps/dgfy-migration-runner/tests/dataSalesApply.test.js"
      provides: "Retry, crash recovery, growing source, finding, timestamp, and cleanup proof"
  key_links:
    - from: "runApplyTransformations"
      to: "legacy_id_map"
      via: "parent/product/location/terminal/staff resolution before mapping"
      pattern: "findLegacyIdMap"
---

<objective>
Persist sales history in dependency order with truthful retry state and historical fidelity.

Purpose: Complete SHM-01 through SHM-04 and VER-01 using the existing checkpoint/ID-map mechanisms without accepting their known defects.
Output: Sales-aware apply orchestration and comprehensive tests.
</objective>

<execution_context>
@/Users/pat/.codex/gsd-core/workflows/execute-plan.md
@/Users/pat/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/START_HERE.md
@docs/architecture/ARCHITECTURE_BOUNDARIES.md
@docs/architecture/ARCHITECTURE_GOVERNANCE.md
@docs/architecture/adr/0029-catalog-inventory-pos-storefront-ownership-boundaries.md
@docs/database/dgfy-data-migration-map.md
@.planning/phases/13-product-inventory-migration/13-05-SUMMARY.md
@.planning/phases/13.5-staff-authentication-model-correction/13.5-04-SUMMARY.md
@.planning/phases/14-sales-history-migration-full-verification/14-01-SUMMARY.md
@.planning/phases/14-sales-history-migration-full-verification/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve mapper timestamps and synchronize findings on every scan</name>
  <files>apps/dgfy-migration-runner/src/data/apply.js, apps/dgfy-migration-runner/tests/dataSalesApply.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/apply.js
    - apps/dgfy-migration-runner/tests/dataSalesApply.test.js (new target; confirm absent before creation)
    - apps/dgfy-migration-runner/src/metadata/dataState.js
    - apps/dgfy-migration-runner/tests/dataProductApply.test.js
    - apps/dgfy-migration-runner/tests/dataApply.test.js
    - .planning/phases/14-sales-history-migration-full-verification/14-RESEARCH.md
  </read_first>
  <behavior>
    - `insertTargetRow()` supplies current timestamps only when payload timestamps are absent.
    - Existing entities still receive defaults; sales headers/lines retain legacy `created_at`/`updated_at`.
    - Current findings synchronize even when an entity checkpoint is already complete.
  </behavior>
  <action>Change the shared insert helper to preserve mapper-supplied timestamps while retaining defaults for legacy mapper payloads that omit them. Replace blanket resolve-then-conditional-record semantics in `applyTenantEntityBatch` with Plan 03 `syncDataQualityFindings` on every scanned entry, independent of `alreadyCompleted`. Keep checkpoint completion as batch evidence, not a row-discovery/finding suppression switch.</action>
  <acceptance_criteria>
    - Regression tests cover existing timestamp defaults and exact historical sales timestamps.
    - Persistent cashier/terminal findings remain open after retry; repaired reasons resolve selectively; a new row after checkpoint gets one finding.
  </acceptance_criteria>
  <verify><automated>cd apps/dgfy-migration-runner &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataSalesApply.test.js tests/dataState.test.js</automated></verify>
  <done>Shared apply behavior preserves history and cannot manufacture false-clean finding state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply headers then dual-FK lines with crash-safe natural keys</name>
  <files>apps/dgfy-migration-runner/src/data/apply.js, apps/dgfy-migration-runner/tests/dataSalesApply.test.js</files>
  <read_first>
    - apps/dgfy-migration-runner/src/data/apply.js
    - apps/dgfy-migration-runner/src/data/legacySource.js
    - apps/dgfy-migration-runner/src/data/mappings.js
    - apps/dgfy-migration-runner/src/metadata/dataState.js
    - apps/dgfy-migration-runner/tests/dataProductApply.test.js
    - .planning/phases/13-product-inventory-migration/13-06-PLAN.md
  </read_first>
  <behavior>
    - Apply checks completed `product_folder`, `product`, `inventory_movement`, and `product_embedding` checkpoints before sales for each tenant.
    - `availment` uses durable map then namespaced `source_reference` target recovery then insert; `availment_item` uses the same three-stage path.
    - All headers apply before lines; each line resolves the `pos_transactions` parent map and Phase 13 `items` product map immediately before mapping.
  </behavior>
  <action>Add `availment` and `availment_item` entries to `ENTITY_TARGET_CONFIG` with `source_reference` natural keys. Read the sales snapshot, resolve header location/terminal/staff maps, apply all headers, then resolve line parent/product maps and product snapshot name before applying lines. D-14-02/D-14-03 keep headers with nullable attribution plus findings; missing parent/product never inserts a partial line. Wrap landlord/source/target connections in creator-owned `try/finally`. Do not map legacy shift IDs or add a watermark.</action>
  <acceptance_criteria>
    - Tests prove prerequisite refusal, order, resolved IDs rather than raw IDs, normal/void payloads, unique references, first-run writes, zero retry inserts, target-first recovery, new-row discovery after checkpoint, and connection closure on success/failure.
    - Six entity checkpoints remain independently reportable; sales does not overwrite Phase 13 checkpoint rows.
    - `npm run check:architecture` passes with no allowlist change.
  </acceptance_criteria>
  <verify><automated>cd apps/dgfy-migration-runner &amp;&amp; node --experimental-vm-modules node_modules/.bin/jest --config jest.config.cjs --runInBand tests/dataSalesApply.test.js &amp;&amp; npm run check:architecture</automated></verify>
  <done>Sales writes are prerequisite-gated, dependency-ordered, idempotent, crash-recoverable, and pool-bounded.</done>
</task>

</tasks>

## Artifacts this phase produces

- `ENTITY_TARGET_CONFIG.availment` and `ENTITY_TARGET_CONFIG.availment_item`.
- Sales prerequisite-checkpoint gate and dependency-ordered apply path.
- Timestamp-preserving `insertTargetRow` behavior.
- Test: `dataSalesApply.test.js`.

<threat_model>
## ASVS Level 1 Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-05-01 | Tampering | duplicate/retry writes | high | mitigate | ID map, unique namespaced natural keys, target-first recovery, and retry tests. |
| T-14-05-02 | Tampering | false-clean findings | high | mitigate | Per-reason current-state synchronization on every scan. |
| T-14-05-03 | Tampering | wrong FK domain | high | mitigate | Product prerequisites and dual durable map lookups; legacy shift is never copied. |
| T-14-05-04 | Denial of Service | connection pools | medium | mitigate | Per-owner `finally` closure on success and failure. |
</threat_model>

<verification>
Focused apply/metadata tests and migration-runner architecture checks pass.
</verification>

<success_criteria>
Apply preserves every valid sale/line exactly once, blocks partial required relationships, and maintains truthful retry evidence.
</success_criteria>

<output>Create `.planning/phases/14-sales-history-migration-full-verification/14-05-SUMMARY.md` when done.</output>
