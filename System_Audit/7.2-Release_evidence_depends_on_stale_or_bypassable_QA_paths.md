# [OPEN] 7.2 Release Evidence Depends on Stale or Bypassable QA Paths

Severity: High
Audit area: Release governance, deployment evidence

## Re-Baseline Update (2026-08-05) — Evidence section below is stale; mechanism it describes no longer governs releases

**Still open, and the underlying risk is worse, not better.** The QA-promotion/bypass mechanism this file describes is gone: `scripts/deploy-remote.sh` is now a 6-line stub that prints "Direct local production deployment is disabled" and exits 1. `docs/ops/RELEASE_CANDIDATE_POLICY.md` (authoritative, 2026-07-30) states the signed-controller model referenced by `NO_STAGING_RELEASE_STANDARD.md`/ADR 0030 was **"never built for this repository"** — there is no `master` branch, no controller installed, and the QA-qualification workflows were failing or permanently skipped before being archived to `.github/workflows-archive/`. Current reality per that policy: *"A push to `main` deploys production… Merging a PR into `main` is the deploy,"* gated only by `pr-checks.yml`, with *"no inventory/regression-risk/QA-evidence gate on `main` today."* `.github/workflows/build-main.yml:37-39` confirms every push to `main` restarts the shared backend beta and prod both depend on, **with no approval gate**. Every SHA/script/bypass reference in the Evidence section below (`deploy-qa-target.ps1`, the June 26 `c375dd90…` deploy) describes infrastructure that no longer exists or no longer runs. What did genuinely improve: a real distinct staging environment now exists (Sentry `STAGING` env, `build-staging.yml`), and local deploy is hard-disabled rather than merely discouraged — but the release-evidence gate (`check:batch-inventory`, `check:regression-risk`, `check:documentation-closure`) was removed from CI entirely rather than fixed; those scripts still exist and pass their own tests, but nothing in `.github/workflows/` invokes them. Severity stays High.

## Finding

Prior release documentation recorded an emergency no-staging bypass for stale QA deployed-head evidence. The June 18, 2026 release evidence included a clean no-staging verdict for target SHA `4cf4c372c9eae91c6ead5932e19476f4c1b98e83`, but later June 22 Storefront map restore deployments, the June 25 Storefront gallery/hours hotfix, and the June 26 platform-admin assisted provisioning release again required owner-authorized emergency bypass because QA deploy-summary SHA evidence was stale. The current source tree includes guarded QA promotion before the production gate and refuses production-as-QA mutation, but the finding remains open until a routine release proves exact QA deployed-head parity without emergency bypass.

## Evidence

- The June 18 clean gate remains historical evidence for target SHA `4cf4c372c9eae91c6ead5932e19476f4c1b98e83`.
- The later June 26 production deploy for target SHA `c375dd90f2db16d8ed924c0b10ecd653319e8b16` records live parity through `/var/www/skupervisor/logs/deploy/deploy_20260626_161139.summary.txt`, production remote `HEAD`, production `.deploy-state/last_deployed_commit`, and `/api/v1/health.services.observability.runtime_sha`.
- The `c375dd90f2db16d8ed924c0b10ecd653319e8b16` deploy still used owner-authorized emergency bypass because QA deploy-summary SHA evidence was stale; all post-deploy production proof points matched the target SHA.
- Source-current `scripts/deploy-remote.sh` now promotes the pushed target SHA to QA before the no-staging hard gate by default and fetches fresh QA summary evidence every run.
- Source-current `scripts/deploy-qa-target.ps1` refuses automatic QA promotion when the configured QA SSH host/app directory match the production host/app directory.

## Why This Matters

Release evidence must be current, reproducible, and tied to the deployed SHA. Runtime proof for `c375dd90f2db16d8ed924c0b10ecd653319e8b16` matches production, but routine release confidence still depends on proving QA parity before production instead of relying on emergency bypass.

## Required Remediation

1. Run the next routine production deploy through guarded QA promotion without emergency bypass.
2. Configure a distinct QA SSH host/app directory, or explicitly set `DEPLOY_PROMOTE_QA_BEFORE_PROD=off` when operating in production-as-QA evidence mode.
3. Keep no-staging bypasses time-bound, audited, and incident-only.
4. Ensure production verification compares deploy summary, deployed state, health runtime SHA, assets, and feature smoke for the exact target SHA.

## Source-Current Implementation Note

The source-current deploy wrapper reviews QA deploy-head evidence, promotes a distinct QA target before the production gate by default, and records runtime SHA parity. This implementation satisfies the source remediation requirement but still needs a routine no-bypass production release to close the finding.

## Acceptance Gates

- Current release evidence contains exact target SHA and deployed SHA parity. `PASS`
- No stale-QA bypass is needed for the release. `FAIL` - the latest `c375dd90f2db16d8ed924c0b10ecd653319e8b16` deploy still required emergency bypass for stale QA deployed-head evidence.
- Authenticated role-based UAT evidence exists where readiness claims require it. `PARTIAL` - POS cashier/admin UAT remains tracked as a non-technical readiness blocker.
- Local/release gates pass before production deploy. `PARTIAL` - source-current gate changes are validated locally, but production no-bypass proof remains pending.
