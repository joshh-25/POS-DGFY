# [REMEDIATED] 2.3 Backend Full Test Gate Has No Current Green Evidence

Severity: High
Audit area: Backend correctness, release evidence

## Finding

The backend full test command did not produce current green evidence during this audit run.

## Evidence

- `npm run test:backend` exceeded the audit timeout during the initial audit.
- Follow-up `npm --prefix backend test` on 2026-06-02 exceeded a 10-minute timeout and was stopped.
- The audit did not capture a successful backend full-suite completion.
- This finding does not claim the backend suite fails; it states that the audit has no current complete passing evidence.
- Remediated on 2026-06-03 by adding a bounded backend test matrix runner and release-gate integration.
- `npm run test:backend:matrix` passed on 2026-06-03 for target SHA `3ee0890f8a765a552546f9977c6197a2db9e5980`.
- Current green matrix evidence:
  - Artifact: `.tmp/release-gates/3ee0890f8a765a552546f9977c6197a2db9e5980/backend-test-matrix/backend_test_matrix.json`
  - Verdict: `pass`
  - Active backend test files: `274`
  - Selected groups: `9`
  - Chunk size: `1`
  - Per-chunk timeout: `600000ms`
  - Schema preflight: `pass` against test database `sku_test`
- The local release gate now runs `npm run test:backend:matrix` through `scripts/gate-release-local.js`.
- CI now runs the same matrix runner from the backend job and uploads backend matrix artifacts for the workflow SHA.
- `npm run gate:release:local` passed on 2026-06-04 Asia/Manila for target SHA `3ee0890f8a765a552546f9977c6197a2db9e5980`, including `backend.test_matrix`, with local readiness artifact `.tmp/release-gates/3ee0890f8a765a552546f9977c6197a2db9e5980/local_readiness.json`.

## Why This Matters

A production release needs repeatable backend test evidence. Long-running or non-terminating suites are operationally equivalent to missing evidence when release decisions depend on them.

## Required Remediation

1. Completed: split backend tests into bounded matrix groups by area.
2. Completed: isolated slow suites and raised test-owned timeouts where current behavior legitimately exceeds the prior default.
3. Completed: added per-chunk timeout, duration, status, and log-file reporting.
4. Completed: added release-gate and CI execution for the backend matrix.
5. Completed: added schema preflight to fail closed when the backend matrix is pointed at a non-test database or a stale test schema.

## Acceptance Gates

- `npm run test:backend:matrix` passes.
- Release artifacts include backend test command, target SHA, active test count, group count, schema preflight result, per-chunk duration, per-chunk status, and per-chunk log paths.
- `npm run gate:release:local` includes the backend matrix before declaring the local release gate green.
- `npm run gate:release:local` passes with `backend.test_matrix` green.

## Residual Notes

- The matrix intentionally uses one test file per chunk to make long-running or order-sensitive suites visible instead of hiding them inside an aggregate timeout.
- Browser E2E test files remain part of the discovered backend Jest inventory; their assertions keep the existing opt-in behavior unless `RUN_BROWSER_E2E=true`.
