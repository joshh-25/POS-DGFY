# [OPEN] 2.1 Local Release Gate Fails

Severity: Critical
Audit area: Release readiness, CI parity

## Finding

The local release gate currently fails.

## Evidence

`npm run gate:release:local` produced `verdict: fail` in:

- `.tmp/release-gates/f57c68f7dd084b3381d2b266cd4995beb4fd6099/local_readiness.json`

Gate results:

- Passed: target SHA, dependency production audit, dependency full audit, docs lint, architecture guardrails, compliance contracts, runtime doctor, frontend lint, scroll contracts.
- Failed: backend lint and frontend budgets.

Backend lint failure:

- `backend/src/modules/compliance/repositories/complianceRepository.js`
- Reported `no-useless-assignment` at line 942.

Frontend budget failure:

- Built assets existed after `npm --prefix frontend run build:all`, but three POS route chunks exceeded their configured budgets:
  - `POSCheckoutTerminal-B8zETmkd.js`: `65.14KB` / `64KB`
  - `POSCheckoutTerminal-Dg-xnDpf.js`: `67.2KB` / `67KB`
  - `SkupervisorPOSPage-B2VUZLx6.js`: `58.05KB` / `58KB`

## Why This Matters

The repository governance requires release gates to be green before production release. A failing local release gate is a hard release blocker regardless of whether individual lower-level checks pass.

## Required Remediation

1. Fix backend lint failure.
2. Reduce the over-budget POS chunks or update the budget contract with documented justification.
3. Re-run `npm run gate:release:local`.
4. Preserve the resulting readiness artifact.

## Acceptance Gates

- `npm run gate:release:local` returns success.
- Readiness artifact reports `verdict: pass`.
- Any skipped gate has an explicit governance-approved reason.
