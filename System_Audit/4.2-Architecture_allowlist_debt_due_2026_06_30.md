# [OPEN] 4.2 Architecture Allowlist Debt — Now Overdue (was due 2026-06-30)

Severity: Medium (recommend escalating to High — see below)
Audit area: Architecture governance

## Re-Baseline Update (2026-08-05) — this finding's "not currently overdue" line below is now factually wrong

**Still open, and now overdue.** `backend/src/config/architectureModelImportAllowlist.js:4` and `architectureGuardrailsAllowlist.js` (lines 4, 12, 16, 20, 24) all still carry `planned_removal_date: 2026-06-30` — a date that passed roughly 5 weeks before this update. All six original exceptions remain untouched (`src/modules/ai/aiCore.js`, `useCaseResponder.js`, `toolRegistryMap.js`, `handleWebhookUseCase.js`, `upgradeToPremiumUseCase.js`, `registerCompanyRequestUseCase.js`); neither allowlist file has been edited since the exceptions were added. Acceptance gate 2 ("remaining entries have current owner, reason, and removal date") fails on two counts, not one: there is no `owner` field on any entry at all, and every `removal_date` is expired. Recommend escalating to High given the compounding governance drift, or at minimum re-title away from a date that has already passed.

## Finding

Architecture checks pass, but active allowlist exceptions remain with planned removal dates of 2026-06-30.

## Evidence

- `npm run check:architecture` passed.
- `backend/src/config/architectureModelImportAllowlist.js` includes an exception for `src/modules/ai/aiCore.js`.
- `backend/src/config/architectureGuardrailsAllowlist.js` includes multiple temporary use-case service import exceptions.
- The current planned removal date for those exceptions is 2026-06-30.

## Why This Matters

The repository's architecture governance allows temporary exceptions only when tracked with removal plans. These are not currently overdue, but they must be closed before the planned date to avoid normalizing boundary drift.

## Required Remediation

1. Assign owners to each allowlist entry.
2. Close or narrow each exception before 2026-06-30.
3. Add removal evidence to the relevant ADR/task.
4. Prevent new exceptions unless an ADR or documented exception path approves them.

## Acceptance Gates

- Architecture checks pass with fewer or no allowlist entries.
- Remaining entries have current owner, reason, and removal date.
- No undocumented cross-boundary imports are introduced.
