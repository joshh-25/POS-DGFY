# Frontend Features

Feature-oriented slices live under this folder:

`features/<domain>/{api,hooks,components,pages,types}`

Legacy `Pages/` and shared hooks remain compatibility layers during migration.
