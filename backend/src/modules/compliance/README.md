# Compliance Module

Tenant-scoped dual-mode compliance policy and lifecycle enforcement.

Boundary flow:
`routes -> compliance handlers -> compliance use-cases -> compliance repository -> landlord models`
