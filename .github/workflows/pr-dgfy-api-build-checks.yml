name: PR DGFY API Build Check

on:
  workflow_call:
    inputs:
      runner_labels_json:
        description: JSON array passed to runs-on for this job
        required: false
        type: string
        default: '["ubuntu-latest"]'

jobs:
  build-check:
    runs-on: ${{ fromJSON(inputs.runner_labels_json) }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      # Same build as deploy-api.yml, minus the push -- just proves the
      # image builds cleanly off this PR's code. Reads the canonical `api`
      # cache scope that deploy-api.yml writes (usually warm from the last
      # develop/staging/main build), but writes to a PR-scoped cache so a
      # broken PR build can never poison the canonical scope -- and repeated
      # pushes to the same PR still hit cache. GitHub evicts the PR-scoped
      # cache automatically once the PR closes.
      - name: Build api image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: infrastructure/docker/dgfy-api/Dockerfile
          push: false
          cache-from: type=gha,scope=api
          cache-to: type=gha,mode=max,scope=api-pr-${{ github.event.pull_request.number }}
