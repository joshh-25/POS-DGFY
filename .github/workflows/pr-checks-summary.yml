name: PR Checks Summary

on:
  workflow_call:
    inputs:
      runner_labels_json:
        description: JSON array passed to runs-on for this job
        required: false
        type: string
        default: '["ubuntu-latest"]'
      conventional_commits_result:
        required: true
        type: string
      code_quality_result:
        required: true
        type: string
      dgfy_api_build_result:
        required: true
        type: string
      migration_runner_build_result:
        required: true
        type: string
      frontend_build_result:
        required: true
        type: string

jobs:
  summary:
    runs-on: ${{ fromJSON(inputs.runner_labels_json) }}
    steps:
      - name: PR checks summary
        run: |
          echo "## PR Checks Summary" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Result |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Conventional Commits | ${{ inputs.conventional_commits_result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Code Quality | ${{ inputs.code_quality_result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| DGFY API Build | ${{ inputs.dgfy_api_build_result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Migration Runner Build | ${{ inputs.migration_runner_build_result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend Build | ${{ inputs.frontend_build_result }} |" >> $GITHUB_STEP_SUMMARY

      - name: Fail if any check failed
        if: >-
          contains(inputs.conventional_commits_result, 'failure') ||
          contains(inputs.code_quality_result, 'failure') ||
          contains(inputs.dgfy_api_build_result, 'failure') ||
          contains(inputs.migration_runner_build_result, 'failure') ||
          contains(inputs.frontend_build_result, 'failure')
        run: exit 1
