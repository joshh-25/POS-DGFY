name: PR Checks

on:
  pull_request:
    branches:
      - develop
      - staging
      - main
    types:
      - opened
      - synchronize
      - reopened
      - edited

concurrency:
  group: pr-checks-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  # Toggle the runner for ALL PR checks here by commenting/uncommenting one line.
  # RUNNER_LABELS_JSON: &runner_labels_json '["ubuntu-latest"]'
  RUNNER_LABELS_JSON: &runner_labels_json '["self-hosted"]'

jobs:
  conventional-commits:
    uses: ./.github/workflows/pr-conventional-commits.yml
    with:
      runner_labels_json: *runner_labels_json

  code-quality:
    uses: ./.github/workflows/pr-code-checks.yml
    with:
      runner_labels_json: *runner_labels_json

  dgfy-api-build-check:
    needs: [conventional-commits, code-quality]
    uses: ./.github/workflows/pr-dgfy-api-build-checks.yml
    with:
      runner_labels_json: *runner_labels_json

  dgfy-migration-runner-build-check:
    needs: [conventional-commits, code-quality]
    uses: ./.github/workflows/pr-migration-runner-build-checks.yml
    with:
      runner_labels_json: *runner_labels_json

  frontend-build-check:
    needs: [conventional-commits, code-quality]
    uses: ./.github/workflows/pr-frontend-build-checks.yml
    with:
      runner_labels_json: *runner_labels_json

  summary:
    needs: [conventional-commits, code-quality, dgfy-api-build-check, dgfy-migration-runner-build-check, frontend-build-check]
    if: always()
    uses: ./.github/workflows/pr-checks-summary.yml
    with:
      runner_labels_json: *runner_labels_json
      conventional_commits_result: ${{ needs.conventional-commits.result }}
      code_quality_result: ${{ needs.code-quality.result }}
      dgfy_api_build_result: ${{ needs.dgfy-api-build-check.result }}
      migration_runner_build_result: ${{ needs.dgfy-migration-runner-build-check.result }}
      frontend_build_result: ${{ needs.frontend-build-check.result }}
