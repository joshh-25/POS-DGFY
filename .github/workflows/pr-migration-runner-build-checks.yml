name: PR Migration Runner Build Check

on:
  workflow_call:
    inputs:
      runner_labels_json:
        description: JSON array passed to runs-on for this job
        required: false
        type: string
        default: '["self-hosted"]'
      platforms:
        description: >-
          Comma-separated platforms to build for this check. Defaults to
          amd64-only: this is a discarded (push:false) proof-of-build, and
          arm64-only breakage still gets caught one step later by the real
          publish build in deploy-migration-runner.yml, which keeps
          dual-arch. Pass "linux/amd64,linux/arm64" to opt a PR back into
          the full check (e.g. when touching Dockerfile/base-image pins).
        required: false
        type: string
        default: linux/amd64

jobs:
  build-check:
    runs-on: ${{ fromJSON(inputs.runner_labels_json) }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      # Only needed when platforms actually requests arm64 -- the default (amd64-only) proof
      # of build has no cross-arch step to emulate. Costs ~20s plus a 31MB binfmt cache entry
      # per ref for nothing when skipped unconditionally (#727).
      - if: contains(inputs.platforms, 'arm64')
        uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      # Same build as deploy-migration-runner.yml, minus the push -- proves the
      # image builds cleanly off this PR's code. Reads the canonical
      # `migration-runner` cache scope that deploy-migration-runner.yml writes
      # (usually warm from the last develop/staging/main build). Deliberately
      # no cache-to: a per-PR mode=max cache write is full-size and nothing
      # ever reads it back (each PR only reads the canonical scope), so it
      # was pure eviction pressure against the repo's 10GB Actions cache
      # quota -- see #412.
      - name: Build migration-runner image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: infrastructure/docker/dgfy-migration-runner/Dockerfile
          platforms: ${{ inputs.platforms }}
          push: false
          cache-from: type=gha,scope=migration-runner
