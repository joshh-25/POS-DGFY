server {
    listen 8083;
    server_name _;
    client_max_body_size 8m;

    root /usr/share/nginx/html/store;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
