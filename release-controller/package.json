{
  "name": "skupervisor-release-controller",
  "version": "2.0.0",
  "private": true,
  "description": "Root-owned external signed release authorization controller",
  "bin": {
    "skupervisor-release-controller": "bin/skupervisor-release-controller.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=24"
  }
}
